# ─────────────────────────────────────────────────────────────
# DevMastery Core — base configuration
# Overridable per environment via application-{dev,prod}.yml
# ─────────────────────────────────────────────────────────────
spring:
  application:
    name: devmastery-core
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  main:
    lazy-initialization: true          # Faster cold start on Render free tier

  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:aws-1-ap-southeast-2.pooler.supabase.com}:${POSTGRES_PORT:5432}/${POSTGRES_DB:postgres}
    username: ${POSTGRES_USER:postgres.diculkpsidcofmbyeqdq}
    password: ${POSTGRES_PASSWORD:#Devmastery@098}
    hikari:
      maximum-pool-size: 3             # Supabase free tier connection limit
      minimum-idle: 1
      connection-timeout: 30000
      idle-timeout: 60000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: none                   # Flyway owns schema — Hibernate must not validate
    open-in-view: false
    properties:
      hibernate.jdbc.time_zone: UTC
      hibernate.format_sql: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  cache:
    type: caffeine
    cache-names:
      - topicsList
      - topicById
      - topicBySlug
      - lessonsByTopic
      - searchResults
      - userProgress
      - pathsList
    caffeine:
      spec: maximumSize=2000,expireAfterWrite=10m

  servlet:
    multipart:
      max-file-size: 200KB
      max-request-size: 200KB

server:
  port: ${SERVER_PORT:8080}
  compression:
    enabled: true
  forward-headers-strategy: framework

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always             # Shows DB status in /actuator/health

springdoc:
  swagger-ui:
    path: /swagger-ui.html

# ─── App-specific ─────────────────────────────────────────────
app:
  cors:
    allowed-origins: ${ALLOWED_ORIGINS:http://localhost:3000}

  base-url: ${APP_BASE_URL:https://devmastery.io}

  jwt:
    secret: ${JWT_SECRET}
    expiry-ms: ${JWT_EXPIRY_MS:86400000}     # 24h

  storage:
    supabase:
      url:              ${SUPABASE_URL}
      service-role-key: ${SUPABASE_SERVICE_ROLE_KEY}

  ai:
    gemini:
      api-key:        ${GEMINI_API_KEY}
      model:          ${GEMINI_MODEL:gemini-2.5-flash}
      # Auto-fallback when the primary model is rate-limited (429) or fails (5xx).
      # gemini-2.5-flash free-tier is ~10 RPM / 250 RPD — far higher than 2.5-pro.
      fallback-model: ${GEMINI_FALLBACK_MODEL:gemini-2.5-flash}

  # External "Try Online" runner URLs per language ({code} = url-encoded source)
  # These are fallback defaults — admin can override via DB runner_templates table
  runners:
    java:       "https://onecompiler.com/java"
    javascript: "https://jsfiddle.net"
    python:     "https://www.programiz.com/python-programming/online-compiler/"
    kotlin:     "https://play.kotlinlang.org"
    sql:        "https://sqliteonline.com"
    default:    "https://onecompiler.com/"

# ─── Judge0 Code Execution ────────────────────────────────────────────────
judge0:
  api-key:      ${JUDGE0_API_KEY:}
  primary-url:  ${JUDGE0_PRIMARY_URL:https://judge029.p.rapidapi.com}
  primary-host: ${JUDGE0_PRIMARY_HOST:judge029.p.rapidapi.com}
  fallback-url:  ${JUDGE0_FALLBACK_URL:https://judge0-ce.p.rapidapi.com}
  fallback-host: ${JUDGE0_FALLBACK_HOST:judge0-ce.p.rapidapi.com}

