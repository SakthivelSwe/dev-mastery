You are a senior engineering author for the DevMastery learning platform.
Rewrite ONLY the following sections of the given topic MDX file, keeping ALL other sections untouched.

Topic slug: lifting-state-up
Path:       react
Level:      3 (1=beginner … 5=expert)
Title:      React: Lifting State Up

Sections to (re)write, each with its exact heading:
  ## WHY
  ## THEORY
  ## REAL_WORLD
  ## INTERVIEW
  ## FEYNMAN CHECK
  ## BUILD

Rules — non-negotiable:
- Preserve the heading text EXACTLY as shown above (case-sensitive).
- WHY: 3–4 paragraphs (problem → solution → failure mode → why seniors care).
- THEORY: 400+ words including at least one Mermaid diagram inside a fenced code block.
- VISUALIZATION_CONFIG: fenced JSON with {"component":"...","state":"..."} keys only.
- CODE: exactly 4 progressive levels (### Level 1 Beginner, Level 2 Intermediate, Level 3 Advanced, Level 4 Expert), each runnable, no TODO stubs, no placeholder names.
- REAL_WORLD: name at least one real company/library using this in production and cite the failure mode.
- INTERVIEW: 5+ Q&A pairs, Junior → Senior progression, each with a full answer.
- FEYNMAN CHECK: analogy + 5 recall Q&As with full answers.
- BUILD: mini-project with setup → implement → test → expected output.
- SPACED REVIEW: 12 numbered questions covering Day 1 / 3 / 7 / 14 buckets.

Return ONLY the requested sections, in order, separated by blank lines. Do NOT wrap the whole output in a code fence. Do NOT include the frontmatter.

Existing MDX (for context — do not repeat unchanged sections):
```mdx
---
slug: "lifting-state-up"
title: "React: Lifting State Up"
level: 3
---

## WHY
In React, data flows strictly downwards (Unidirectional Data Flow). A parent can pass data to a child via `props`, but a child cannot directly modify the parent's data, nor can sibling components talk to each other directly.

What happens when you have two sibling components (like a `SearchInput` and a `ProductList`) that need to share the exact same state? The `SearchInput` needs to update the search term, and the `ProductList` needs to read it to filter the items. 
Because siblings cannot communicate, you must **Lift the State Up** to their closest common ancestor.

## THEORY
### The Shared Ancestor
To share state between two components, you find the closest parent component that wraps them both. 
You move the `useState` definition into that parent. 
Then, you pass the `state` value down to the component that needs to *read* it, and you pass the `setState` function down to the component that needs to *update* it.

## VISUALIZATION_CONFIG
```json
{
  "language": "javascript",
  "fileName": "lifting.jsx",
  "steps": [
    {
      "title": "Identify shared state",
      "description": "When two siblings need the same state, lift it to their common ancestor.",
      "code": "// Before: duplicated temperature state\nfunction CelsiusInput() { const [c, setC] = useState(0); }\nfunction FahrenheitInput() { const [f, setF] = useState(32); }\n\n// After: lifted to common parent\nfunction TemperatureConverter() {\n    const [celsius, setCelsius] = useState(0);\n    const fahrenheit = celsius * 9/5 + 32;\n    return (\n        <>\n            <CelsiusInput value={celsius} onChange={setCelsius} />\n            <FahrenheitDisplay value={fahrenheit} />\n        </>\n    );\n}",
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "sibling A needs state X",
            "done": true
          },
          {
            "label": "sibling B also needs state X",
            "done": true
          },
          {
            "label": "lift X to parent: single source of truth",
            "active": true
          },
          {
            "label": "pass down via props"
          },
          null
        ]
      }
    },
    {
      "title": "Lifting with callbacks",
      "description": "Parent owns state. Child calls parent callback to request state change.",
      "code": "function TodoApp() {\n    const [todos, setTodos] = useState([]);\n    const addTodo    = text => setTodos(prev => [...prev, {id:Date.now(), text, done:false}]);\n    const toggleTodo = id   => setTodos(prev => prev.map(t => t.id===id ? {...t, done:!t.done} : t));\n    const deleteTodo = id   => setTodos(prev => prev.filter(t => t.id !== id));\n    return (\n        <>\n            <AddForm    onAdd={addTodo} />\n            <TodoList   todos={todos} onToggle={toggleTodo} onDelete={deleteTodo} />\n        </>\n    );\n}",
      "diagram": {
        "kind": "sequence",
        "actors": [
          "AddForm",
          "TodoApp",
          "TodoList"
        ],
        "messages": [
          {
            "from": "AddForm",
            "to": "TodoApp",
            "label": "onAdd(\"Task\")"
          },
          {
            "from": "TodoApp",
            "to": "TodoApp",
            "label": "setState([...todos, new])"
          },
          {
            "from": "TodoApp",
            "to": "TodoList",
            "label": "todos prop updated"
          }
        ]
      }
    },
    {
      "title": "Avoid over-lifting",
      "description": "Lift only as high as needed. Over-lifting causes unnecessary re-renders in unrelated subtrees.",
      "code": "// Over-lifted: ALL components re-render when modal opens\nfunction App() {\n    const [modalOpen, setModalOpen] = useState(false);\n    return <Page modalOpen={modalOpen} setModalOpen={setModalOpen} />;\n}\n\n// Better: state owned at usage site\nfunction Page() {\n    const [modalOpen, setModalOpen] = useState(false);\n    return <button onClick={() => setModalOpen(true)}>...</button>;\n}",
      "diagram": {
        "kind": "boxes",
        "title": "State placement",
        "items": [
          {
            "label": "lift only to lowest common ancestor",
            "color": "#10b981",
            "highlight": true
          },
          {
            "label": "too high: unnecessary re-renders",
            "color": "#ef4444"
          },
          {
            "label": "rule: state lives where it is used",
            "color": "#818cf8"
          }
        ]
      }
    },
    {
      "title": "State colocation",
      "description": "Keep state as close to where it is used as possible. Improves performance and maintainability.",
      "code": "// Colocated: toggle only in its subtree\nfunction NavMenu() {\n    const [open, setOpen] = useState(false);\n    return (\n        <nav>\n            <button onClick={() => setOpen(o => !o)}>Menu</button>\n            {open && <DropdownMenu />}\n        </nav>\n    );\n    // App does not know/care about menu state\n}",
      "diagram": {
        "kind": "boxes",
        "title": "State colocation",
        "items": [
          {
            "label": "menu open state: in NavMenu (not App)",
            "color": "#10b981",
            "highlight": true
          },
          {
            "label": "only NavMenu subtree re-renders on toggle",
            "color": "#10b981"
          },
          {
            "label": "App unaffected",
            "color": "#818cf8"
          }
        ]
      }
    },
    {
      "title": "Server state vs client state",
      "description": "Server state (react-query): remote data, caching, sync. Client state (useState/Zustand): UI state, forms, navigation.",
      "code": "// Server state — use react-query\nconst { data: posts } = useQuery([\"posts\"], fetchPosts);\n\n// Client state — useState or Zustand\nconst [isOpen, setIsOpen] = useState(false);  // UI\n
```