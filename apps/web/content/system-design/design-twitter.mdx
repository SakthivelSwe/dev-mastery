---
slug: "design-twitter"
title: "Design: Twitter / X"
level: 5
---

## WHY
Designing Twitter is one of the most comprehensive system design challenges. It tests your understanding of the full spectrum: massive-scale databases, distributed caching, asynchronous messaging, the Fan-Out Problem, and how to serve personalized feeds to 300 million users in under 100ms.

Twitter is the canonical example of a **write-once, read-many-times** system. Users tweet rarely but read their feeds constantly. The entire architecture is optimized for the read path.

## THEORY
### The Core Challenge: The Fan-Out Problem
When Lady Gaga (with 100 million followers) posts a tweet, how do you make that tweet appear on all 100 million followers' feeds? 

If you compute the feed at read time (query all tweets from followed accounts) — it requires massive JOINs across tables for every feed request. With 300k reads/second, this is impossible.

Twitter's solution: **Pre-compute** the feeds! When a tweet is posted, push it to every follower's "home timeline" cache immediately. Reading the feed is then a simple O(1) cache lookup.

## VISUALIZATION_CONFIG
```json
{
  "steps": [
    {
      "title": "Twitter Scale Requirements",
      "description": "100M DAU, 500M tweets/day, 28B timeline reads/day — extreme read:write ratio.",
      "code": "// Scale numbers\n// Users: 100M DAU, 400M total\n// Tweets: 500M/day = 5,787/sec writes\n// Timeline reads: 28B/day = 324K reads/sec\n// Followers per celebrity: up to 100M\n\n// Key challenge: fanout\n// When @elonmusk tweets → push to 100M timelines\n// Simple push: 100M writes per tweet!\n\n// Solution: push vs pull hybrid\n// Regular users: push to follower cache (fanout on write)\n// Celebrities: pull and merge on read (fanout on read)",
      "highlight": [
        5,
        6,
        7,
        11,
        12,
        13
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "Tweet created",
          "Fanout service",
          "Regular → push to followers",
          "Celebrity → pull on read",
          "Timeline assembled"
        ]
      }
    },
    {
      "title": "Timeline Generation",
      "description": "Each user has a pre-computed timeline cache in Redis — sorted by tweet timestamp.",
      "code": "// Timeline stored in Redis sorted set\n// Key: timeline:{userId}\n// Score: tweet timestamp\n// Value: tweetId\n\n// Write path: fanout\nasync function postTweet(authorId, content) {\n  const tweet = await saveTweet({ authorId, content });\n  const followers = await getFollowers(authorId); // could be 100M!\n\n  // Async fanout via queue\n  await fanoutQueue.add({ tweetId: tweet.id, followers });\n}\n\n// Fanout worker\nfollowers.forEach(uid => {\n  redis.zadd(`timeline:${uid}`, tweet.timestamp, tweet.id);\n  redis.zremrangebyrank(`timeline:${uid}`, 0, -801); // keep latest 800\n});",
      "highlight": [
        7,
        8,
        11,
        14,
        15,
        16
      ],
      "diagram": {
        "kind": "boxes",
        "boxes": [
          {
            "id": "t",
            "label": "Tweet stored",
            "color": "#1e88e5"
          },
          {
            "id": "q",
            "label": "Fanout queue",
            "color": "#43a047"
          },
          {
            "id": "c",
            "label": "Redis timelines",
            "color": "#fb8c00"
          }
        ],
        "connections": [
          {
            "from": "t",
            "to": "q"
          },
          {
            "from": "q",
            "to": "c"
          }
        ]
      }
    },
    {
      "title": "Timeline Read",
      "description": "Read timeline from Redis, hydrate tweet details, merge celebrity tweets.",
      "code": "async function getTimeline(userId, cursor, limit = 20) {\n  // 1. Get tweet IDs from Redis sorted set\n  const tweetIds = await redis.zrevrange(\n    `timeline:${userId}`, cursor, cursor + limit - 1\n  );\n\n  // 2. Get celebrity tweets (fanout on read)\n  const followedCelebs = await getCelebFollows(userId);\n  const celebTweets = await getCelebRecentTweets(followedCelebs);\n\n  // 3. Merge and sort\n  const allIds = mergeSorted([tweetIds, celebTweets]);\n\n  // 4. Batch fetch tweet details\n  const tweets = await redis.mget(allIds.map(id => `tweet:${id}`));\n  return tweets;\n}",
      "highlight": [
        3,
        4,
        8,
        9,
        11,
        14
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "Get IDs from Redis",
          "Merge celeb tweets",
          "Sort by timestamp",
          "Batch fetch details",
          "Return timeline"
        ]
      }
    },
    {
      "title": "Search Architecture",
      "description": "Real-time tweet search with Elasticsearch — index tweets as they arrive.",
      "code": "// Kafka → Elasticsearch pipeline\nkafkaConsumer.run({\n  eachMessage: async ({ message }) => {\n    const tweet = JSON.parse(message.value);\n\n    // Index in ES for search\n    await elasticsearch.index({\n      index: 'tweets',\n      id: tweet.id,\n      document: {\n        text: tweet.content,\n        author: tweet.authorId,\n        timestamp: tweet.createdAt,\n        hashtags: extractHashtags(tweet.content)\n      }\n    });\n  }\n});",
      "highlight": [
        6,
        7,
        8,
        9,
        10,
        11,
        12,
        13
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "Tweet created",
          "Kafka topic",
          "ES consumer",
          "Indexed in ES",
          "Searchable instantly"
        ]
      }
    },
    {
      "title": "Trending Topics",
      "description": "Count hashtag frequency in sliding windows using Redis sorted sets.",
      "code": "// Count hashtag occurrences in 24hr window\n// Use time-bucketed counters (1hr buckets)\nasync function trackHashtag(hashtag) {\n  const bucket = Math.floor(Date.now() / 3600000); // 1hr bucket\n  await redis.zincrby(`trends:${bucket}`, 1, hashtag);\n  await redis.expire(`trends:${bucket}`, 86400); // keep 24hrs\n}\n\n// Get trending: sum last 24 buckets\nasync function getTrending() {\n  const buckets = last24Buckets();\n  const counts = await Promise.all(\n    buckets.map(b => redis.zrevrangewithscores(`trends:${b}`, 0, 99))\n  );\n  return mergeSortedCounts(counts).slice(0, 10); // top 10\n}",
      "highlight": [
        4,
        5,
        9,
        11,
        12,
        14
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "Tweet with #AI",
          "zincrby trends:bucket",
          "24 hourly buckets",
          "Sum all buckets",
          "Top 10 trends"
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner (Estimation)
```
SCALE ASSUMPTIONS:
- 300M Daily Active Users (DAU)
- 600M tweets per day (~7,000 tweets/second, peak ~15,000/sec)
- Average user follows 200 accounts
- 100B timeline reads per day (~1.15M reads/second, peak ~2.3M/sec)

READ-TO-WRITE RATIO: ~1:150 (heavily read-dominant!)

STORAGE:
- Tweet metadata: 280 chars + metadata = ~500 bytes per tweet
- 600M * 500B = 300 GB of tweet data per day
- 5-Year Storage: 300GB * 365 * 5 = ~550 Terabytes (just for tweets)
- Media (photos/videos): ~500x more storage → need dedicated Object Storage (S3)

CONCLUSION: Must aggressively cache. A database cannot serve 1M reads/second.
```

### Level 2 — Building Blocks (Data Model)
```sql
-- Users table
CREATE TABLE users (
    id        BIGINT PRIMARY KEY,    -- Snowflake ID
    username  VARCHAR(15) UNIQUE,
    email     VARCHAR(255) UNIQUE,
    bio       TEXT,
    followers_count INT DEFAULT 0,
    following_count INT DEFAULT 0
);

-- Tweets table
CREATE TABLE tweets (
    id          BIGINT PRIMARY KEY,  -- Snowflake ID (encodes timestamp!)
    author_id   BIGINT REFERENCES users(id),
    content     VARCHAR(280),
    media_urls  TEXT[],              -- Array of S3 URLs
    created_at  TIMESTAMP DEFAULT NOW(),
    -- Denormalized for read performance:
    likes_count    INT DEFAULT 0,
    retweet_count  INT DEFAULT 0
);

-- Social Graph (follow relationships)
-- This is queried EXTREMELY frequently. Must be indexed heavily!
CREATE TABLE followers (
    follower_id  BIGINT,
    following_id BIGINT,
    PRIMARY KEY (follower_id, following_id)
);
CREATE INDEX idx_followers_following ON followers(following_id);
```

### Level 3 — Intermediate (The Fan-Out Architecture)
```
WRITE PATH (Posting a Tweet):
1. User posts tweet
2. Tweet is written to the Tweets DB (PostgreSQL/Cassandra)
3. Tweet is published to a Kafka Topic ("new-tweet")
4. Fan-Out Service reads from Kafka and:
   a. Fetches all follower IDs of the author from the Social Graph DB
   b. For EACH follower: pushes the tweet ID into their "Home Timeline" cache (Redis)

HYBRID FAN-OUT (Twitter's actual approach):
- For regular users (<1000 followers): Fan-Out on WRITE (pre-compute)
- For celebrities (>1M followers): Fan-Out on READ (compute at read time)
  → Pushing to 100M caches on every Lady Gaga tweet would take too long!
  
READ PATH (Loading Home Timeline):
1. User opens app
2. Timeline Service reads tweet IDs from the user's Redis Timeline Cache
3. For celebrity accounts the user follows, it MERGES their recent tweets on the fly
4. Batch-fetches the full tweet content from the Tweet Cache (Redis)
5. Returns assembled timeline (total latency: ~50ms)
```

### Level 4 — Advanced (System Architecture Diagram)
```
                        ┌──────────────────────────────┐
                        │         API Gateway           │
                        │     (Rate Limiting, Auth)     │
                        └──────────┬───────────────────-┘
                    ┌──────────────┼────────────────┐
                    ▼              ▼                 ▼
             ┌──────────┐  ┌──────────┐     ┌──────────────┐
             │  Write   │  │  Read    │     │  Search      │
             │  Service │  │  Service │     │  Service     │
             └────┬─────┘  └────┬─────┘     └──────────────┘
                  │             │                  │
                  ▼             ▼              (Elasticsearch)
           ┌────────────┐  ┌─────────────┐
           │   Kafka    │  │    Redis    │
           │ (new-tweet │  │  (Timeline  │
           │   topic)   │  │   Cache)    │
           └────┬───────┘  └─────────────┘
                │
          ┌─────▼──────────────────────────┐
          │         Fan-Out Service         │
          │ (Reads followers → pushes to    │
          │  Redis timeline caches)         │
          └─────────────┬──────────────────┘
                        ▼
               ┌────────────────┐
               │  Social Graph  │
               │  DB (follows)  │
               └────────────────┘
```

## REAL_WORLD
Twitter's actual infrastructure (as revealed by various engineering blog posts):
- **Tweet Storage:** MySQL (billions of rows, sharded by user ID) + Manhattan (Twitter's custom distributed key-value store).
- **Timeline Cache:** Redis clusters storing serialized lists of tweet IDs per user.
- **Media Storage:** Custom Blob storage (now partially migrated to cloud).
- **Search:** Earlybird (a customized Apache Lucene cluster for real-time tweet search).
- **Fan-Out:** Multiple tiers — immediate fan-out for regular users, delayed fan-out for viral tweets.

## INTERVIEW
**Q: How do you handle the case where a user is offline when a tweet is posted?**
A: The Redis timeline cache stores the N most recent tweet IDs (e.g., last 800). When an offline user comes back, their cache has the recent tweets. For older tweets, the Read Service performs an on-demand "fan-in" query, fetching tweets from all followed accounts in the Tweets DB, sorted by time, and merges them with the cached timeline.

**Q: How would you design the "Trending Topics" feature?**
A: Use a sliding window counter in Redis. Each time a hashtag appears in a tweet, increment a `ZINCRBY` in a Redis Sorted Set with a time-windowed key (e.g., `trending:2025-05-28:14`). Every 5 minutes, `ZREVRANGE` the top N hashtags. Age-weight older windows by multiplying their scores by a decay factor. This gives you real-time trending without any SQL aggregations.

**Q: What is the N+1 problem in the context of a Twitter feed?**
A: When rendering a feed of 50 tweets, a naive implementation fetches the tweet data (1 query), then fetches each author's profile separately (50 queries) — total 51 queries! Solution: Batch-fetch all unique author IDs in one cache lookup using Redis MGET or a SQL `WHERE id IN (...)` query.

## FEYNMAN CHECK

### Explain Design: Twitter / X Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Design: Twitter / X** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Design: Twitter / X, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Design: Twitter / X solve? Why would JavaScript/web development be harder without it?**
> **A:** Design: Twitter / X solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Design: Twitter / X?**
> **A:** The single most important thing is understanding the *mental model*: Design: Twitter / X operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Design: Twitter / X and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Design: Twitter / X operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Design: Twitter / X interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Design: Twitter / X interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Design: Twitter / X to a senior engineer in two sentences, what would you say?**
> **A:** "Design: Twitter / X works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Design: Twitter / X Utility

**Goal:** Implement a real, working Design: Twitter / X module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `designtwitter` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir design-twitter-demo && cd design-twitter-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Design: Twitter / X Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Design: Twitter / X.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createDesign:Twitter/X(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Design: Twitter / X] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Design: Twitter / X logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Design: Twitter / X] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Design: Twitter / X] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createDesign:Twitter/X({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Design: Twitter / X] Input cannot be null or undefined`);
    }
    const instance = createDesign:Twitter/X();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Design: Twitter / X] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Design: Twitter / X Tests ──');

test('initialises correctly', () => {
  const m = createDesign:Twitter/X();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createDesign:Twitter/X();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Design: Twitter / X] Initialized with: hello world
[Design: Twitter / X] Processed: hello world
Result: hello world
[Design: Twitter / X] Destroyed.

── Design: Twitter / X Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Design: Twitter / X in one sentence.**
<details><summary>Answer</summary>

Design: Twitter / X is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Design: Twitter / X?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Design: Twitter / X.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Design: Twitter / X and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Design: Twitter / X | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Design: Twitter / X] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Design: Twitter / X causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Design: Twitter / X usage].
</details>

**Q6: What does Design: Twitter / X look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Design: Twitter / X typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Design: Twitter / X correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Design: Twitter / X:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_design_twitter(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Design: Twitter / X in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Design: Twitter / X in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Design: Twitter / X', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Design: Twitter / X at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Design: Twitter / X uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Design: Twitter / X interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Design: Twitter / X is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Design: Twitter / X relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Design: Twitter / X
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Design: Twitter / X to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Design: Twitter / X fits in the data/control flow.
2. **Choose** the right variant/pattern of Design: Twitter / X for the scale requirement.
3. **Handle** failure modes: what happens when Design: Twitter / X is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Design: Twitter / X is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
