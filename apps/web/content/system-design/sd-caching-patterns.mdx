---
slug: "sd-caching-patterns"
title: "Caching Patterns"
level: 4
---

## WHY
A cache saves a computed or fetched result so you don't have to recompute or re-fetch it. But deciding **how to fill the cache** and **how to keep it up-to-date** requires careful design. The wrong caching pattern leads to stale data, cache stampedes, or data loss on restarts.

The four fundamental caching patterns — Cache-Aside, Write-Through, Write-Behind, and Read-Through — define the relationship between the cache, the application, and the database. Mastering them is essential for any System Design interview.

## THEORY
### The Cache Invalidation Problem
"There are only two hard things in Computer Science: cache invalidation and naming things." — Phil Karlton

The hardest part of caching is deciding when to evict (delete) an entry from the cache when the underlying data changes. If you don't invalidate the cache after an update, users will see stale data. If you invalidate too aggressively, your cache hit rate plummets and you lose the performance benefit.

## VISUALIZATION_CONFIG
```json
{
  "steps": [
    {
      "title": "CDN Caching",
      "description": "CDNs cache static and dynamic content at edge locations close to users.",
      "code": "// Static assets: long TTL\nCache-Control: public, max-age=31536000, immutable\n// Fingerprinted: /app.abc123.js\n\n// API responses: short TTL\nCache-Control: public, max-age=60, stale-while-revalidate=30\n\n// Dynamic: no cache\nCache-Control: private, no-store\n\n// CDN providers:\n// CloudFront (AWS), Fastly, Cloudflare\n// Edge caching reduces origin load 80-95%",
      "highlight": [
        2,
        6,
        8,
        10,
        11
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "User request",
          "Nearest CDN edge",
          "Cache HIT → serve",
          "MISS → origin fetch",
          "Cache & serve"
        ]
      }
    },
    {
      "title": "Database Query Caching",
      "description": "Cache expensive DB query results — invalidate when underlying data changes.",
      "code": "// Cache expensive aggregation\nasync function getDashboard(userId) {\n  const cacheKey = `dashboard:${userId}`;\n  const ttl = 300; // 5 minutes\n\n  return getOrSet(cacheKey, ttl, async () => {\n    const [orders, revenue, topProducts] = await Promise.all([\n      db.countOrders(userId),\n      db.calcRevenue(userId),\n      db.getTopProducts(userId, 5),\n    ]);\n    return { orders, revenue, topProducts };\n  });\n}",
      "highlight": [
        3,
        4,
        6,
        7,
        8,
        9,
        10
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "Dashboard request",
          "Check cache",
          "HIT → instant return",
          "MISS → 3 DB queries",
          "Cache 5 min TTL"
        ]
      }
    },
    {
      "title": "Distributed Caching with Redis Cluster",
      "description": "Redis Cluster shards data across multiple nodes for horizontal cache scaling.",
      "code": "// Redis Cluster: auto-sharding via hash slots\n// 16384 hash slots distributed across nodes\n\n// Node A: slots 0-5460\n// Node B: slots 5461-10922\n// Node C: slots 10923-16383\n\n// Key → hash → slot → node\n// CLUSTER KEYSLOT user:123 → 6244 → Node B\n\n// Client uses cluster-aware client\nconst redis = new Redis.Cluster([\n  { host: 'node-a', port: 7000 },\n  { host: 'node-b', port: 7001 },\n  { host: 'node-c', port: 7002 },\n]);",
      "highlight": [
        4,
        5,
        6,
        8,
        11,
        12,
        13,
        14
      ],
      "diagram": {
        "kind": "boxes",
        "boxes": [
          {
            "id": "a",
            "label": "Node A: slots 0-5460",
            "color": "#1e88e5"
          },
          {
            "id": "b",
            "label": "Node B: slots 5461-10922",
            "color": "#43a047"
          },
          {
            "id": "c",
            "label": "Node C: slots 10923-16383",
            "color": "#fb8c00"
          }
        ],
        "connections": []
      }
    },
    {
      "title": "Cache Invalidation Strategies",
      "description": "Cache invalidation is hard — time-based TTL, event-based, or version tags.",
      "code": "// Strategy 1: TTL (time-based)\nawait redis.setex('user:123', 300, data);\n// Simple but data can be stale up to 5 min\n\n// Strategy 2: Event-driven invalidation\n// On user update:\nusers.on('update', async (userId) => {\n  await redis.del(`user:${userId}`);\n  await redis.del(`user:${userId}:posts`);\n});\n\n// Strategy 3: Cache versioning\n// Bump version on schema change\nconst version = 'v3';\nawait redis.get(`${version}:user:${userId}`);",
      "highlight": [
        2,
        6,
        7,
        8,
        13,
        14
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "Data changes",
          "Event emitted",
          "Delete cache keys",
          "Next request",
          "Cache miss → DB",
          "Re-populate"
        ]
      }
    },
    {
      "title": "Multi-Level Caching",
      "description": "L1 (in-process), L2 (Redis), L3 (DB) — serve from nearest available cache.",
      "code": "// L1: In-process cache (fastest, small)\nconst localCache = new LRU({ max: 1000, ttl: 60_000 });\n\n// L2: Redis (fast, shared across servers)\nconst redis = new Redis();\n\nasync function get(key) {\n  // Try L1\n  if (localCache.has(key)) return localCache.get(key);\n\n  // Try L2\n  const redisVal = await redis.get(key);\n  if (redisVal) {\n    localCache.set(key, redisVal);\n    return JSON.parse(redisVal);\n  }\n\n  // Load from DB (L3)\n  const dbVal = await db.get(key);\n  await redis.setex(key, 300, JSON.stringify(dbVal));\n  localCache.set(key, dbVal);\n  return dbVal;\n}",
      "highlight": [
        2,
        8,
        12,
        17,
        18,
        19
      ],
      "diagram": {
        "kind": "boxes",
        "boxes": [
          {
            "id": "l1",
            "label": "L1: local (60s)",
            "color": "#1b5e20"
          },
          {
            "id": "l2",
            "label": "L2: Redis (5min)",
            "color": "#43a047"
          },
          {
            "id": "l3",
            "label": "L3: Database",
            "color": "#1e88e5"
          }
        ],
        "connections": [
          {
            "from": "l1",
            "to": "l2"
          },
          {
            "from": "l2",
            "to": "l3"
          }
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner (Cache-Aside / Lazy Loading)
This is the most common pattern. The Application manages the cache. The cache only stores data that has actually been requested. 

```
READ:
App → Cache → HIT? Return data.
App → Cache → MISS? → App reads from DB → App writes to Cache → Return data.

WRITE:
App → DB (write) → App INVALIDATES (deletes) the cache entry.
Next read will be a cache miss, triggering a fresh DB read and cache fill.
```

```python
def get_user(user_id: int):
    # 1. Try the cache first
    cached = redis.get(f"user:{user_id}")
    if cached:
        return json.loads(cached) # Cache HIT!

    # 2. Cache MISS — fetch from database
    user = db.query("SELECT * FROM users WHERE id = %s", user_id)

    # 3. Write to cache with a TTL (Time To Live)
    redis.setex(f"user:{user_id}", 3600, json.dumps(user)) # Expires in 1 hour

    return user

def update_user(user_id: int, new_data: dict):
    db.execute("UPDATE users SET ... WHERE id = %s", user_id)
    
    # INVALIDATE the cache! Next read will be fresh.
    redis.delete(f"user:{user_id}")
```

### Level 2 — Building Blocks (Write-Through)
Every write goes SIMULTANEOUSLY to both the cache AND the database.

```
WRITE:
App → Cache (write) → DB (write synchronously) → Return success.
The cache is always up-to-date. No stale reads!

DOWNSIDE: Every write is slower (must wait for BOTH cache and DB to confirm).
Best for: High read frequency + data that changes frequently and MUST be consistent.
Examples: User profile data, product inventory.
```

### Level 3 — Intermediate (Write-Behind / Write-Back)
The application writes to the cache ONLY. The cache asynchronously persists to the database in the background.

```
WRITE:
App → Cache (write) → Immediately return success! (Ultra-fast!)
Background worker → Reads dirty cache entries → Writes to DB.

DOWNSIDE: Risk of data loss! If the cache server crashes before the background
          worker flushes to DB, the writes in the cache are permanently lost!
Best for: Non-critical, high-velocity writes where some loss is acceptable.
Examples: Like counts, view counters, analytics events.
```

### Level 4 — Advanced (The Thundering Herd / Cache Stampede)
When a very popular cache entry expires, thousands of concurrent requests simultaneously get a cache MISS and all try to query the database at the same moment, overwhelming it.

```python
# SOLUTION 1: Mutex Lock (Probabilistic Early Expiration)
import threading

_lock = threading.Lock()

def get_popular_data(key: str):
    cached = redis.get(key)
    if cached:
        return json.loads(cached)
    
    # Cache MISS — only let ONE thread populate the cache!
    with _lock:
        # Double-check: another thread might have filled it while we waited!
        cached = redis.get(key)
        if cached:
            return json.loads(cached)
        
        data = db.query(...)
        redis.setex(key, 3600, json.dumps(data))
        return data

# SOLUTION 2: Stale-While-Revalidate
# Return the stale cached value immediately, refresh in the background!
def get_with_stale_refresh(key: str):
    entry = redis.get(key)
    
    if entry:
        data = json.loads(entry)
        if data["expires_at"] < time.time():
            # Asynchronously refresh without blocking the request!
            threading.Thread(target=refresh_cache, args=(key,)).start()
        return data["value"] # Return stale but valid data immediately
    
    return fetch_and_cache(key) # Cold start: fetch and cache
```

## REAL_WORLD
A sophisticated caching architecture layers multiple cache levels, called **Multi-Layer Caching** (inspired by CPU L1/L2/L3 caches):

1. **L1 (In-Process Cache):** A small cache inside the application's own memory (e.g., `ConcurrentHashMap` in Java, `lru-cache` in Node.js). Single-digit microseconds. Evicted when the process restarts.
2. **L2 (Distributed Cache — Redis):** Shared across all application instances. Single-digit milliseconds. Survives process restarts.
3. **L3 (CDN — CloudFront/Fastly):** For static and semi-static content. Single-digit milliseconds from the nearest edge node worldwide.
4. **L4 (Database Query Cache — pgBouncer/ProxySQL):** Caches frequent query results at the database connection layer.

## INTERVIEW
**Q: How do you cache paginated results?**
A: Caching paginated results (e.g., `/api/products?page=2&limit=20`) is tricky because any insertion or deletion changes the pagination of ALL subsequent pages. A common approach is to cache only page 1 (the most requested), or to use cursor-based pagination (where the cache key is the cursor ID, not the page number), which is stable against insertions and deletions.

**Q: What is a "Cache Warming" strategy?**
A: Pre-populating the cache before traffic arrives, to avoid cold-start cache misses on deployment. Common strategies: replay recent production traffic against the new cache, pre-load popular item IDs from the DB into the cache during deployment, or use a blue-green deployment where the new instance pre-warms before receiving traffic.

## FEYNMAN CHECK

### Explain Caching Patterns Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Caching Patterns** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Caching Patterns, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Caching Patterns solve? Why would JavaScript/web development be harder without it?**
> **A:** Caching Patterns solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Caching Patterns?**
> **A:** The single most important thing is understanding the *mental model*: Caching Patterns operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Caching Patterns and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Caching Patterns operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Caching Patterns interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Caching Patterns interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Caching Patterns to a senior engineer in two sentences, what would you say?**
> **A:** "Caching Patterns works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Caching Patterns Utility

**Goal:** Implement a real, working Caching Patterns module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `sdcachingpatterns` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir sd-caching-patterns-demo && cd sd-caching-patterns-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Caching Patterns Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Caching Patterns.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createCachingPatterns(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Caching Patterns] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Caching Patterns logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Caching Patterns] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Caching Patterns] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createCachingPatterns({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Caching Patterns] Input cannot be null or undefined`);
    }
    const instance = createCachingPatterns();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Caching Patterns] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Caching Patterns Tests ──');

test('initialises correctly', () => {
  const m = createCachingPatterns();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createCachingPatterns();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Caching Patterns] Initialized with: hello world
[Caching Patterns] Processed: hello world
Result: hello world
[Caching Patterns] Destroyed.

── Caching Patterns Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Caching Patterns in one sentence.**
<details><summary>Answer</summary>

Caching Patterns is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Caching Patterns?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Caching Patterns.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Caching Patterns and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Caching Patterns | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Caching Patterns] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Caching Patterns causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Caching Patterns usage].
</details>

**Q6: What does Caching Patterns look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Caching Patterns typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Caching Patterns correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Caching Patterns:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_sd_caching_patterns(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Caching Patterns in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Caching Patterns in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Caching Patterns', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Caching Patterns at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Caching Patterns uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Caching Patterns interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Caching Patterns is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Caching Patterns relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Caching Patterns
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Caching Patterns to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Caching Patterns fits in the data/control flow.
2. **Choose** the right variant/pattern of Caching Patterns for the scale requirement.
3. **Handle** failure modes: what happens when Caching Patterns is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Caching Patterns is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
