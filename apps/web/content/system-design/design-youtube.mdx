---
slug: "design-youtube"
title: "Design: YouTube / Netflix"
level: 5
---

## WHY
Video streaming is one of the most technically demanding system design challenges. Netflix alone serves 200 million subscribers, delivering 1 Petabyte of video per day. The challenge spans distributed storage, video transcoding pipelines, adaptive bitrate streaming, CDN architecture, and recommendation systems.

YouTube and Netflix share similar core challenges, making this a dual-purpose design problem.

## THEORY
### The Two Major Systems
1. **Video Ingestion Pipeline:** When a creator uploads a video, it goes through a complex backend pipeline before going live.
2. **Video Delivery System:** How the video is served to millions of concurrent viewers globally with minimal buffering.

## VISUALIZATION_CONFIG
```json
{
  "steps": [
    {
      "title": "Video Upload Flow",
      "description": "Upload, transcode to multiple formats, store in CDN, update metadata.",
      "code": "// Upload pipeline\n// 1. User uploads raw video → S3 temp storage\n// 2. Upload service returns upload ID\n// 3. Transcoding job added to queue (SQS/Kafka)\n// 4. Transcoding workers process:\n//    - 1080p, 720p, 480p, 360p, 240p, 144p\n//    - Create thumbnail images at 5s intervals\n//    - Extract audio track\n// 5. Processed files → CDN (CloudFront)\n// 6. Metadata saved to PostgreSQL\n// 7. Search index updated (Elasticsearch)\n// 8. Notification: \"Your video is ready!\"",
      "highlight": [
        4,
        5,
        6,
        7,
        8,
        9
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Upload to S3"
          },
          {
            "label": "Queue transcode job"
          },
          {
            "label": "Workers transcode"
          },
          {
            "label": "CDN storage"
          },
          {
            "label": "Metadata to DB"
          },
          {
            "label": "Search indexed"
          }
        ]
      }
    },
    {
      "title": "Video Delivery Architecture",
      "description": "Adaptive bitrate streaming + CDN edge servers minimize buffering worldwide.",
      "code": "// Adaptive Bitrate Streaming (ABR)\n// Client measures bandwidth → picks quality level\n\n// HLS (HTTP Live Streaming):\n// video/playlist.m3u8  (master playlist)\n// video/1080p/stream.m3u8  (1080p playlist)\n// video/720p/stream.m3u8   (720p playlist)\n// Each playlist → many 6-second chunks (.ts files)\n\n// Player logic:\n// Start at low quality → measure download speed\n// Gradually increase if bandwidth allows\n// Drop quality if buffer runs low\n\n// CDN: cache .m3u8 playlists for 5s (frequently changing)\n//      cache .ts chunks for 24hr (immutable)",
      "highlight": [
        4,
        5,
        6,
        7,
        9,
        10,
        11,
        13,
        14
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Client connects"
          },
          {
            "label": "Download master.m3u8"
          },
          {
            "label": "Measure bandwidth"
          },
          {
            "label": "Select quality"
          },
          {
            "label": "Download chunks"
          },
          {
            "label": "Play video"
          }
        ]
      }
    },
    {
      "title": "View Count",
      "description": "Naive DB increment at scale causes hot-row contention — use buffered counting.",
      "code": "// ❌ Naive: UPDATE views = views + 1 every view\n// At 10K views/sec → DB lock contention on hot row\n\n// ✅ Buffered approach\n// 1. Redis INCR (very fast)\nawait redis.incr(`views:${videoId}`);\n\n// 2. Periodic flush to DB (every 10 min)\nsetInterval(async () => {\n  const videos = await redis.keys('views:*');\n  for (const key of videos) {\n    const count = await redis.getdel(key);\n    const videoId = key.replace('views:', '');\n    await db.query(\n      'UPDATE videos SET view_count = view_count + $1 WHERE id = $2',\n      [count, videoId]\n    );\n  }\n}, 10 * 60 * 1000);",
      "highlight": [
        5,
        9,
        10,
        11,
        13,
        14,
        15
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "View event"
          },
          {
            "label": "Redis INCR"
          },
          {
            "label": "Buffer 10 min"
          },
          {
            "label": "Batch flush to DB"
          },
          {
            "label": "Accurate count"
          }
        ]
      }
    },
    {
      "title": "Recommendation System",
      "description": "Collaborative filtering + content-based recommendations using video embeddings.",
      "code": "// Hybrid recommendation\n// 1. Collaborative filtering: what similar users watched\n// 2. Content-based: similar topics/tags\n// 3. Real-time: what's trending now\n\n// Pre-compute recommendations offline\n// Store in Redis/Cassandra per user\nawait redis.set(`recs:${userId}`, JSON.stringify(recommendations), 'EX', 3600);\n\n// Update signals:\n// Watch duration > 70% → strong positive signal\n// Skip in first 30s → negative signal\n// Like/share → strong positive\n// Context: time of day, device type",
      "highlight": [
        7,
        8,
        11,
        12,
        13,
        14
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "User behavior"
          },
          {
            "label": "Kafka events"
          },
          {
            "label": "ML pipeline"
          },
          {
            "label": "Update embeddings"
          },
          {
            "label": "Recs to Redis"
          },
          {
            "label": "Serve user"
          }
        ]
      }
    },
    {
      "title": "Comment System",
      "description": "Handle nested comments with pagination using cursor-based approach.",
      "code": "// Comments: threaded, paginated\nCREATE TABLE comments (\n  id         BIGSERIAL PRIMARY KEY,\n  video_id   BIGINT NOT NULL,\n  parent_id  BIGINT REFERENCES comments(id), -- null = top-level\n  user_id    BIGINT NOT NULL,\n  content    TEXT NOT NULL,\n  like_count INT DEFAULT 0,\n  created_at TIMESTAMPTZ DEFAULT NOW()\n);\n\nCREATE INDEX idx_comments_video ON comments(video_id, created_at DESC);\nCREATE INDEX idx_comments_parent ON comments(parent_id, like_count DESC);\n\n-- Top-level: paginate by cursor\n-- Replies: load on expand (lazy)",
      "highlight": [
        4,
        5,
        11,
        12,
        14,
        15
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Video page load"
          },
          {
            "label": "Top comments: ORDER BY likes"
          },
          {
            "label": "Cursor pagination"
          },
          {
            "label": "Load replies on click"
          },
          {
            "label": "Nested display"
          }
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner (Scale Estimates)
```
USERS: 2 billion active users per month. 500 hours of video uploaded per minute.

UPLOAD SCALE:
- 500 hours/minute = 30,000 hours/hour of video uploaded
- A 1-hour video at 1080p ≈ 2 GB raw
- Raw storage needed: 30,000 * 2 GB = 60 TB per hour of new content!
- After transcoding (multiple resolutions + compression): still ~10-15 TB/hour

VIEWING SCALE:
- ~1 billion videos viewed per day
- Peak concurrent viewers: ~30-50 million
- Average video chunk size: 10 seconds * 5 Mbps (1080p) = 6.25 MB per chunk
- Peak bandwidth: 50M viewers * 5 Mbps = 250 Tbps of egress! (MASSIVE CDN required)
```

### Level 2 — Building Blocks (The Video Upload Pipeline)
```
RAW VIDEO UPLOAD:
Creator → Upload Service → Raw Video Storage (S3/Blob)
                         → Video Processing Queue (Kafka)
                         → Publish "video-uploaded" event

VIDEO PROCESSING PIPELINE (The heavy lifting):
Kafka Event → Video Transcoding Workers:
  1. Extract metadata (duration, resolution, codec)
  2. Generate thumbnail frames
  3. Transcode to multiple resolutions:
     - 4K UHD (3840×2160) @ 20 Mbps
     - 1080p FHD (1920×1080) @ 8 Mbps
     - 720p HD (1280×720) @ 5 Mbps
     - 480p SD (854×480) @ 2.5 Mbps
     - 360p Low (640×360) @ 1 Mbps
     (This allows Adaptive Bitrate Streaming!)
  4. Split each resolution into 10-second chunks (HLS segments)
  5. Upload all chunks to CDN Origin Storage (S3)
  6. Update video metadata in database (status: "ready")
```

### Level 3 — Intermediate (Adaptive Bitrate Streaming — HLS)
```
ADAPTIVE BITRATE STREAMING (HLS - HTTP Live Streaming):

Instead of one video file, the server serves a "manifest" file (.m3u8):

#EXTM3U
#EXT-X-VERSION:3
#EXT-X-STREAM-INF:BANDWIDTH=8000000,RESOLUTION=1920x1080
https://cdn.youtube.com/videos/abc123/1080p/chunk000.ts
https://cdn.youtube.com/videos/abc123/1080p/chunk001.ts
...

#EXT-X-STREAM-INF:BANDWIDTH=5000000,RESOLUTION=1280x720
https://cdn.youtube.com/videos/abc123/720p/chunk000.ts

The PLAYER (browser/app) monitors the user's download speed:
- If downloading chunks faster than playing: switch UP to higher quality
- If buffering: switch DOWN to lower quality

This is why YouTube smoothly switches from 1080p to 480p when your WiFi gets weak!
All without any server-side session state — pure client-side adaptation!

HLS CHUNKS stored in S3:
  /videos/abc123/
    manifest.m3u8          ← The index file
    1080p/
      chunk_000.ts         ← 10-second video segment
      chunk_001.ts
    720p/
      chunk_000.ts
    480p/
      chunk_000.ts
```

### Level 4 — Advanced (The CDN Strategy for Video)
```
CHALLENGE: 50 million concurrent viewers. Server bandwidth is finite!

SOLUTION: Push-Based CDN (vs Pull-Based for normal content)

Popular Videos (viral or new releases):
  YouTube proactively PUSHES the video chunks to hundreds of CDN edge nodes
  BEFORE anyone requests them, based on trending predictions.
  Result: The video is already at the CDN edge closest to each user.
  No origin server is ever hit.

Long-tail Videos (older, less popular):
  Pull-through CDN: The first viewer in a region triggers the CDN to fetch
  from origin S3 and cache it locally for subsequent viewers.

LIVE STREAMING (additional complexity):
  - No chunks can be pre-generated!
  - Broadcaster sends video stream to ingest servers
  - Chunks are created in near real-time (2-second segments)
  - Published to CDN edge nodes within ~10 seconds of capture
  - Viewers experience ~10-30 second delay vs live (the "live latency")
```

## REAL_WORLD
Netflix's CDN — called **Open Connect** — is the world's largest content delivery network:
- Netflix owns 17,000+ custom CDN servers embedded INSIDE ISPs (Comcast, AT&T, etc.).
- During the day, Netflix analyzes what content users are likely to watch tonight.
- It pre-loads the predicted popular content to the embedded servers overnight.
- By 8pm when people start watching, 95%+ of Netflix traffic is served from inside the ISP, never touching the public internet!
- This is why Netflix can serve 200 million subscribers without owning a traditional cloud CDN.

## INTERVIEW
**Q: How does YouTube handle video search indexing?**
A: Full-text search across billions of videos requires a massive Elasticsearch cluster. Video titles, descriptions, transcripts (auto-generated captions), and tags are indexed. YouTube also uses ML to understand video content — analyzing audio for spoken keywords, visual frames for object recognition (identifying product brands, faces, etc.), to improve search relevance beyond just text matching.

**Q: What is DASH vs HLS for adaptive streaming?**
A: Both are adaptive bitrate streaming protocols. **HLS (HTTP Live Streaming)** was developed by Apple and uses `.ts` segments. Best support on Apple devices. **DASH (Dynamic Adaptive Streaming over HTTP)** is the open standard, using `.mp4` segments. YouTube uses DASH. Both achieve the same goal. Modern players typically support both.

**Q: How would you design the "Recommended Videos" system?**
A: This is a two-stage recommendation pipeline: (1) **Candidate Generation** (fast, broad): For each user, fetch hundreds of candidate videos based on watch history, collaborative filtering (users similar to you watched these), and trending videos. (2) **Ranking** (slow, precise): Run a Deep Neural Network ranking model on the candidates, using 100+ user signals (watch time, click probability, freshness) to rank the best 10-20 recommendations. This is how Netflix and YouTube work — two separate ML models.

## FEYNMAN CHECK

### Explain Design: YouTube / Netflix Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Design: YouTube / Netflix** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Design: YouTube / Netflix, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Design: YouTube / Netflix solve? Why would JavaScript/web development be harder without it?**
> **A:** Design: YouTube / Netflix solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Design: YouTube / Netflix?**
> **A:** The single most important thing is understanding the *mental model*: Design: YouTube / Netflix operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Design: YouTube / Netflix and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Design: YouTube / Netflix operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Design: YouTube / Netflix interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Design: YouTube / Netflix interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Design: YouTube / Netflix to a senior engineer in two sentences, what would you say?**
> **A:** "Design: YouTube / Netflix works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Design: YouTube / Netflix Utility

**Goal:** Implement a real, working Design: YouTube / Netflix module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `designyoutube` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir design-youtube-demo && cd design-youtube-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Design: YouTube / Netflix Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Design: YouTube / Netflix.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createDesign:YouTube/Netflix(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Design: YouTube / Netflix] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Design: YouTube / Netflix logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Design: YouTube / Netflix] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Design: YouTube / Netflix] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createDesign:YouTube/Netflix({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Design: YouTube / Netflix] Input cannot be null or undefined`);
    }
    const instance = createDesign:YouTube/Netflix();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Design: YouTube / Netflix] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Design: YouTube / Netflix Tests ──');

test('initialises correctly', () => {
  const m = createDesign:YouTube/Netflix();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createDesign:YouTube/Netflix();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Design: YouTube / Netflix] Initialized with: hello world
[Design: YouTube / Netflix] Processed: hello world
Result: hello world
[Design: YouTube / Netflix] Destroyed.

── Design: YouTube / Netflix Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Design: YouTube / Netflix in one sentence.**
<details><summary>Answer</summary>

Design: YouTube / Netflix is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Design: YouTube / Netflix?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Design: YouTube / Netflix.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Design: YouTube / Netflix and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Design: YouTube / Netflix | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Design: YouTube / Netflix] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Design: YouTube / Netflix causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Design: YouTube / Netflix usage].
</details>

**Q6: What does Design: YouTube / Netflix look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Design: YouTube / Netflix typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Design: YouTube / Netflix correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Design: YouTube / Netflix:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_design_youtube(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Design: YouTube / Netflix in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Design: YouTube / Netflix in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Design: YouTube / Netflix', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Design: YouTube / Netflix at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Design: YouTube / Netflix uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Design: YouTube / Netflix interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Design: YouTube / Netflix is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Design: YouTube / Netflix relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Design: YouTube / Netflix
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Design: YouTube / Netflix to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Design: YouTube / Netflix fits in the data/control flow.
2. **Choose** the right variant/pattern of Design: YouTube / Netflix for the scale requirement.
3. **Handle** failure modes: what happens when Design: YouTube / Netflix is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Design: YouTube / Netflix is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
