---
slug: "design-netflix"
title: "Design Netflix (Video Streaming)"
level: 5
---

## WHY
"Design Netflix" (or YouTube) is the ultimate test of your understanding of **Network Bandwidth**, **CDNs**, and **Data Storage**. 
Streaming high-definition video to 100 million concurrent users requires pushing Exabytes of data across the internet. If you try to serve video files directly from a central AWS S3 bucket, the latency will be horrific, and AWS egress fees will bankrupt the company in 24 hours.

This problem is divided into two distinct parts:
1. **The Control Plane:** The microservices that handle user login, recommendations, and payments (Standard API design).
2. **The Data Plane:** The physical infrastructure that actually delivers the video files to the user's TV.

## THEORY
### The Magic of Adaptive Bitrate Streaming (ABS)
When you watch Netflix, why does the video sometimes get blurry for 5 seconds and then become HD again, without the video ever pausing to buffer?

**Adaptive Bitrate Streaming (ABS).**
Netflix does not store "The Matrix.mp4" as a single massive 10GB file. 
When a movie is uploaded, the **Transcoding Engine** slices it into 4-second chunks (Chunk 1, Chunk 2). 
It then compresses *each* chunk into 5 different quality levels (4K, 1080p, 720p, 480p, 240p).
The client (your TV) downloads a tiny Manifest file listing these chunks. 
Your TV downloads Chunk 1 in 4K. If your WiFi suddenly drops, your TV detects the network bottleneck and instantly requests Chunk 2 in 480p! Because the chunks are only 4 seconds long, the video perfectly adapts to your network speed in real-time, preventing buffering.

## VISUALIZATION_CONFIG
```json
{
  "steps": [
    {
      "title": "Content Storage Architecture",
      "description": "Netflix stores 15K titles × 4K+ video files each → 100+ PB. Content split: master storage + CDN.",
      "code": "// Content lifecycle\n// 1. Ingest: original master file → S3 (100GB-1TB per title)\n// 2. Encode: 4K, 1080p, 720p, 480p, 360p\n//    + HDR, Dolby Vision, Atmos audio\n//    + Multiple languages (30+ audio tracks)\n//    + Subtitles (20+ languages)\n// 3. QA: automated quality checks\n// 4. Publish: distribute to Open Connect CDN\n// 5. Serve: users stream from nearest CDN node\n\n// Open Connect: Netflix's own CDN\n// 15,000+ CDN servers in 5,000+ locations",
      "highlight": [
        3,
        4,
        5,
        6,
        10,
        11,
        12
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Original file"
          },
          {
            "label": "S3 master"
          },
          {
            "label": "Encoding jobs"
          },
          {
            "label": "Multiple formats"
          },
          {
            "label": "Open Connect CDN"
          },
          {
            "label": "Users stream"
          }
        ]
      }
    },
    {
      "title": "Microservices Architecture",
      "description": "Netflix runs 1000+ microservices — each team owns their service end-to-end.",
      "code": "// Key Netflix services\nconst services = {\n  zuul:        'API Gateway: routing, auth, rate limiting',\n  eureka:      'Service discovery: find service instances',\n  hystrix:     'Circuit breaker: prevent cascade failures',\n  ribbon:      'Client-side load balancing',\n  archaius:    'Dynamic configuration management',\n  zuul2:       'Edge gateway (v2)',\n  conductor:   'Workflow orchestration',\n  iceberg:     'Table format for data lake',\n};\n\n// Each service:\n// - Deployed independently via Spinnaker\n// - Runs on AWS EC2/ECS\n// - Uses Cassandra or MySQL for storage",
      "highlight": [
        3,
        4,
        5,
        6,
        7,
        13,
        14,
        15
      ],
      "diagram": {
        "kind": "boxes",
        "items": [
          {
            "label": "Zuul (Gateway)",
            "color": "#1e88e5"
          },
          {
            "label": "Eureka (Discovery)",
            "color": "#43a047"
          },
          {
            "label": "Hystrix (Circuit)",
            "color": "#fb8c00"
          }
        ]
      }
    },
    {
      "title": "Recommendation Engine",
      "description": "Netflix uses ML-based recommendations — collaborative filtering + content features.",
      "code": "// Recommendation signals:\n// - Watch history + completion rate\n// - Similar users (collaborative filtering)\n// - Content metadata (genre, cast, themes)\n// - Time of day, device, country\n// - Trending in your region\n\n// A/B testing at scale:\n// Netflix runs 300+ A/B tests simultaneously\n// Different algorithms for different user segments\n\n// Offline pipeline (hourly/daily):\n// Spark jobs → ML model training → Feature store\n// Precomputed recs stored in Cassandra per user\n// Served by recommendation API (< 10ms)",
      "highlight": [
        2,
        3,
        4,
        5,
        8,
        12,
        13,
        14
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "User behavior signals"
          },
          {
            "label": "Spark ML pipeline"
          },
          {
            "label": "Trained model"
          },
          {
            "label": "Cassandra recs store"
          },
          {
            "label": "API serves < 10ms"
          }
        ]
      }
    },
    {
      "title": "Chaos Engineering",
      "description": "Netflix invented Chaos Monkey — deliberately break things in production to find weaknesses.",
      "code": "// Chaos Engineering principles\n// 1. Define steady state (normal metrics)\n// 2. Hypothesize it will hold under chaos\n// 3. Introduce chaos variables\n// 4. Measure if steady state maintained\n\n// Chaos tools Netflix built:\n// Chaos Monkey: kills random EC2 instances\n// Chaos Gorilla: takes down entire AZ\n// Chaos Kong: simulates regional failure\n// Latency Monkey: adds network delays\n\n// Goal: build systems that survive failures\n// Not if, but when — production testing builds confidence",
      "highlight": [
        7,
        8,
        9,
        10,
        12,
        13
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Normal operation"
          },
          {
            "label": "Chaos Monkey kills server"
          },
          {
            "label": "Auto-recovery?"
          },
          {
            "label": "Measure impact"
          },
          {
            "label": "Fix weaknesses"
          },
          {
            "label": "Repeat"
          }
        ]
      }
    },
    {
      "title": "Caching Strategy",
      "description": "EVCache (Memcached-based) replaces Redis for Netflix — deployed in every AWS AZ.",
      "code": "// EVCache: Netflix's distributed cache\n// Built on Memcached + replication across AZs\n\n// Per-user data:\n// User profile: 1 hour TTL\n// Watch history: 5 min TTL (updates often)\n// My List: 30 sec TTL\n\n// Content metadata:\n// Movie details: 24 hours (rarely changes)\n// Personalized rows: 5 minutes\n\n// Cache hit rate: 99%+\n// EVCache handles 30M operations/sec\n// Deployed across 100K+ instances globally",
      "highlight": [
        5,
        6,
        7,
        10,
        11,
        13,
        14
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Request"
          },
          {
            "label": "EVCache (AZ-local)"
          },
          {
            "label": "99% HIT"
          },
          {
            "label": "MISS → backend service"
          },
          {
            "label": "Populate cache"
          }
        ]
      }
    }
  ]
}
```

## CODE

*(In a System Design interview, you draw the architecture diagram and explain the data flow!)*

### Level 1 — The Control Plane (Standard Microservices)
**The Explanation:**
1. "The user opens the Netflix app. The app connects to the **API Gateway** via REST/GraphQL."
2. "The Gateway routes the request to the **User Service** (Auth/Billing) and the **Recommendation Engine**."
3. "Because this data (Movie Titles, Thumbnails) is tiny, we can store it in a standard distributed database (like Cassandra or PostgreSQL) and cache it heavily in Redis."
4. "The user clicks 'Play'."

### Level 2 — The Transcoding Pipeline (Write Path)
*The Interviewer Trap:* "A studio uploads a 500GB master file of a new movie. Walk me through the upload process."

**The Explanation:**
1. "The studio uploads the massive raw file into **AWS S3** via Multi-part Upload."
2. "This triggers an Event in Kafka, waking up the **Transcoding Workers**."
3. "Transcoding is incredibly CPU intensive. We use a Map-Reduce approach. We split the 2-hour movie into 10-minute segments."
4. "We distribute these segments across hundreds of GPU-enabled worker nodes. They transcode the video into H.264/H.265 formats, slice it into 4-second chunks, and create the ABS Manifest files."
5. "The finished chunks are saved back into S3 (The Master Storage)."

### Level 3 — The Data Plane (Content Delivery Networks)
*The Interviewer Trap:* "The Master Chunks are in AWS S3 in Virginia. A user in Tokyo clicks Play. How does the video get to them without stuttering?"

**The Explanation:**
1. "We absolutely do NOT serve the video from S3. The trans-pacific latency would cause constant buffering."
2. "We use a **CDN (Content Delivery Network)**. A CDN is a massive global network of proxy servers."
3. "When the Tokyo user clicks Play, DNS routing automatically connects them to a CDN Edge Server physically located in Tokyo."
4. "If the Tokyo Edge Server already has 'The Matrix' in its local hard drive (Cache Hit), it streams it directly to the user over the ultra-fast local network."
5. "If it doesn't have it (Cache Miss), the Edge Server pulls the chunks from the Master S3 bucket in Virginia, streams it to the user, and *saves a copy locally* so the next person in Tokyo gets a Cache Hit."

### Level 4 — Open Connect (Netflix's Secret Weapon)
*The Advanced Interview Move:* If you want to ace the Netflix question, you must mention **Open Connect**.

"Relying on commercial CDNs (like Akamai or Cloudflare) for 15% of the world's internet traffic is too expensive. Netflix built their own custom CDN called **Open Connect**."
"Netflix literally builds physical, custom red server boxes (Open Connect Appliances - OCAs). They mail these hardware boxes directly to local Internet Service Providers (like Comcast or AT&T) and tell them to plug them directly into their local neighborhood routers."
"During the night (when traffic is low), Netflix pre-computes what movies will be popular the next day (using machine learning). They proactively push the video chunks from AWS directly onto the hard drives of those red boxes sitting inside the Comcast facility."
"When you click Play, the video doesn't even travel across the public internet! It streams directly from the Comcast router down the street into your living room."

## REAL_WORLD
**The Thundering Herd Problem (Live Streaming)**
Netflix is VOD (Video on Demand). But what if you are asked to design Twitch or YouTube Live?
Live streaming breaks standard CDNs. If 1 million people watch the SuperBowl, they all request the exact same 4-second video chunk at the exact same millisecond. 
If the CDN Edge Server doesn't have the chunk yet, it might panic and send 1 million simultaneous "Cache Miss" requests back to the Origin Server (AWS), instantly crashing the Origin server. This is the **Thundering Herd Problem**.

*The Solution:* **Request Coalescing (Collapsing).**
When the CDN Edge Server receives 1 million requests for the SuperBowl chunk, it intelligently pauses 999,999 of them. It sends exactly ONE request to the Origin Server. When the chunk arrives a millisecond later, the Edge Server instantly copies it and serves it to all 1 million waiting connections simultaneously, protecting the Origin server from the massive traffic spike.

## INTERVIEW
**Q: A user searches for a movie. Does the Search request go through the CDN or the API Gateway?**
A: **The API Gateway.**
You must fundamentally separate Static Media (Video Chunks, Image Thumbnails, JavaScript bundles) from Dynamic API requests (Search, Login, Payment). 
CDNs are designed to cache massive, static binary files. A Search Request (`GET /search?q=matrix`) is a dynamic database query. It must bypass the CDN and hit the core API Gateway, which routes it to the Elasticsearch cluster. 
*(Exception: API Gateways can have their own caching layers, but in a system design interview diagram, you must visually separate the CDN serving video files from the Application Load Balancer serving JSON data).*

## FEYNMAN CHECK

### Explain Design Netflix (Video Streaming) Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Design Netflix (Video Streaming)** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Design Netflix (Video Streaming), they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Design Netflix (Video Streaming) solve? Why would JavaScript/web development be harder without it?**
> **A:** Design Netflix (Video Streaming) solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Design Netflix (Video Streaming)?**
> **A:** The single most important thing is understanding the *mental model*: Design Netflix (Video Streaming) operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Design Netflix (Video Streaming) and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Design Netflix (Video Streaming) operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Design Netflix (Video Streaming) interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Design Netflix (Video Streaming) interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Design Netflix (Video Streaming) to a senior engineer in two sentences, what would you say?**
> **A:** "Design Netflix (Video Streaming) works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Design Netflix (Video Streaming) Utility

**Goal:** Implement a real, working Design Netflix (Video Streaming) module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `designnetflix` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir design-netflix-demo && cd design-netflix-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Design Netflix (Video Streaming) Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Design Netflix (Video Streaming).
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createDesignNetflix(VideoStreaming)(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Design Netflix (Video Streaming)] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Design Netflix (Video Streaming) logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Design Netflix (Video Streaming)] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Design Netflix (Video Streaming)] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createDesignNetflix(VideoStreaming)({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Design Netflix (Video Streaming)] Input cannot be null or undefined`);
    }
    const instance = createDesignNetflix(VideoStreaming)();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Design Netflix (Video Streaming)] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Design Netflix (Video Streaming) Tests ──');

test('initialises correctly', () => {
  const m = createDesignNetflix(VideoStreaming)();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createDesignNetflix(VideoStreaming)();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Design Netflix (Video Streaming)] Initialized with: hello world
[Design Netflix (Video Streaming)] Processed: hello world
Result: hello world
[Design Netflix (Video Streaming)] Destroyed.

── Design Netflix (Video Streaming) Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Design Netflix (Video Streaming) in one sentence.**
<details><summary>Answer</summary>

Design Netflix (Video Streaming) is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Design Netflix (Video Streaming)?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Design Netflix (Video Streaming).**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Design Netflix (Video Streaming) and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Design Netflix (Video Streaming) | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Design Netflix (Video Streaming)] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Design Netflix (Video Streaming) causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Design Netflix (Video Streaming) usage].
</details>

**Q6: What does Design Netflix (Video Streaming) look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Design Netflix (Video Streaming) typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Design Netflix (Video Streaming) correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Design Netflix (Video Streaming):
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_design_netflix(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Design Netflix (Video Streaming) in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Design Netflix (Video Streaming) in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Design Netflix (Video Streaming)', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Design Netflix (Video Streaming) at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Design Netflix (Video Streaming) uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Design Netflix (Video Streaming) interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Design Netflix (Video Streaming) is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Design Netflix (Video Streaming) relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Design Netflix (Video Streaming)
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Design Netflix (Video Streaming) to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Design Netflix (Video Streaming) fits in the data/control flow.
2. **Choose** the right variant/pattern of Design Netflix (Video Streaming) for the scale requirement.
3. **Handle** failure modes: what happens when Design Netflix (Video Streaming) is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Design Netflix (Video Streaming) is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
