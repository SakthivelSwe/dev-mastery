---
slug: "sd-requirements"
title: "Gathering Requirements"
level: 3
---

## WHY
In Software Engineering, the #1 cause of project failure is not choosing the wrong database, nor is it writing inefficient algorithms. The #1 cause of project failure is **building the wrong system**. 

If you dive straight into drawing architecture diagrams without understanding the exact constraints, scale, and business goals of the product, you will over-engineer a simple internal tool, or drastically under-engineer a massive global platform. 

In a System Design Interview, if the interviewer says "Design Twitter," and you immediately start talking about Cassandra databases and Redis clusters, **you have already failed.** The very first step of any system design process is **Requirements Gathering**.

## THEORY
### Functional vs. Non-Functional Requirements
To design a system, you must slice the requirements into two distinct categories:

1. **Functional Requirements (The "What")**
   - What exactly does the system do?
   - What are the core features the user will interact with?
   - *Example (Twitter)*: Users can post tweets. Users can follow other users. Users can see a timeline of tweets from people they follow.

2. **Non-Functional Requirements (The "How" / Constraints)**
   - How well does the system perform?
   - What are the scale, latency, availability, and consistency constraints?
   - *Example (Twitter)*: The system must be Highly Available (HA). Latency for viewing the timeline must be under 200ms. The system must handle 100 million Daily Active Users (DAU).

## VISUALIZATION_CONFIG
```json
{
  "steps": [
    {
      "title": "Functional vs Non-Functional Requirements",
      "description": "Functional = what the system does. Non-functional = how well it does it (performance, reliability, security).",
      "code": "// Functional Requirements (WHAT)\n// - Users can post tweets up to 280 characters\n// - Users can follow/unfollow other users\n// - Users can see a timeline of followed users' tweets\n// - Users can like and retweet\n\n// Non-Functional Requirements (HOW)\n// - 99.99% availability (< 52 min/year downtime)\n// - Timeline load < 200ms at p99\n// - Support 100M daily active users\n// - Write: 100K tweets/sec, Read: 1M reads/sec",
      "highlight": [
        2,
        3,
        4,
        5,
        8,
        9,
        10,
        11
      ],
      "diagram": {
        "kind": "boxes",
        "boxes": [
          {
            "id": "f",
            "label": "Functional: WHAT",
            "color": "#1e88e5"
          },
          {
            "id": "n",
            "label": "Non-Functional: HOW WELL",
            "color": "#43a047"
          }
        ],
        "connections": []
      }
    },
    {
      "title": "Back of the Envelope Math",
      "description": "Estimate scale before designing — requests/sec, storage, bandwidth.",
      "code": "// Twitter-scale estimation\n// DAU: 100M users, 50% active/day\n// Tweets/day: 50M users × 5 tweets = 250M tweets/day\n// Writes/sec: 250M / 86400 ≈ 2900 writes/sec\n\n// Storage\n// Tweet: 280 chars × 2 bytes = 560 bytes\n// + metadata: ~500 bytes → ~1KB/tweet\n// Daily storage: 250M × 1KB = 250GB/day\n// 5 years: 250 × 365 × 5 = 456TB\n\n// Reads (read-heavy: 100:1 read:write)\n// Read QPS: 2900 × 100 = 290K reads/sec",
      "highlight": [
        3,
        4,
        7,
        8,
        9,
        12
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "DAU estimate",
          "Action rate",
          "QPS calc",
          "Storage calc",
          "Bandwidth calc"
        ]
      }
    },
    {
      "title": "CAP Theorem in Requirements",
      "description": "Decide early: does your system need strong consistency or high availability?",
      "code": "// Twitter timeline: choose AP (availability + partition)\n// - Users can tolerate slightly stale timelines\n// - We prefer it always loads vs. strict consistency\n\n// Banking: choose CP (consistency + partition)\n// - Account balance MUST be accurate\n// - Better to fail than show wrong balance\n\n// Cassandra: AP — always writes, eventual consistency\n// MongoDB: CP — consistent reads, may reject during partition",
      "highlight": [
        2,
        3,
        6,
        7,
        9,
        10
      ],
      "diagram": {
        "kind": "boxes",
        "boxes": [
          {
            "id": "c",
            "label": "Consistency",
            "color": "#1e88e5"
          },
          {
            "id": "a",
            "label": "Availability",
            "color": "#43a047"
          },
          {
            "id": "p",
            "label": "Partition Tolerance",
            "color": "#fb8c00"
          }
        ],
        "connections": [
          {
            "from": "c",
            "to": "p"
          },
          {
            "from": "a",
            "to": "p"
          }
        ]
      }
    },
    {
      "title": "Defining SLAs",
      "description": "SLA = contract. SLO = internal target. SLI = measured metric.",
      "code": "// SLI (metric): actual measured value\n// 99.95% of requests completed in < 200ms\n\n// SLO (objective): internal target\n// 99.9% availability per month\n// p99 latency < 300ms\n\n// SLA (agreement): customer-facing contract\n// 99.9% uptime; if breached → credit\n\n// Error budget = 1 - SLO = 0.1%\n// 0.1% of 30 days = 43.2 minutes of allowed downtime",
      "highlight": [
        2,
        5,
        6,
        9,
        11,
        12
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "SLI (metric)",
          "SLO (internal target)",
          "SLA (contract)",
          "Error Budget"
        ]
      }
    },
    {
      "title": "Capacity Planning",
      "description": "Plan for 10x current load — design headroom into every component.",
      "code": "// Capacity planning example\nconst currentQPS = 1000;\nconst growthFactor = 10; // 10x for headroom\nconst targetQPS = currentQPS * growthFactor; // 10,000\n\n// Per server capacity\nconst qpsPerServer = 500;\nconst serversNeeded = Math.ceil(targetQPS / qpsPerServer); // 20\n\n// Add redundancy (N+2 rule)\nconst totalServers = serversNeeded + 2; // 22\n\n// Auto-scaling target: 60-70% utilization max",
      "highlight": [
        3,
        4,
        7,
        10,
        12
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "Current Load",
          "10x Factor",
          "Per-server Capacity",
          "Server Count",
          "+2 Redundancy"
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner (The "Happy Path" Functional Requirements)
Start by defining the absolute minimum viable product (MVP) features. Do not overcomplicate the system with edge cases yet.

**Scenario:** "Design a URL Shortener (like Bitly)"

**Functional Requirements:**
1. Given a long URL, the system returns a much shorter, unique URL.
2. When a user clicks the short URL, they are instantly redirected to the original long URL.
3. Users can optionally pick a custom alias for their short URL (e.g., `bit.ly/my-custom-name`).
4. Links expire after a default timespan (e.g., 5 years).

### Level 2 — Building Blocks (Non-Functional Requirements)
Once the features are defined, you must define the constraints that will dictate your database choices and server architecture.

**Non-Functional Requirements for the URL Shortener:**
1. **High Availability:** If the service goes down, millions of links across the internet stop working. Availability is strictly prioritized over Consistency.
2. **Low Latency:** URL redirection should happen in real-time with minimal delay (e.g., < 100ms).
3. **Scale:** The system will generate 100 million new URLs per month.
4. **Read/Write Ratio:** The system is extremely read-heavy. (For every 1 link created, it is clicked 100 times). The Read:Write ratio is 100:1.

### Level 3 — Intermediate (Defining the API Contracts)
Once you have the requirements, you immediately define the API boundaries. This proves you understand how the Client and Server will communicate before you worry about how the Server stores the data.

```typescript
// 1. Create Short URL API
POST /api/v1/data/shorten
Request Body:
{
  "original_url": "https://www.example.com/very/long/path/article",
  "custom_alias": "my-article", // Optional
  "expiration_date": "2030-01-01T00:00:00Z" // Optional
}

Response (201 Created):
{
  "short_url": "https://tiny.url/xyz123"
}
```

```typescript
// 2. Redirect API
GET /api/v1/{short_alias}
Response (301 Moved Permanently):
Headers: {
  "Location": "https://www.example.com/very/long/path/article"
}
```

### Level 4 — Advanced (Extended / Out-of-Scope Requirements)
A Senior Engineer knows exactly where to draw the line. In a 45-minute interview (or a tight Agile sprint), you cannot build the entire universe. You must explicitly state what you are **NOT** building.

**Out of Scope (To be handled later):**
- User Accounts and Authentication.
- Advanced Analytics (tracking exactly which countries clicked the link).
- Billing and Subscription tiers.
- A frontend web UI.

By explicitly stating these are out of scope, you prevent scope creep and focus your architecture entirely on the core scaling problem.

## REAL_WORLD
In the real world, Non-Functional Requirements are often dictated by **Service Level Agreements (SLAs)**. 

If you are building an API for a bank, the SLA might guarantee **99.999% Uptime** (known as "Five Nines"). Five nines means the system is legally only allowed to be down for **5 minutes and 15 seconds per YEAR**. 
If you fail to meet this requirement, your company owes the client millions of dollars in penalty payouts. Designing a system for 99.9% uptime is relatively easy. Designing it for 99.999% uptime requires massive geographic redundancy, automated failovers, and chaos engineering, multiplying the engineering cost by 10x.

## INTERVIEW
**Q: The interviewer asks "Design Netflix." What is the very first thing you say?**
A: You ask clarifying questions to narrow the scope. *"Are we designing the video streaming backend (content delivery), or the client-facing web application (browsing, recommendations, user profiles)?"* You cannot design both in 45 minutes.

**Q: Why do we care about the Read/Write Ratio?**
A: It dictates your caching and database strategy. If a system is Read-Heavy (like Twitter or a URL Shortener), you will heavily invest in caching layers (Redis/Memcached) and Database Read Replicas. If a system is Write-Heavy (like IoT sensor logging or financial transaction ledgers), caching won't help you; you need a database optimized for massive concurrent appends (like Cassandra or a Time-Series DB).

**Q: What is the difference between a 301 and 302 redirect for our URL Shortener?**
A: A `301 (Moved Permanently)` tells the browser to cache the redirect forever. This drastically reduces the load on your servers, but makes it impossible to track analytics (because the browser stops asking your server where the link goes). A `302 (Found / Temporary)` forces the browser to hit your server every single time, increasing server load but allowing you to track exactly how many times the link was clicked.

## FEYNMAN CHECK

### Explain Gathering Requirements Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Gathering Requirements** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Gathering Requirements, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Gathering Requirements solve? Why would JavaScript/web development be harder without it?**
> **A:** Gathering Requirements solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Gathering Requirements?**
> **A:** The single most important thing is understanding the *mental model*: Gathering Requirements operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Gathering Requirements and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Gathering Requirements operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Gathering Requirements interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Gathering Requirements interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Gathering Requirements to a senior engineer in two sentences, what would you say?**
> **A:** "Gathering Requirements works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Gathering Requirements Utility

**Goal:** Implement a real, working Gathering Requirements module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `sdrequirements` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir sd-requirements-demo && cd sd-requirements-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Gathering Requirements Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Gathering Requirements.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createGatheringRequirements(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Gathering Requirements] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Gathering Requirements logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Gathering Requirements] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Gathering Requirements] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createGatheringRequirements({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Gathering Requirements] Input cannot be null or undefined`);
    }
    const instance = createGatheringRequirements();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Gathering Requirements] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Gathering Requirements Tests ──');

test('initialises correctly', () => {
  const m = createGatheringRequirements();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createGatheringRequirements();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Gathering Requirements] Initialized with: hello world
[Gathering Requirements] Processed: hello world
Result: hello world
[Gathering Requirements] Destroyed.

── Gathering Requirements Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Gathering Requirements in one sentence.**
<details><summary>Answer</summary>

Gathering Requirements is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Gathering Requirements?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Gathering Requirements.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Gathering Requirements and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Gathering Requirements | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Gathering Requirements] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Gathering Requirements causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Gathering Requirements usage].
</details>

**Q6: What does Gathering Requirements look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Gathering Requirements typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Gathering Requirements correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Gathering Requirements:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_sd_requirements(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Gathering Requirements in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Gathering Requirements in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Gathering Requirements', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Gathering Requirements at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Gathering Requirements uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Gathering Requirements interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Gathering Requirements is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Gathering Requirements relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Gathering Requirements
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Gathering Requirements to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Gathering Requirements fits in the data/control flow.
2. **Choose** the right variant/pattern of Gathering Requirements for the scale requirement.
3. **Handle** failure modes: what happens when Gathering Requirements is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Gathering Requirements is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
