---
slug: "design-distributed-cache"
title: "Design a Distributed Cache (Redis)"
level: 5
---

## WHY
If your application queries the database for the exact same data 1,000 times a second, the database will eventually crash under the load. 
A Cache sits between the application and the database. It stores the results of expensive queries in RAM, returning them in microseconds instead of milliseconds.

However, a single server only has so much RAM. When you need to cache 5 Terabytes of data, you must design a **Distributed Cache** (like Memcached or Redis Cluster) that seamlessly spans across dozens of independent servers while presenting a unified interface to the application.

## THEORY
### Cache Eviction Policies
If you have 10GB of RAM, and you try to write the 11th Gigabyte, the cache must delete old data to make room. How does it decide what to delete?
1. **LRU (Least Recently Used):** The industry standard. Evicts the data that hasn't been requested in the longest amount of time.
2. **LFU (Least Frequently Used):** Evicts data that has the lowest total access count. (Complex to implement efficiently).
3. **FIFO (First In, First Out):** Evicts the oldest data, regardless of how popular it is. (Rarely used in production).

## VISUALIZATION_CONFIG
```json
{
  "steps": [
    {
      "title": "Distributed Cache Design",
      "description": "Design Memcached/Redis-like distributed cache with consistent hashing.",
      "code": "// Requirements:\n// - Store key-value pairs (up to 1MB per value)\n// - get(key), set(key, value, ttl), delete(key)\n// - 1M QPS, < 1ms latency\n// - Distributed: data spread across N nodes\n// - Fault tolerant: node failure doesn't cause data loss\n\n// Scale estimates:\n// 10M cached items, avg 1KB = 10GB total data\n// 10 nodes × 16GB RAM each = 160GB capacity\n// Each node handles 100K QPS",
      "highlight": [
        2,
        3,
        4,
        5,
        6,
        8,
        9,
        10
      ],
      "diagram": {
        "kind": "boxes",
        "boxes": [
          {
            "id": "c",
            "label": "Client",
            "color": "#9e9e9e"
          },
          {
            "id": "n1",
            "label": "Cache Node 1",
            "color": "#1e88e5"
          },
          {
            "id": "n2",
            "label": "Cache Node 2",
            "color": "#43a047"
          },
          {
            "id": "n3",
            "label": "Cache Node 3",
            "color": "#fb8c00"
          }
        ],
        "connections": [
          {
            "from": "c",
            "to": "n1"
          },
          {
            "from": "c",
            "to": "n2"
          },
          {
            "from": "c",
            "to": "n3"
          }
        ]
      }
    },
    {
      "title": "Consistent Hashing for Routing",
      "description": "Route cache keys to nodes using consistent hashing — minimal data movement on changes.",
      "code": "class DistributedCache {\n  constructor(nodes) {\n    this.ring = new ConsistentHashRing(nodes, virtualNodes=150);\n  }\n\n  async get(key) {\n    const node = this.ring.getNode(key);\n    return node.get(key);\n  }\n\n  async set(key, value, ttl) {\n    const node = this.ring.getNode(key);\n    return node.set(key, value, ttl);\n  }\n\n  async addNode(newNode) {\n    this.ring.addNode(newNode);\n    await this.rebalance(); // move ~1/N of keys\n  }\n}",
      "highlight": [
        3,
        6,
        7,
        11,
        12,
        16,
        17
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "key → hash",
          "Consistent hash ring",
          "Find next node",
          "GET/SET on that node",
          "Add node → 1/N migrates"
        ]
      }
    },
    {
      "title": "Eviction and Memory Management",
      "description": "LRU eviction with a doubly-linked list + hash map for O(1) operations.",
      "code": "class LRUCache {\n  constructor(capacity) {\n    this.capacity = capacity;\n    this.map = new Map();            // key → node\n    this.head = { next: null };      // dummy head (most recent)\n    this.tail = { prev: null };      // dummy tail (least recent)\n    this.head.next = this.tail;\n    this.tail.prev = this.head;\n  }\n\n  get(key) {\n    if (!this.map.has(key)) return -1;\n    const node = this.map.get(key);\n    this.moveToFront(node); // mark as recently used\n    return node.value;\n  }\n\n  set(key, value) {\n    if (this.map.size >= this.capacity) this.evict();\n    // ...\n  }\n}",
      "highlight": [
        4,
        5,
        6,
        13,
        14,
        18,
        19
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "GET: move to front",
          "SET: add to front",
          "Capacity full",
          "Evict from tail (LRU)",
          "O(1) all operations"
        ]
      }
    },
    {
      "title": "Replication for HA",
      "description": "Each shard has primary + replica. Failover when primary dies.",
      "code": "// Master-replica per shard\nclass CacheCluster {\n  constructor() {\n    this.shards = [\n      { primary: 'node-1', replica: 'node-1r' },\n      { primary: 'node-2', replica: 'node-2r' },\n      { primary: 'node-3', replica: 'node-3r' },\n    ];\n  }\n\n  // Writes go to primary, sync to replica\n  async set(key, value, ttl) {\n    const shard = this.getShard(key);\n    await shard.primary.set(key, value, ttl);\n    await shard.replica.set(key, value, ttl); // async replication\n  }\n\n  // Reads: try primary, fallback to replica\n  async get(key) {\n    try { return await shard.primary.get(key); }\n    catch { return shard.replica.get(key); }\n  }\n}",
      "highlight": [
        5,
        6,
        7,
        13,
        14,
        19,
        20
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "Write → Primary",
          "Replicate → Replica",
          "Primary fails",
          "Promote replica",
          "Reads continue"
        ]
      }
    },
    {
      "title": "Monitoring and Metrics",
      "description": "Track hit rate, eviction rate, latency p99 — alert on degradation.",
      "code": "// Key cache metrics\nconst metrics = {\n  hitRate: hits / (hits + misses),     // target: > 95%\n  evictionRate: evictions / requests,  // alert if > 1%\n  memoryUsage: usedMem / totalMem,    // alert if > 80%\n  p99Latency: latencyHistogram.p99,   // target: < 1ms\n  connectionsActive: pool.size,\n};\n\n// Prometheus metrics\ncacheHitsTotal.inc();\ncacheMissesTotal.inc();\ncacheLatency.observe(latencyMs);\n\n// Alert rules:\n// hit_rate < 0.90 → investigate eviction/TTL\n// p99 > 10ms → investigate hotspots",
      "highlight": [
        3,
        4,
        5,
        6,
        9,
        10,
        11,
        14,
        15
      ],
      "diagram": {
        "kind": "boxes",
        "boxes": [
          {
            "id": "h",
            "label": "Hit Rate > 95%",
            "color": "#43a047"
          },
          {
            "id": "l",
            "label": "p99 < 1ms",
            "color": "#1e88e5"
          },
          {
            "id": "m",
            "label": "Memory < 80%",
            "color": "#fb8c00"
          }
        ],
        "connections": []
      }
    }
  ]
}
```

## CODE

*(In a System Design interview, you don't write code, you draw the architecture diagram and explain the data flow!)*

### Level 1 — The Data Structure (LRU Cache)
*The Interviewer:* "How do you implement an LRU cache efficiently on a single machine?"

**Your Answer:** "We need $O(1)$ lookups and $O(1)$ evictions. We combine two data structures: a **Hash Map** and a **Doubly Linked List**."
1. "The Hash Map stores the `Key` mapped to the specific Node in the Linked List. This gives us $O(1)$ lookups."
2. "The Doubly Linked List tracks the recency. The 'Head' of the list is the most recently used item. The 'Tail' is the oldest."
3. "When a user requests a Key, we find it in the Hash Map, remove the node from the middle of the Linked List, and move it to the Head. This takes $O(1)$ time."
4. "When the cache is full, we simply delete the node at the Tail of the Linked List and remove it from the Hash Map."

### Level 2 — Distributing the Data (Consistent Hashing)
*The Interviewer:* "Great, but our data is 5 Terabytes. We need 10 cache servers. How does the application know which server holds the data?"

**The Explanation:**
1. "We use **Consistent Hashing**. We conceptually place our 10 cache servers onto a Hash Ring."
2. "When the application wants to read or write `user:123`, the Cache Client library hashes the string `user:123`."
3. "It walks clockwise on the Hash Ring to find the responsible server (e.g., Server 3)."
4. "The client opens a TCP connection directly to Server 3 and reads/writes the data."
*(Crucially: There is no central routing server. The application clients do the math themselves, eliminating any Single Point of Failure or network bottleneck).*

### Level 3 — High Availability (Replication)
*The Interviewer Trap:* "Server 3's motherboard burns out. It crashes. All the cache data on Server 3 is gone. Suddenly, all the traffic destined for Server 3 misses the cache and hits the database, causing a Thundering Herd that crashes our primary PostgreSQL database. How do we prevent this?"

**The Solution: Master-Slave Replication within the Ring.**
1. "We cannot rely on a single physical node. We modify our Hash Ring so that every position on the ring is a **Replica Set**."
2. "Server 3 is actually a Master node and two Slave (Read Replica) nodes."
3. "When the client writes to Server 3 (Master), it asynchronously replicates that data to the two Slaves."
4. "If Server 3 (Master) explodes, an automated monitoring system (like Redis Sentinel or Zookeeper) detects the heartbeat failure."
5. "The Sentinel promotes one of the Slaves to become the new Master and updates the routing table. The cache data survives, and the database is protected!"

### Level 4 — Cache Invalidation Patterns
The hardest problem in computer science is Cache Invalidation. If the data in PostgreSQL changes, how do we update the cache?

1. **Cache-Aside (The Standard):** 
   - The application reads from the Cache. 
   - If miss, it reads from the DB, saves to the Cache, and returns.
   - When *writing* data, the application updates the DB, and then explicitly deletes the key from the Cache. The next read will naturally pull the fresh data.
2. **Write-Through:**
   - The application writes directly to the Cache. The Cache then synchronously writes to the DB. It guarantees consistency but slows down writes.
3. **Write-Behind (Write-Back):**
   - The application writes ONLY to the Cache and returns to the user instantly. 
   - The Cache asynchronously writes batches of data to the DB in the background. Incredible write speed, but extreme risk of data loss if the cache crashes before syncing.

## REAL_WORLD
**Redis vs. Memcached**
Historically, Memcached was the king of distributed caching. It is a pure, multi-threaded, in-memory key-value store. It is incredibly fast but completely volatile (if the server restarts, the RAM is wiped).
Today, **Redis** is the industry standard.
Redis is single-threaded (which mathematically eliminates race conditions and complex locking mechanisms), making it blisteringly fast for its operations. More importantly, Redis supports complex data structures (Lists, Sets, Sorted Sets, Hashes) and offers **Persistence**. Redis can save snapshots (RDB) or append-only logs (AOF) to the hard drive, allowing the cache to survive a server restart!

## INTERVIEW
**Q: A celebrity tweets, and 5 million users simultaneously request the exact same cache key: `tweet:999`. The Consistent Hash ring routes all 5 million requests to a single cache server (Server 4). Server 4's CPU hits 100% and it crashes (The Hot Key Problem). How do you solve this?**
A: **Local In-Memory Caching and Randomization.**
You cannot solve a Hot Key problem by simply adding more servers to the ring, because the hash function will always route `tweet:999` to the exact same server.
1. **Local Caching (L1 Cache):** We add a tiny, 5-second LRU cache directly inside the memory of the Node.js application servers (using a library like `node-cache`). When the 5 million requests hit the 1,000 Node.js servers, the Node servers only ask Redis for the tweet *once every 5 seconds*. The other 4.9 million requests are served instantly from the Node server's local RAM!
2. **Key Duplication:** If we must serve it from Redis, we dynamically append a random number to the key (`tweet:999#1`, `tweet:999#2`, up to `#10`). We replicate the exact same tweet data across 10 different keys. The application randomly picks a number 1-10 when requesting the tweet, perfectly distributing the read load for that single hot item across 10 different Redis servers on the Hash Ring!

## FEYNMAN CHECK

### Explain Design a Distributed Cache (Redis) Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Design a Distributed Cache (Redis)** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Design a Distributed Cache (Redis), they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Design a Distributed Cache (Redis) solve? Why would JavaScript/web development be harder without it?**
> **A:** Design a Distributed Cache (Redis) solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Design a Distributed Cache (Redis)?**
> **A:** The single most important thing is understanding the *mental model*: Design a Distributed Cache (Redis) operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Design a Distributed Cache (Redis) and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Design a Distributed Cache (Redis) operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Design a Distributed Cache (Redis) interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Design a Distributed Cache (Redis) interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Design a Distributed Cache (Redis) to a senior engineer in two sentences, what would you say?**
> **A:** "Design a Distributed Cache (Redis) works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Design a Distributed Cache (Redis) Utility

**Goal:** Implement a real, working Design a Distributed Cache (Redis) module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `designdistributedcache` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir design-distributed-cache-demo && cd design-distributed-cache-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Design a Distributed Cache (Redis) Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Design a Distributed Cache (Redis).
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createDesignaDistributedCache(Redis)(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Design a Distributed Cache (Redis)] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Design a Distributed Cache (Redis) logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Design a Distributed Cache (Redis)] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Design a Distributed Cache (Redis)] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createDesignaDistributedCache(Redis)({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Design a Distributed Cache (Redis)] Input cannot be null or undefined`);
    }
    const instance = createDesignaDistributedCache(Redis)();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Design a Distributed Cache (Redis)] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Design a Distributed Cache (Redis) Tests ──');

test('initialises correctly', () => {
  const m = createDesignaDistributedCache(Redis)();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createDesignaDistributedCache(Redis)();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Design a Distributed Cache (Redis)] Initialized with: hello world
[Design a Distributed Cache (Redis)] Processed: hello world
Result: hello world
[Design a Distributed Cache (Redis)] Destroyed.

── Design a Distributed Cache (Redis) Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Design a Distributed Cache (Redis) in one sentence.**
<details><summary>Answer</summary>

Design a Distributed Cache (Redis) is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Design a Distributed Cache (Redis)?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Design a Distributed Cache (Redis).**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Design a Distributed Cache (Redis) and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Design a Distributed Cache (Redis) | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Design a Distributed Cache (Redis)] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Design a Distributed Cache (Redis) causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Design a Distributed Cache (Redis) usage].
</details>

**Q6: What does Design a Distributed Cache (Redis) look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Design a Distributed Cache (Redis) typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Design a Distributed Cache (Redis) correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Design a Distributed Cache (Redis):
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_design_distributed_cache(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Design a Distributed Cache (Redis) in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Design a Distributed Cache (Redis) in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Design a Distributed Cache (Redis)', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Design a Distributed Cache (Redis) at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Design a Distributed Cache (Redis) uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Design a Distributed Cache (Redis) interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Design a Distributed Cache (Redis) is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Design a Distributed Cache (Redis) relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Design a Distributed Cache (Redis)
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Design a Distributed Cache (Redis) to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Design a Distributed Cache (Redis) fits in the data/control flow.
2. **Choose** the right variant/pattern of Design a Distributed Cache (Redis) for the scale requirement.
3. **Handle** failure modes: what happens when Design a Distributed Cache (Redis) is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Design a Distributed Cache (Redis) is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
