---
slug: "sd-back-of-envelope"
title: "Back of the Envelope Estimation"
level: 3
---

## WHY
In a system design interview, if you propose an architecture that requires 500 terabytes of RAM to cache user profiles, you have failed the interview. The interviewer knows that 500TB of RAM is physically impossible or astronomically expensive. 

To design a system that works in the real world, you must prove that your architecture can physically handle the requested traffic using standard, affordable hardware. You do this through **Back-of-the-Envelope Estimation**—a series of quick, rough mathematical calculations to determine required storage, network bandwidth, and server capacity.

## THEORY
### The Numbers Every Engineer Must Know
To perform these calculations on the fly, you must memorize a few fundamental powers of 10 and data sizes:

**Data Sizes:**
- 1 Byte = 8 bits (A single ASCII character: 'a')
- 1 Kilobyte (KB) = 1,000 Bytes (A short paragraph of text)
- 1 Megabyte (MB) = 1,000 KB (A highly compressed photo)
- 1 Gigabyte (GB) = 1,000 MB (An hour of HD video)
- 1 Terabyte (TB) = 1,000 GB
- 1 Petabyte (PB) = 1,000 TB

**Time & Traffic Constraints:**
- 1 day = 86,400 seconds (Round to **100,000 seconds** for easy math).
- If a system has 1 million Daily Active Users (DAU), and each user makes 1 request per day, your system handles **10 Requests Per Second (RPS)**. *(1,000,000 / 100,000 = 10).*

## VISUALIZATION_CONFIG
```json
{
  "steps": [
    {
      "title": "Powers of 2 Reference",
      "description": "Quick memory reference for estimation: every 10 powers of 2 ≈ 1000x.",
      "code": "// Powers of 2 — memorize these!\n// 2^10  = 1 KB        (Kilobyte)\n// 2^20  = 1 MB        (Megabyte)\n// 2^30  = 1 GB        (Gigabyte)\n// 2^40  = 1 TB        (Terabyte)\n// 2^50  = 1 PB        (Petabyte)\n\n// Latency numbers:\n// L1 cache: 1ns\n// RAM: 100ns\n// SSD random read: 100μs = 100,000ns\n// HDD random read: 10ms = 10,000,000ns\n// Network round trip (same DC): 500μs",
      "highlight": [
        2,
        3,
        4,
        5,
        9,
        10,
        11,
        12,
        13
      ],
      "diagram": {
        "kind": "boxes",
        "boxes": [
          {
            "id": "kb",
            "label": "2^10 = 1KB",
            "color": "#90caf9"
          },
          {
            "id": "mb",
            "label": "2^20 = 1MB",
            "color": "#42a5f5"
          },
          {
            "id": "gb",
            "label": "2^30 = 1GB",
            "color": "#1e88e5"
          },
          {
            "id": "tb",
            "label": "2^40 = 1TB",
            "color": "#1565c0"
          }
        ],
        "connections": []
      }
    },
    {
      "title": "Traffic Estimation",
      "description": "Estimate QPS from daily active users and average actions per user.",
      "code": "// Twitter estimation\n// DAU: 100M\n// Average: 5 tweets/day + 50 reads/day\n\n// Write QPS:\n// 100M × 5 / 86,400 sec/day = 5,787 writes/sec\n// Round: ~6,000 writes/sec\n// Peak (3x): ~18,000 writes/sec\n\n// Read QPS:\n// 100M × 50 / 86,400 = 57,870 reads/sec\n// Round: ~58K reads/sec\n// Peak: ~175K reads/sec\n\n// Rule: reads >> writes (10:1 to 100:1 typical)",
      "highlight": [
        5,
        6,
        7,
        10,
        11,
        13
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "DAU × actions",
          "÷ 86400 sec",
          "= avg QPS",
          "×3 for peak",
          "Design for peak"
        ]
      }
    },
    {
      "title": "Storage Estimation",
      "description": "Estimate storage from object size and volume per day/year.",
      "code": "// Photo sharing app (Instagram-like)\n// 100M DAU, 10% upload photos\n// 10M photos/day\n// Average photo: 1MB\n\n// Daily storage: 10M × 1MB = 10TB/day\n// Yearly: 10TB × 365 = 3.65PB/year\n// 5 years: ~18PB\n\n// With replication (3x): 54PB\n// With thumbnails (+20%): ~65PB\n\n// Cost estimate:\n// AWS S3 Standard: $0.023/GB\n// 65PB × 1024 × 1024 × 0.023 = ~$1.5M/month",
      "highlight": [
        5,
        6,
        7,
        9,
        13,
        14
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "Objects/day",
          "× Object size",
          "Daily storage",
          "× 365",
          "Yearly",
          "× Replication"
        ]
      }
    },
    {
      "title": "Bandwidth Estimation",
      "description": "Estimate network bandwidth from requests and payload sizes.",
      "code": "// Video streaming (YouTube-like)\n// 1B users, 5M concurrent viewers\n// Average video quality: 5 Mbps\n\n// Bandwidth needed:\n// 5M concurrent × 5 Mbps = 25 Tbps!\n// This is why CDN is essential\n\n// CDN serves 99% → origin needs only:\n// 25 Tbps × 1% = 250 Gbps from origin\n\n// Upload bandwidth:\n// 500 hours of video uploaded per minute\n// 500 × 60 min × 1 Gbps per video = 30 Tbps upload\n// CDN ingestion network capacity needed",
      "highlight": [
        5,
        6,
        8,
        9,
        11,
        12,
        13
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "Concurrent users",
          "× Bandwidth/user",
          "Total needed",
          "99% CDN",
          "1% origin"
        ]
      }
    },
    {
      "title": "Complete Estimation Example",
      "description": "URL shortener system: put it all together.",
      "code": "// URL Shortener (bit.ly scale)\n// Write: 100M URLs/day\n// Write QPS: 100M / 86400 = ~1,160 writes/sec\n// Read: 10B clicks/day (100:1 ratio)\n// Read QPS: 10B / 86400 = ~115K reads/sec\n\n// Storage per URL:\n// long_url: 2KB avg + short_key: 7 bytes\n// metadata: ~50 bytes\n// Total: ~2.1KB per URL\n\n// Daily storage: 100M × 2.1KB = 210GB/day\n// 10 years: 210GB × 365 × 10 = 766TB\n\n// Servers needed:\n// Read: 115K QPS / 50K per server = 3 servers (+ LB)",
      "highlight": [
        3,
        5,
        8,
        11,
        12,
        14,
        15
      ],
      "diagram": {
        "kind": "boxes",
        "boxes": [
          {
            "id": "w",
            "label": "1160 writes/sec",
            "color": "#43a047"
          },
          {
            "id": "r",
            "label": "115K reads/sec",
            "color": "#1e88e5"
          },
          {
            "id": "s",
            "label": "766TB in 10 years",
            "color": "#fb8c00"
          }
        ],
        "connections": []
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner (Estimating Traffic / RPS)
Always start by estimating the **Requests Per Second (RPS)**. This dictates how many web servers you will need.

**Scenario:** Design Twitter. We have 300 million Daily Active Users (DAU). On average, each user posts 2 tweets per day, and views 100 tweets per day.

**Write RPS (Posting Tweets):**
- Total Tweets per day = `300M DAU * 2 = 600 Million tweets/day`.
- Seconds in a day = `~100,000 seconds`.
- Write RPS = `600,000,000 / 100,000 = 6,000 Writes Per Second`.

**Read RPS (Viewing Tweets):**
- Total Views per day = `300M DAU * 100 = 30 Billion views/day`.
- Read RPS = `30,000,000,000 / 100,000 = 300,000 Reads Per Second`.

*Conclusion: The system is heavily Read-Biased (1:50 ratio). We will absolutely need massive caching layers to handle 300k reads/second!*

### Level 2 — Building Blocks (Estimating Storage)
Next, calculate how much hard drive space you need to buy to store this data for **5 years**.

**Storage per Tweet:**
- Text = 140 chars = 140 Bytes.
- Metadata (User ID, Timestamp, Location) = ~60 Bytes.
- Media: 20% of tweets have a photo (Average 1 MB).
- Average Tweet Size = `(80% * 200 Bytes) + (20% * 1 MB)` = `~200 KB per tweet`.

**Storage per Day:**
- 600 Million tweets/day * 200 KB = **120 Terabytes per day**.

**Storage for 5 Years:**
- 120 TB/day * 365 days * 5 years = **~220 Petabytes (PB)** of total storage required!

*Conclusion: We cannot store 220 Petabytes in a single SQL database. We MUST shard the database or use a distributed NoSQL blob store (like Amazon S3) for the images!*

### Level 3 — Intermediate (Estimating Bandwidth)
If you have massive amounts of data flowing into and out of your servers, you might saturate the physical network cables.

**Ingress (Incoming Traffic from Writes):**
- 6,000 Writes/sec * 200 KB = **1.2 GB per second**.

**Egress (Outgoing Traffic from Reads):**
- 300,000 Reads/sec * 200 KB = **60 GB per second**.

*Conclusion: 60 GB/sec of egress is massive and will cost millions of dollars in AWS bandwidth fees. We MUST use a Global CDN to offload the image downloads to edge servers!*

### Level 4 — Advanced (Peak Traffic and The "Rule of 2")
Traffic is never perfectly distributed evenly across all 86,400 seconds of the day. People sleep at night, and they check their phones during lunch. 

A standard industry heuristic is the **"80/20 Rule"**: 80% of the traffic occurs during 20% of the day.
To calculate **Peak RPS**, you simply take your Average RPS and multiply it by **2** (or sometimes 3, depending on the app's nature).

- Average Write RPS: 6,000
- **Peak Write RPS:** 12,000 

If you design your architecture to handle exactly 6,000 writes per second, it will crash every single day at lunchtime when the traffic spikes to 12,000. You must always design for the Peak.

## REAL_WORLD
Why does this matter? Because hardware has limits.
A standard, beefy modern web server (e.g., AWS `c5.2xlarge`) can handle roughly 10,000 concurrent connections, and roughly 1,000 to 3,000 HTTP Requests Per Second (RPS) depending on the framework (Node.js vs Go vs Rust). 

If you calculated your Peak Read RPS to be 300,000, and a single server can handle 3,000, you instantly know that you will need to horizontally scale to a cluster of exactly **100 Web Servers** behind a Load Balancer to keep the system online. 

## INTERVIEW
**Q: Do I need exact math in the interview?**
A: **Absolutely not.** The interviewer is not testing your ability to do long division. They are testing your mental models. You should round aggressively to make the math easy. Round 86,400 seconds to 100,000. Round 365 days to 400. If you try to calculate `(300,000,000 / 86400) * 365` in your head under pressure, you will freeze and fail the interview.

**Q: Should I memorize latency numbers?**
A: Yes! You should memorize the "Latency Numbers Every Programmer Should Know" (originally compiled by Peter Norvig):
- L1 Cache reference: 0.5 nanoseconds
- Main memory (RAM) reference: 100 ns
- Read 1 MB sequentially from SSD: 1 millisecond (ms)
- Read 1 MB sequentially from Magnetic Disk: 20 ms
- Send a packet from California to Europe and back: 150 ms

**Q: What if the interviewer doesn't give me the DAU numbers?**
A: You must ask them! "What is the expected DAU?" If they say "You decide," you pick a reasonable number and clearly state your assumption. "Let's assume this is a rapidly growing startup with 1 Million DAU to start." State your assumptions out loud before doing any math.

## FEYNMAN CHECK

### Explain Back of the Envelope Estimation Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Back of the Envelope Estimation** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Back of the Envelope Estimation, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Back of the Envelope Estimation solve? Why would JavaScript/web development be harder without it?**
> **A:** Back of the Envelope Estimation solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Back of the Envelope Estimation?**
> **A:** The single most important thing is understanding the *mental model*: Back of the Envelope Estimation operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Back of the Envelope Estimation and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Back of the Envelope Estimation operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Back of the Envelope Estimation interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Back of the Envelope Estimation interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Back of the Envelope Estimation to a senior engineer in two sentences, what would you say?**
> **A:** "Back of the Envelope Estimation works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Back of the Envelope Estimation Utility

**Goal:** Implement a real, working Back of the Envelope Estimation module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `sdbackofenvelope` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir sd-back-of-envelope-demo && cd sd-back-of-envelope-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Back of the Envelope Estimation Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Back of the Envelope Estimation.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createBackoftheEnvelopeEstimation(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Back of the Envelope Estimation] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Back of the Envelope Estimation logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Back of the Envelope Estimation] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Back of the Envelope Estimation] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createBackoftheEnvelopeEstimation({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Back of the Envelope Estimation] Input cannot be null or undefined`);
    }
    const instance = createBackoftheEnvelopeEstimation();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Back of the Envelope Estimation] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Back of the Envelope Estimation Tests ──');

test('initialises correctly', () => {
  const m = createBackoftheEnvelopeEstimation();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createBackoftheEnvelopeEstimation();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Back of the Envelope Estimation] Initialized with: hello world
[Back of the Envelope Estimation] Processed: hello world
Result: hello world
[Back of the Envelope Estimation] Destroyed.

── Back of the Envelope Estimation Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Back of the Envelope Estimation in one sentence.**
<details><summary>Answer</summary>

Back of the Envelope Estimation is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Back of the Envelope Estimation?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Back of the Envelope Estimation.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Back of the Envelope Estimation and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Back of the Envelope Estimation | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Back of the Envelope Estimation] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Back of the Envelope Estimation causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Back of the Envelope Estimation usage].
</details>

**Q6: What does Back of the Envelope Estimation look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Back of the Envelope Estimation typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Back of the Envelope Estimation correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Back of the Envelope Estimation:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_sd_back_of_envelope(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Back of the Envelope Estimation in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Back of the Envelope Estimation in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Back of the Envelope Estimation', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Back of the Envelope Estimation at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Back of the Envelope Estimation uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Back of the Envelope Estimation interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Back of the Envelope Estimation is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Back of the Envelope Estimation relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Back of the Envelope Estimation
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Back of the Envelope Estimation to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Back of the Envelope Estimation fits in the data/control flow.
2. **Choose** the right variant/pattern of Back of the Envelope Estimation for the scale requirement.
3. **Handle** failure modes: what happens when Back of the Envelope Estimation is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Back of the Envelope Estimation is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
