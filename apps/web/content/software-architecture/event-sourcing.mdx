---
slug: "event-sourcing"
title: "Event Sourcing"
level: 4
---

## WHY
In a traditional database (like a SQL database storing a User's bank account), when a user deposits $50, you update the `balance` row from `$100` to `$150`. The old state ($100) is gone forever. If a bug accidentally sets the balance to $0, or if an auditor asks *why* the balance is $150, you have no way to prove it. You only have the current state, not the history.

**Event Sourcing** flips this entirely. Instead of storing the *current state* of an entity, you store every *event* that ever happened to it as an immutable log. To find the current state, you replay all the events from the beginning of time.

## THEORY
### Core Concepts
1. **The Event Store:** A specialized database (or an append-only log like Kafka) that stores Events.
2. **Immutability:** Events happened in the past. They can NEVER be updated or deleted. You cannot change history.
3. **Rehydration / Projection:** The process of reading the event log and applying a `reduce()` function to calculate the current state.

If you make a mistake (e.g., an event added $50 instead of $5), you do not edit the event. You append a *new* Compensating Event (e.g., "Deducted $45 for correction"). This is exactly how accounting ledgers have worked for centuries.

## VISUALIZATION_CONFIG
```json
{
  "language": "java",
  "fileName": "EventSourcing.java",
  "steps": [
    {
      "title": "What is Event Sourcing?",
      "description": "Instead of storing current state, Event Sourcing stores the sequence of events that led to the current state. State is derived by replaying events.",
      "code": "// Traditional: store current state\nUPDATE accounts SET balance = 950 WHERE id = 1;\n\n// Event Sourcing: store events\nINSERT INTO events (aggregate_id, type, data, sequence)\nVALUES ('acc-1', 'MoneyDeposited', '{amount: 1000}', 1);\nINSERT INTO events (aggregate_id, type, data, sequence)\nVALUES ('acc-1', 'MoneyWithdrawn', '{amount: 50}', 2);\n// Balance = 1000 - 50 = 950 (derived by replaying)",
      "highlight": [
        5,
        6,
        7,
        8,
        9
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Event 1: AccountOpened {balance: 0}",
            "done": true
          },
          {
            "label": "Event 2: MoneyDeposited {amount: 1000}",
            "done": true
          },
          {
            "label": "Event 3: MoneyWithdrawn {amount: 50}",
            "active": true
          },
          {
            "label": "Replay → current balance: 950"
          }
        ]
      }
    },
    {
      "title": "Event store and aggregate replay",
      "description": "The event store is an append-only log. To get current state, load all events for an aggregate and replay them from the beginning (or from a snapshot).",
      "code": "public class Account {\n    private BigDecimal balance = BigDecimal.ZERO;\n    private List<AccountEvent> uncommittedEvents = new ArrayList<>();\n\n    // Apply events to rebuild state\n    public void apply(AccountEvent event) {\n        if (event instanceof MoneyDeposited e)\n            this.balance = balance.add(e.amount());\n        else if (event instanceof MoneyWithdrawn e)\n            this.balance = balance.subtract(e.amount());\n    }\n\n    // Load from event store\n    public static Account replay(List<AccountEvent> events) {\n        Account acc = new Account();\n        events.forEach(acc::apply);\n        return acc;\n    }\n}",
      "highlight": [
        6,
        7,
        8,
        9,
        10,
        14,
        15,
        16
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Event sourcing components",
        "items": [
          {
            "label": "Event Store",
            "color": "#10b981",
            "value": "Append-only log of domain events",
            "highlight": true
          },
          {
            "label": "apply(event)",
            "color": "#818cf8",
            "value": "Mutates state from event data"
          },
          {
            "label": "replay(events)",
            "color": "#f59e0b",
            "value": "Rebuild current state from scratch"
          },
          {
            "label": "Snapshot",
            "color": "#60a5fa",
            "value": "Checkpoint to avoid replaying all history"
          }
        ]
      }
    },
    {
      "title": "Snapshots for performance",
      "description": "Replaying thousands of events is slow. Snapshots save the aggregate state periodically. On load: fetch latest snapshot + events after snapshot.",
      "code": "// Save snapshot every 100 events\npublic void saveSnapshot(Account account, long version) {\n    if (version % 100 == 0) {\n        AccountSnapshot snap = new AccountSnapshot(\n            account.id(), account.balance(), version\n        );\n        snapshotStore.save(snap);\n    }\n}\n\n// Load with snapshot optimisation\npublic Account load(AccountId id) {\n    AccountSnapshot snap = snapshotStore.findLatest(id);\n    long fromSequence = snap != null ? snap.version() : 0;\n    List<AccountEvent> events =\n        eventStore.loadAfter(id, fromSequence);\n    Account acc = snap != null ? Account.fromSnapshot(snap) : new Account();\n    events.forEach(acc::apply);\n    return acc;\n}",
      "highlight": [
        3,
        4,
        5,
        6,
        13,
        14,
        16,
        17,
        18
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Load snapshot (e.g. at version 1000)",
            "done": true
          },
          {
            "label": "Load events AFTER snapshot version",
            "done": true
          },
          {
            "label": "Apply only new events (e.g. 15 events)",
            "active": true
          },
          {
            "label": "Much faster than replaying all 1015 events"
          }
        ]
      }
    },
    {
      "title": "Projections from event store",
      "description": "Event Sourcing naturally integrates with CQRS. Projections subscribe to events and build read models. They can be rebuilt at any time by replaying the event log.",
      "code": "// Projection: rebuild any time from event log\n@EventHandler\npublic class BalanceSummaryProjection {\n    @EventHandler\n    public void on(MoneyDeposited event) {\n        view.incrementBalance(event.accountId(), event.amount());\n    }\n\n    @EventHandler\n    public void on(MoneyWithdrawn event) {\n        view.decrementBalance(event.accountId(), event.amount());\n    }\n\n    // Replay: delete view + replay all events → fresh view\n    public void rebuild() {\n        view.deleteAll();\n        eventStore.loadAll().forEach(this::dispatch);\n    }\n}",
      "highlight": [
        4,
        5,
        6,
        9,
        10,
        11,
        15,
        16,
        17
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Event Sourcing benefits",
        "items": [
          {
            "label": "Full audit trail",
            "color": "#10b981",
            "value": "Every change recorded forever",
            "highlight": true
          },
          {
            "label": "Time travel",
            "color": "#818cf8",
            "value": "See state at any point in history"
          },
          {
            "label": "Rebuild projections",
            "color": "#f59e0b",
            "value": "New view? Replay events to build it"
          },
          {
            "label": "Debugging",
            "color": "#60a5fa",
            "value": "Reproduce exact sequence of events"
          }
        ]
      }
    },
    {
      "title": "Event Sourcing trade-offs",
      "description": "Event Sourcing adds complexity: eventual consistency, eventual schema evolution, event versioning. Best for domains with complex audit requirements.",
      "code": "// Schema evolution: handle old event versions\npublic AccountEvent deserialize(EventRecord record) {\n    return switch (record.type()) {\n        case \"MoneyDeposited_v1\" -> upgradeV1(record);\n        case \"MoneyDeposited_v2\" -> new MoneyDeposited(record);\n        default -> throw new UnknownEventType(record.type());\n    };\n}\n\n// Upcaster: transform old event to new version\npublic MoneyDeposited_v2 upcast(MoneyDeposited_v1 old) {\n    return new MoneyDeposited_v2(\n        old.accountId(),\n        new Money(old.amount(), Currency.USD) // added currency\n    );\n}",
      "highlight": [
        3,
        4,
        5,
        11,
        12,
        13,
        14
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Event Sourcing trade-offs",
        "items": [
          {
            "label": "+ Audit trail",
            "color": "#10b981",
            "value": "Complete history — never lose data",
            "highlight": true
          },
          {
            "label": "+ Replay projections",
            "color": "#10b981",
            "value": "New query models from old events"
          },
          {
            "label": "- Schema evolution",
            "color": "#ef4444",
            "value": "Old events must stay deserializable"
          },
          {
            "label": "- Complexity",
            "color": "#ef4444",
            "value": "Not needed for simple CRUD apps"
          }
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner (The Mental Model)
Think of Event Sourcing like a massive Array `reduce()` function in JavaScript.

```javascript
// The Event Store (Immutable Log)
const events = [
  { type: 'ACCOUNT_OPENED', payload: { id: 1, name: 'Alice' }, timestamp: 1000 },
  { type: 'DEPOSITED', payload: { amount: 100 }, timestamp: 1005 },
  { type: 'WITHDRAWN', payload: { amount: 30 }, timestamp: 1010 },
  { type: 'DEPOSITED', payload: { amount: 50 }, timestamp: 1015 }
];

// The Reducer (Rehydration)
function calculateCurrentState(eventHistory) {
  return eventHistory.reduce((state, event) => {
    switch (event.type) {
      case 'ACCOUNT_OPENED':
        return { ...state, ...event.payload, balance: 0, status: 'OPEN' };
      case 'DEPOSITED':
        return { ...state, balance: state.balance + event.payload.amount };
      case 'WITHDRAWN':
        return { ...state, balance: state.balance - event.payload.amount };
      default:
        return state;
    }
  }, {}); // Start with empty object
}

// Current State: { id: 1, name: 'Alice', balance: 120, status: 'OPEN' }
const currentState = calculateCurrentState(events);
```

### Level 2 — Building Blocks (Projections and Read Models)
If calculating the current state requires reading 10,000 events, querying your application will be incredibly slow.
Event Sourcing is almost always paired with **CQRS (Command Query Responsibility Segregation)**.

1. **Write Side (Commands):** Appends events to the Event Store.
2. **The Projector:** A background worker that listens for new events and updates a separate "Read Database" (like MongoDB or Redis).
3. **Read Side (Queries):** The frontend queries the fast Read Database, which stores the pre-calculated current state.

```text
[User clicks Deposit] 
    ---> [Command Service] 
    ---> Appends {Type: DEPOSITED, amount: 50} to [Kafka / Event Store]

[Projector Service] (Listens to Kafka)
    ---> Hears DEPOSITED event.
    ---> Executes: UPDATE read_db SET balance = balance + 50 WHERE id = 1

[User visits Dashboard]
    ---> [Query Service] 
    ---> SELECT balance FROM read_db WHERE id = 1
    ---> (Fast! No event replaying required for reads).
```

### Level 3 — Intermediate (Snapshots)
Even with a Read Database, sometimes the Command side needs to know the current state before validating a new command (e.g., "Does the user have enough money to withdraw?"). Loading a 10-year history of 50,000 transactions is too slow.

**Snapshots** solve this. Every 100 events, you save the current calculated state into a Snapshot table.
When you need to rehydrate, you don't start from Event #1. You load the Snapshot at Event #100, and then only replay Events #101 to #105.

### Level 4 — Advanced (Time Travel and Replaying)
Because you have the raw events, you can create *new* projections that you didn't think of originally.

Imagine management says: *"We want to build a Fraud Detection system that analyzes how fast users deposit and withdraw money on their first day."*
In a traditional CRUD system, you can't do this. That data wasn't captured.

In Event Sourcing, you write a new Projector for the Fraud Database, point it at Event #1 from five years ago, and let it run. It will replay history from the beginning of time, populating the new database as if the feature existed for five years. This is the ultimate superpower of Event Sourcing.

## REAL_WORLD
Event Sourcing is notoriously difficult to implement correctly. It introduces massive complexity:
- **Eventual Consistency:** The Read Database is updated asynchronously. A user might deposit money, refresh the page instantly, and see their old balance because the Projector hasn't processed the event yet.
- **Event Schema Evolution:** What happens if you need to change the payload structure of an event? You can't change the old events in the database! You have to write complex "Upcaster" functions that convert old event formats into new formats on the fly during rehydration.

**Where is it used?** Banking, E-commerce Carts, Gaming, and Git. (Git is literally an Event Sourcing system: every commit is an immutable event, and your working directory is the projected read model!).

## INTERVIEW
**Q: How do you handle a scenario where a user legally requests all their data to be deleted (GDPR Right to Erasure) in an immutable Event Store?**
A: This is one of the hardest problems in Event Sourcing. You cannot delete the `ACCOUNT_OPENED` event, because it breaks immutability and cryptographic hashes. The standard solution is **Crypto-Shredding**. You encrypt all Personally Identifiable Information (PII) inside the event payload using a unique encryption key for that specific user. When they request deletion, you do not touch the event store; you simply delete their encryption key from the Key Management System. The events remain, but the PII is permanently rendered as mathematically unreadable gibberish.

## FEYNMAN CHECK

### Explain Event Sourcing Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Event Sourcing** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Event Sourcing, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Event Sourcing solve? Why would JavaScript/web development be harder without it?**
> **A:** Event Sourcing solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Event Sourcing?**
> **A:** The single most important thing is understanding the *mental model*: Event Sourcing operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Event Sourcing and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Event Sourcing operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Event Sourcing interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Event Sourcing interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Event Sourcing to a senior engineer in two sentences, what would you say?**
> **A:** "Event Sourcing works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Event Sourcing Utility

**Goal:** Implement a real, working Event Sourcing module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `eventsourcing` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir event-sourcing-demo && cd event-sourcing-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Event Sourcing Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Event Sourcing.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createEventSourcing(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Event Sourcing] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Event Sourcing logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Event Sourcing] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Event Sourcing] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createEventSourcing({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Event Sourcing] Input cannot be null or undefined`);
    }
    const instance = createEventSourcing();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Event Sourcing] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Event Sourcing Tests ──');

test('initialises correctly', () => {
  const m = createEventSourcing();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createEventSourcing();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Event Sourcing] Initialized with: hello world
[Event Sourcing] Processed: hello world
Result: hello world
[Event Sourcing] Destroyed.

── Event Sourcing Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Event Sourcing in one sentence.**
<details><summary>Answer</summary>

Event Sourcing is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Event Sourcing?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Event Sourcing.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Event Sourcing and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Event Sourcing | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Event Sourcing] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Event Sourcing causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Event Sourcing usage].
</details>

**Q6: What does Event Sourcing look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Event Sourcing typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Event Sourcing correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Event Sourcing:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_event_sourcing(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Event Sourcing in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Event Sourcing in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Event Sourcing', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Event Sourcing at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Event Sourcing uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Event Sourcing interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Event Sourcing is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Event Sourcing relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Event Sourcing
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Event Sourcing to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Event Sourcing fits in the data/control flow.
2. **Choose** the right variant/pattern of Event Sourcing for the scale requirement.
3. **Handle** failure modes: what happens when Event Sourcing is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Event Sourcing is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
