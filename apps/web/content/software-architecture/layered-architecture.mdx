---
slug: "layered-architecture"
title: "N-Tier / Layered Architecture"
level: 4
---

## WHY
If you write your database SQL queries inside the same function that renders your HTML buttons, your application will quickly become a monolithic, untestable nightmare. 
**Layered Architecture (N-Tier)** is the most common architectural pattern in the world. By dividing an application into horizontal layers—each with a specific responsibility—you ensure that changes to the UI don't accidentally break the database, and you make it possible to swap out underlying technologies in the future.

## THEORY
### The Layers
Most enterprise applications are divided into three or four standard layers:
1. **Presentation Layer (Web/API):** Handles HTTP requests, JSON parsing, and routing. (Controllers)
2. **Business/Domain Layer:** The heart of the application. Contains the rules and logic (e.g., "A user cannot withdraw more money than their balance"). (Services)
3. **Data Access / Persistence Layer:** Handles communication with the physical database. (Repositories / DAOs)
4. **Database Layer:** The actual PostgreSQL or MongoDB server.

### The Golden Rule of Dependencies
**Dependencies must always point strictly downward.**
The Presentation layer is allowed to call the Business layer.
The Business layer is allowed to call the Persistence layer.
The Persistence layer is **never** allowed to call the Business layer. Upward communication is strictly forbidden.

## VISUALIZATION_CONFIG
```json
{
  "language": "java",
  "fileName": "LayeredArch.java",
  "steps": [
    {
      "title": "Traditional 3-tier architecture",
      "description": "The classic 3-tier: Presentation (UI/API), Business Logic, Data Access. Each layer depends only on the layer below it.",
      "code": "// Layer 1: Presentation (Controller)\n@RestController\npublic class TopicController {\n    @Autowired TopicService service;\n    @GetMapping(\"/topics/{id}\")\n    public ResponseEntity<TopicDto> get(@PathVariable Long id) {\n        return ResponseEntity.ok(service.getById(id));\n    }\n}\n\n// Layer 2: Business Logic (Service)\n@Service\npublic class TopicService {\n    @Autowired TopicRepository repo;\n    public TopicDto getById(Long id) {\n        return TopicMapper.toDto(repo.findById(id).orElseThrow());\n    }\n}\n\n// Layer 3: Data Access (Repository)\npublic interface TopicRepository extends JpaRepository<Topic, Long> {}",
      "highlight": [
        3,
        12,
        20
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Presentation: HTTP in → DTO out",
            "done": true
          },
          {
            "label": "Business Logic: domain rules + orchestration",
            "done": true
          },
          {
            "label": "Data Access: SQL / ORM queries",
            "active": true
          },
          {
            "label": "DB: PostgreSQL / MongoDB / Redis"
          }
        ]
      }
    },
    {
      "title": "N-tier with service + DAO",
      "description": "Enterprise apps often add a Service layer above DAO (Data Access Object). Controllers call Services; Services call DAOs. DTOs flow between layers.",
      "code": "// DTO: data transfer between layers\npublic record TopicDto(Long id, String title, int level) {}\n\n// Controller → DTO (no entity in controller)\n@PostMapping\npublic TopicDto create(@RequestBody CreateTopicRequest req) {\n    return service.create(req);\n}\n\n// Service → entity (business logic here)\npublic TopicDto create(CreateTopicRequest req) {\n    Topic topic = Topic.builder()\n        .title(req.title())\n        .level(req.level())\n        .build();\n    return TopicMapper.toDto(repo.save(topic));\n}\n// Entities never escape service layer!",
      "highlight": [
        6,
        11,
        12,
        13,
        14,
        15,
        17
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Layer responsibilities",
        "items": [
          {
            "label": "Controller",
            "color": "#10b981",
            "value": "HTTP ↔ DTO conversion only",
            "highlight": true
          },
          {
            "label": "Service",
            "color": "#818cf8",
            "value": "Business rules, transactions"
          },
          {
            "label": "Repository",
            "color": "#f59e0b",
            "value": "DB queries — no business logic"
          },
          {
            "label": "DTO vs Entity",
            "color": "#60a5fa",
            "value": "Entities stay in service layer"
          }
        ]
      }
    },
    {
      "title": "Cross-cutting concerns",
      "description": "Cross-cutting concerns affect all layers: logging, security, caching, transactions. Spring AOP handles these without cluttering business logic.",
      "code": "// AOP: logging across all service methods\n@Aspect @Component\npublic class LoggingAspect {\n    @Around(\"execution(* com.devmastery.service.*.*(..))\")\n    public Object log(ProceedingJoinPoint jp) throws Throwable {\n        log.info(\"Calling {}\", jp.getSignature());\n        long start = System.currentTimeMillis();\n        Object result = jp.proceed();\n        log.info(\"{} took {}ms\", jp.getSignature(),\n            System.currentTimeMillis() - start);\n        return result;\n    }\n}\n// No logging code in any service class!",
      "highlight": [
        4,
        6,
        8,
        9,
        10,
        14
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Cross-cutting concerns",
        "items": [
          {
            "label": "Logging (AOP)",
            "color": "#10b981",
            "value": "Method entry/exit without code in service",
            "highlight": true
          },
          {
            "label": "Security (@PreAuthorize)",
            "color": "#818cf8",
            "value": "Method-level auth via AOP"
          },
          {
            "label": "Transactions (@Transactional)",
            "color": "#f59e0b",
            "value": "Auto commit/rollback"
          },
          {
            "label": "Caching (@Cacheable)",
            "color": "#60a5fa",
            "value": "Auto cache results"
          }
        ]
      }
    },
    {
      "title": "Anti-patterns: fat controllers, anemic models",
      "description": "Fat Controller does business logic in controller. Anemic Domain Model has entities with only getters/setters — all logic in service. Both violate layering.",
      "code": "// FAT CONTROLLER: bad\n@PostMapping(\"/checkout\")\npublic void checkout(@RequestBody Cart cart) {\n    // Business logic in controller!!\n    double total = cart.items().stream().mapToDouble(i -> i.price() * i.qty()).sum();\n    if (total > 10000) applyBulkDiscount(total);\n    paymentGateway.charge(total);\n    emailService.sendReceipt(cart.userId(), total);\n    // All this belongs in CheckoutService\n}\n\n// ANEMIC: entity is just data\npublic class Order {\n    private double total; // no methods!\n    // All business logic lives in OrderService\n    // Violates OOP: data + behaviour should be together\n}",
      "highlight": [
        5,
        6,
        7,
        8,
        13,
        14,
        15,
        16
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Anti-patterns",
        "items": [
          {
            "label": "Fat Controller",
            "color": "#ef4444",
            "value": "Business logic in HTTP layer",
            "highlight": true
          },
          {
            "label": "Anemic Model",
            "color": "#ef4444",
            "value": "Entity = DTO with no behaviour"
          },
          {
            "label": "Rich Domain Model",
            "color": "#10b981",
            "value": "Entity has business methods"
          },
          {
            "label": "Thin Controller",
            "color": "#10b981",
            "value": "Only delegates to service"
          }
        ]
      }
    },
    {
      "title": "Choosing architecture style",
      "description": "Layered is simple and suits most CRUD apps. Hexagonal adds flexibility for complex domains. Microservices for team scaling. Choose based on team size and domain complexity.",
      "code": "// Decision matrix:\n// Small team, CRUD app:\n//   → Layered (monolith) — simple, fast delivery\n\n// Complex domain, single team:\n//   → Clean/Hexagonal Architecture\n//   → Domain model is core; ports + adapters around it\n\n// Multiple teams, independent deploy:\n//   → Microservices\n//   → Each service: layered or hexagonal internally\n\n// Rule: don't over-architect early!\n// Start monolith → extract services as pain appears\n// \"The best architecture is the one that serves the current needs\"\n// — evolve incrementally",
      "highlight": [
        3,
        6,
        10,
        13,
        14,
        15
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Architecture decision",
        "items": [
          {
            "label": "CRUD + small team",
            "color": "#10b981",
            "value": "Layered monolith — fastest delivery",
            "highlight": true
          },
          {
            "label": "Complex domain",
            "color": "#818cf8",
            "value": "Hexagonal / Clean Architecture"
          },
          {
            "label": "Multiple teams",
            "color": "#f59e0b",
            "value": "Microservices — team autonomy"
          },
          {
            "label": "Rule",
            "color": "#60a5fa",
            "value": "Start simple, evolve as pain appears"
          }
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner (The Presentation Layer)
The Presentation Layer (usually a REST Controller) should be incredibly thin. It does absolutely no business logic. Its only job is to receive HTTP traffic, pass it to the Business Layer, and return an HTTP response.

```java
// UserController.java (Presentation Layer)
@RestController
@RequestMapping("/users")
public class UserController {
    
    // Dependency injection points downwards to the Business Layer
    private final UserService userService;

    public UserController(UserService userService) {
        this.userService = userService;
    }

    @PostMapping
    public ResponseEntity<User> createUser(@RequestBody CreateUserDto dto) {
        // No logic here! Just passing data down.
        User createdUser = userService.registerNewUser(dto);
        return ResponseEntity.status(201).body(createdUser);
    }
}
```

### Level 2 — Building Blocks (The Business Layer)
The Business Layer (Service) contains the actual rules of the application. It knows nothing about HTTP, JSON, or SQL.

```java
// UserService.java (Business Layer)
@Service
public class UserService {
    
    // Dependency injection points downwards to the Persistence Layer
    private final UserRepository userRepository;

    public UserService(UserRepository userRepository) {
        this.userRepository = userRepository;
    }

    public User registerNewUser(CreateUserDto dto) {
        // Core Business Logic!
        if (dto.getAge() < 18) {
            throw new IllegalArgumentException("User must be 18 or older.");
        }
        
        if (userRepository.existsByEmail(dto.getEmail())) {
            throw new IllegalStateException("Email already in use.");
        }

        User user = new User(dto.getName(), dto.getEmail());
        
        // Pass to the persistence layer to be saved
        return userRepository.save(user);
    }
}
```

### Level 3 — Intermediate (The Persistence Layer)
The Persistence Layer (Repository) handles the ugly details of talking to the database. It knows nothing about business rules.

```java
// UserRepository.java (Persistence Layer)
@Repository
public class UserRepository {
    
    private final JdbcTemplate db;

    public UserRepository(JdbcTemplate db) {
        this.db = db;
    }

    public boolean existsByEmail(String email) {
        String sql = "SELECT COUNT(*) FROM users WHERE email = ?";
        int count = db.queryForObject(sql, Integer.class, email);
        return count > 0;
    }

    public User save(User user) {
        String sql = "INSERT INTO users (name, email) VALUES (?, ?) RETURNING id";
        String id = db.queryForObject(sql, String.class, user.getName(), user.getEmail());
        user.setId(id);
        return user;
    }
}
```

### Level 4 — Advanced (The Sinkhole Anti-Pattern)
A common problem in Layered Architecture is the "Sinkhole Anti-Pattern" (also known as the Anemic Domain Model).
If 80% of the methods in your Business Layer look like this:
```java
public User getUserById(String id) {
    return userRepository.findById(id); // Literally just passing through
}
```
...then your Business Layer is acting as an empty sinkhole. The request flows through the Presentation layer, through the empty Business layer, straight to the Database. This usually happens when an application is entirely CRUD (Create, Read, Update, Delete) driven and has no actual business rules. If your application is pure CRUD, strict layered architecture might be overkill!

## REAL_WORLD
**Skipping Layers**
Is the Presentation layer allowed to skip the Business layer and talk directly to the Database layer? 
- **Strict Layered Architecture:** No. Layers must be closed. A request must pass through the layer immediately below it. This ensures the Business layer can always intercept requests to add caching or auditing later.
- **Relaxed Layered Architecture:** Yes. Some teams allow "open" layers, letting the Controller talk directly to the Repository for simple read-only queries (like fetching a dropdown list), avoiding the Sinkhole Anti-Pattern.

## INTERVIEW
**Q: What is the primary weakness of Layered Architecture compared to Clean/Hexagonal Architecture?**
A: In Layered Architecture, the Database sits at the very bottom, meaning the Business Layer depends on the Database Layer. Because dependencies flow downwards, the Business logic (the most important part of the app) becomes tightly coupled to the specific database technology (e.g., SQL tables). In Clean/Hexagonal Architecture, the Dependency Inversion Principle is used to flip this relationship, putting the Business Logic at the absolute center of the application, completely isolated from both the UI and the Database.

## FEYNMAN CHECK

### Explain N-Tier / Layered Architecture Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **N-Tier / Layered Architecture** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about N-Tier / Layered Architecture, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does N-Tier / Layered Architecture solve? Why would JavaScript/web development be harder without it?**
> **A:** N-Tier / Layered Architecture solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about N-Tier / Layered Architecture?**
> **A:** The single most important thing is understanding the *mental model*: N-Tier / Layered Architecture operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with N-Tier / Layered Architecture and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which N-Tier / Layered Architecture operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does N-Tier / Layered Architecture interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** N-Tier / Layered Architecture interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of N-Tier / Layered Architecture to a senior engineer in two sentences, what would you say?**
> **A:** "N-Tier / Layered Architecture works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a N-Tier / Layered Architecture Utility

**Goal:** Implement a real, working N-Tier / Layered Architecture module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `layeredarchitecture` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir layered-architecture-demo && cd layered-architecture-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── N-Tier / Layered Architecture Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of N-Tier / Layered Architecture.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createN-Tier/LayeredArchitecture(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[N-Tier / Layered Architecture] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual N-Tier / Layered Architecture logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[N-Tier / Layered Architecture] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[N-Tier / Layered Architecture] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createN-Tier/LayeredArchitecture({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[N-Tier / Layered Architecture] Input cannot be null or undefined`);
    }
    const instance = createN-Tier/LayeredArchitecture();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[N-Tier / Layered Architecture] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── N-Tier / Layered Architecture Tests ──');

test('initialises correctly', () => {
  const m = createN-Tier/LayeredArchitecture();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createN-Tier/LayeredArchitecture();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[N-Tier / Layered Architecture] Initialized with: hello world
[N-Tier / Layered Architecture] Processed: hello world
Result: hello world
[N-Tier / Layered Architecture] Destroyed.

── N-Tier / Layered Architecture Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define N-Tier / Layered Architecture in one sentence.**
<details><summary>Answer</summary>

N-Tier / Layered Architecture is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of N-Tier / Layered Architecture?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of N-Tier / Layered Architecture.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between N-Tier / Layered Architecture and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | N-Tier / Layered Architecture | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of N-Tier / Layered Architecture] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing N-Tier / Layered Architecture causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct N-Tier / Layered Architecture usage].
</details>

**Q6: What does N-Tier / Layered Architecture look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, N-Tier / Layered Architecture typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use N-Tier / Layered Architecture correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using N-Tier / Layered Architecture:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_layered_architecture(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test N-Tier / Layered Architecture in isolation? What would you mock?**
<details><summary>Answer</summary>

To test N-Tier / Layered Architecture in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('N-Tier / Layered Architecture', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of N-Tier / Layered Architecture at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how N-Tier / Layered Architecture uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common N-Tier / Layered Architecture interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "N-Tier / Layered Architecture is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does N-Tier / Layered Architecture relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
N-Tier / Layered Architecture
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use N-Tier / Layered Architecture to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where N-Tier / Layered Architecture fits in the data/control flow.
2. **Choose** the right variant/pattern of N-Tier / Layered Architecture for the scale requirement.
3. **Handle** failure modes: what happens when N-Tier / Layered Architecture is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you N-Tier / Layered Architecture is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
