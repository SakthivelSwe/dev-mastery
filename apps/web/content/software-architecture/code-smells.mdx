---
slug: "code-smells"
title: "Code Smells"
level: 3
---

## WHY
A "Code Smell" is a surface indication that usually corresponds to a deeper problem in your system. It doesn't mean the code is broken (it might pass all unit tests!), but it indicates weakness in design that will slow down future development or breed bugs. Learning to identify these smells allows you to refactor proactively before the codebase rots completely.

## THEORY
### What is a Code Smell?
Coined by Kent Beck in Martin Fowler's legendary book *Refactoring*, a code smell is an intuition. It's the moment you look at a file and think, "Something feels wrong here."

Code smells are divided into categories:
1. **Bloaters:** Code, methods, or classes that have grown so large they are hard to work with.
2. **Object-Orientation Abusers:** Incomplete or incorrect application of OOP principles.
3. **Change Preventers:** Code that forces you to change multiple files just to make one simple modification.
4. **Dispensables:** Pointless, dead, or redundant code.
5. **Couplers:** Excessive coupling between classes.

## VISUALIZATION_CONFIG
```json
{ "component": "CodeRunner", "state": "code-smells" }
```

## CODE

### Bloater 1: Long Method
The most common smell. A method should ideally fit on a single laptop screen (roughly 20-30 lines). If a method is 200 lines long, it is doing too much.
**The Fix:** Extract Method. Highlight blocks of logic and pull them into their own well-named private methods.

### Bloater 2: Primitive Obsession
Using basic primitives (`string`, `int`, `array`) to represent complex domain concepts.

```java
// ❌ BAD: Primitive Obsession
public void processOrder(String currency, double amount, String zipCode, String street) { ... }

// ✅ GOOD: Wrap primitives in Domain Objects
public void processOrder(Money price, Address address) { ... }
```
*Why?* Because a `zipCode` isn't just a String. It has validation rules (it must be 5 digits). If you use a String, you have to validate it everywhere. If you use an `Address` object, the object validates itself upon creation!

### Change Preventer: Shotgun Surgery
You want to add a new "PayPal" payment type. You find yourself opening `Order.java`, `Invoice.java`, `PaymentController.java`, and `DatabaseMapper.java`, adding a small `if (type == "paypal")` to every single file.
This is Shotgun Surgery: a single responsibility is spread across the entire codebase.
**The Fix:** Move Method / Move Field. Consolidate the logic into a single `PaymentProcessor` Strategy class.

### Object-Orientation Abuser: Switch Statements
In pure Object-Oriented code, long `switch` statements or massive `if/else if` chains based on an object's "type" are considered a smell.

```java
// ❌ BAD: A switch statement based on type
public double calculateShipping(Package pkg) {
    switch (pkg.type) {
        case "STANDARD": return 5.00;
        case "EXPRESS": return 15.00;
        case "OVERNIGHT": return 25.00;
    }
}
```
**The Fix:** Polymorphism. The `Package` interface should define a `calculateShipping()` method. The `StandardPackage` and `ExpressPackage` classes should implement their own specific logic.

### Coupler: Feature Envy
When a method in Class A spends more time talking to Class B than it does talking to its own class.

```java
// ❌ BAD: The User class is envious of the ShoppingCart's data!
class User {
    public double getCartTotal(ShoppingCart cart) {
        double total = 0;
        for (Item item : cart.getItems()) {
            total += item.getPrice() * cart.getTaxRate();
        }
        return total;
    }
}
```
**The Fix:** Move Method. The `getCartTotal()` logic clearly belongs inside the `ShoppingCart` class, not the `User` class!

## REAL_WORLD
**Data Clumps**
Do you often see the exact same three variables traveling together as parameters across 15 different methods?
`startDate` and `endDate`.
`x`, `y`, and `z`.

These are called Data Clumps. If data always travels together, it wants to be an Object.
Refactor `(Date startDate, Date endDate)` into a single `DateRange range` object. This shrinks method signatures instantly and gives you a place to put logic (like `range.contains(someDate)`).

## INTERVIEW
**Q: What is a "God Object" and how do you fix it?**
A: A God Object (or God Class) is a massive class that knows too much or does too much (e.g., a `SystemManager` or `ApplicationCore` class with 5,000 lines of code and 100 methods). It violates the Single Responsibility Principle and becomes a bottleneck because every developer is constantly modifying the same file, causing merge conflicts. You fix it by identifying distinct responsibilities within the class and extracting them into smaller, focused classes using the "Extract Class" refactoring technique.

## FEYNMAN CHECK

### Explain Code Smells Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Code Smells** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Code Smells, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Code Smells solve? Why would JavaScript/web development be harder without it?**
> **A:** Code Smells solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Code Smells?**
> **A:** The single most important thing is understanding the *mental model*: Code Smells operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Code Smells and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Code Smells operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Code Smells interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Code Smells interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Code Smells to a senior engineer in two sentences, what would you say?**
> **A:** "Code Smells works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Code Smells Utility

**Goal:** Implement a real, working Code Smells module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `codesmells` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir code-smells-demo && cd code-smells-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Code Smells Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Code Smells.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createCodeSmells(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Code Smells] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Code Smells logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Code Smells] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Code Smells] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createCodeSmells({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Code Smells] Input cannot be null or undefined`);
    }
    const instance = createCodeSmells();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Code Smells] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Code Smells Tests ──');

test('initialises correctly', () => {
  const m = createCodeSmells();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createCodeSmells();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Code Smells] Initialized with: hello world
[Code Smells] Processed: hello world
Result: hello world
[Code Smells] Destroyed.

── Code Smells Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Code Smells in one sentence.**
<details><summary>Answer</summary>

Code Smells is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Code Smells?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Code Smells.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Code Smells and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Code Smells | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Code Smells] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Code Smells causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Code Smells usage].
</details>

**Q6: What does Code Smells look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Code Smells typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Code Smells correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Code Smells:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_code_smells(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Code Smells in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Code Smells in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Code Smells', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Code Smells at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Code Smells uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Code Smells interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Code Smells is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Code Smells relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Code Smells
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Code Smells to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Code Smells fits in the data/control flow.
2. **Choose** the right variant/pattern of Code Smells for the scale requirement.
3. **Handle** failure modes: what happens when Code Smells is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Code Smells is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
