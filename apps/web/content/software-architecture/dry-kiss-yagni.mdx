---
slug: "dry-kiss-yagni"
title: "DRY, KISS, and YAGNI"
level: 2
---

## WHY
Software development is inherently complex. Without guiding principles, codebases naturally tend toward chaos, duplication, and over-engineering. Three famous acronyms—DRY, KISS, and YAGNI—serve as the foundational mantras for writing sane, maintainable code. They are not absolute rules, but rather heuristics to help you avoid the most common traps in software engineering.

## THEORY
### 1. DRY (Don't Repeat Yourself)
Every piece of knowledge must have a single, unambiguous, authoritative representation within a system.
**The Trap:** Copying and pasting code. If you find a bug in the original code, you have to remember to fix it in all 5 places you pasted it.

### 2. KISS (Keep It Simple, Stupid)
Systems work best if they are kept simple rather than made complicated.
**The Trap:** Trying to show off by writing a 1-line regex puzzle instead of 4 lines of highly readable, boring `if/else` statements.

### 3. YAGNI (You Aren't Gonna Need It)
Always implement things when you actually need them, never when you just foresee that you need them.
**The Trap:** Building a massive, scalable plugin architecture for a feature that the business might pivot away from next week.

## VISUALIZATION_CONFIG
```json
{ "component": "CodeRunner", "state": "dry-kiss-yagni" }
```

## CODE

### Level 1 — DRY (Don't Repeat Yourself)
```javascript
// ❌ BAD: WET (Write Everything Twice)
function calculateAdminDiscount(price) {
    const tax = price * 0.2;
    const finalPrice = price + tax;
    return finalPrice * 0.8; // 20% discount
}

function calculateUserDiscount(price) {
    const tax = price * 0.2; // Duplicated logic!
    const finalPrice = price + tax;
    return finalPrice * 0.9; // 10% discount
}

// ✅ GOOD: DRY
function calculateTax(price) {
    return price * 0.2;
}

function calculateDiscount(price, discountRate) {
    const finalPrice = price + calculateTax(price);
    return finalPrice * (1 - discountRate);
}
```

### Level 2 — KISS (Keep It Simple, Stupid)
Developers often conflate "fewer lines of code" with "better code."

```javascript
// ❌ BAD: Overly clever, hard to read, hard to debug.
const isEligible = (u) => u.age > 18 ? u.hasLicense ? u.tickets === 0 ? true : false : false : false;

// ✅ GOOD: Boring, readable, easy to step through in a debugger.
const isEligible = (user) => {
    if (user.age <= 18) return false;
    if (!user.hasLicense) return false;
    if (user.tickets > 0) return false;
    
    return true;
};
```

### Level 3 — YAGNI (You Aren't Gonna Need It)
Imagine you are asked to build a function that fetches a User from the database.

```java
// ❌ BAD: YAGNI Violation. 
// "Maybe one day we will need to fetch from Redis, or an XML file, or a GraphQL API!"
public interface UserFetcher { ... }
public class DatabaseUserFetcher implements UserFetcher { ... }
public class CacheUserFetcher implements UserFetcher { ... }
public class UserFetcherFactory { ... }

// ✅ GOOD: Build exactly what is needed TODAY.
public class UserRepository {
    public User fetchUser(String id) {
        return db.query("SELECT * FROM users WHERE id = ?", id);
    }
}
```
If the requirements change next year, *then* you can refactor it into an Interface. Until then, the Factory and Interface are just dead weight slowing down the team.

### Level 4 — Advanced (The Rule of Three)
The biggest mistake Junior Developers make is **Premature Abstraction** (Applying DRY too early).
If you see two pieces of code that look similar, and you immediately try to extract them into a shared function, you might accidentally bind two unrelated concepts together. When one concept needs to change but the other doesn't, your shared function becomes a nightmare of `if/else` flags.

**The Rule of Three:**
1. First time: Write the code.
2. Second time: Copy and paste the code (Violate DRY).
3. Third time: Now that you have 3 distinct use cases, you actually understand the exact boundaries of the pattern. *Now* you refactor into a shared, DRY abstraction.

*Duplication is far cheaper than the wrong abstraction.*

## REAL_WORLD
**When DRY goes wrong (Microservices)**
In a Monolithic application, if you have a `User` entity, you share it everywhere (DRY).
When moving to Microservices, teams often try to create a shared `common-lib` library holding the `User` entity that all 50 microservices must import. This violates the core tenet of Microservices (Independent Deployability). If the Billing team adds a field to the `User` object, they have to force the Inventory team to update their `common-lib` version and re-deploy. 

In Microservices, **WET (Write Everything Twice) is often preferred over DRY.** The Billing service should have its own representation of a `User`, and the Inventory service should have its own representation.

## INTERVIEW
**Q: How do you balance DRY and KISS?**
A: DRY and KISS are often at odds. Extracting duplicated code into a highly configurable generic factory (DRY) often introduces massive complexity, making the code harder to read (violating KISS). As a general rule, readability and simplicity (KISS) should trump deduplication (DRY). If extracting 10 lines of duplicated code requires passing 5 boolean flags into a complex generic function, it is better to leave the duplication alone.

## FEYNMAN CHECK

### Explain DRY, KISS, and YAGNI Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **DRY, KISS, and YAGNI** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about DRY, KISS, and YAGNI, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does DRY, KISS, and YAGNI solve? Why would JavaScript/web development be harder without it?**
> **A:** DRY, KISS, and YAGNI solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about DRY, KISS, and YAGNI?**
> **A:** The single most important thing is understanding the *mental model*: DRY, KISS, and YAGNI operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with DRY, KISS, and YAGNI and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which DRY, KISS, and YAGNI operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does DRY, KISS, and YAGNI interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** DRY, KISS, and YAGNI interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of DRY, KISS, and YAGNI to a senior engineer in two sentences, what would you say?**
> **A:** "DRY, KISS, and YAGNI works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a DRY, KISS, and YAGNI Utility

**Goal:** Implement a real, working DRY, KISS, and YAGNI module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `drykissyagni` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir dry-kiss-yagni-demo && cd dry-kiss-yagni-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── DRY, KISS, and YAGNI Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of DRY, KISS, and YAGNI.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createDRY,KISS,andYAGNI(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[DRY, KISS, and YAGNI] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual DRY, KISS, and YAGNI logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[DRY, KISS, and YAGNI] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[DRY, KISS, and YAGNI] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createDRY,KISS,andYAGNI({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[DRY, KISS, and YAGNI] Input cannot be null or undefined`);
    }
    const instance = createDRY,KISS,andYAGNI();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[DRY, KISS, and YAGNI] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── DRY, KISS, and YAGNI Tests ──');

test('initialises correctly', () => {
  const m = createDRY,KISS,andYAGNI();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createDRY,KISS,andYAGNI();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[DRY, KISS, and YAGNI] Initialized with: hello world
[DRY, KISS, and YAGNI] Processed: hello world
Result: hello world
[DRY, KISS, and YAGNI] Destroyed.

── DRY, KISS, and YAGNI Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define DRY, KISS, and YAGNI in one sentence.**
<details><summary>Answer</summary>

DRY, KISS, and YAGNI is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of DRY, KISS, and YAGNI?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of DRY, KISS, and YAGNI.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between DRY, KISS, and YAGNI and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | DRY, KISS, and YAGNI | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of DRY, KISS, and YAGNI] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing DRY, KISS, and YAGNI causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct DRY, KISS, and YAGNI usage].
</details>

**Q6: What does DRY, KISS, and YAGNI look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, DRY, KISS, and YAGNI typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use DRY, KISS, and YAGNI correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using DRY, KISS, and YAGNI:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_dry_kiss_yagni(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test DRY, KISS, and YAGNI in isolation? What would you mock?**
<details><summary>Answer</summary>

To test DRY, KISS, and YAGNI in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('DRY, KISS, and YAGNI', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of DRY, KISS, and YAGNI at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how DRY, KISS, and YAGNI uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common DRY, KISS, and YAGNI interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "DRY, KISS, and YAGNI is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does DRY, KISS, and YAGNI relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
DRY, KISS, and YAGNI
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use DRY, KISS, and YAGNI to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where DRY, KISS, and YAGNI fits in the data/control flow.
2. **Choose** the right variant/pattern of DRY, KISS, and YAGNI for the scale requirement.
3. **Handle** failure modes: what happens when DRY, KISS, and YAGNI is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you DRY, KISS, and YAGNI is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
