---
title: templates
slug: templates
---

### Layer 1: Core Mechanics & Architecture

Angular's template compilation phase is a multi-step pipeline executed by the `@angular/compiler` package. It parses HTML templates, builds an Abstract Syntax Tree (AST), optimizes expression