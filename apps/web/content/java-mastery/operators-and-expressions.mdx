---
slug: "operators-and-expressions"
title: "Operators and Expressions"
level: 1
---

## WHY

At its core, programming is manipulating data. You take inputs, evaluate them through logic, calculate new values, and produce outputs. Without operators, a programming language would be completely static. 

However, mathematical manipulation in computers is not as simple as high-school algebra. If you add two extremely large positive numbers together in a computer, the result might suddenly become a massive negative number! If you check whether a complex file stream is ready before checking if the file actually exists, your program will crash with a Null Pointer Exception. 

Operators in Java define the precise rules of computation, logical evaluation, and byte-level bit manipulation. Understanding these operators deeply—specifically concepts like short-circuit evaluation, integer promotion, and operator precedence—is the difference between code that works purely by accident and code that is provably correct under all edge cases.

## THEORY

An **Expression** in Java is any combination of variables, literals, method calls, and operators that evaluates to a single value.

### Arithmetic and Integer Promotion
Java supports standard arithmetic (`+`, `-`, `*`, `/`, `%`). 
**The Trap**: When performing arithmetic on smaller types (`byte`, `short`, `char`), Java automatically applies **Integer Promotion**. It converts them to `int` before performing the calculation.
```java
byte b1 = 10;
byte b2 = 20;
// byte sum = b1 + b2; // COMPILER ERROR! b1+b2 evaluates to an 'int'
byte sum = (byte) (b1 + b2); // Requires explicit cast back to byte
```

### Logical and Short-Circuit Operators
Java has bitwise logical operators (`&`, `|`) and short-circuit logical operators (`&&`, `||`).
- `A && B` (Short-Circuit AND): If `A` is `false`, Java completely skips evaluating `B` because the result is guaranteed to be `false`. This is extremely useful for null-checks.
- `A & B` (Bitwise AND): Evaluates BOTH `A` and `B`, regardless of the value of `A`.

### Bitwise and Shift Operators
These operators manipulate numbers directly at the binary level.
- `>>` (Signed Right Shift): Shifts bits to the right. It fills the new leftmost bits with the *sign bit* (preserves whether the number is negative or positive). Effectively divides by 2.
- `>>>` (Unsigned Right Shift): Shifts bits to the right, but always fills the new leftmost bits with `0`, regardless of the sign. Unique to Java because Java doesn't have unsigned numeric types.
- `<<` (Left Shift): Shifts bits to the left, filling the right with `0`. Effectively multiplies by 2.

### Compound Assignment Hidden Casts
Operators like `+=`, `-=`, `*=` are not just syntax sugar. They include a **hidden cast**.
```java
short s = 10;
s = s + 5; // ERROR: s + 5 is an int
s += 5;    // SUCCESS: compiles to s = (short)(s + 5)
```

## VISUALIZATION_CONFIG
```json
{
  "language": "java",
  "fileName": "Operators.java",
  "steps": [
    {
      "title": "Arithmetic operators",
      "description": "Standard arithmetic: +, -, *, /, %. Integer division truncates (5/2 = 2). % gives the remainder. Unary -/+ also exist.",
      "code": "int a = 10, b = 3;\nSystem.out.println(a + b);   // 13\nSystem.out.println(a / b);   // 3  (truncated, not 3.33)\nSystem.out.println(a % b);   // 1  (remainder)\ndouble d = 10.0 / 3;         // 3.3333... (double division)",
      "highlight": [
        3,
        4,
        5
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Arithmetic",
        "items": [
          {
            "label": "10 / 3",
            "value": "3 (truncated)",
            "color": "#f59e0b",
            "highlight": true
          },
          {
            "label": "10 % 3",
            "value": "1 (remainder)",
            "color": "#818cf8"
          },
          {
            "label": "10.0 / 3",
            "value": "3.333...",
            "color": "#10b981"
          }
        ]
      }
    },
    {
      "title": "Comparison and logical operators",
      "description": "==, !=, <, >, <=, >= produce boolean. && and || are short-circuit: if the left side determines the result, the right side is NOT evaluated.",
      "code": "int x = 5;\nboolean gt = x > 3;           // true\nboolean and = (x > 0) && (x < 10);  // true (both evaluated)\nboolean sc  = (x > 10) && riskyCall(); // false: riskyCall NOT called\nboolean or  = (x > 0)  || riskyCall(); // true:  riskyCall NOT called",
      "highlight": [
        4,
        5
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "(x > 10) → false",
            "done": true
          },
          {
            "label": "&& short-circuit: right side SKIPPED",
            "active": true
          },
          {
            "label": "final result: false"
          }
        ]
      }
    },
    {
      "title": "Increment / decrement — pre vs post",
      "description": "++i increments BEFORE the value is used. i++ increments AFTER. This matters when used inside larger expressions.",
      "code": "int i = 5;\nint a = i++;   // a = 5, i = 6  (post: use then increment)\nint b = ++i;   // b = 7, i = 7  (pre: increment then use)\nSystem.out.println(a + \" \" + b + \" \" + i);  // 5 7 7",
      "highlight": [
        2,
        3
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Pre vs post increment",
        "items": [
          {
            "label": "i++",
            "value": "use first, add 1",
            "color": "#f59e0b",
            "highlight": true
          },
          {
            "label": "++i",
            "value": "add 1 first, use",
            "color": "#10b981"
          }
        ]
      }
    },
    {
      "title": "Bitwise operators",
      "description": "&, |, ^, ~, <<, >>, >>> operate on individual bits. Most common in flags, hash functions, and performance-critical code.",
      "code": "int a = 0b1010;   // 10\nint b = 0b1100;   // 12\nSystem.out.println(a & b);   // 0b1000 = 8  (AND)\nSystem.out.println(a | b);   // 0b1110 = 14 (OR)\nSystem.out.println(a << 1);  // 0b10100 = 20 (left shift = *2)",
      "highlight": [
        3,
        4,
        5
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Bitwise ops",
        "items": [
          {
            "label": "a = 1010",
            "color": "#818cf8"
          },
          {
            "label": "b = 1100",
            "color": "#818cf8"
          },
          {
            "label": "a & b = 1000 (8)",
            "color": "#10b981",
            "highlight": true
          },
          {
            "label": "a | b = 1110 (14)",
            "color": "#10b981"
          },
          {
            "label": "a << 1 = 10100 (20)",
            "color": "#f59e0b"
          }
        ]
      }
    },
    {
      "title": "Ternary and instanceof",
      "description": "The ternary `condition ? a : b` is a compact if-else expression. `instanceof` tests the runtime type; Java 16+ pattern matching avoids the explicit cast.",
      "code": "int x = 7;\nString label = (x % 2 == 0) ? \"even\" : \"odd\";  // \"odd\"\n\nObject obj = \"Hello\";\nif (obj instanceof String s) {  // pattern matching (Java 16+)\n    System.out.println(s.toUpperCase());  // s already a String\n}",
      "highlight": [
        2,
        4,
        5
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "condition evaluated",
            "done": true
          },
          {
            "label": "true  → return a",
            "done": true
          },
          {
            "label": "false → return b",
            "active": true
          }
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner
```java
public class BasicOperators {
    public static void main(String[] args) {
        int a = 10;
        int b = 3;
        
        // Integer division truncates the decimal!
        System.out.println(a / b); // Prints 3, NOT 3.33
        
        // Modulo gets the remainder
        System.out.println(a % b); // Prints 1
        
        // Post-increment (evaluates, THEN increments)
        int x = 5;
        System.out.println(x++); // Prints 5
        System.out.println(x);   // Prints 6
    }
}
```

### Level 2 — Building Blocks
```java
public class ShortCircuit {
    public static void main(String[] args) {
        String text = null;
        
        // The JVM evaluates left-to-right.
        // text != null is FALSE. 
        // Because it's &&, the JVM short-circuits and skips text.length() > 0.
        // This prevents a NullPointerException!
        if (text != null && text.length() > 0) {
            System.out.println("Valid text");
        }
    }
}
```

### Level 3 — Intermediate
```java
public class BitMasking {
    public static void main(String[] args) {
        // Flags stored as bits in a single byte:
        // 00000001 = READ (1)
        // 00000010 = WRITE (2)
        // 00000100 = EXECUTE (4)
        
        byte READ = 1;
        byte WRITE = 2;
        
        // Combine permissions using Bitwise OR
        byte myPermissions = (byte) (READ | WRITE); // 00000011 (3)
        
        // Check permissions using Bitwise AND
        boolean canWrite = (myPermissions & WRITE) == WRITE;
        System.out.println("Can write? " + canWrite); // true
    }
}
```

### Level 4 — Advanced
```java
public class OverflowTrap {
    public static void main(String[] args) {
        int max = Integer.MAX_VALUE; // 2,147,483,647 (011111...1)
        
        // Adding 1 flips the sign bit!
        int overflow = max + 1;
        
        System.out.println(overflow); // Prints -2147483648
        
        // To safely calculate without silent overflow, use Math.addExact
        try {
            Math.addExact(max, 1);
        } catch (ArithmeticException e) {
            System.out.println("Safely caught the overflow!");
        }
    }
}
```

## REAL_WORLD

In heavily optimized Java libraries like `java.util.concurrent.ConcurrentHashMap` or hashing algorithms, you will rarely see division (`/`) or modulo (`%`) operators used in hot paths. 

Why? Because CPU instructions for division are notoriously slow compared to bitwise operators. 

If you look at the source code of `HashMap` in the JDK, you will see how it calculates the array bucket index for a given key. Instead of doing `hash % array.length`, it does this:
```java
// REAL JDK HashMap Source Code (simplified)
int index = (n - 1) & hash;
```
Because `HashMap` heavily enforces that the array size `n` is always a power of 2, `(n - 1)` creates a bitmask (e.g., `16 - 1 = 15`, which is `00001111` in binary). Doing a bitwise `&` with the hash is mathematically identical to `hash % n`, but executes drastically faster at the CPU level.

Another common real-world issue is the **Ternary Operator NPE trap**.
```java
Integer a = null;
int b = 2;
// If the condition is true, Java attempts to unbox 'a' to match 'b's primitive type!
// This throws a NullPointerException silently on the line below.
int result = (true) ? a : b; 
```
When using the ternary operator `? :`, if the second and third operands are a mix of primitives and wrappers, Java will automatically attempt to unbox the wrapper to match the primitive. If the wrapper is null, it crashes.

## INTERVIEW

**Q: What is the difference between `>>` and `>>>` in Java?**
A: Both shift bits to the right. The signed right shift `>>` preserves the sign bit (the leftmost bit), meaning negative numbers stay negative. The unsigned right shift `>>>` ignores the sign and always fills the new leftmost bits with `0`, making the result positive. Java added `>>>` because unlike C++, Java does not have unsigned integer data types.

**Q: Explain Short-Circuit evaluation and why it is critical.**
A: Short-circuit operators (`&&`, `||`) stop evaluating expressions as soon as the outcome is determined. In `A && B`, if `A` is false, `B` is never executed. This is critical for preventing runtime exceptions (like checking if an object is null before calling a method on it) and avoiding expensive, unnecessary method calls.

**Q: Will the following code compile? `byte b = 5; b = b + 2;`**
A: No, it will cause a compiler error. Due to Integer Promotion, the expression `b + 2` is automatically promoted to an `int` during calculation. You cannot assign an `int` back to a `byte` variable without an explicit cast `b = (byte)(b + 2)`. However, `b += 2` would compile because compound assignment operators include a hidden cast.

**Q: What happens if an integer overflows in Java? Does it throw an exception?**
A: No, standard arithmetic operators gracefully overflow and underflow silently. Adding 1 to `Integer.MAX_VALUE` will wrap around to `Integer.MIN_VALUE` due to Two's Complement binary representation. If you want an exception to be thrown on overflow, you must use the `Math.addExact()` utility methods introduced in Java 8.

**Q: Why does `0.1 + 0.2 != 0.3` in Java?**
A: `double` and `float` follow the IEEE 754 binary floating-point standard. Decimal fractions like 0.1 cannot be represented exactly in base-2 (just like 1/3 cannot be represented exactly in base-10). The closest representable binary value for 0.1 is slightly off, so the sum produces `0.30000000000000004`. For money or anything requiring exact decimals, use `BigDecimal` constructed from a `String` (`new BigDecimal("0.1")`), never from a `double`.

**Q: Order of operations and operator precedence: explain `a + b << 2`.**
A: `+` has higher precedence than `<<`, so it parses as `(a + b) << 2`. Java applies the standard precedence table (postfix > unary > multiplicative > additive > shift > relational > equality > bitwise-AND > bitwise-XOR > bitwise-OR > logical-AND > logical-OR > ternary > assignment). When in doubt, parenthesize — the compiler doesn't care, but the next human reader will.

**Q: What does `instanceof` actually do at the bytecode level?**
A: It compiles to a single JVM bytecode instruction (`instanceof`) that checks whether the object reference's runtime class is the same as, or a subtype of, the target class. The check returns `false` for `null` (a key safety property). Since Java 16, you can also use **pattern matching** (`if (obj instanceof String s)`) which combines the check and the cast into a single statement.

**Q: Why does `String s = "a" + "b" + "c"` not create three intermediate strings?**
A: The Java compiler folds compile-time-constant `String` concatenations directly into a single literal in the constant pool. `"a" + "b" + "c"` becomes literally `"abc"` in the `.class` file. However, when one operand is a variable (`"hello " + name`), the compiler uses `StringBuilder` (Java 8) or `invokedynamic` with `StringConcatFactory` (Java 9+) — never naive `+` chaining that would allocate per-step.

---

## FEYNMAN CHECK

Explain operators to a beginner in 60 seconds:

> Operators are the *verbs* of code. Variables are the nouns ("Alice", "five", "the bank balance"); operators are how you act on them: add, compare, shift, choose. Most operators come from school math (`+`, `-`, `<`, `=`). A few are special to computers: `&` and `|` work on the binary bits of a number, `<<` shifts those bits left like multiplying by powers of two, `>>>` does the same in the opposite direction without caring about the minus sign, and `&&` is a sneaky `and` that doesn't bother checking its second condition if the first one is already false. Once you know the precedence table — like remembering BODMAS in primary school — you can read any expression at a glance.

**Self-test questions:**
1. What is the precedence of `&&` vs `||`?
2. Why does `b = b + 1` fail for `byte b` but `b += 1` compile?
3. What does `Integer.MAX_VALUE + 1` evaluate to and why?
4. What is the difference between `==` and `.equals()` for `Integer` objects?
5. Why is `>>>` not in C++ but is in Java?
6. In what order are operands of `a && b` evaluated, and what is the side-effect implication?
7. Explain how `String s = "a" + i + "b"` is compiled.

If you can answer all seven, you have a deep grasp of Java's expression model — far more than most working developers.

---

## BUILD

**Mini-project: a tiny expression evaluator.**

Goal: write a class `ExpressionEvaluator` that takes a string like `"3 + 4 * (2 - 1) >> 1"` and returns the integer result, using nothing but your understanding of operator precedence and the Java standard library.

### Requirements
1. Tokenize the input into numbers, operators, and parentheses. Support `+ - * / % << >> >>> & | ^ && || == != < <= > >= ! ~`.
2. Implement the **shunting-yard algorithm** to convert the infix token stream into a postfix (RPN) stream. Reference the precedence table from the THEORY section. Right-associative operators (`**` if you add one, the ternary `? :`) must be respected.
3. Evaluate the RPN stream with a stack.
4. Add a `--debug` flag that prints, for each token, the operator stack and the output queue. This visualizes precedence in action.
5. Add unit tests for at least 30 expressions including: integer overflow (`2_147_483_647 + 1`), short-circuit (`0 != 0 && (10/0 > 0)` must NOT divide by zero), bitwise (`0b1010 & 0b1100`), and unary minus.
6. Add support for `Math.addExact` mode where overflow throws instead of wrapping.

### Stretch goals
* Add floating-point support and reproduce the `0.1 + 0.2` bug. Then add `BigDecimal` mode and show that the bug disappears.
* Add Java's full ternary `? :` operator.
* Support variables: `let x = 10; let y = 20; x * y + 5`.

This single project burns every operator concept into your bones because you have to *implement* the precedence and associativity rules, not just use them.

---

## SPACED REVIEW

**Day 1 — Recall:**
* List Java's operators from highest to lowest precedence.
* What is the difference between `&` and `&&`, and `|` and `||`?
* What does `~5` evaluate to and why?

**Day 3 — Apply:**
* Predict the output of: `int x = 5; System.out.println(x++ + ++x);` — then run it and verify.
* Predict the output of: `System.out.println(0.1 + 0.2 == 0.3);` — then explain to a friend.
* Write a one-liner that swaps two `int` variables using `^=` without a temp.

**Day 7 — Synthesize:**
* Read `java.lang.Math` and list every `*Exact` method. Explain when you would use them in financial code.
* Compare Java's `>>` and `>>>` with C++'s shift operators. What does C++ do for signed shifts?
* Write a 200-word essay on why `BigDecimal` exists.

**Day 21 — Teach:**
* In a whiteboard session, derive Two's Complement representation from scratch and explain why `Integer.MIN_VALUE * -1` overflows.

**Day 60 — Production audit:**
* `grep` your codebase for `==` used on boxed types (`Integer`, `Long`, `Double`). Every match is a potential bug. Fix at least one and write a regression test.
