---
title: ts-intro
slug: ts-intro
---

# TypeScript Introduction — Why Types, tsc, tsconfig

TypeScript is a typed superset of JavaScript that compiles to plain JavaScript. It introduces a static type system to JavaScript's dynamic runtime, allowing developers to catch type-related defects during development rather than at runtime.

---

## 1. Structural vs. Nominal Typing

Unlike languages like Java or C++, which use **nominal typing** (where compatibility is determined by explicit declarations and class names), TypeScript uses a **structural type system** (often referred to as "duck typing"). 

In a structural system, type compatibility is determined solely by the shape and members of the type. If two objects have the same properties with compatible types, they are considered to be of the same type, regardless of their nominal declarations.

```typescript
class Vector2D {
  constructor(public x: number, public y: number) {}
}

class Point {
  constructor(public x: number, public y: number) {}
}

// Perfectly valid because the structures are identical
const point: Point = new Vector2D(10, 20);
```

---

## 2. Type Erasure: Compile-Time vs. Runtime

TypeScript is a build-time tool. The TypeScript compiler (`tsc`) performs static analysis, checks for type safety, and then strips away all type annotations, interfaces, type aliases, and other type-level constructs. This process is called **type erasure**. 

The generated JavaScript contains no type information, meaning there is **zero runtime performance overhead** associated with TypeScript types.

### Side-by-Side Comparison

```typescript
// --- TypeScript Source (input.ts) ---
interface User {
  id: string;
  email: string;
}

function getUserEmail(user: User): string {
  return user.email;
}

const user: User = { id: "u1", email: "user@domain.com" };
const email = getUserEmail(user);
```

```javascript
// --- Compiled JavaScript (tsc Output - output.js) ---
"use strict";
function getUserEmail(user) {
    return user.email;
}
const user = { id: "u1", email: "user@domain.com" };
const email = getUserEmail(user);
```

---

## 3. The `tsconfig.json` Configuration

The `tsconfig.json` file specifies the root files and the compiler options required to compile the project. Below is an enterprise-grade configuration using `"strict": true` along with explanations of its downstream implications.

```json
{
  "compilerOptions": {
    "target": "ES2022",                          /* Specify ECMAScript target version */
    "module": "NodeNext",                        /* Specify module code generation */
    "moduleResolution": "NodeNext",              /* Determine how modules get resolved */
    "lib": ["ES2022", "DOM", "DOM.Iterable"],    /* Specify library files to be included */
    "outDir": "./dist",                          /* Redirect output structure to the directory */
    "rootDir": "./src",                          /* Specify the root directory of input files */
    
    /* Strict Type-Checking Options */
    "strict": true,                              /* Enable all strict type-checking options */
    "noImplicitAny": true,                       /* Raise error on expressions and declarations with an implied 'any' type */
    "strictNullChecks": true,                    /* Enable strict null checks */
    "strictFunctionTypes": true,                 /* Enable strict checking of function types */
    "strictBindCallApply": true,                 /* Enable strict 'bind', 'call', and 'apply' methods on functions */
    "strictPropertyInitialization": true,        /* Enable strict checking of property initialization in classes */
    "noImplicitThis": true,                      /* Raise error on 'this' expressions with an implied 'any' type */
    "useUnknownInCatchVariables": true,          /* Default catch clause variables to 'unknown' instead of 'any' */
    "alwaysStrict": true,                        /* Parse in strict mode and emit "use strict" for each source file */

    /* Additional Checks */
    "noUnusedLocals": true,                      /* Report errors on unused locals */
    "noUnusedParameters": true,                  /* Report errors on unused parameters */
    "noImplicitReturns": true,                   /* Report error when not all code paths in function return a value */
    "noFallthroughCasesInSwitch": true,          /* Report errors for fallthrough cases in switch statement */
    
    "esModuleInterop": true,                     /* Enables emit interoperability between CommonJS and ES Modules */
    "skipLibCheck": true,                        /* Skip type checking of declaration files */
    "forceConsistentCasingInFileNames": true     /* Disallow inconsistently-cased references to the same file */
  },
  "include": ["src/**/*"]
}
```

### Key Flags Enabled by `"strict": true`
*   **`noImplicitAny`**: Prevents TypeScript from inferring `any` when a type cannot be determined. If you do not annotate a parameter, it throws `TS7006`.
*   **`strictNullChecks`**: Ensures `null` and `undefined` are not assignable to other types. This eliminates the infamous "Cannot read property of undefined" runtime errors.
*   **`strictPropertyInitialization`**: Throws an error (`TS2564`) if a class property is declared but not initialized in the constructor (unless marked as optional or using the definite assignment assertion `!`).

---

## 4. TypeScript Errors vs. Runtime Errors

TypeScript catches syntax, structural, and semantic errors at **compile time**. It does not alter the execution characteristics of JavaScript. If a compilation error occurs, `tsc` will still emit JS files unless configured with `"noEmitOnError": true`.

```typescript
// TypeScript Error Example
const config = { port: 8080 };
config.port = "8081"; 
// Compiler Error: TS2322: Type 'string' is not assignable to type 'number'.

// Runtime Error Example (Uncaught by static typing alone if bypassed with 'any')
const badData: any = "not an object";
console.log(badData.user.name); 
// Compiles fine, but throws at runtime: TypeError: Cannot read properties of undefined (reading 'name')
```

---

## 5. When TypeScript Helps Most

1.  **Large-Scale Refactoring**: Renaming a property or changing a function signature across thousands of files becomes trivial and safe. The compiler flags every single broken reference.
2.  **API Contracts**: Establishing clear boundary definitions between microservices or frontend/backend models.
3.  **Self-Documenting Codebases**: Types serve as living documentation that stays in sync with execution logic, reducing the cognitive load on engineering teams.

---

## Reference
*   [TypeScript Handbook: The Basics](https://www.typescriptlang.org/docs/handbook/2/basic-types.html)