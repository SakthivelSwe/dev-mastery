---
slug: "html-basics"
title: "HTML Basics"
level: 2
---

## WHY
If a website is a house, HTML is the concrete foundation and wooden framing. 
It doesn't care about paint colors (CSS) or electricity (JavaScript). Its only job is to define the **structure** and **content** of the page.
Before a browser can make a button look pretty or make a menu interactive, it needs to know that a button and a menu physically exist. **HyperText Markup Language (HTML)** provides this structural blueprint using a standard vocabulary of "tags" that every browser on Earth understands.

## THEORY
### Elements and Tags
HTML is not a programming language; it has no logic, variables, or loops. It is a *markup* language. You write standard text, and you wrap that text in HTML **Tags** to give it semantic meaning.
- `<p>` means Paragraph.
- `<h1>` means Heading 1 (The most important title).
- `<button>` means Button.

An **Element** consists of the Opening Tag, the Content, and the Closing Tag (which has a forward slash `/`).
Example: `<h1>Welcome to my Website!</h1>`

### The DOM Tree (Nesting)
HTML elements are designed to be nested inside one another, creating a Parent-Child hierarchy. This hierarchy forms a massive upside-down tree structure known as the **Document Object Model (DOM)**. When you write CSS or JavaScript later, you will use this DOM tree to precisely target and manipulate specific HTML elements!

## VISUALIZATION_CONFIG
```json
{
  "steps": [
    {
      "title": "Document Structure",
      "description": "Every HTML document has DOCTYPE, html, head, body — the foundation.",
      "code": "<!DOCTYPE html>\n<html>\n  <head><!-- metadata --></head>\n  <body><!-- content --></body>\n</html>",
      "highlight": [
        1,
        2,
        3,
        4
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "DOCTYPE declaration",
          "html root",
          "head (metadata)",
          "body (content)"
        ]
      }
    },
    {
      "title": "Text Elements",
      "description": "h1-h6 headings, p paragraphs, a links, strong/em emphasis.",
      "code": "<h1>Main Title</h1>\n<h2>Section</h2>\n<p>A paragraph with <strong>bold</strong> and <em>italic</em>.</p>\n<a href=\"/link\">Link</a>",
      "highlight": [
        1,
        2,
        3,
        4
      ],
      "diagram": {
        "kind": "boxes",
        "boxes": [
          {
            "id": "h",
            "label": "h1-h6 headings",
            "color": "#1e88e5"
          },
          {
            "id": "p",
            "label": "paragraphs",
            "color": "#43a047"
          },
          {
            "id": "l",
            "label": "links",
            "color": "#fb8c00"
          }
        ],
        "connections": []
      }
    },
    {
      "title": "Media Elements",
      "description": "img, video, audio, picture — embed media with accessibility attributes.",
      "code": "<img src=\"hero.jpg\" alt=\"Hero image\" width=\"1200\" height=\"600\" />\n\n<video controls poster=\"thumb.jpg\">\n  <source src=\"video.mp4\" type=\"video/mp4\" />\n</video>\n\n<audio controls src=\"song.mp3\"></audio>",
      "highlight": [
        1,
        3,
        4,
        6
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "img with alt",
          "video + sources",
          "audio",
          "Accessibility important"
        ]
      }
    }
  ]
}
```

## CODE
### 1. The Standard HTML Boilerplate
Every single HTML file in existence requires this exact foundational shell.

```html
<!-- Tells the browser this is modern HTML5, not ancient HTML from 1999 -->
<!DOCTYPE html> 

<!-- The root element that wraps the entire document -->
<html lang="en"> 

    <!-- The HEAD contains metadata. Nothing here is visible on the actual web page! -->
    <head>
        <meta charset="UTF-8">
        <title>My First Website</title> <!-- The text in the browser tab! -->
    </head>

    <!-- The BODY contains the actual visible content of the page! -->
    <body>
        
        <!-- We write our tags in here! -->

    </body>
</html>
```

### 2. Core Text Elements
```html
<h1>This is the Main Title (Only use one per page for SEO!)</h1>
<h2>This is a Subtitle</h2>
<h3>This is a smaller Sub-subtitle</h3>

<p>This is a standard paragraph of text. Browsers automatically add spacing around paragraphs.</p>

<!-- Some elements are Self-Closing (They have no content inside them!) -->
<br /> <!-- Line Break (Forces a return carriage) -->
<hr /> <!-- Horizontal Rule (Draws a line across the page) -->
```

### 3. Links and Images (Attributes)
Tags can have **Attributes** injected into their opening tag. Attributes provide extra configuration data that the tag needs to function.

```html
<!-- The Anchor tag <a> creates a hyperlink. 
     The 'href' attribute tells it WHERE to go when clicked! -->
<a href="https://google.com">Click here to search</a>

<!-- The Image tag <img> is self-closing. 
     The 'src' attribute points to the image file. 
     The 'alt' attribute provides text for screen readers (Accessibility)! -->
<img src="cute-dog.jpg" alt="A fluffy golden retriever" />
```

### 4. Layout Elements (The Div)
To structure a complex page, we group related elements together into invisible boxes. The most common box is the `<div>` (Division).

```html
<!-- A Div acts as an invisible container holding the profile data together -->
<div>
    <h2>User Profile</h2>
    <img src="avatar.png" alt="User Avatar" />
    <p>Name: John Doe</p>
    
    <!-- A button element for user interaction -->
    <button>Send Message</button> 
</div>
```

## REAL_WORLD
**Semantic HTML and Accessibility**
In the early days of web development, developers used `<div>` tags for absolutely everything. They built navigation bars with `<div>`, footers with `<div>`, and articles with `<div>`.
Visually, this looked fine. But structurally, it was a disaster. 
Search engines (like Google) and Screen Readers (used by blind users) could not look at a massive pile of 500 `<div>` tags and understand what the page was about.
Modern HTML5 introduced **Semantic Tags**. Instead of using a generic `<div>`, you should use `<nav>`, `<header>`, `<main>`, `<article>`, and `<footer>`. These tags function exactly like `<div>` boxes, but they carry deep structural meaning. When a blind user's Screen Reader encounters a `<nav>` tag, it explicitly announces "Navigation Menu", drastically improving Web Accessibility (a legal requirement for modern applications!).

## INTERVIEW
**Q: An interviewer looks at your code: `<img src="logo.png">`. They ask: "Why is it absolutely critical to include the `alt` attribute on every image tag, even if the image is just a decorative background swoosh?"**
A: **Accessibility and SEO.**
1. **Accessibility (A11y):** Visually impaired users rely on screen readers. If an image lacks an `alt` attribute, the screen reader might confusingly read the raw file name (e.g., "Image: logo underscore final version 2 dot png"). If the image is purely decorative, you MUST include an empty alt attribute (`alt=""`). This explicitly commands the screen reader to completely ignore the image and skip it!
2. **SEO (Search Engine Optimization):** Google's web crawlers cannot "see" images. They rely entirely on the `alt` text to understand the context of the image. If you omit the `alt` text, your images will never appear in Google Image Search, harming your website's organic traffic.

## FEYNMAN CHECK

### Explain HTML Basics Like I'm 10 Years Old
> HTML is the skeleton of a webpage. Tags like `<h1>`, `<p>`, `<button>` describe what each chunk MEANS, not what it looks like. Semantic tags (`<nav>`, `<main>`, `<article>`) tell screen readers and search engines the role of each section. The non-obvious part: visual styling can fake structure (a styled div can LOOK like a button) but assistive tech sees the underlying tag — a styled div is silent to a blind user. This is why semantic HTML is the foundation of accessibility, not optional decoration.

---

### 5 Deep Questions
**Q1: Why is semantic HTML better than div soup?**
> **A:** Screen readers, browser keyboard navigation, SEO crawlers, reader-mode tools, and CSS targeting all benefit from semantic tags. `<button>` gets keyboard activation, focus ring, role announcement free; `<div onClick>` gets none.

**Q2: ONE model.**
> **A:** "HTML = tree of typed nodes. Each tag carries semantics (what), accessibility (who), and default behaviour (how). Styling layers on top; structure should never be invented in CSS."

**Q3: Misconception with code.**
> **A:** ```html
> <!-- ❌ Div pretending to be a heading — invisible to SR users -->
> <div class="big-text">Welcome</div>
> <!-- ✅ Semantic heading — screen reader announces "heading level 1" -->
> <h1>Welcome</h1>
> ```

**Q4: How does HTML interact with the DOM at runtime?**
> **A:** Browser parses HTML into a tree of nodes (the DOM). JavaScript mutates the DOM directly via `document.querySelector` etc. The DOM is the live representation — HTML is only the initial snapshot.

**Q5: Senior one-liner.**
> **A:** "HTML is a tree of semantically-typed elements that double as the accessibility tree and the SEO contract — which is why divs-everywhere creates parallel maintenance costs in CSS, JS, ARIA, and SEO that semantic markup avoids."

## BUILD

### 🏗️ Mini Project: Semantic Blog Post Page
**Time:** 20 minutes

#### Step 1 — Setup
```bash
mkdir semantic-blog && cd semantic-blog && touch index.html
```

#### Step 2 — Core
```html
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Building accessible UIs — DevMastery</title>
  <meta name="description" content="Why semantic HTML matters">
</head>
<body>
  <header>
    <nav aria-label="Main">
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/blog">Blog</a></li>
      </ul>
    </nav>
  </header>
  <main>
    <article>
      <header>
        <h1>Building accessible UIs</h1>
        <p><time datetime="2026-06-29">29 June 2026</time> by <a href="/authors/jane">Jane Doe</a></p>
      </header>
      <section>
        <h2>Why semantic HTML</h2>
        <p>Semantic tags give meaning to assistive technologies…</p>
      </section>
      <footer><p>Tagged: <a href="/tag/a11y">accessibility</a></p></footer>
    </article>
  </main>
  <footer><p>© 2026 DevMastery</p></footer>
</body>
</html>
```

#### Step 5 — Verify
- Open in browser → reader-view should show clean content
- Run axe-core or Lighthouse → 100/100 accessibility
- Inspect with screen reader (NVDA/VoiceOver) → hear landmarks

**Expected Output:** Lighthouse a11y score 100, all landmarks announced.

## SPACED REVIEW

### Day 1
**Q1:** Name 6 semantic HTML5 landmark elements.
**Q2:** Why is `<button>` better than `<div onClick>`?
**Q3:** Write a semantic blog article skeleton in 15 lines.

### Day 3
**Q4:** `<section>` vs `<article>` vs `<div>` — when each?
**Q5:** Show the styled-div-as-button bug and the fix.
**Q6:** Why does `<h1>` matter for SEO?

### Day 7
**Q7:** Convert a div-soup page to semantic HTML and run Lighthouse before/after.
**Q8:** PR uses `<div role="button">` for 30 controls. Describe SR + keyboard impact.
**Q9:** What ARIA do you need on top of semantic HTML, and what does HTML give for free?

### Day 14
**Q10:** ★ "Audit a real news site's HTML structure — what would you change and why?"
**Q11:** Link HTML → ARIA → CSS → JS.
**Q12:** ★ "Your SEO traffic drops 40% after a redesign. How does HTML structure cause this?"
