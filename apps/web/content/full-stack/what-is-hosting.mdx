---
slug: "what-is-hosting"
title: "What Is Hosting?"
level: 1
---

## WHY
You've built a beautiful website on your laptop. You can open `index.html` in your browser and it works perfectly. But if you text the link `C:\Users\John\Desktop\index.html` to your friend, they can't see it. Why? Because the files only exist on your physical hard drive, and your laptop isn't connected to the public Internet as a server.
To share your website with the world, those files must live on a computer that is **always turned on**, **always connected to the Internet**, and **configured to serve files to anyone who asks**. This process of renting space on a public computer is called **Web Hosting**.

## THEORY
### The Landlord and the Tenant
Think of building a website like building a house.
- The **Domain Name** (like `google.com`) is the *Street Address*. It tells people where to go.
- The **HTML/CSS/JS files** are the *House* itself.
- **Web Hosting** is the *Plot of Land* the house sits on. 

Without land, your house has nowhere to exist. You rent this "land" (server space) from hosting companies. These companies own massive warehouses (Data Centers) filled with thousands of specialized computers (Servers) that are physically wired directly into the backbone of the global Internet.

### Types of Hosting
Just like renting property, there are different tiers of hosting:
1. **Shared Hosting:** You rent a tiny room in a massive apartment building. You share the server's CPU and RAM with thousands of other websites. It's very cheap, but if another website gets a massive spike in traffic, *your* website slows down!
2. **Virtual Private Server (VPS):** You rent an entire floor of the apartment building. You still share the physical hardware, but the server uses virtualization to guarantee you a strict, dedicated slice of CPU and RAM that nobody else can touch.
3. **Dedicated Hosting:** You rent the entire physical mansion. You have exclusive access to a bare-metal server. It is incredibly expensive, used only by massive enterprise applications.
4. **Cloud Hosting (Serverless):** You don't rent a specific room or building. You simply upload your code to a "cloud" (a massive interconnected network of servers like AWS or Vercel). The cloud automatically copies your website to hundreds of servers worldwide and scales the resources up and down dynamically based on exact traffic demands!

## VISUALIZATION_CONFIG
```json
{
  "steps": [
    {
      "title": "What is Web Hosting?",
      "description": "Servers that host your website files, database, and code.",
      "code": "// Types of hosting:\n// Shared: cheap, multi-tenant, limited\n// VPS: virtualized private server\n// Dedicated: full physical server\n// Cloud: elastic, pay-as-you-go\n// Serverless: no servers to manage\n// Managed: platform handles ops (Heroku, Vercel)\n\n// Static hosting (JAMstack):\n// GitHub Pages, Netlify, Vercel, Cloudflare Pages\n// Free tier, global CDN, auto SSL",
      "highlight": [
        2,
        3,
        4,
        5,
        6,
        7,
        10,
        11
      ],
      "diagram": {
        "kind": "boxes",
        "items": [
          {
            "label": "Shared",
            "color": "#9e9e9e"
          },
          {
            "label": "VPS",
            "color": "#1e88e5"
          },
          {
            "label": "Cloud",
            "color": "#43a047"
          },
          {
            "label": "Serverless",
            "color": "#fb8c00"
          }
        ]
      }
    },
    {
      "title": "IaaS vs PaaS vs SaaS",
      "description": "Levels of cloud abstraction: from bare VMs to fully-managed apps.",
      "code": "// IaaS: Infrastructure as a Service\n// AWS EC2, GCP Compute Engine, Azure VMs\n// You manage: OS, runtime, app, data\n// Provider manages: hardware, virtualization\n\n// PaaS: Platform as a Service\n// Heroku, Google App Engine, Vercel\n// You manage: app, data\n// Provider manages: everything else\n\n// SaaS: Software as a Service\n// Gmail, Slack, Notion\n// You manage: nothing (just use it)\n\n// Trend: more abstraction, less ops",
      "highlight": [
        1,
        2,
        3,
        4,
        6,
        7,
        8,
        9,
        11,
        12,
        13,
        14,
        15
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "IaaS: VMs"
          },
          {
            "label": "PaaS: platforms"
          },
          {
            "label": "SaaS: apps"
          },
          {
            "label": "FaaS: functions"
          },
          {
            "label": "More abstraction"
          }
        ]
      }
    },
    {
      "title": "Serverless & Edge",
      "description": "Serverless runs functions on demand; edge computing runs at CDN nodes.",
      "code": "// Serverless (AWS Lambda, Vercel Functions)\nexport async function handler(event) {\n  return {\n    statusCode: 200,\n    body: JSON.stringify({ hello: 'world' })\n  };\n}\n// Pay per invocation, auto-scale, cold starts\n\n// Edge functions (Cloudflare Workers, Vercel Edge)\nexport default {\n  async fetch(request) {\n    return new Response('Hello from edge!');\n  }\n};\n// Runs in 300+ locations, <10ms cold start\n// No Node.js — V8 isolates",
      "highlight": [
        2,
        3,
        4,
        5,
        6,
        7,
        9,
        10,
        11,
        12,
        13,
        14,
        15,
        16
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Request"
          },
          {
            "label": "Nearest edge node"
          },
          {
            "label": "Function executes"
          },
          {
            "label": "Response"
          },
          {
            "label": "<10ms latency"
          }
        ]
      }
    },
    {
      "title": "Deployment Platforms",
      "description": "Vercel, Netlify, Railway, Fly.io — modern platforms for full-stack apps.",
      "code": "// Vercel: Next.js optimized\nvercel --prod\n\n// Netlify: JAMstack\nnetlify deploy --prod\n\n// Railway: DB + backend\nrailway up\n\n// Fly.io: Docker containers globally\nfly deploy\n\n// Cloudflare Pages: static + Workers\nwrangler pages deploy dist\n\n// All feature:\n// - Git-based deploys\n// - Automatic HTTPS\n// - Preview deployments per PR\n// - Rollbacks",
      "highlight": [
        2,
        5,
        8,
        11,
        14,
        16,
        17,
        18,
        19
      ],
      "diagram": {
        "kind": "boxes",
        "items": [
          {
            "label": "Vercel: Next.js",
            "color": "#000000"
          },
          {
            "label": "Netlify: JAMstack",
            "color": "#00c7b7"
          },
          {
            "label": "Railway: DB+API",
            "color": "#1e88e5"
          },
          {
            "label": "Fly.io: containers",
            "color": "#8e24aa"
          }
        ]
      }
    },
    {
      "title": "Choosing Hosting",
      "description": "Match hosting to your app type: static, dynamic, database-heavy, real-time.",
      "code": "// Decision tree:\n// Static site only? → Vercel/Netlify/Cloudflare Pages\n// Static + serverless API? → Vercel/Netlify\n// Traditional app (DB + backend)? → Railway/Render/Fly\n// Full control needed? → VPS (DigitalOcean, Hetzner)\n// Enterprise/scale? → AWS/GCP/Azure\n\n// Cost considerations:\n// Free tier limits\n// Bandwidth: often the killer\n// Function invocations\n// Database size and connections\n\n// Vendor lock-in risk:\n// Serverless & platform features may not transfer\n// Docker + K8s = portable",
      "highlight": [
        2,
        3,
        4,
        5,
        6,
        9,
        10,
        11,
        12,
        15,
        16
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "App type?"
          },
          {
            "label": "Static? → JAMstack"
          },
          {
            "label": "DB backed? → PaaS"
          },
          {
            "label": "Full control? → VPS"
          },
          {
            "label": "Enterprise? → cloud"
          }
        ]
      }
    }
  ]
}
```

## CODE
As a modern web developer, you will rarely deal with ancient Shared Hosting (like GoDaddy or HostGator). You will use modern **Platform as a Service (PaaS)** or **Static Site Generators**. 

Deploying a website today doesn't require dragging and dropping files into an FTP client. It's done through the command line or automatically via GitHub!

### Example: Hosting a Static Website on Vercel
Vercel is a modern Cloud Hosting provider. If you have an HTML/CSS website, you can host it globally for free in 3 commands:

```bash
# 1. Install the Vercel Command Line Tool
npm i -g vercel

# 2. Navigate to your project folder
cd my-website

# 3. Deploy it to the cloud!
vercel
```
Vercel will instantly upload your files to their global CDN (Content Delivery Network), meaning your website is physically hosted on servers in New York, London, Tokyo, and Sydney simultaneously, ensuring blazing fast load times for any user in the world!

## REAL_WORLD
**The Slashdot Effect (The Hug of Death)**
In the early 2000s, if a small blog hosted on "Shared Hosting" went viral and was linked on a massive news aggregator like *Slashdot* or *Reddit*, millions of users would click the link simultaneously. 
The shared server, expecting maybe 10 visitors a day, would suddenly receive 10,000 HTTP requests per second. The CPU would max out at 100%, the RAM would overflow, and the physical server would crash, taking the website (and all the other websites sharing that server) completely offline. This became known as the "Hug of Death".
Modern **Cloud Hosting** solves this. When AWS or Vercel detects a massive spike in traffic, their automated Load Balancers instantly boot up 50 extra clone servers in milliseconds to handle the load, and shut them back down when the traffic subsides!

## INTERVIEW
**Q: A client asks you: "I bought `mycompany.com` from GoDaddy. Does this mean my website is now hosted and live on the internet?"**
A: **No! Buying a Domain Name is entirely separate from Web Hosting.**
Purchasing a Domain Name is simply registering a name in the global DNS registry (reserving the street address). It does not provide you with any physical server space to store your website files. 
You must purchase Web Hosting separately, and then you must configure the Domain Name's DNS Records (specifically the A-Record or CNAME) to point that address to the specific IP Address of the Web Hosting server you rented!

## FEYNMAN CHECK

### Explain Hosting Like I'm 10 Years Old
> Hosting is renting a computer that's always on and always connected to the internet. When you "deploy" your website, you copy your files to that rented computer, which then serves them to anyone who asks. The non-obvious part: hosting tiers differ in how much of the computer is yours. Shared hosting = bunk-bed in a hostel; VPS = your own apartment; dedicated server = your own house; cloud = on-demand magic that grows when you need it. This is why "my site is slow" usually means you've outgrown your tier.

---

### 5 Deep Questions
**Q1: Shared vs VPS vs dedicated vs cloud — what differs?**
> **A:** Shared: many sites on one OS, noisy-neighbour risk. VPS: virtualised slice with guaranteed CPU/RAM. Dedicated: whole physical box. Cloud (IaaS): VMs you provision via API, elastic. PaaS (Heroku, Vercel): you ship code, they run it. Serverless (Lambda): you ship functions, they run on demand. Cost and control scale inversely with abstraction level.

**Q2: ONE model.**
> **A:** "Hosting = compute + storage + network behind a public IP. Tiers trade convenience for control. Pick the highest abstraction that still meets your latency, cost, and compliance constraints."

**Q3: Misconception with code.**
> **A:** Storing user uploads on local disk in PaaS:
> ```js
> // ❌ fs.writeFile on Heroku/Vercel — file disappears on next deploy
> fs.writeFileSync('/tmp/avatar.jpg', upload);
> // ✅ Use object storage
> await s3.putObject({ Bucket: 'avatars', Key: id, Body: upload });
> ```

**Q4: How does hosting interact with CDNs?**
> **A:** Origin = your hosting server. CDN (Cloudflare, CloudFront) caches static assets at edge POPs near users — origin only sees cache misses. A well-configured CDN reduces origin load by 90% and cuts latency from 200ms (cross-continent) to 20ms (edge).

**Q5: Senior one-liner.**
> **A:** "Hosting is a stack of abstractions from bare metal to functions-as-a-service, where each step up trades flexibility for reduced operational burden — choosing wrong inflates either your AWS bill or your on-call pager volume."

## BUILD

### 🏗️ Mini Project: Deploy a Static Site to 3 Hosting Tiers
**Time:** 35 minutes

#### Step 1 — Setup
```bash
mkdir host-demo && cd host-demo
echo "<h1>Hello from hosting!</h1>" > index.html
```

#### Step 2 — Tier 1: Static (Vercel/Netlify/Cloudflare Pages)
```bash
npx vercel    # follow prompts — deploys in ~10s, free
```

#### Step 3 — Tier 2: PaaS (Render/Fly.io for Node app)
```bash
# Create a tiny Node server
cat > server.js << 'EOF'
require('http').createServer((req,res)=>{res.end('Hello from PaaS')}).listen(process.env.PORT||3000);
EOF
# Deploy via Render: connect repo, auto-detects Node
```

#### Step 4 — Tier 3: IaaS (AWS EC2)
```bash
# Launch t3.micro, SSH in, install Node, run server, attach Elastic IP
ssh ec2-user@<ip>
sudo yum install nodejs -y && node server.js &
```

#### Step 5 — Compare
| Tier | Deploy time | Cost/mo | Control | Use case |
|------|-------------|---------|---------|---------|
| Static | 10s | $0 | None | Marketing site |
| PaaS | 60s | $5+ | Code only | Standard app |
| IaaS | 10min | $10+ | Full | Custom workloads |

**Expected Output:** Same hello-world served from 3 URLs with vastly different operational profiles.

## SPACED REVIEW

### Day 1
**Q1:** Name 5 hosting tiers from least to most abstracted.
**Q2:** Why does PaaS lose your local-disk writes?
**Q3:** Write the simplest possible Vercel deploy command.

### Day 3
**Q4:** Shared vs VPS — when does shared bite you in production?
**Q5:** Show the local-fs-on-PaaS bug and the object-storage fix.
**Q6:** What does a CDN do for your origin?

### Day 7
**Q7:** Containerise a Node app and deploy it to two providers; compare cold-start latency.
**Q8:** PR puts SQLite on Vercel for state. Describe the deployment-day incident.
**Q9:** When does serverless become more expensive than a VM?

### Day 14
**Q10:** ★ "Design hosting for a SaaS launching in 3 countries with spiky traffic — what tier and why?"
**Q11:** Link hosting → CDN → DNS → load-balancing.
**Q12:** ★ "Your app on $5 PaaS gets a Hacker News surge. What breaks first and what's the fix?"
