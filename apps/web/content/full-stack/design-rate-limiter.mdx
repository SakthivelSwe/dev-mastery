---
title: design-rate-limiter
slug: design-rate-limiter
---

# Design a Distributed Rate Limiter | Level: 5

## 1. Requirements Gathering

### Candidate-Interviewer Dialogue

*   **Candidate:** "Where will this rate limiter sit? Is it a client-side SDK, a sidecar proxy (like Envoy), an API Gateway middleware, or a centralized distributed service?"
*   **Interviewer:** "We want to design a centralized, highly available distributed rate limiting service that can be integrated as a middleware layer in our API Gateway."
*   **Candidate:** "What entities are we rate limiting? Is it based on User ID, IP address, API Token, or a combination of these?"
*   **Interviewer:** "We need to support multiple dimensions. For authenticated users, we rate limit by `user_id`. For unauthenticated public endpoints, we fall back to `IP address` or `API key