---
slug: "html-attributes"
title: "HTML Attributes"
level: 2
---

## WHY
If HTML tags (`<img>`, `<a>`, `<input>`) are the nouns of the web, **Attributes** are the adjectives. Tags alone are often useless — an `<img>` tag without an attribute telling it *what* image to display does nothing. Attributes provide additional information, configuration, and behavior to HTML elements.

## THEORY
### The Structure of an Attribute
Attributes are always specified in the **opening tag** of an element. They usually consist of a name and a value, separated by an equals sign: `name="value"`.

- **Global Attributes:** Can be used on *any* HTML element (e.g., `id`, `class`, `style`).
- **Element-Specific Attributes:** Can only be used on specific elements (e.g., `src` on `<img>`, `href` on `<a>`).
- **Boolean Attributes:** Have no value. Their mere presence makes them true (e.g., `disabled`, `required`).

## VISUALIZATION_CONFIG
```json
{
  "language": "html",
  "fileName": "attributes.html",
  "steps": [
    {
      "title": "Global attributes",
      "description": "Global attributes work on ANY HTML element: id, class, style, title, data-*, lang, tabindex, hidden, contenteditable.",
      "code": "<div\n  id=\"main-content\"          <!-- unique identifier -->\n  class=\"card highlight\"     <!-- CSS classes -->\n  data-user-id=\"42\"          <!-- custom data -->\n  lang=\"en\"                  <!-- language -->\n  title=\"Tooltip text\"       <!-- hover tooltip -->\n  hidden                     <!-- not rendered -->\n  tabindex=\"0\"               <!-- keyboard focus -->\n>Content</div>",
      "highlight": [
        2,
        3,
        4,
        5,
        6,
        7,
        8
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Global attributes",
        "items": [
          {
            "label": "id",
            "color": "#10b981",
            "value": "Unique per page — CSS #id, JS getElementById"
          },
          {
            "label": "class",
            "color": "#818cf8",
            "value": "Multiple allowed — CSS .class",
            "highlight": true
          },
          {
            "label": "data-*",
            "color": "#f59e0b",
            "value": "Custom data — read via dataset.userId"
          },
          {
            "label": "tabindex",
            "color": "#60a5fa",
            "value": "0=focusable, -1=programmatic, n=order"
          }
        ]
      }
    },
    {
      "title": "Boolean attributes",
      "description": "Boolean attributes are true when present, false when absent. You do NOT write value=\"true\". Just the attribute name suffices.",
      "code": "<!-- Boolean: present = true, absent = false -->\n<input type=\"checkbox\" checked>       <!-- checked -->\n<input type=\"checkbox\">               <!-- NOT checked -->\n<button disabled>Can't click</button> <!-- disabled -->\n<details open>                        <!-- open -->\n<script defer>                        <!-- deferred -->\n<input required>                      <!-- required -->",
      "highlight": [
        2,
        4,
        5,
        6,
        7
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Boolean attributes",
        "items": [
          {
            "label": "checked",
            "color": "#10b981",
            "value": "checkbox/radio pre-selected",
            "highlight": true
          },
          {
            "label": "disabled",
            "color": "#ef4444",
            "value": "form element non-interactive"
          },
          {
            "label": "required",
            "color": "#f59e0b",
            "value": "form field must be filled"
          },
          {
            "label": "open / defer / async / readonly",
            "color": "#818cf8",
            "value": "Various boolean flags"
          }
        ]
      }
    },
    {
      "title": "ARIA attributes",
      "description": "ARIA (Accessible Rich Internet Applications) attributes extend semantics for screen readers. Use native HTML first; add ARIA only when needed.",
      "code": "<!-- ARIA landmark roles -->\n<div role=\"dialog\" aria-modal=\"true\" aria-labelledby=\"dlg-title\">\n  <h2 id=\"dlg-title\">Confirm Delete</h2>\n  <button aria-label=\"Close dialog\">✕</button>\n</div>\n\n<!-- Live region -->\n<div aria-live=\"polite\" aria-atomic=\"true\">\n  <!-- Content changes announced to screen reader -->\n</div>",
      "highlight": [
        2,
        3,
        4,
        8
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Key ARIA patterns",
        "items": [
          {
            "label": "role=",
            "color": "#818cf8",
            "value": "Overrides implicit semantics"
          },
          {
            "label": "aria-label / aria-labelledby",
            "color": "#10b981",
            "value": "Accessible name",
            "highlight": true
          },
          {
            "label": "aria-hidden=\"true\"",
            "color": "#ef4444",
            "value": "Hide decorative elements from AT"
          },
          {
            "label": "aria-live=\"polite\"",
            "color": "#f59e0b",
            "value": "Announce dynamic content changes"
          }
        ]
      }
    },
    {
      "title": "Event handler attributes (inline — avoid)",
      "description": "You can write onclick=\"fn()\" in HTML but this is bad practice. Separation of concerns: keep JS in .js files using addEventListener.",
      "code": "<!-- BAD: inline handler -->\n<button onclick=\"alert('hi')\">Click</button>\n\n<!-- GOOD: addEventListener -->\n<button id=\"btn\">Click</button>\n<script>\n  document.getElementById('btn')\n    .addEventListener('click', () => alert('hi'));\n</script>",
      "highlight": [
        5,
        6,
        7,
        8
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Inline onclick= mixes HTML & JS logic",
            "done": true
          },
          {
            "label": "Hard to test, hard to debounce/remove",
            "done": true
          },
          {
            "label": "addEventListener separates concerns",
            "active": true
          },
          {
            "label": "Can add multiple listeners per event ✓"
          }
        ]
      }
    },
    {
      "title": "Custom data attributes (data-*)",
      "description": "data-* attributes store private data in HTML. Read them in JS via element.dataset. Perfect for storing IDs, config, or state without adding hidden inputs.",
      "code": "<button\n  data-product-id=\"SKU-123\"\n  data-price=\"29.99\"\n  data-in-stock=\"true\"\n  class=\"add-to-cart\"\n>Add to Cart</button>\n\n<script>\n  document.querySelector('.add-to-cart')\n    .addEventListener('click', (e) => {\n      const { productId, price } = e.target.dataset;\n      addToCart(productId, parseFloat(price));\n    });\n</script>",
      "highlight": [
        2,
        3,
        4,
        11,
        12
      ],
      "diagram": {
        "kind": "boxes",
        "title": "data-* usage",
        "items": [
          {
            "label": "HTML: data-product-id=\"SKU-123\"",
            "color": "#10b981",
            "highlight": true
          },
          {
            "label": "JS: element.dataset.productId",
            "color": "#818cf8",
            "value": "camelCase auto-conversion"
          },
          {
            "label": "JS: element.getAttribute(\"data-price\")",
            "color": "#f59e0b",
            "value": "string access"
          },
          {
            "label": "No type info — always strings in HTML",
            "color": "#6b7280"
          }
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner (Core Attributes)
```html
<!-- The id attribute: Must be UNIQUE on the entire page. 
     Used for linking, styling, and JavaScript targeting. -->
<h1 id="main-title">Welcome to Dev Mastery</h1>

<!-- The class attribute: Can be reused on multiple elements. 
     Primarily used for CSS styling. -->
<p class="highlight-text error-message">This text has two classes.</p>

<!-- Element-specific attributes -->
<!-- The src (source) and alt (alternative text) attributes on an image -->
<img src="https://example.com/logo.png" alt="Dev Mastery Company Logo">

<!-- The href (hypertext reference) attribute on an anchor link -->
<a href="https://google.com">Search the web</a>
```

### Level 2 — Building Blocks (Form Attributes)
Forms rely heavily on attributes to function correctly.

```html
<form action="/submit-data" method="POST">
    
    <!-- type dictates the kind of input (text, password, email, date) -->
    <!-- name is the key sent to the server (e.g., username=alice) -->
    <!-- placeholder shows gray hint text -->
    <input type="text" name="username" placeholder="Enter your username">
    
    <!-- Boolean Attributes: Notice they don't need ="true" -->
    <!-- required: The browser won't let you submit the form if empty -->
    <!-- disabled: The input is grayed out and cannot be interacted with -->
    <input type="password" name="pwd" required>
    <button type="submit" disabled>Submit</button>

</form>
```

### Level 3 — Intermediate (Data Attributes)
Sometimes you need to store extra information on an HTML element that doesn't have a standard attribute. You can create custom attributes using the `data-*` prefix!

```html
<!-- Storing a product ID and category directly in the HTML -->
<div class="product-card" 
     data-product-id="98765" 
     data-category="electronics" 
     data-in-stock="true">
    <h2>Super Widget</h2>
</div>

<script>
    // These data attributes are incredibly easy to access in JavaScript!
    const card = document.querySelector('.product-card');
    
    // Access them via the 'dataset' property:
    console.log(card.dataset.productId); // "98765"
    console.log(card.dataset.category);  // "electronics"
    
    // Note how data-product-id automatically becomes camelCase: productId
</script>
```

### Level 4 — Advanced (Accessibility Attributes - ARIA)
When building complex, interactive UI components (like a custom dropdown or a modal), standard HTML tags aren't enough for screen readers (used by visually impaired users) to understand what's happening.
**ARIA** (Accessible Rich Internet Applications) attributes fill this gap.

```html
<!-- A standard div doesn't mean anything to a screen reader. -->
<!-- role="button" tells the screen reader to treat it like a button. -->
<!-- tabindex="0" makes it focusable with the keyboard (Tab key). -->
<div role="button" tabindex="0" class="custom-btn">Click Me</div>

<!-- A custom toggle switch -->
<!-- aria-checked tells the screen reader the current state of the switch -->
<div role="switch" aria-checked="true" class="toggle-on">
    Settings are enabled
</div>

<!-- aria-hidden="true" completely hides the element from screen readers.
     Useful for purely decorative icons. -->
<svg aria-hidden="true" class="icon-star">...</svg>
```

## REAL_WORLD
**Security Warning:** Never store sensitive data (like passwords, API keys, or personal information) in `data-*` attributes! Anyone can right-click the page, select "Inspect Element", and read all the HTML attributes immediately.

**SEO Impact:** The `alt` attribute on `<img>` tags is not just for accessibility. Google Image Search heavily relies on the `alt` text to understand what the image represents and rank it in search results. Missing `alt` attributes will hurt your SEO score.

## INTERVIEW
**Q: What is the difference between `id` and `class`?**
A: `id` must be absolutely unique within a single HTML document; you cannot have two elements with `id="header"`. `class` can be used on as many elements as you want. In CSS, `id` has a much higher specificity (priority) than `class`. 

**Q: If a boolean attribute like `disabled` is set to `disabled="false"`, is it disabled?**
A: **YES!** This is a common trap. For boolean attributes in HTML, their mere presence on the element makes them true, regardless of what value you assign to them. `<input disabled="false">` will be disabled. To enable the input, you must completely remove the `disabled` attribute from the tag.

## FEYNMAN CHECK

### Explain HTML Attributes Like I'm 10 Years Old
> Attributes are the configuration knobs of an HTML tag. `<input type="email" required maxlength="100">` is one tag with three knobs. Boolean attributes are special — their PRESENCE alone means true; the value is irrelevant. `<input disabled>`, `<input disabled="disabled">`, and `<input disabled="false">` are ALL disabled, which trips up almost every junior dev once. `data-*` attributes are an officially-blessed namespace for your own metadata — JavaScript reads them as `element.dataset.userId` (camelCased).

---

### 5 Deep Conceptual Questions

**Q1: What problem do HTML attributes fundamentally solve?**
> **A:** They let one tag handle many variations without inventing 50 new tags. There's one `<input>` with 25 attribute combinations instead of `<text-input>`, `<email-input>`, etc. Cost: parsing rules are tag-specific (boolean attributes, enumerated attributes, IDL vs content attributes), which makes the HTML spec longer than most programming languages.

**Q2: What is the ONE mental model that makes attributes click?**
> **A:** There are TWO parallel attribute spaces: the CONTENT attribute (string in markup, accessed via `getAttribute`) and the IDL attribute (JS property, e.g. `input.value`). They start in sync but can drift. `<input value="hi">` initialises both; typing changes IDL but NOT content. This is why `form.reset()` works — it copies the original content attribute back to IDL.

**Q3: What is the most dangerous misconception? Show it with code.**
> **A:** That `<button disabled="false">` enables the button. Boolean attributes are presence-only:
> ```html
> <!-- ❌ All THREE are disabled. The value is completely ignored. -->
> <button disabled>Save</button>
> <button disabled="">Save</button>
> <button disabled="false">Save</button>
>
> <!-- ✅ The ONLY way to enable is to omit the attribute entirely -->
> <button>Save</button>
> <!-- Or in JS: button.removeAttribute('disabled') / button.disabled = false -->
> ```

**Q4: How do `data-*` attributes interact with JavaScript at the runtime level?**
> **A:** Every `data-*` is exposed on `element.dataset` with hyphenated→camelCase conversion: `data-user-id="42"` → `el.dataset.userId === "42"`. Crucially, dataset values are ALWAYS strings — `data-count="5"` gives `"5"` not `5`. The DOM stores both representations in sync, so `el.dataset.userId = '99'` immediately updates the HTML attribute.

**Q5: Write a one-sentence FAANG-precise definition.**
> **A:** "HTML attributes are key-value pairs the browser parses into a NamedNodeMap on each element, separated into content attributes (string values living in markup) and IDL attributes (typed values living on the DOM node prototype) — which is why `input.checked = true` updates the live state without altering the original `checked` content attribute the form-reset algorithm reads from."

## BUILD

### 🏗️ Mini Project: Declarative Web-Component Loader Built on `data-*`

**What you will build:** A 60-line vanilla-JS module that scans the page for `<div data-component="...">`, automatically instantiates the right widget, and observes the DOM for dynamically added ones. The pattern Angular's selector engine, htmx, and Alpine.js use under the hood.
**Why this project:** Forces you to understand `dataset`, type coercion, attribute mutation, and `MutationObserver` in one tight piece of code.
**Time estimate:** 30 minutes

---

#### Step 1 — Setup
```bash
mkdir data-loader && cd data-loader
ni index.html, loader.js -ItemType File
```

#### Step 2 — Markup That Drives the System
```html
<!DOCTYPE html>
<html lang="en"><head><meta charset="UTF-8"><title>Data-driven widgets</title></head>
<body>
  <div data-component="counter" data-start="10" data-step="2"></div>
  <div data-component="clock" data-format="24h"></div>
  <button id="add">Add another counter dynamically</button>
  <script type="module" src="./loader.js"></script>
</body></html>
```

#### Step 3 — The Loader (dataset + MutationObserver)
```javascript
// loader.js — component registry keyed by data-component
const registry = {
  counter(host) {
    let value = Number(host.dataset.start ?? 0);  // dataset is ALWAYS string
    const step = Number(host.dataset.step ?? 1);
    host.innerHTML = '<output>' + value + '</output> <button>+</button>';
    const out = host.querySelector('output');
    host.querySelector('button').addEventListener('click', () => {
      value += step;
      out.textContent = value;
    });
  },
  clock(host) {
    const fmt = host.dataset.format === '24h' ? 'en-GB' : 'en-US';
    const tick = () => host.textContent = new Date().toLocaleTimeString(fmt);
    tick();
    setInterval(tick, 1000);
  },
};

function hydrate(root) {
  const nodes = root.querySelectorAll('[data-component]:not([data-hydrated])');
  for (const host of nodes) {
    const init = registry[host.dataset.component];
    if (!init) { console.warn('Unknown:', host.dataset.component); continue; }
    init(host);
    host.dataset.hydrated = 'true';
  }
}

hydrate(document);

// Auto-hydrate future insertions
new MutationObserver((records) => {
  for (const r of records)
    for (const node of r.addedNodes)
      if (node.nodeType === 1) hydrate(node);
}).observe(document.body, { childList: true, subtree: true });

// Demo: add a counter at runtime — MutationObserver hydrates it
document.getElementById('add').addEventListener('click', () => {
  const div = document.createElement('div');
  div.dataset.component = 'counter';
  div.dataset.start = '100';
  div.dataset.step = '5';
  document.body.appendChild(div);
});
```

#### Step 4 — Error Handling
```javascript
// Wrap init in try/catch so one broken widget can't break the page
function hydrate(root) {
  const nodes = root.querySelectorAll('[data-component]:not([data-hydrated])');
  for (const host of nodes) {
    const init = registry[host.dataset.component];
    if (!init) continue;
    try {
      init(host);
      host.dataset.hydrated = 'true';
    } catch (err) {
      host.dataset.hydrated = 'error';
      host.setAttribute('aria-invalid', 'true');
      console.error('[' + host.dataset.component + ']', err);
    }
  }
}
```

#### Step 5 — Tests
```javascript
console.assert(document.querySelector('[data-hydrated]'),
  'at least one widget should be hydrated');
console.assert(document.querySelector('output').textContent === '10',
  'counter should start at 10');
```

**Expected Output:**
```
- Counter shows 10, clicking increments by 2
- Clock shows current time in 24h format
- "Add another counter" inserts a new widget that hydrates automatically (starts at 100, steps by 5)
```

**Stretch Challenges:**
- [ ] Add `data-lazy="viewport"` flag deferring init until element scrolls into view (IntersectionObserver)
- [ ] Support nested JSON config: `data-config='{"theme":"dark"}'` parsed once
- [ ] Add teardown path: when host is removed, run the component's cleanup

## SPACED REVIEW

---

### Day 1 — Recall

**Q1:** What is a boolean attribute? Write the THREE syntactically valid ways to mark an input as disabled.

**Q2:** What is the difference between `element.getAttribute('value')` and `element.value` on an `<input>`? When do they diverge?

**Q3:** Convert `data-user-profile-id="42"` to the JS expression that reads it. Then write the expression that updates it to 99.

---

### Day 3 — Comprehension

**Q4:** Name 6 global attributes valid on every HTML element. For each, give a one-line use case.

**Q5:** Describe the production bug caused by writing `<input disabled={isDisabled}>` in a non-JSX templating engine. Show buggy vs fixed.

**Q6:** Refactor this for security and accessibility:
```html
<a href="javascript:void(0)" onclick="open(${url})">Open</a>
<input type="text" placeholder="Email" required="false" />
```

---

### Day 7 — Application

**Q7:** Build a `data-track="event-name"` analytics system that captures clicks across the entire page using ONE delegated listener on `document`.

**Q8:** Every form uses `<input type="text" name="email">` with JS validation. List 3 attribute changes that would eliminate the JS validation entirely.

**Q9:** What is `contenteditable`? Build a 20-line in-browser markdown preview where typing in a `contenteditable` div instantly renders to HTML.

---

### Day 14 — Synthesis & Interview Prep

**Q10:** ★ Interview: "Explain the difference between an HTML content attribute and a DOM IDL attribute. Give one case where they intentionally diverge."

**Q11:** Draw the dependency graph: html-attributes ↔ forms-html ↔ form-validation ↔ accessibility-html.

**Q12:** ★ System design: "Design attribute conventions for a server-rendered framework that's progressively enhanced. How do you encode component state, hydration boundaries, and analytics without inventing non-standard names?"
