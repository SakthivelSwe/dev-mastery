---
title: "HTML Forms — Inputs, Validation & Accessibility"
slug: "forms-html"
level: 1
---

## WHY

Forms are the primary way users interact with web applications — login, registration, checkout, search, contact. Yet forms are also the #1 source of accessibility failures, UX frustrations, and security vulnerabilities on the web. A poorly built form costs real money: research by the Baymard Institute shows that **69% of e-commerce checkouts are abandoned**, with "too many form fields" and "confusing forms" among the top reasons. A form that confuses a keyboard user, screen reader user, or mobile user is a form that loses customers.

Before HTML5, forms had to rely entirely on JavaScript for validation — every developer wrote their own validators, inconsistent across sites and completely broken without JavaScript. HTML5 introduced **native browser validation** (`required`, `type="email"`, `pattern`, `min`, `max`) that works without JavaScript, has built-in localized error messages, and can be controlled via CSS via the `:valid`/`:invalid` pseudo-classes.

Understanding HTML form internals — how data is encoded and sent, when to use GET vs POST, how `autocomplete` saves users time, how `fieldset`/`legend` group related inputs, and how `aria-describedby` connects inputs to help text — is essential for building forms that are fast, accessible, and conversion-optimized.

## THEORY

### The `<form>` Element
The `<form>` element wraps all form controls and defines how data is submitted:

| Attribute | Values | Effect |
|-----------|--------|--------|
| `action` | URL string | Where form data is sent (default: current URL) |
| `method` | `GET`, `POST` | GET: data in URL query string; POST: data in request body |
| `enctype` | `application/x-www-form-urlencoded` (default), `multipart/form-data` (for files), `text/plain` | How form data is encoded |
| `novalidate` | boolean | Disables browser's native validation (use for custom JS validation) |
| `autocomplete` | `on`, `off` | Enables/disables browser form autofill |
| `target` | `_self`, `_blank` | Where to display the response |

### Input Types (HTML5)
HTML5 added many new `type` values that provide native UI controls and validation:

| Type | Description | Key Attributes |
|------|-------------|----------------|
| `text` | Single-line text | `minlength`, `maxlength`, `pattern`, `autocomplete` |
| `email` | Email address | Browser validates `@` and domain |
| `password` | Hidden text | `minlength`, `autocomplete="new-password"` |
| `number` | Numeric input | `min`, `max`, `step` |
| `tel` | Phone number (no format validation) | `pattern` for custom format |
| `url` | URL (validates protocol) | |
| `search` | Search field | Adds clear button in some browsers |
| `date` | Date picker | `min`, `max` (YYYY-MM-DD format) |
| `time` | Time picker | `min`, `max`, `step` |
| `datetime-local` | Date + time picker | |
| `month` / `week` | Month/week picker | |
| `range` | Slider | `min`, `max`, `step`, `value` |
| `color` | Color picker | |
| `file` | File upload | `accept`, `multiple` |
| `checkbox` | Toggle (independent) | `checked`, `value` |
| `radio` | Toggle (grouped, same `name`) | `checked`, `value` |
| `hidden` | Invisible data field | `name`, `value` |
| `submit` | Submit button | `value` = button label |
| `reset` | Reset to defaults | |
| `button` | Button (no default action) | |
| `image` | Image submit button | `src`, `alt` |

### Labels — Non-Negotiable for Accessibility
Every form control **must** have an associated `<label>`. There are three methods:

```html
<!-- Method 1: for/id pairing (preferred) -->
<label for="email">Email</label>
<input type="email" id="email" name="email">

<!-- Method 2: wrapping (label wraps input) -->
<label>
  Email
  <input type="email" name="email">
</label>

<!-- Method 3: aria-label (when no visible label) -->
<input type="search" aria-label="Search courses" name="q">
```

Without a label, screen readers announce the input as "Edit text" with no context. Users with motor disabilities using voice control (Dragon NaturallySpeaking) cannot activate the field by saying its name.

### `<fieldset>` and `<legend>`
Use `<fieldset>` to group related form controls. The `<legend>` provides a caption for the group — read by screen readers as context before each control in the group.

```html
<!-- Without fieldset/legend, radio buttons are orphaned -->
<fieldset>
  <legend>Payment Method</legend>
  <label><input type="radio" name="payment" value="card"> Credit Card</label>
  <label><input type="radio" name="payment" value="paypal"> PayPal</label>
</fieldset>
<!-- Screen reader announces: "Payment Method, Credit Card, radio button, 1 of 2" -->
```

### Native HTML5 Validation Attributes

| Attribute | Applies To | Description |
|-----------|-----------|-------------|
| `required` | Most inputs | Field must not be empty |
| `minlength` / `maxlength` | text, password, etc. | Min/max character count |
| `min` / `max` | number, date, range | Min/max value |
| `step` | number, date, range | Valid value increments |
| `pattern` | text, email, url, tel | Regex pattern must match |
| `multiple` | email, file | Allow multiple values |

### The `autocomplete` Attribute
Setting `autocomplete` correctly dramatically improves UX — browsers autofill correctly and password managers work properly:

```html
<input type="text" autocomplete="given-name" name="fname">
<input type="text" autocomplete="family-name" name="lname">
<input type="email" autocomplete="email" name="email">
<input type="tel" autocomplete="tel" name="phone">
<input type="new-password" autocomplete="new-password" name="password">
<input type="text" autocomplete="cc-number" name="card">
```

## VISUALIZATION_CONFIG
```json
{
  "language": "html",
  "fileName": "forms.html",
  "steps": [
    {
      "title": "Form basics",
      "description": "<form> wraps all form controls. action= is where data is sent, method= is GET or POST. Always label every input with <label for=\"id\">.",
      "code": "<form action=\"/submit\" method=\"POST\">\n  <div>\n    <label for=\"email\">Email</label>\n    <input\n      type=\"email\"\n      id=\"email\"\n      name=\"email\"\n      required\n      autocomplete=\"email\"\n    >\n  </div>\n  <button type=\"submit\">Submit</button>\n</form>",
      "highlight": [
        1,
        3,
        4,
        5,
        6,
        12
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "User fills form fields",
            "done": true
          },
          {
            "label": "Browser validates (required, type)",
            "done": true
          },
          {
            "label": "Submit: POST data to action URL",
            "active": true
          },
          {
            "label": "Server processes → redirect / response"
          }
        ]
      }
    },
    {
      "title": "Input types",
      "description": "HTML5 added many input types. The browser renders the best native UI for each: date picker for type=date, colour picker for type=color, on-screen numeric keyboard for type=number on mobile.",
      "code": "<input type=\"text\">          <!-- free text -->\n<input type=\"email\">         <!-- email keyboard on mobile -->\n<input type=\"number\" min=\"0\" max=\"100\" step=\"5\">\n<input type=\"date\">          <!-- native date picker -->\n<input type=\"color\">         <!-- colour picker -->\n<input type=\"range\" min=\"0\" max=\"10\">   <!-- slider -->\n<input type=\"tel\">           <!-- phone keyboard on mobile -->\n<input type=\"search\">        <!-- search with clear button -->",
      "highlight": [
        1,
        2,
        3,
        4,
        5,
        6
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Input type benefits",
        "items": [
          {
            "label": "type=\"email\"",
            "color": "#10b981",
            "value": "@ keyboard on mobile + basic validation",
            "highlight": true
          },
          {
            "label": "type=\"number\"",
            "color": "#818cf8",
            "value": "Numeric keyboard + min/max/step"
          },
          {
            "label": "type=\"date\"",
            "color": "#f59e0b",
            "value": "Native date picker — no JS needed"
          },
          {
            "label": "type=\"tel\"",
            "color": "#60a5fa",
            "value": "Phone number keyboard on mobile"
          }
        ]
      }
    },
    {
      "title": "Select, textarea, fieldset",
      "description": "<select> creates a dropdown. <textarea> for multi-line text. <fieldset> + <legend> groups related controls.",
      "code": "<!-- Dropdown -->\n<label for=\"lang\">Language</label>\n<select id=\"lang\" name=\"language\">\n  <option value=\"\">-- Select --</option>\n  <option value=\"js\">JavaScript</option>\n  <option value=\"java\" selected>Java</option>\n</select>\n\n<!-- Grouped radios -->\n<fieldset>\n  <legend>Experience level</legend>\n  <label><input type=\"radio\" name=\"level\" value=\"junior\"> Junior</label>\n  <label><input type=\"radio\" name=\"level\" value=\"senior\"> Senior</label>\n</fieldset>",
      "highlight": [
        3,
        10,
        11,
        12,
        13
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Form grouping",
        "items": [
          {
            "label": "<fieldset> + <legend>",
            "color": "#818cf8",
            "value": "Groups related fields — accessible",
            "highlight": true
          },
          {
            "label": "<select> + <option>",
            "color": "#10b981",
            "value": "Dropdown choice"
          },
          {
            "label": "<datalist>",
            "color": "#f59e0b",
            "value": "Autocomplete suggestions for <input>"
          },
          {
            "label": "<optgroup>",
            "color": "#60a5fa",
            "value": "Groups options inside <select>"
          }
        ]
      }
    },
    {
      "title": "Form validation",
      "description": "HTML5 built-in validation attributes: required, minlength, maxlength, min, max, pattern (regex), type. The browser validates before submit and shows native error UI.",
      "code": "<input\n  type=\"password\"\n  id=\"pwd\"\n  required\n  minlength=\"8\"\n  maxlength=\"64\"\n  pattern=\"(?=.*\\d)(?=.*[a-z])(?=.*[A-Z]).{8,}\"\n  title=\"8+ chars, must include upper, lower, number\"\n>\n\n<!-- Custom JS validation with Constraint Validation API -->\ninput.setCustomValidity(\"Passwords do not match\");\ninput.reportValidity();",
      "highlight": [
        4,
        5,
        6,
        7,
        8,
        12,
        13
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "required — field must have value",
            "done": true
          },
          {
            "label": "type + min/max — range validation",
            "done": true
          },
          {
            "label": "pattern — regex validation",
            "active": true
          },
          {
            "label": "setCustomValidity — programmatic error"
          }
        ]
      }
    },
    {
      "title": "Form submission & FormData",
      "description": "Modern JS forms use FormData API to collect values and fetch() to send data without page reload.",
      "code": "<form id=\"signupForm\">\n  <input type=\"text\" name=\"username\" required>\n  <input type=\"email\" name=\"email\" required>\n  <button type=\"submit\">Sign Up</button>\n</form>\n\n<script>\ndocument.getElementById('signupForm')\n  .addEventListener('submit', async (e) => {\n    e.preventDefault();  // stop page reload\n    const data = new FormData(e.target);\n    const res = await fetch('/api/signup', {\n      method: 'POST',\n      body: data,\n    });\n    const json = await res.json();\n  });\n</script>",
      "highlight": [
        9,
        10,
        11,
        12,
        13
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "e.preventDefault() — no page reload",
            "done": true
          },
          {
            "label": "new FormData(form) — collect all fields",
            "done": true
          },
          {
            "label": "fetch POST to API endpoint",
            "active": true
          },
          {
            "label": "Handle JSON response → update UI"
          }
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner: Simple Contact Form
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Contact Form</title>
</head>
<body>

  <h1>Contact Us</h1>

  <!-- action="/contact": submits to /contact on your server -->
  <!-- method="POST": data goes in request body (not visible in URL) -->
  <form action="/contact" method="POST">

    <!-- LABEL + INPUT pair: 'for' matches input 'id' -->
    <div>
      <label for="name">Your Name:</label>
      <input
        type="text"
        id="name"
        name="name"
        placeholder="Jane Smith"
        required
        minlength="2"
        maxlength="100"
        autocomplete="name"
      >
      <!-- required: Browser blocks submission if empty -->
    </div>

    <div>
      <label for="email">Email Address:</label>
      <input
        type="email"
        id="email"
        name="email"
        placeholder="jane@example.com"
        required
        autocomplete="email"
      >
      <!-- type="email": Browser validates email format automatically -->
    </div>

    <div>
      <label for="subject">Subject:</label>
      <select id="subject" name="subject" required>
        <option value="">Choose a topic...</option>
        <option value="general">General Inquiry</option>
        <option value="support">Technical Support</option>
        <option value="billing">Billing</option>
      </select>
    </div>

    <div>
      <label for="message">Message:</label>
      <textarea
        id="message"
        name="message"
        rows="5"
        minlength="10"
        maxlength="1000"
        required
        placeholder="Describe your question or issue..."></textarea>
    </div>

    <button type="submit">Send Message</button>

  </form>

</body>
</html>
<!-- Expected: Browser validates email format, required fields, min/max length
     BEFORE submitting. Shows browser-native error messages in the user's language. -->
```

### Level 2 — Building Blocks: Registration Form with All Input Types
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Registration Form</title>
</head>
<body>
<form action="/register" method="POST" autocomplete="on">

  <fieldset>
    <legend>Personal Details</legend>

    <label for="fname">First Name *</label>
    <input type="text" id="fname" name="firstName"
           required autocomplete="given-name" minlength="2">

    <label for="lname">Last Name *</label>
    <input type="text" id="lname" name="lastName"
           required autocomplete="family-name">

    <label for="dob">Date of Birth</label>
    <input type="date" id="dob" name="dateOfBirth"
           min="1900-01-01" max="2008-01-01"
           autocomplete="bday">

    <label for="phone">Phone Number</label>
    <input type="tel" id="phone" name="phone"
           placeholder="+1 (555) 000-0000"
           pattern="[+]?[\d\s\-()]{7,15}"
           autocomplete="tel">
  </fieldset>

  <fieldset>
    <legend>Account Setup</legend>

    <label for="email">Email Address *</label>
    <input type="email" id="email" name="email"
           required autocomplete="email">

    <label for="username">Username *</label>
    <input type="text" id="username" name="username"
           required minlength="3" maxlength="20"
           pattern="[a-zA-Z0-9_]+"
           aria-describedby="username-hint">
    <p id="username-hint">3-20 characters, letters, numbers, underscores only</p>

    <label for="password">Password *</label>
    <input type="password" id="password" name="password"
           required minlength="8" autocomplete="new-password"
           aria-describedby="password-requirements">
    <ul id="password-requirements">
      <li>At least 8 characters</li>
      <li>Mix of letters and numbers recommended</li>
    </ul>

    <label for="confirm-pwd">Confirm Password *</label>
    <input type="password" id="confirm-pwd" name="confirmPassword"
           required autocomplete="new-password">
  </fieldset>

  <fieldset>
    <legend>Preferences</legend>

    <p>Experience level: <span aria-required="true">*</span></p>
    <label>
      <input type="radio" name="level" value="beginner" required>
      Beginner
    </label>
    <label>
      <input type="radio" name="level" value="intermediate">
      Intermediate
    </label>
    <label>
      <input type="radio" name="level" value="advanced">
      Advanced
    </label>

    <p>Interests:</p>
    <label>
      <input type="checkbox" name="interests" value="html">
      HTML &amp; CSS
    </label>
    <label>
      <input type="checkbox" name="interests" value="javascript">
      JavaScript
    </label>
    <label>
      <input type="checkbox" name="interests" value="backend">
      Backend Development
    </label>

    <label for="country">Country</label>
    <select id="country" name="country" autocomplete="country-name">
      <option value="">Select your country...</option>
      <optgroup label="North America">
        <option value="US">United States</option>
        <option value="CA">Canada</option>
      </optgroup>
      <optgroup label="Europe">
        <option value="GB">United Kingdom</option>
        <option value="DE">Germany</option>
      </optgroup>
      <optgroup label="Asia">
        <option value="IN">India</option>
        <option value="JP">Japan</option>
      </optgroup>
    </select>

    <label for="bio">Short Bio</label>
    <textarea id="bio" name="bio" rows="3" maxlength="280"
              placeholder="Tell us about yourself (optional)"></textarea>
  </fieldset>

  <fieldset>
    <legend>Account Agreement</legend>
    <label>
      <input type="checkbox" name="terms" required>
      I agree to the <a href="/terms" target="_blank">Terms of Service</a>
      and <a href="/privacy" target="_blank">Privacy Policy</a> *
    </label>
    <label>
      <input type="checkbox" name="newsletter">
      Subscribe to the DevMastery newsletter (optional)
    </label>
  </fieldset>

  <!-- Hidden field: track form version for A/B testing -->
  <input type="hidden" name="formVersion" value="v2">

  <button type="submit">Create Account</button>
  <button type="reset">Clear Form</button>

</form>
</body>
</html>
```

### Level 3 — Intermediate: File Upload, Range, Color & Advanced Input Patterns
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Advanced Input Types</title>
</head>
<body>
<form action="/profile" method="POST" enctype="multipart/form-data">
  <!-- enctype="multipart/form-data" is REQUIRED when uploading files -->

  <h2>Profile Settings</h2>

  <!-- FILE UPLOAD -->
  <fieldset>
    <legend>Profile Photo</legend>
    <label for="avatar">Upload avatar (JPEG, PNG, max 5MB)</label>
    <input type="file" id="avatar" name="avatar"
           accept="image/jpeg, image/png, image/webp"
           capture="user">
    <!-- accept: restricts to these MIME types -->
    <!-- capture="user": uses front camera on mobile -->
  </fieldset>

  <!-- FILE with MULTIPLE -->
  <fieldset>
    <legend>Portfolio Images (up to 10 files)</legend>
    <input type="file" name="portfolio"
           accept="image/*"
           multiple>
    <!-- multiple: allows selecting multiple files at once -->
  </fieldset>

  <!-- RANGE SLIDER with live value display -->
  <fieldset>
    <legend>Notification Preferences</legend>
    <label for="frequency">
      Email frequency:
      <output id="frequency-display">7</output> days
    </label>
    <input type="range" id="frequency" name="emailFrequency"
           min="1" max="30" step="1" value="7"
           oninput="document.getElementById('frequency-display').value = this.value"
           aria-describedby="frequency-desc">
    <p id="frequency-desc">How many days between digest emails (1=daily, 30=monthly)</p>
  </fieldset>

  <!-- COLOR PICKER -->
  <fieldset>
    <legend>Theme Color</legend>
    <label for="theme-color">Choose your accent color:</label>
    <input type="color" id="theme-color" name="themeColor" value="#6366f1">
  </fieldset>

  <!-- DATALIST: autocomplete suggestions (not a select!) -->
  <fieldset>
    <legend>Favorite Technology</legend>
    <label for="tech">Your favorite tech:</label>
    <input type="text" id="tech" name="favoritetech"
           list="tech-options"
           placeholder="Start typing...">
    <datalist id="tech-options">
      <option value="JavaScript">
      <option value="TypeScript">
      <option value="Python">
      <option value="Rust">
      <option value="Go">
      <option value="Java">
    </datalist>
    <!-- Unlike <select>, users can type anything; datalist just suggests -->
  </fieldset>

  <!-- DATE RANGE -->
  <fieldset>
    <legend>Availability</legend>
    <label for="start-date">Available from:</label>
    <input type="date" id="start-date" name="startDate"
           min="2026-01-01">

    <label for="end-date">Available until:</label>
    <input type="date" id="end-date" name="endDate">
  </fieldset>

  <!-- COMBINED DATE+TIME -->
  <fieldset>
    <legend>Schedule Demo</legend>
    <label for="demo-time">Preferred demo time (UTC):</label>
    <input type="datetime-local" id="demo-time" name="demoTime"
           min="2026-06-23T00:00"
           step="1800">
    <!-- step="1800": 30-minute increments (1800 seconds) -->
  </fieldset>

  <button type="submit">Save Profile</button>
</form>
</body>
</html>
```

### Level 4 — Advanced: Custom Validation with Constraint Validation API
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Custom Form Validation</title>
  <style>
    input:invalid { border-color: red; }
    input:valid   { border-color: green; }
    input:user-invalid { /* Only show after user interacts — CSS 4 */ }
    .error-msg { color: red; font-size: 0.875rem; display: none; }
    .error-msg.visible { display: block; }
  </style>
</head>
<body>
<form id="payment-form" action="/pay" method="POST" novalidate>
  <!-- novalidate: disables browser default validation UI
       We'll use the Constraint Validation API for custom messages -->

  <fieldset>
    <legend>Payment Details</legend>

    <label for="card-number">Card Number</label>
    <input type="text" id="card-number" name="cardNumber"
           inputmode="numeric"       <!-- Mobile: shows numeric keyboard -->
           autocomplete="cc-number"
           pattern="[0-9\s]{13,19}"
           maxlength="19"
           required
           aria-describedby="card-error">
    <span id="card-error" class="error-msg" role="alert"></span>

    <label for="expiry">Expiry Date (MM/YY)</label>
    <input type="text" id="expiry" name="expiry"
           inputmode="numeric"
           autocomplete="cc-exp"
           pattern="(0[1-9]|1[0-2])\/([0-9]{2})"
           maxlength="5"
           placeholder="MM/YY"
           required
           aria-describedby="expiry-error">
    <span id="expiry-error" class="error-msg" role="alert"></span>

    <label for="cvv">CVV</label>
    <input type="text" id="cvv" name="cvv"
           inputmode="numeric"
           autocomplete="cc-csc"
           pattern="[0-9]{3,4}"
           maxlength="4"
           required
           aria-describedby="cvv-error">
    <span id="cvv-error" class="error-msg" role="alert"></span>
  </fieldset>

  <button type="submit">Pay Now</button>
</form>

<script>
  const form = document.getElementById('payment-form');

  // Format card number with spaces every 4 digits
  document.getElementById('card-number').addEventListener('input', (e) => {
    let value = e.target.value.replace(/\D/g, ''); // digits only
    value = value.match(/.{1,4}/g)?.join(' ') || value;
    e.target.value = value;
  });

  // Format expiry as MM/YY
  document.getElementById('expiry').addEventListener('input', (e) => {
    let value = e.target.value.replace(/\D/g, '');
    if (value.length >= 2) {
      value = value.substring(0, 2) + '/' + value.substring(2);
    }
    e.target.value = value;
  });

  // CONSTRAINT VALIDATION API: setCustomValidity for custom messages
  function validateField(input, errorId, message) {
    const errorEl = document.getElementById(errorId);
    if (!input.validity.valid) {
      // Set custom error message instead of browser's generic one
      if (input.validity.valueMissing) {
        input.setCustomValidity(`${input.labels[0].textContent} is required`);
      } else if (input.validity.patternMismatch) {
        input.setCustomValidity(message);
      }
      errorEl.textContent = input.validationMessage;
      errorEl.classList.add('visible');
      input.setAttribute('aria-invalid', 'true');
    } else {
      input.setCustomValidity('');
      errorEl.classList.remove('visible');
      input.removeAttribute('aria-invalid');
    }
  }

  document.getElementById('card-number').addEventListener('blur', (e) => {
    validateField(e.target, 'card-error', 'Enter a valid 13-19 digit card number');
  });

  document.getElementById('expiry').addEventListener('blur', (e) => {
    validateField(e.target, 'expiry-error', 'Format must be MM/YY (e.g., 06/28)');
  });

  document.getElementById('cvv').addEventListener('blur', (e) => {
    validateField(e.target, 'cvv-error', 'Enter the 3 or 4 digit code on your card');
  });

  form.addEventListener('submit', (e) => {
    e.preventDefault();
    let isValid = true;

    form.querySelectorAll('input[required]').forEach(input => {
      input.dispatchEvent(new Event('blur'));
      if (!input.validity.valid) isValid = false;
    });

    if (isValid) {
      console.log('Form is valid — submitting...');
      form.submit();
    } else {
      // Focus the first invalid field
      form.querySelector(':invalid')?.focus();
    }
  });
</script>
</body>
</html>
```

### Level 5 — Expert: Multi-Step Form, FormData API & Progressive Enhancement
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Multi-Step Form with FormData API</title>
</head>
<body>

<!-- MULTI-STEP FORM using fieldsets with hidden/shown states -->
<!-- Accessible: uses aria-current and role="group" -->
<form id="wizard" aria-label="Course enrollment wizard">

  <!-- Step indicator: accessible progress -->
  <nav aria-label="Form steps">
    <ol role="list">
      <li aria-current="step">
        <span aria-hidden="true">1</span> Personal Info
      </li>
      <li><span aria-hidden="true">2</span> Course Selection</li>
      <li><span aria-hidden="true">3</span> Payment</li>
    </ol>
  </nav>

  <!-- STEP 1 -->
  <fieldset id="step-1" aria-labelledby="step-1-legend">
    <legend id="step-1-legend">Step 1: Personal Information</legend>

    <label for="s1-name">Full Name *</label>
    <input type="text" id="s1-name" name="fullName" required autocomplete="name">

    <label for="s1-email">Email *</label>
    <input type="email" id="s1-email" name="email" required autocomplete="email">

    <button type="button" id="next-1">Next: Course Selection →</button>
  </fieldset>

  <!-- STEP 2 (hidden initially) -->
  <fieldset id="step-2" hidden aria-labelledby="step-2-legend">
    <legend id="step-2-legend">Step 2: Course Selection</legend>

    <label for="course">Choose Course *</label>
    <select id="course" name="course" required>
      <option value="">Select...</option>
      <option value="html-css">HTML & CSS ($49)</option>
      <option value="javascript">JavaScript ($99)</option>
      <option value="react">React ($149)</option>
    </select>

    <label for="start-date">Start Date *</label>
    <input type="date" id="start-date" name="startDate" required
           min="2026-07-01">

    <button type="button" id="prev-1">← Back</button>
    <button type="button" id="next-2">Next: Payment →</button>
  </fieldset>

  <!-- STEP 3 (hidden initially) -->
  <fieldset id="step-3" hidden aria-labelledby="step-3-legend">
    <legend id="step-3-legend">Step 3: Payment</legend>

    <label for="card">Card Number *</label>
    <input type="text" id="card" name="cardNumber"
           autocomplete="cc-number" inputmode="numeric"
           pattern="[0-9\s]{13,19}" required>

    <button type="button" id="prev-2">← Back</button>
    <button type="submit">Enroll Now ($<span id="price">0</span>)</button>
  </fieldset>

</form>

<script>
  // Multi-step navigation
  function showStep(current, next) {
    document.getElementById(`step-${current}`).hidden = true;
    const nextEl = document.getElementById(`step-${next}`);
    nextEl.hidden = false;

    // Update progress nav
    document.querySelectorAll('nav[aria-label="Form steps"] li').forEach((li, i) => {
      li.removeAttribute('aria-current');
      if (i + 1 === next) li.setAttribute('aria-current', 'step');
    });

    // Focus the legend for screen reader announcement
    nextEl.querySelector('legend').setAttribute('tabindex', '-1');
    nextEl.querySelector('legend').focus();
  }

  document.getElementById('next-1').addEventListener('click', () => {
    // Validate step 1 before proceeding
    const step1Inputs = document.querySelectorAll('#step-1 input[required]');
    let valid = true;
    step1Inputs.forEach(input => {
      if (!input.checkValidity()) { input.reportValidity(); valid = false; }
    });
    if (valid) showStep(1, 2);
  });

  document.getElementById('next-2').addEventListener('click', () => {
    const step2Inputs = document.querySelectorAll('#step-2 input[required], #step-2 select[required]');
    let valid = true;
    step2Inputs.forEach(input => {
      if (!input.checkValidity()) { input.reportValidity(); valid = false; }
    });
    if (valid) {
      // Update price display based on course selection
      const prices = { 'html-css': 49, 'javascript': 99, 'react': 149 };
      const course = document.getElementById('course').value;
      document.getElementById('price').textContent = prices[course] || 0;
      showStep(2, 3);
    }
  });

  document.getElementById('prev-1').addEventListener('click', () => showStep(2, 1));
  document.getElementById('prev-2').addEventListener('click', () => showStep(3, 2));

  // FORMDATA API: Read form data programmatically before sending
  document.getElementById('wizard').addEventListener('submit', async (e) => {
    e.preventDefault();

    const formData = new FormData(e.target);

    // Iterate all form fields:
    for (const [name, value] of formData.entries()) {
      console.log(`${name}: ${value}`);
      // fullName: Jane Smith
      // email: jane@example.com
      // course: react
      // startDate: 2026-07-15
      // cardNumber: 4111 1111 1111 1111
    }

    // Convert to plain object:
    const data = Object.fromEntries(formData.entries());

    // Submit as JSON (not default form encoding):
    try {
      const response = await fetch('/api/enroll', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data)
      });

      const result = await response.json();
      if (result.success) {
        window.location.href = '/dashboard?enrolled=true';
      }
    } catch (err) {
      console.error('Enrollment failed:', err);
    }
  });
</script>

</body>
</html>
<!-- Expert Concepts:
     - Multi-step form with accessibility (aria-current, focus management)
     - FormData API for reading form data without manual DOM traversal
     - Sending form data as JSON via fetch
     - checkValidity() vs reportValidity()
     - inputmode for mobile keyboard type
     - autocomplete tokens for autofill/password managers
-->

## REAL_WORLD

### How GitHub & Stripe Use Native HTML Forms

GitHub's issue-creation form, Stripe's Checkout, and Shopify's storefronts all use plain `<form method="POST" action="/submit">` as the base layer — JavaScript is layered ON TOP for nicer UX, but the form still submits if JS fails to load. This is **progressive enhancement** and it's why GitHub still works on a 2G connection.

```html
<!-- GitHub-style issue form: works without JS, enhanced with JS -->
<form action="/repo/issues" method="POST" enctype="multipart/form-data" id="issue-form">
  <input type="hidden" name="csrf_token" value="abc123xyz">

  <label for="issue-title">Title</label>
  <input id="issue-title" name="title" type="text" required
         maxlength="256" autocomplete="off" autofocus>

  <label for="issue-body">Description</label>
  <textarea id="issue-body" name="body" rows="10" required
            minlength="10" maxlength="65535"
            aria-describedby="body-help"></textarea>
  <small id="body-help">Supports Markdown. Drag and drop images.</small>

  <label for="issue-files">Attach files</label>
  <input id="issue-files" name="attachments" type="file"
         multiple accept="image/*,.pdf,.txt">

  <fieldset>
    <legend>Labels</legend>
    <label><input type="checkbox" name="labels" value="bug"> Bug</label>
    <label><input type="checkbox" name="labels" value="enhancement"> Enhancement</label>
  </fieldset>

  <button type="submit">Submit new issue</button>
  <button type="button" formnovalidate onclick="saveDraft()">Save draft</button>
</form>
```

### Production Gotcha: Double-Submit-After-Click Memory Leak
The #1 form bug at scale — accidentally double-submitting payments because the button isn't disabled fast enough:

```javascript
// ❌ DANGEROUS — double-clicks send two POST requests, two charges
form.addEventListener('submit', async (e) => {
  e.preventDefault();
  await fetch('/charge', { method: 'POST', body: new FormData(form) });
});

// ✅ PRODUCTION-SAFE — disable button synchronously BEFORE await
form.addEventListener('submit', async (e) => {
  e.preventDefault();
  const btn = form.querySelector('button[type=submit]');
  if (btn.disabled) return;          // already submitting
  btn.disabled = true;               // synchronous — blocks double-click
  btn.textContent = 'Submitting...';
  try {
    await fetch('/charge', { method: 'POST', body: new FormData(form) });
  } finally {
    btn.disabled = false;
    btn.textContent = 'Pay';
  }
});
```
**Why it happens:** Between `fetch()` returning a promise and the network round-trip completing, the button stays enabled — a quick second click queues a second request before the first resolves.

### Form Element Quick Reference

| Element | Submits to server | Default `type` | Common gotcha |
|---------|------------------|-----------------|---------------|
| `<input type="text">` | as `name=value` | text | autocomplete defaults vary by browser |
| `<button>` inside form | yes (type defaults to "submit") | submit | always specify `type` explicitly |
| `<select multiple>` | as repeated `name=value` | — | server must parse as array |
| `<input type="file">` | needs `enctype="multipart/form-data"` | — | silent fail without correct enctype |
| `<input type="hidden">` | always | — | never put secrets here — visible in DevTools |

## INTERVIEW

**Q1 (Junior): What is the difference between `method="GET"` and `method="POST"`?**
A: GET appends form data to the URL as query parameters (visible, bookmarkable, cacheable, length-limited to ~2KB); POST sends data in the request body (hidden from URL, no size limit, never cached). Use GET for searches and filters where the URL should reflect state; use POST for anything that mutates server state. Common bug: using GET for a login form puts the password in browser history and server access logs.

**Q2 (Junior): Why must a file upload form have `enctype="multipart/form-data"`?**
A: The default `application/x-www-form-urlencoded` encoding cannot represent binary data — it serialises everything as URL-encoded strings. Multipart encoding splits the body into separate parts, each with its own headers, allowing files to transmit as native binary. Without it, your server receives only the filename string, not the bytes.
```html
<!-- ❌ Silently fails — server gets filename string only -->
<form action="/upload" method="POST">
  <input type="file" name="avatar">
</form>

<!-- ✅ Correct — server receives actual file bytes -->
<form action="/upload" method="POST" enctype="multipart/form-data">
  <input type="file" name="avatar">
</form>
```

**Q3 (Mid): What happens when a `<button>` inside a `<form>` has no `type` attribute?**
A: It defaults to `type="submit"` and submits the form when clicked or when Enter is pressed in any field. This causes one of the most common React/Angular bugs: a "Cancel" or "Add row" button inside a form unexpectedly submits. Always specify `type="button"` for any in-form button that should NOT submit. Zero runtime cost; enormous bug-prevention payoff.

**Q4 (Mid): What is the role of the `name` attribute vs the `id` attribute on form controls?**
A: `name` is what gets serialised in the submission payload — `<input name="email" value="x">` sends `email=x` to the server. `id` is purely for DOM/CSS/label-association and is NEVER sent to the server. A control without a `name` is invisible to form submission. The `id` is required only because `<label for="x">` needs it to associate a label for accessibility and click-to-focus.

**Q5 (Senior): How does the browser's autofill engine decide which fields to autofill?**
A: It reads the `autocomplete` attribute, `name`/`id` heuristics, the input `type`, and surrounding `<label>` text. The WHATWG spec defines ~50 `autocomplete` token values (`given-name`, `family-name`, `postal-code`, `cc-number`, `one-time-code`) telling the browser exactly what to suggest. Setting `autocomplete="off"` on a password field is actively bad — modern browsers ignore it for security (it would defeat password managers). For sensitive new-password fields use `autocomplete="new-password"` to opt into "suggest a strong password" UX.

**Q6 (Senior): What is the difference between `<input type="submit">` and `<button type="submit">`?**
A: Functionally similar, but `<button>` can contain arbitrary HTML (icons, formatting, SVG), is far easier to style (no browser-specific quirks), and can have a separate `value` attribute submitted alongside form data. `<input type="submit">` accepts only a plain text label via `value`. In 2026 there's essentially no reason to use `<input type="submit">` outside legacy. The "implicit submission" rule still applies to both: Enter in any single-line text input triggers submission via the first `type="submit"` element.

**Q7 (Senior+): Design a checkout submission strategy that survives: network failure, tab close, back button, double-submit.**
A: (1) Persist each step's data to `sessionStorage` on every `change` event so a tab close + reopen restores state. (2) Use a hidden `<input name="idempotency_key" value="<uuid>">` generated once at flow start so the server dedupes a double-submission at the DB layer. (3) Disable the final pay button SYNCHRONOUSLY inside the submit handler before any `await` (see REAL_WORLD gotcha). (4) For back button, use `history.pushState` to give each step a distinct URL and listen to `popstate` to restore the right section. (5) On reconnect after network failure, retry with the SAME idempotency_key — the server returns the cached result instead of charging twice. This combination is how Stripe Checkout achieves "exactly-once" payment over an "at-least-once" network.

## FEYNMAN CHECK

### Explain HTML Forms Like I'm 10 Years Old
> A form is a stamped envelope: each input is a labelled line, and clicking Submit folds the page up and posts it to the address in `action`. The browser does the entire mailing for free — it gathers every `name=value` pair, encodes them, opens the network connection, and follows the response — without one line of JavaScript. This is why `<form>` is the ONLY HTML element that can mutate server state without scripting, and why every JS framework's form library is just a fancier wrapper around it.

---

### 5 Deep Conceptual Questions

**Q1: What problem do HTML forms fundamentally solve?**
> **A:** They give the browser a built-in, accessible, network-aware mechanism to collect typed data and ship it to a server using either HTTP method, multiple encodings, and progressive enhancement. Replacing with custom JS loses: automatic Enter-key submission, keyboard accessibility, screen-reader form-mode, password manager integration, autofill heuristics, and the ability to submit without JS.

**Q2: What is the ONE mental model that makes forms click?**
> **A:** A form is a serialiser: every NAMED, ENABLED control inside the form contributes one (or more) `name=value` pair to the submission, encoded per `enctype`. Controls without `name`, controls outside the `<form>`, and disabled controls are silently EXCLUDED. Once you internalise "no name = no submission," 90% of form mysteries disappear.

**Q3: What is the most dangerous misconception? Show it with code.**
> **A:** That a `<button>` inside a form is just a click target:
> ```html
> <!-- ❌ Clicking "Cancel" submits the form. Pressing Enter in title submits the form. -->
> <form action="/save" method="POST">
>   <input name="title">
>   <button onclick="closeDialog()">Cancel</button>
>   <button>Save</button>
> </form>
>
> <!-- ✅ Cancel is type=button (no submit). Save is explicit submit. -->
> <form action="/save" method="POST">
>   <input name="title">
>   <button type="button" onclick="closeDialog()">Cancel</button>
>   <button type="submit">Save</button>
> </form>
> ```

**Q4: How does form submission interact with the browser's networking stack?**
> **A:** On submit, the browser walks the form's elements collection in DOM order, gathers every successful control (named, non-disabled, valid value), encodes the dataset using `enctype`, opens an XHR-equivalent request to `action` with the form's `method`, and either replaces the current document with the response (default) or does nothing if `event.preventDefault()` was called. The serialisation is identical to `new FormData(form)`, which is why fetch-based handlers can pass `new FormData(form)` directly as the body.

**Q5: Write a one-sentence FAANG-precise definition.**
> **A:** "An HTML form is a serialisation boundary defined by a `<form>` element that collects named, non-disabled successful controls in DOM order, encodes them per `enctype`, and dispatches an HTTP request to `action` via `method` — providing keyboard accessibility, autofill, password-manager integration, and progressive enhancement that no JavaScript form library can fully replicate without re-implementing decades of browser UX."

## BUILD

### 🏗️ Mini Project: No-JS Multi-Step Wizard With Full Validation and Autosave

**What you will build:** A 3-step registration wizard that works WITHOUT JavaScript using native HTML form features (`required`, `pattern`, `type`, `autocomplete`, hidden fields for step state) — then enhanced with 30 lines of JS for autosave to `sessionStorage`.
**Why this project:** Proves you understand native form capabilities deeply enough to ship a working flow before reaching for React-Hook-Form or Formik.
**Time estimate:** 45 minutes

---

#### Step 1 — Setup
```bash
mkdir wizard-form && cd wizard-form
ni step-1.html, step-2.html, step-3.html, server.mjs -ItemType File
```

#### Step 2 — Step 1 (Personal Info) — pure HTML
```html
<!DOCTYPE html>
<html lang="en"><head><meta charset="UTF-8"><title>Register · 1 of 3</title></head>
<body>
  <form action="/wizard/step2" method="POST" autocomplete="on">
    <progress value="1" max="3"></progress>
    <h1>Personal Info</h1>

    <label for="fn">First name</label>
    <input id="fn" name="firstName" type="text" required
           minlength="1" maxlength="50" autocomplete="given-name" autofocus>

    <label for="ln">Last name</label>
    <input id="ln" name="lastName" type="text" required
           minlength="1" maxlength="50" autocomplete="family-name">

    <label for="dob">Date of birth</label>
    <input id="dob" name="dob" type="date" required
           max="2010-01-01" autocomplete="bday">

    <button type="submit">Next →</button>
  </form>
</body></html>
```

#### Step 3 — Step 2 (Account) — hidden inputs carry prior data
```html
<form action="/wizard/step3" method="POST" autocomplete="on">
  <progress value="2" max="3"></progress>
  <h1>Account Setup</h1>

  <input type="hidden" name="firstName" value="{{ firstName }}">
  <input type="hidden" name="lastName"  value="{{ lastName }}">
  <input type="hidden" name="dob"       value="{{ dob }}">

  <label for="em">Email</label>
  <input id="em" name="email" type="email" required
         autocomplete="email" maxlength="254">

  <label for="pw">Password</label>
  <input id="pw" name="password" type="password" required
         minlength="12" autocomplete="new-password"
         pattern=".*[A-Z].*" title="Must include at least one uppercase letter">

  <button type="button" formnovalidate
          onclick="history.back()">← Back</button>
  <button type="submit">Next →</button>
</form>
```

#### Step 4 — Error Handling: Autosave + Double-Submit Prevention
```html
<script>
// Persist every change to sessionStorage so reload/back doesn't lose data
document.querySelectorAll('input, select, textarea').forEach(el => {
  const key = 'wizard:' + el.name;
  if (sessionStorage.getItem(key)) el.value = sessionStorage.getItem(key);
  el.addEventListener('input', () => sessionStorage.setItem(key, el.value));
});

// Prevent double-submit (the #1 production form bug)
const form = document.querySelector('form');
form.addEventListener('submit', () => {
  const btn = form.querySelector('button[type=submit]');
  btn.disabled = true;                 // synchronous — no await yet
  btn.textContent = 'Submitting...';
}, { once: true });
</script>
```

#### Step 5 — Tests
```javascript
// server.mjs — minimal Node server that validates each step
import http from 'node:http';
import { parse } from 'node:querystring';

http.createServer((req, res) => {
  if (req.method !== 'POST') return res.end('OK');
  let body = '';
  req.on('data', c => body += c);
  req.on('end', () => {
    const data = parse(body);
    // Server-side validation — NEVER trust client validation alone
    if (req.url === '/wizard/step2' && (!data.firstName || !data.lastName))
      return res.writeHead(400).end('Missing fields');
    if (req.url === '/wizard/step3') {
      if (!/.+@.+\..+/.test(data.email))         return res.writeHead(400).end('Bad email');
      if ((data.password || '').length < 12)     return res.writeHead(400).end('Short pw');
    }
    res.writeHead(303, { Location: req.url.replace(/step\d/, m => 'step' + (+m.slice(-1) + 1)) }).end();
  });
}).listen(8080);
```

**Expected Output:**
```
- With JS disabled: full wizard works, server handles each step
- With JS enabled: data persists across reload, button disables on submit
- Empty submission: browser tooltip "Please fill in this field"
- Bad email: tooltip "Please include an '@' in the email address"
- Double-click on Submit: only ONE POST hits the server
```

**Stretch Challenges:**
- [ ] Add `autocomplete="one-time-code"` to a 6-digit SMS verification step (iOS auto-paste)
- [ ] Add a CSP header and prove the inline script still works using a nonce
- [ ] Replace autosave with the `FormData` API + IndexedDB for resumability across sessions

## SPACED REVIEW

---

### Day 1 — Recall

**Q1:** Define an HTML form in one sentence: what it collects, how it serialises, where it sends.

**Q2:** What attribute MUST be on every form control that you want submitted? What happens if you omit it?

**Q3:** Write a 12-line HTML form with: required email field, min-12-char password field, autocomplete hints, submit button.

---

### Day 3 — Comprehension

**Q4:** What is the difference between `<button>` (no type) and `<button type="button">` inside a form? Give one bug each causes.

**Q5:** Describe the "double-submit payment" production bug. Write the buggy submit handler and the fix.

**Q6:** Refactor for progressive enhancement, accessibility, and security:
```html
<form>
  <div>Email</div>
  <input id="x">
  <div>Password</div>
  <input id="y" type="password">
  <div onclick="submit()">Go</div>
</form>
```

---

### Day 7 — Application

**Q7:** Build a file upload form that supports: multiple files, only images and PDFs, drag-and-drop, and a visible "X selected" count — using only HTML attributes and 5 lines of JS.

**Q8:** A form POSTs JSON via fetch and Lighthouse a11y flags it. Name 4 missing form features that would be present if it had used native `<form action method>`.

**Q9:** Explain the `autocomplete` token system. Give the correct token for: a one-time SMS code, a new password during signup, a credit card expiry month.

---

### Day 14 — Synthesis & Interview Prep

**Q10:** ★ Interview: "Walk me through what happens between Submit click and the browser navigating to the response — without JavaScript involved."

**Q11:** Draw the dependency graph: forms-html ↔ form-validation ↔ html-attributes ↔ accessibility-html.

**Q12:** ★ System design: "Design checkout form handling that survives: tab close, network failure mid-flow, back button, double-click on pay, submission without JS."
