---
slug: "change-streams"
title: "MongoDB Change Streams"
level: 4
---

## WHY
In modern applications, users expect real-time reactivity. If an Uber driver accepts your ride, your app shouldn't force you to press a "Refresh" button to see the update. The data should instantly push to your screen.

Historically, to achieve this, backend developers had to write complex polling loops (querying the database every 5 seconds to ask, "Did anything change?"). This destroyed database performance. 
**Change Streams** allow your Node.js application to subscribe to a MongoDB collection and receive instant, real-time push notifications the exact millisecond a document is inserted, updated, or deleted!

## THEORY
### The Oplog (Operations Log)
Change Streams are built directly on top of the MongoDB **Oplog**. 
As we learned in the Replication module, the Oplog is the internal ledger that Primary nodes use to synchronize with Secondary nodes. When a document is updated, the Primary writes the change to the Oplog.

Change Streams simply open a persistent connection (a cursor) to this Oplog. Instead of syncing another database server, the Oplog events are streamed directly over the network to your Node.js application layer!

*Note: Because Change Streams rely on the Oplog, they **only work** if your MongoDB cluster is configured as a Replica Set or a Sharded Cluster. They do not work on standalone instances!*

## VISUALIZATION_CONFIG
```json
{
  "language": "javascript",
  "fileName": "change-streams.js",
  "steps": [
    {
      "title": "What are change streams?",
      "description": "Change streams provide real-time notifications of database changes using the oplog. Applications can react to inserts, updates, deletes, and collection drops.",
      "code": "// Watch entire collection\nconst cs = db.collection('orders').watch();\n\ncs.on('change', (event) => {\n  switch(event.operationType) {\n    case 'insert':  handleNew(event.fullDocument); break;\n    case 'update':  handleUpdate(event.documentKey._id, event.updateDescription); break;\n    case 'delete':  handleDelete(event.documentKey._id); break;\n    case 'replace': handleReplace(event.fullDocument); break;\n  }\n});\n\n// Watch with filter\nconst filtered = col.watch([\n  { $match: { \"fullDocument.status\": \"pending\" } }\n]);",
      "highlight": [
        2,
        4,
        6,
        7,
        8,
        9,
        14,
        15
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Change event types",
        "items": [
          {
            "label": "insert",
            "color": "#10b981",
            "value": "New document added",
            "highlight": true
          },
          {
            "label": "update",
            "color": "#818cf8",
            "value": "Document fields modified"
          },
          {
            "label": "delete",
            "color": "#ef4444",
            "value": "Document removed"
          },
          {
            "label": "replace",
            "color": "#f59e0b",
            "value": "Whole document replaced"
          }
        ]
      }
    },
    {
      "title": "Resume tokens",
      "description": "Every change event has a _id (resume token). Save it to resume a change stream after a disconnect — no missed events.",
      "code": "let resumeToken = null;\n\nasync function watchOrders() {\n  const options = resumeToken\n    ? { resumeAfter: resumeToken }\n    : {};\n\n  const cs = col.watch([], options);\n\n  cs.on('change', (event) => {\n    resumeToken = event._id;  // save token\n    handleChange(event);\n    saveTokenToRedis(resumeToken);  // persist externally\n  });\n\n  cs.on('error', async (err) => {\n    await cs.close();\n    // Restart using saved token\n    watchOrders();\n  });\n}",
      "highlight": [
        1,
        4,
        5,
        6,
        11,
        13,
        18
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "cs.watch() opened — receive events",
            "done": true
          },
          {
            "label": "Save event._id (resumeToken) each event",
            "done": true
          },
          {
            "label": "Disconnect → reconnect with resumeAfter",
            "active": true
          },
          {
            "label": "MongoDB delivers missed events from oplog"
          }
        ]
      }
    },
    {
      "title": "Full document lookup",
      "description": "By default, update events include only changed fields. Use fullDocument: \"updateLookup\" to also return the complete document after the update.",
      "code": "const cs = col.watch([], {\n  fullDocument: \"updateLookup\",  // fetch full doc on update\n  fullDocumentBeforeChange: \"whenAvailable\"  // doc before update\n});\n\ncs.on('change', (event) => {\n  if (event.operationType === 'update') {\n    console.log('Before:', event.fullDocumentBeforeChange);\n    console.log('After:', event.fullDocument);\n    console.log('Changed fields:', event.updateDescription.updatedFields);\n    console.log('Removed fields:', event.updateDescription.removedFields);\n  }\n});",
      "highlight": [
        2,
        3,
        8,
        9,
        10,
        11
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Full document options",
        "items": [
          {
            "label": "default",
            "color": "#818cf8",
            "value": "updateDescription only (changed fields)"
          },
          {
            "label": "updateLookup",
            "color": "#10b981",
            "value": "Also fetches full document after update",
            "highlight": true
          },
          {
            "label": "whenAvailable",
            "color": "#f59e0b",
            "value": "Pre-change doc if available"
          },
          {
            "label": "required",
            "color": "#60a5fa",
            "value": "Pre-change doc or error"
          }
        ]
      }
    },
    {
      "title": "Change streams in Spring Boot",
      "description": "Spring Data MongoDB supports change streams via ReactiveMongoOperations or MongoTemplate. Use @Tailable with reactive streams.",
      "code": "// Spring Boot reactive change stream\n@Service\npublic class OrderChangeListener {\n\n  @Autowired\n  ReactiveMongoOperations mongo;\n\n  @PostConstruct\n  public void watchOrders() {\n    ChangeStreamOptions options = ChangeStreamOptions.builder()\n      .filter(newAggregation(\n          match(where(\"operationType\").in(\"insert\",\"update\"))\n      ))\n      .fullDocumentLookup(FullDocument.UPDATE_LOOKUP)\n      .build();\n\n    mongo.changeStream(\"orders\", options, Order.class)\n      .doOnNext(event -> processChange(event))\n      .subscribe();\n  }\n}",
      "highlight": [
        10,
        11,
        12,
        14,
        17,
        18
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "ReactiveMongoOperations.changeStream()",
            "done": true
          },
          {
            "label": "ChangeStreamOptions: filter + fullDocument",
            "done": true
          },
          {
            "label": "Reactive: .doOnNext() for each event",
            "active": true
          },
          {
            "label": ".subscribe() starts listening"
          }
        ]
      }
    },
    {
      "title": "Real-world use cases",
      "description": "Change streams power real-time features: live dashboards, cache invalidation, event-driven microservices, notification systems, and audit logs.",
      "code": "// Cache invalidation on update\nproductsCol.watch().on('change', async (event) => {\n  const id = event.documentKey._id.toString();\n  await redis.del(`product:${id}`);\n  console.log(`Cache invalidated for product ${id}`);\n});\n\n// Audit log\nusersCol.watch().on('change', async (event) => {\n  await auditCol.insertOne({\n    collection: 'users',\n    operation: event.operationType,\n    documentId: event.documentKey._id,\n    changedFields: event.updateDescription?.updatedFields,\n    timestamp: new Date()\n  });\n});",
      "highlight": [
        2,
        3,
        4,
        9,
        10,
        11,
        12,
        13,
        14
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Change stream use cases",
        "items": [
          {
            "label": "Cache invalidation",
            "color": "#10b981",
            "value": "Invalidate Redis on document change",
            "highlight": true
          },
          {
            "label": "Event-driven services",
            "color": "#818cf8",
            "value": "Trigger downstream microservices"
          },
          {
            "label": "Real-time dashboards",
            "color": "#f59e0b",
            "value": "Push live metrics to UI via WebSocket"
          },
          {
            "label": "Audit trail",
            "color": "#60a5fa",
            "value": "Record all changes for compliance"
          }
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner (Listening to a Collection)
Listening to a collection in Node.js is incredibly simple. It uses the standard JavaScript EventEmitter pattern.

```javascript
// 1. Target the collection you want to monitor
const collection = db.collection('orders');

// 2. Open the Change Stream
const changeStream = collection.watch();

// 3. Listen for the 'change' event!
changeStream.on('change', (next) => {
    console.log("A change occurred in the database!");
    console.log("Operation Type:", next.operationType); // 'insert', 'update', 'delete', etc.
    
    if (next.operationType === 'insert') {
        console.log("New document inserted:", next.fullDocument);
    }
});
```
If another developer opens a MongoDB Shell and runs `db.orders.insertOne(...)`, your Node.js console will instantly log the new document!

### Level 2 — Building Blocks (Filtering the Stream)
If your `users` collection receives 1,000 updates a second, opening a raw Change Stream will flood your Node.js server with useless events, crashing its memory. 

You should always filter the Change Stream so MongoDB only sends the events you actually care about. You do this by passing a standard **Aggregation Pipeline** into the `watch()` method!

```javascript
// We only want to be notified when a HIGH VALUE order is INSERTED.
const pipeline = [
    { 
        $match: { 
            'operationType': 'insert',
            'fullDocument.totalPrice': { $gt: 500 }
        } 
    }
];

const changeStream = db.collection('orders').watch(pipeline);

changeStream.on('change', (next) => {
    // Send an email to the VIP Sales Team!
    emailService.sendVipAlert(next.fullDocument);
});
```

### Level 3 — Intermediate (Update Document Lookups)
When a document is *inserted*, the Change Stream event contains a `fullDocument` object representing the new data.

However, when a document is *updated* (`db.users.updateOne`), MongoDB strives for maximum efficiency. The Oplog only records the *delta* (the exact fields that changed). By default, the Change Stream event will **not** contain the full document, it will only contain an `updateDescription` object showing which fields were modified.

If your application needs to see the entire document after the update, you must configure `fullDocument: 'updateLookup'`.

```javascript
const changeStream = db.collection('users').watch([], {
    // Tells MongoDB to instantly execute an extra query to fetch 
    // the most current version of the document and attach it to the event!
    fullDocument: 'updateLookup' 
});

changeStream.on('change', (next) => {
    if (next.operationType === 'update') {
        // Now you have access to the entire document, not just the delta!
        console.log("The fully updated document is:", next.fullDocument);
    }
});
```

### Level 4 — Advanced (Resume Tokens)
Networks are unstable. What happens if your Node.js server loses connection to MongoDB for 5 seconds? Does it miss the events that happened during those 5 seconds?

Yes, unless you use **Resume Tokens**.
Every single event emitted by a Change Stream contains a unique `_id` field called the Resume Token. This token acts as a bookmark in the Oplog.

```javascript
changeStream.on('change', (next) => {
    // 1. Save this token to a persistent storage (like Redis or a file)
    const resumeToken = next._id;
    redis.set('lastMongoEventToken', JSON.stringify(resumeToken));
});
```
If your Node.js server crashes and reboots, it can pull the last token from Redis and tell MongoDB to rewind history and replay all the events it missed while it was offline!

```javascript
const lastToken = JSON.parse(await redis.get('lastMongoEventToken'));

// 2. Resume exactly where we left off!
const changeStream = db.collection('users').watch([], {
    resumeAfter: lastToken
});
```

## REAL_WORLD
**WebSockets and Reactivity**
Change Streams are the backend mechanism for generating real-time events. To push these events all the way to the user's browser, you pair Change Streams with **WebSockets** (using a library like `Socket.io`).

1. The React Frontend connects to Node.js via WebSocket.
2. The Node.js server opens a Change Stream to MongoDB.
3. When MongoDB fires a change event, Node.js catches it, transforms it into a WebSocket payload, and emits it to the React frontend.
4. React updates the UI state, and the user sees the screen change instantly!

## INTERVIEW
**Q: I have 5 identical Node.js servers running behind a load balancer. I put a Change Stream in my code to send a welcome email when a user registers. But now, when a user registers, they receive 5 identical welcome emails! Why?**
A: Because Change Streams are stateful network connections, not distributed queues! Every single Node.js server opened its own independent connection to the MongoDB Oplog. When a user is inserted, MongoDB happily streams that event to ALL 5 servers simultaneously. Each server executes the email logic. 

To fix this, you should never trigger side effects (like sending emails) directly from a Change Stream in a multi-server architecture. Instead, you should use the Change Stream on a single, dedicated worker service, OR have the Change Stream push the event into a message queue (like RabbitMQ/Kafka), which then distributes a single task to the worker pool.

## FEYNMAN CHECK

### Explain MongoDB Change Streams Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **MongoDB Change Streams** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about MongoDB Change Streams, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does MongoDB Change Streams solve? Why would JavaScript/web development be harder without it?**
> **A:** MongoDB Change Streams solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about MongoDB Change Streams?**
> **A:** The single most important thing is understanding the *mental model*: MongoDB Change Streams operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with MongoDB Change Streams and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which MongoDB Change Streams operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does MongoDB Change Streams interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** MongoDB Change Streams interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of MongoDB Change Streams to a senior engineer in two sentences, what would you say?**
> **A:** "MongoDB Change Streams works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a MongoDB Change Streams Utility

**Goal:** Implement a real, working MongoDB Change Streams module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `changestreams` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir change-streams-demo && cd change-streams-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── MongoDB Change Streams Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of MongoDB Change Streams.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createMongoDBChangeStreams(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[MongoDB Change Streams] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual MongoDB Change Streams logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[MongoDB Change Streams] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[MongoDB Change Streams] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createMongoDBChangeStreams({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[MongoDB Change Streams] Input cannot be null or undefined`);
    }
    const instance = createMongoDBChangeStreams();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[MongoDB Change Streams] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── MongoDB Change Streams Tests ──');

test('initialises correctly', () => {
  const m = createMongoDBChangeStreams();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createMongoDBChangeStreams();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[MongoDB Change Streams] Initialized with: hello world
[MongoDB Change Streams] Processed: hello world
Result: hello world
[MongoDB Change Streams] Destroyed.

── MongoDB Change Streams Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define MongoDB Change Streams in one sentence.**
<details><summary>Answer</summary>

MongoDB Change Streams is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of MongoDB Change Streams?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of MongoDB Change Streams.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between MongoDB Change Streams and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | MongoDB Change Streams | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of MongoDB Change Streams] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing MongoDB Change Streams causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct MongoDB Change Streams usage].
</details>

**Q6: What does MongoDB Change Streams look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, MongoDB Change Streams typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use MongoDB Change Streams correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using MongoDB Change Streams:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_change_streams(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test MongoDB Change Streams in isolation? What would you mock?**
<details><summary>Answer</summary>

To test MongoDB Change Streams in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('MongoDB Change Streams', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of MongoDB Change Streams at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how MongoDB Change Streams uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common MongoDB Change Streams interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "MongoDB Change Streams is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does MongoDB Change Streams relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
MongoDB Change Streams
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use MongoDB Change Streams to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where MongoDB Change Streams fits in the data/control flow.
2. **Choose** the right variant/pattern of MongoDB Change Streams for the scale requirement.
3. **Handle** failure modes: what happens when MongoDB Change Streams is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you MongoDB Change Streams is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
