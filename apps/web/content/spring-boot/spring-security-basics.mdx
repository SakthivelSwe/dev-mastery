---
slug: "spring-security-basics"
title: "Spring Security Basics"
level: 4
---

## WHY
If you deploy a Spring Boot API without any security, any anonymous user on the internet can delete your database, read all user data, and impersonate other users. Every production API needs Authentication (Who are you?) and Authorization (What are you allowed to do?).

Spring Security is the de-facto standard for securing Spring applications. It provides a powerful, pluggable security filter chain that intercepts every HTTP request before it reaches your Controllers, verifying the caller's identity and permissions.

## THEORY
### The Filter Chain Architecture
Spring Security works by installing a chain of Servlet Filters in front of your application. Every HTTP request passes through this chain in a specific order. Key filters include:
- `UsernamePasswordAuthenticationFilter`: Handles login form submissions.
- `BearerTokenAuthenticationFilter`: Handles JWT tokens in the `Authorization` header.
- `AuthorizationFilter`: The last filter, which enforces access control rules on matched URLs.

### Authentication vs Authorization
- **Authentication:** Verifying identity ("I am Alice, with password 'secret123'").
- **Authorization:** Verifying permissions ("Alice is an ADMIN and can access `/admin/dashboard`").

## VISUALIZATION_CONFIG
```json
{
  "language": "java",
  "fileName": "SecurityConfig.java",
  "steps": [
    {
      "title": "Security filter chain",
      "description": "Spring Security intercepts every HTTP request through a chain of filters. The most important: UsernamePasswordAuthenticationFilter and JwtAuthenticationFilter.",
      "code": "@Configuration\n@EnableWebSecurity\npublic class SecurityConfig {\n    @Bean\n    SecurityFilterChain chain(HttpSecurity http) throws Exception {\n        return http\n            .authorizeHttpRequests(auth -> auth\n                .requestMatchers(\"/api/public/**\").permitAll()\n                .anyRequest().authenticated())\n            .sessionManagement(s -> s.sessionCreationPolicy(STATELESS))\n            .build();\n    }\n}",
      "highlight": [
        7,
        8,
        9,
        10
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "HTTP Request arrives",
            "done": true
          },
          {
            "label": "DelegatingFilterProxy → Security filter chain",
            "done": true
          },
          {
            "label": "/api/public/** → permitAll",
            "done": true
          },
          {
            "label": "other → authenticated?",
            "active": true
          }
        ]
      }
    },
    {
      "title": "JWT authentication flow",
      "description": "Client sends credentials → server returns JWT token → client sends token in Authorization header on every subsequent request.",
      "code": "// 1. POST /auth/login {username, password}\n// 2. Server verifies credentials → creates JWT\n// 3. Returns: { \"token\": \"eyJh...\" }\n// 4. Client: Authorization: Bearer eyJh...\n// 5. Filter validates JWT, sets SecurityContext",
      "diagram": {
        "kind": "sequence",
        "actors": [
          "Client",
          "AuthController",
          "JwtFilter",
          "API"
        ],
        "messages": [
          {
            "from": "Client",
            "to": "AuthController",
            "label": "POST /login"
          },
          {
            "from": "AuthController",
            "to": "Client",
            "label": "200 { token }"
          },
          {
            "from": "Client",
            "to": "JwtFilter",
            "label": "GET /api/... Bearer token"
          },
          {
            "from": "JwtFilter",
            "to": "API",
            "label": "valid → set context → proceed"
          }
        ]
      }
    },
    {
      "title": "UserDetailsService",
      "description": "Spring Security calls loadUserByUsername to get the user's credentials and roles for authentication.",
      "code": "@Service\npublic class UserDetailsServiceImpl implements UserDetailsService {\n    @Override\n    public UserDetails loadUserByUsername(String username) {\n        User user = userRepo.findByUsername(username).orElseThrow();\n        return new org.springframework.security.core.userdetails.User(\n            user.username(), user.passwordHash(), user.authorities());\n    }\n}",
      "highlight": [
        3,
        5,
        6,
        7
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "login attempt with username",
            "done": true
          },
          {
            "label": "loadUserByUsername called",
            "done": true
          },
          {
            "label": "fetch from DB → return UserDetails",
            "active": true
          },
          {
            "label": "authenticate: compare passwords"
          }
        ]
      }
    },
    {
      "title": "Method security",
      "description": "@PreAuthorize / @Secured on methods check permissions before the method runs. Requires @EnableMethodSecurity.",
      "code": "@PreAuthorize(\"hasRole('ADMIN')\")\npublic void deleteUser(Long id) { ... }\n\n@PreAuthorize(\"hasRole('USER') and #id == authentication.principal.id\")\npublic User getMyProfile(Long id) { ... }",
      "highlight": [
        1,
        4
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Method security",
        "items": [
          {
            "label": "@PreAuthorize",
            "value": "SpEL expression before call",
            "color": "#10b981",
            "highlight": true
          },
          {
            "label": "@PostAuthorize",
            "value": "SpEL expression after call",
            "color": "#818cf8"
          },
          {
            "label": "@Secured",
            "value": "role check",
            "color": "#818cf8"
          }
        ]
      }
    },
    {
      "title": "CORS configuration",
      "description": "Cross-Origin Resource Sharing: your API must explicitly allow browser requests from a different origin.",
      "code": "http.cors(c -> c.configurationSource(request -> {\n    var cfg = new CorsConfiguration();\n    cfg.setAllowedOrigins(List.of(\"https://myapp.com\"));\n    cfg.setAllowedMethods(List.of(\"GET\",\"POST\",\"PUT\",\"DELETE\"));\n    return cfg;\n}))",
      "highlight": [
        3,
        4
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Browser sends CORS preflight OPTIONS",
            "done": true
          },
          {
            "label": "Spring checks allowedOrigins",
            "active": true
          },
          {
            "label": "allowed → proceed OR 403 Forbidden"
          }
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner (The SecurityFilterChain Bean)
In Spring Security 6 (Spring Boot 3.x), you configure security by defining a `SecurityFilterChain` bean. The old `WebSecurityConfigurerAdapter` class has been removed.

```java
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.config.annotation.web.configuration.EnableWebSecurity;
import org.springframework.security.web.SecurityFilterChain;

@Configuration
@EnableWebSecurity
public class SecurityConfig {

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            // Disable CSRF for stateless REST APIs (we'll use JWT instead)
            .csrf(csrf -> csrf.disable())
            
            // Define Authorization Rules
            .authorizeHttpRequests(auth -> auth
                // Public endpoints — no authentication required
                .requestMatchers("/api/auth/**", "/api/public/**").permitAll()
                
                // Only users with ROLE_ADMIN can access /admin/**
                .requestMatchers("/api/admin/**").hasRole("ADMIN")
                
                // All other endpoints require authentication
                .anyRequest().authenticated()
            );

        return http.build();
    }
}
```

### Level 2 — Building Blocks (UserDetailsService)
Spring Security needs to load user details from your database to verify the provided credentials. You implement the `UserDetailsService` interface to provide this.

```java
import org.springframework.security.core.userdetails.*;
import org.springframework.stereotype.Service;

@Service
public class CustomUserDetailsService implements UserDetailsService {

    private final UserRepository userRepository;

    @Override
    public UserDetails loadUserByUsername(String username) throws UsernameNotFoundException {
        // Find the user in your database
        User user = userRepository.findByUsername(username)
                .orElseThrow(() -> new UsernameNotFoundException("User not found: " + username));

        // Wrap it in Spring Security's UserDetails format
        return org.springframework.security.core.userdetails.User.builder()
                .username(user.getUsername())
                .password(user.getPasswordHash()) // Must be BCrypt encoded!
                .roles(user.getRole())            // e.g., "ADMIN", "USER"
                .build();
    }
}
```

### Level 3 — Intermediate (Password Encoding)
**NEVER store plain-text passwords in your database.** Always use BCrypt, a one-way adaptive hashing algorithm. Spring Security provides a built-in `BCryptPasswordEncoder`.

```java
@Configuration
public class SecurityConfig {

    // Define BCryptPasswordEncoder as a Bean so it can be injected anywhere
    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder(12); // Work factor: 12 (higher = slower = safer)
    }
}

@Service
public class AuthService {
    
    private final PasswordEncoder passwordEncoder;

    public void registerUser(String rawPassword) {
        // Hash the password before saving to DB
        String hash = passwordEncoder.encode(rawPassword); // "$2a$12$abc123..."
        userRepository.save(new User(hash));
    }

    public boolean verifyPassword(String rawPassword, String storedHash) {
        // BCrypt compares the raw password against the stored hash
        return passwordEncoder.matches(rawPassword, storedHash);
    }
}
```

### Level 4 — Advanced (Method-Level Security with @PreAuthorize)
Instead of configuring URL patterns in `SecurityFilterChain`, you can secure individual methods using annotations. This is much more granular.

```java
// Enable this in your Security config class
@EnableMethodSecurity

// In your Service or Controller:
@Service
public class AdminService {

    // Only executes if the logged-in user has the ROLE_ADMIN role
    @PreAuthorize("hasRole('ADMIN')")
    public void deleteAllUsers() {
        userRepository.deleteAll();
    }

    // Only executes if the user is accessing their OWN resource
    // #userId is a reference to the method parameter!
    @PreAuthorize("hasRole('ADMIN') or #userId == authentication.principal.id")
    public UserDto getUser(Long userId) {
        return userRepository.findById(userId).map(UserDto::from).orElseThrow();
    }
}
```

## REAL_WORLD
In modern REST APIs, the standard authentication mechanism is **JWT (JSON Web Tokens)**, not session-based authentication. With JWTs, the server is completely stateless: it doesn't store any user sessions in memory or in a database. Instead, it issues a signed token to the client after login. On every subsequent request, the client sends the token in the `Authorization: Bearer <token>` header. The server cryptographically verifies the token signature to confirm the user's identity.

Combining Spring Security with a JWT library (like `java-jwt` from Auth0 or `nimbus-jose-jwt`) is the industry standard setup for modern microservice architectures.

## INTERVIEW
**Q: What is the difference between `permitAll()` and `anonymous()`?**
A: `permitAll()` allows ALL users — both authenticated and anonymous. `anonymous()` allows ONLY unauthenticated (anonymous) users. So a logged-in user would be denied by `anonymous()`.

**Q: Why is CSRF protection disabled for REST APIs?**
A: CSRF attacks exploit browser cookies to make unauthorized requests. Since REST APIs use `Authorization: Bearer <JWT>` headers instead of cookies, the browser can never automatically attach the token to malicious cross-site requests. Therefore, CSRF protection is irrelevant for stateless JWT-based APIs.

**Q: What is `@Secured` vs `@PreAuthorize`?**
A: `@Secured("ROLE_ADMIN")` is the older, simpler annotation. It only supports role checks. `@PreAuthorize("hasRole('ADMIN') or #id == principal.id")` is the modern annotation that supports full SpEL (Spring Expression Language) expressions, enabling much more complex access control rules.

## FEYNMAN CHECK

### Explain Spring Security Basics Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Spring Security Basics** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Spring Security Basics, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Spring Security Basics solve? Why would JavaScript/web development be harder without it?**
> **A:** Spring Security Basics solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Spring Security Basics?**
> **A:** The single most important thing is understanding the *mental model*: Spring Security Basics operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Spring Security Basics and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Spring Security Basics operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Spring Security Basics interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Spring Security Basics interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Spring Security Basics to a senior engineer in two sentences, what would you say?**
> **A:** "Spring Security Basics works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Spring Security Basics Utility

**Goal:** Implement a real, working Spring Security Basics module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `springsecuritybasics` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir spring-security-basics-demo && cd spring-security-basics-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Spring Security Basics Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Spring Security Basics.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createSpringSecurityBasics(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Spring Security Basics] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Spring Security Basics logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Spring Security Basics] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Spring Security Basics] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createSpringSecurityBasics({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Spring Security Basics] Input cannot be null or undefined`);
    }
    const instance = createSpringSecurityBasics();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Spring Security Basics] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Spring Security Basics Tests ──');

test('initialises correctly', () => {
  const m = createSpringSecurityBasics();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createSpringSecurityBasics();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Spring Security Basics] Initialized with: hello world
[Spring Security Basics] Processed: hello world
Result: hello world
[Spring Security Basics] Destroyed.

── Spring Security Basics Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Spring Security Basics in one sentence.**
<details><summary>Answer</summary>

Spring Security Basics is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Spring Security Basics?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Spring Security Basics.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Spring Security Basics and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Spring Security Basics | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Spring Security Basics] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Spring Security Basics causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Spring Security Basics usage].
</details>

**Q6: What does Spring Security Basics look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Spring Security Basics typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Spring Security Basics correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Spring Security Basics:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_spring_security_basics(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Spring Security Basics in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Spring Security Basics in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Spring Security Basics', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Spring Security Basics at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Spring Security Basics uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Spring Security Basics interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Spring Security Basics is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Spring Security Basics relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Spring Security Basics
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Spring Security Basics to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Spring Security Basics fits in the data/control flow.
2. **Choose** the right variant/pattern of Spring Security Basics for the scale requirement.
3. **Handle** failure modes: what happens when Spring Security Basics is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Spring Security Basics is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
