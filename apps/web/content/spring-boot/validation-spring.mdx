---
slug: "validation-spring"
title: "Validation in Spring Boot"
level: 3
---

## WHY
Never trust client input.
If you build a registration API, a malicious (or confused) user might send a `POST` request with an empty string for their password, a negative number for their age, or "not-an-email" for their email address.
If you don't validate this data, it will crash your database or corrupt your application's logic.
You *could* write massive blocks of `if/else` statements in your Controller: `if (user.getAge() < 0) { return error; }`. But this is tedious, ugly, and hard to maintain.
**Spring Boot Validation (Jakarta Bean Validation)** solves this elegantly. It allows you to place simple Annotations (like `@NotNull` or `@Email`) directly onto your Java Classes. Spring intercepts the incoming HTTP JSON payload, automatically runs all the validation rules, and instantly rejects the request with a `400 Bad Request` if any rules are broken, before the code ever reaches your database!

## THEORY
### Jakarta Bean Validation
Spring Boot doesn't actually write the validation logic itself; it uses the standard **Jakarta Bean Validation API** (formerly Java EE), usually implemented by the **Hibernate Validator** engine under the hood.

### The Two-Step Process
To make validation work, you must do two things:
1. **Define the Rules:** Place validation annotations (e.g., `@NotBlank`, `@Min`) on the fields of your DTO (Data Transfer Object).
2. **Trigger the Rules:** Place the `@Valid` annotation on the `@RequestBody` parameter in your Controller. If you forget `@Valid`, Spring will completely ignore the rules!

## VISUALIZATION_CONFIG
```json
{
  "language": "java",
  "fileName": "Validation.java",
  "steps": [
    {
      "title": "Bean Validation annotations",
      "description": "Annotate DTO fields with @NotNull, @NotBlank, @Size, @Email, @Min, @Max, @Pattern. Trigger with @Valid on the @RequestBody parameter.",
      "code": "public record CreateUserRequest(\n    @NotBlank @Size(min=2, max=50) String name,\n    @Email @NotBlank               String email,\n    @Size(min=8, max=100)          String password,\n    @Min(0) @Max(120)              int age\n) {}\n\n@PostMapping\npublic User create(@RequestBody @Valid CreateUserRequest req) { ... }",
      "highlight": [
        2,
        3,
        4,
        5,
        8
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Bean Validation annotations",
        "items": [
          {
            "label": "@NotNull / @NotBlank",
            "color": "#10b981",
            "highlight": true
          },
          {
            "label": "@Size(min,max)",
            "color": "#10b981"
          },
          {
            "label": "@Email",
            "color": "#818cf8"
          },
          {
            "label": "@Min / @Max",
            "color": "#818cf8"
          },
          {
            "label": "@Pattern(regexp)",
            "color": "#818cf8"
          }
        ]
      }
    },
    {
      "title": "@Validated for service-layer validation",
      "description": "@Valid works on @RequestBody. Use @Validated on a Spring bean to validate method parameters or return values anywhere.",
      "code": "@Service @Validated\nclass TransferService {\n    void transfer(\n        @Positive Long fromId,\n        @Positive Long toId,\n        @DecimalMin(\"0.01\") BigDecimal amount\n    ) { ... }\n}",
      "highlight": [
        1,
        3,
        4,
        5,
        6
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "transfer(-1, 2, 10) called",
            "done": true
          },
          {
            "label": "@Positive on fromId: -1 is invalid",
            "active": true
          },
          {
            "label": "ConstraintViolationException thrown"
          }
        ]
      }
    },
    {
      "title": "Custom constraint annotation",
      "description": "Create a custom constraint when built-in annotations aren't enough.",
      "code": "@Documented\n@Constraint(validatedBy = UniquEmailValidator.class)\n@Target(ElementType.FIELD)\n@Retention(RetentionPolicy.RUNTIME)\npublic @interface UniqueEmail {\n    String message() default \"Email already registered\";\n    Class<?>[] groups() default {};\n    Class<? extends Payload>[] payload() default {};\n}",
      "highlight": [
        1,
        2,
        5
      ],
      "diagram": {
        "kind": "sequence",
        "actors": [
          "@UniqueEmail",
          "UniqueEmailValidator",
          "UserRepository"
        ],
        "messages": [
          {
            "from": "@UniqueEmail",
            "to": "UniqueEmailValidator",
            "label": "isValid(email, ctx)"
          },
          {
            "from": "UniqueEmailValidator",
            "to": "UserRepository",
            "label": "existsByEmail(email)"
          },
          {
            "from": "UserRepository",
            "to": "UniqueEmailValidator",
            "label": "false (unique) → valid"
          }
        ]
      }
    },
    {
      "title": "Validation groups — conditional validation",
      "description": "Groups let you apply different validation rules for create vs update scenarios.",
      "code": "interface OnCreate {}\ninterface OnUpdate {}\n\npublic record UserRequest(\n    @NotNull(groups=OnCreate.class) String password,  // only on create\n    @NotNull(groups={OnCreate.class, OnUpdate.class}) String name\n) {}\n\n@PostMapping @Validated(OnCreate.class)\npublic User create(@RequestBody UserRequest req) {}",
      "highlight": [
        5,
        6,
        9
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Validation groups",
        "items": [
          {
            "label": "OnCreate: password required",
            "color": "#10b981",
            "highlight": true
          },
          {
            "label": "OnUpdate: password optional",
            "color": "#818cf8"
          },
          {
            "label": "@Validated(group) selects rules",
            "color": "#818cf8"
          }
        ]
      }
    },
    {
      "title": "Error message customization",
      "description": "Override messages in ValidationMessages.properties or per-annotation.",
      "code": "# ValidationMessages.properties\njakarta.validation.constraints.NotBlank.message=Field is required\njakarta.validation.constraints.Size.message=Must be between {min} and {max} characters\njakarta.validation.constraints.Email.message=Enter a valid email address",
      "language": "properties",
      "highlight": [
        2,
        3,
        4
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Message sources",
        "items": [
          {
            "label": "annotation message=\"\"",
            "color": "#818cf8",
            "value": "highest priority"
          },
          {
            "label": "ValidationMessages.properties",
            "color": "#10b981",
            "highlight": true
          },
          {
            "label": "default (built-in messages)",
            "color": "#818cf8",
            "value": "lowest"
          }
        ]
      }
    }
  ]
}
```

## CODE

To use validation, you must ensure the dependency is in your `pom.xml` (it is not included by default in `spring-boot-starter-web` anymore!).
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-validation</artifactId>
</dependency>
```

### 1. Defining the Rules (The DTO)
We define a plain Java class that represents the expected JSON payload, and we decorate it with rules!

```java
import jakarta.validation.constraints.*;

public class UserRegistrationDto {

    // Cannot be null, and cannot be an empty string ("") or just spaces ("   ")
    @NotBlank(message = "Username is absolutely required!")
    @Size(min = 3, max = 20, message = "Username must be between 3 and 20 characters.")
    private String username;

    // Automatically checks if the string contains an '@' and a valid domain format!
    @NotBlank(message = "Email is required")
    @Email(message = "Please provide a valid email address.")
    private String email;

    @NotNull(message = "Age cannot be null")
    @Min(value = 18, message = "You must be at least 18 years old to register.")
    @Max(value = 120, message = "Age must be realistic.")
    private Integer age;

    // Getters and Setters omitted...
}
```

### 2. Triggering the Rules (The Controller)
We use the `@Valid` annotation to tell Spring: *"Hey, before you execute this method, check the JSON against the rules!"*

```java
@RestController
@RequestMapping("/api/users")
public class UserController {

    @PostMapping("/register")
    public ResponseEntity<String> registerUser(
        // CRITICAL: The @Valid annotation triggers the Hibernate Validator!
        @Valid @RequestBody UserRegistrationDto dto
    ) {
        
        // If the code reaches this line, it is MATHEMATICALLY GUARANTEED 
        // that the username is not blank, the email is valid, and the age >= 18!
        // We do not need a single if/else statement!
        
        System.out.println("Registering user: " + dto.getUsername());
        return ResponseEntity.ok("Registration successful!");
    }
}
```

## REAL_WORLD
**Custom Validation Annotations**
Sometimes the built-in annotations (`@Email`, `@Min`, `@Size`) aren't enough. 
Imagine you are building a banking app, and you need to validate that an employee ID strictly starts with the letters "EMP-" followed by exactly 4 digits.
You can create your very own custom validation annotations!
1. You create a custom Java interface `@interface ValidEmployeeId`.
2. You create a Validator class `EmployeeIdValidator implements ConstraintValidator<ValidEmployeeId, String>` containing an `isValid` method where you write custom Regex logic.
3. You can now use your custom `@ValidEmployeeId` annotation on any field across your entire enterprise application, ensuring perfectly reusable, DRY (Don't Repeat Yourself) business logic!

## INTERVIEW
**Q: You annotate your DTO with `@NotBlank`, and your Controller parameter with `@Valid`. The frontend sends an invalid JSON payload with an age of 12. Spring automatically blocks the request and throws a `MethodArgumentNotValidException`. However, the user's browser receives a massive, ugly, 500-line Java stack trace. How do you intercept this exception and send a clean JSON object containing exactly which fields failed validation?**
A: **By using an `@ExceptionHandler` inside an `@ControllerAdvice` class.**
When validation fails, Spring throws an exception before the Controller method even executes. To intercept this, you must set up Global Exception Handling.
You create a class annotated with `@ControllerAdvice` and define a method annotated with `@ExceptionHandler(MethodArgumentNotValidException.class)`. 
Inside this method, you can catch the exception, extract the `BindingResult` (which contains a list of every single field that failed and its associated error message), map those errors into a clean Java `Map<String, String>`, and return that Map wrapped in a `ResponseEntity.badRequest()`. This translates the ugly Java stack trace into a beautiful, machine-readable JSON error object for the frontend!

## FEYNMAN CHECK

### Explain Validation in Spring Boot Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Validation in Spring Boot** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Validation in Spring Boot, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Validation in Spring Boot solve? Why would JavaScript/web development be harder without it?**
> **A:** Validation in Spring Boot solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Validation in Spring Boot?**
> **A:** The single most important thing is understanding the *mental model*: Validation in Spring Boot operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Validation in Spring Boot and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Validation in Spring Boot operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Validation in Spring Boot interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Validation in Spring Boot interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Validation in Spring Boot to a senior engineer in two sentences, what would you say?**
> **A:** "Validation in Spring Boot works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Validation in Spring Boot Utility

**Goal:** Implement a real, working Validation in Spring Boot module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `validationspring` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir validation-spring-demo && cd validation-spring-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Validation in Spring Boot Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Validation in Spring Boot.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createValidationinSpringBoot(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Validation in Spring Boot] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Validation in Spring Boot logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Validation in Spring Boot] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Validation in Spring Boot] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createValidationinSpringBoot({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Validation in Spring Boot] Input cannot be null or undefined`);
    }
    const instance = createValidationinSpringBoot();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Validation in Spring Boot] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Validation in Spring Boot Tests ──');

test('initialises correctly', () => {
  const m = createValidationinSpringBoot();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createValidationinSpringBoot();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Validation in Spring Boot] Initialized with: hello world
[Validation in Spring Boot] Processed: hello world
Result: hello world
[Validation in Spring Boot] Destroyed.

── Validation in Spring Boot Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Validation in Spring Boot in one sentence.**
<details><summary>Answer</summary>

Validation in Spring Boot is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Validation in Spring Boot?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Validation in Spring Boot.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Validation in Spring Boot and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Validation in Spring Boot | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Validation in Spring Boot] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Validation in Spring Boot causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Validation in Spring Boot usage].
</details>

**Q6: What does Validation in Spring Boot look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Validation in Spring Boot typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Validation in Spring Boot correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Validation in Spring Boot:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_validation_spring(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Validation in Spring Boot in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Validation in Spring Boot in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Validation in Spring Boot', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Validation in Spring Boot at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Validation in Spring Boot uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Validation in Spring Boot interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Validation in Spring Boot is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Validation in Spring Boot relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Validation in Spring Boot
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Validation in Spring Boot to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Validation in Spring Boot fits in the data/control flow.
2. **Choose** the right variant/pattern of Validation in Spring Boot for the scale requirement.
3. **Handle** failure modes: what happens when Validation in Spring Boot is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Validation in Spring Boot is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
