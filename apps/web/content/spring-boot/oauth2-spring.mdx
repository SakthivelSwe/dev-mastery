---
slug: "oauth2-spring"
title: "OAuth2 and OIDC in Spring Security"
level: 4
---

## WHY
In the "JWT Authentication" module, you learned how to verify a username and password against a database and issue a token.
But modern users hate creating new accounts. They want to click **"Log in with Google"** or **"Log in with GitHub"**.
Furthermore, in Enterprise environments, hundreds of internal microservices shouldn't each manage their own user databases. They should delegate authentication to a centralized Identity Provider (IdP) like Okta, Auth0, or Keycloak.
**OAuth2** and **OpenID Connect (OIDC)** are the industry standard protocols for delegated authorization and authentication. Spring Security provides world-class, automated support for these protocols, allowing you to integrate Google Login into your application with literally zero Java code!

## THEORY
### OAuth2 vs OpenID Connect (OIDC)
These two protocols are often confused, but they serve entirely different purposes:
1. **OAuth2 is for AUTHORIZATION:** It allows an application to access a user's data on another server. (e.g., "Allow MyApp to read your Google Calendar"). It does *not* technically prove the user's identity.
2. **OIDC is for AUTHENTICATION:** It is built *on top* of OAuth2. It provides a standardized JWT called an **ID Token** that mathematically proves: *"This user is Alice, her email is alice@example.com, and Google verified her identity at 2:00 PM."*

### The OAuth2 Dance (Authorization Code Flow)
1. User clicks "Login with Google".
2. Spring redirects the browser to `accounts.google.com`.
3. User logs into Google and clicks "Approve".
4. Google redirects the browser back to Spring with a temporary, one-time `code`.
5. Spring takes this `code`, secretly contacts Google backend-to-backend, and exchanges the code for the final Access/ID Tokens!

## VISUALIZATION_CONFIG
```json
{
  "language": "java",
  "fileName": "OAuth2Config.java",
  "steps": [
    {
      "title": "OAuth2 roles",
      "description": "Resource Owner (user), Client (your app), Authorization Server (Google/GitHub/Okta), Resource Server (your API).",
      "code": "// Authorization Code flow:\n// 1. User clicks \"Login with Google\"\n// 2. Redirect to Google /authorize?client_id=...&redirect_uri=...\n// 3. User grants → Google redirects back with ?code=...\n// 4. Your server exchanges code for access_token + id_token\n// 5. Your server validates id_token → create session",
      "diagram": {
        "kind": "sequence",
        "actors": [
          "User",
          "App",
          "Google"
        ],
        "messages": [
          {
            "from": "User",
            "to": "App",
            "label": "Login with Google"
          },
          {
            "from": "App",
            "to": "Google",
            "label": "redirect: /authorize?client_id=..."
          },
          {
            "from": "Google",
            "to": "App",
            "label": "callback: ?code=xyz"
          },
          {
            "from": "App",
            "to": "Google",
            "label": "POST /token code=xyz"
          },
          {
            "from": "Google",
            "to": "App",
            "label": "access_token + id_token"
          }
        ]
      }
    },
    {
      "title": "Spring Security OAuth2 Client",
      "description": "Add spring-boot-starter-oauth2-client. Configure provider in application.yml. Login button is auto-configured.",
      "code": "# application.yml\nspring:\n  security:\n    oauth2:\n      client:\n        registration:\n          google:\n            client-id: ${GOOGLE_CLIENT_ID}\n            client-secret: ${GOOGLE_CLIENT_SECRET}\n            scope: openid, profile, email",
      "language": "yaml",
      "highlight": [
        6,
        7,
        8,
        9,
        10
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "user visits /oauth2/authorization/google",
            "done": true
          },
          {
            "label": "redirect to Google consent screen",
            "done": true
          },
          {
            "label": "callback: /login/oauth2/code/google",
            "active": true
          },
          {
            "label": "Spring exchanges code, creates auth, session"
          }
        ]
      }
    },
    {
      "title": "Resource Server — validate JWT bearer tokens",
      "description": "Add spring-boot-starter-oauth2-resource-server. Configure JWT issuer URI. Requests with valid JWT pass the security filter.",
      "code": "# application.yml\nspring:\n  security:\n    oauth2:\n      resourceserver:\n        jwt:\n          issuer-uri: https://accounts.google.com\n          # Spring fetches JWKS from issuer to validate tokens",
      "language": "yaml",
      "highlight": [
        5,
        6,
        7,
        8
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Bearer token received",
            "done": true
          },
          {
            "label": "validate signature using issuer JWKS",
            "done": true
          },
          {
            "label": "check exp, iss, aud claims",
            "active": true
          },
          {
            "label": "valid → set SecurityContext → proceed"
          }
        ]
      }
    },
    {
      "title": "Custom OAuth2UserService",
      "description": "Implement DefaultOAuth2UserService to enrich the OAuth2User with app-specific data (roles, preferences from your DB).",
      "code": "@Service\npublic class CustomOAuth2UserService extends DefaultOAuth2UserService {\n    @Override\n    public OAuth2User loadUser(OAuth2UserRequest req) {\n        OAuth2User googleUser = super.loadUser(req);\n        String email = googleUser.getAttribute(\"email\");\n        User appUser = userRepo.findByEmailOrCreate(email);  // upsert\n        return new CustomOAuth2User(googleUser, appUser.roles());\n    }\n}",
      "highlight": [
        4,
        5,
        6,
        7,
        8
      ],
      "diagram": {
        "kind": "sequence",
        "actors": [
          "Spring",
          "CustomOAuth2UserService",
          "UserRepository"
        ],
        "messages": [
          {
            "from": "Spring",
            "to": "CustomOAuth2UserService",
            "label": "loadUser(request)"
          },
          {
            "from": "CustomOAuth2UserService",
            "to": "UserRepository",
            "label": "findByEmailOrCreate(email)"
          },
          {
            "from": "UserRepository",
            "to": "CustomOAuth2UserService",
            "label": "User with roles"
          }
        ]
      }
    },
    {
      "title": "Access the current user",
      "description": "Get the authenticated principal in controller methods via @AuthenticationPrincipal.",
      "code": "@GetMapping(\"/profile\")\npublic Profile profile(\n    @AuthenticationPrincipal OAuth2User principal) {\n    String email = principal.getAttribute(\"email\");\n    return profileService.getByEmail(email);\n}",
      "highlight": [
        3,
        4
      ],
      "diagram": {
        "kind": "boxes",
        "title": "@AuthenticationPrincipal",
        "items": [
          {
            "label": "OAuth2User (Google login)",
            "color": "#818cf8"
          },
          {
            "label": "UserDetails (form login)",
            "color": "#818cf8"
          },
          {
            "label": "JWT claims map",
            "color": "#818cf8"
          },
          {
            "label": "@AuthenticationPrincipal injects all",
            "color": "#10b981",
            "highlight": true
          }
        ]
      }
    }
  ]
}
```

## CODE

Spring Security makes OIDC integration shockingly easy.

### 1. The Dependencies
You need the OAuth2 Client starter.

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-oauth2-client</artifactId>
</dependency>
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-security</artifactId>
</dependency>
```

### 2. The Configuration (Zero Java Code)
To enable "Login with Google" or "Login with GitHub", Spring Boot relies heavily on auto-configuration. You just need to provide your exact Client ID and Secret in the `application.yml` file.

```yaml
spring:
  security:
    oauth2:
      client:
        registration:
          # Spring has built-in defaults for Google, GitHub, Facebook, and Okta!
          google:
            client-id: "YOUR_GOOGLE_CLIENT_ID"
            client-secret: "YOUR_GOOGLE_CLIENT_SECRET"
            scope: openid, profile, email
          github:
            client-id: "YOUR_GITHUB_CLIENT_ID"
            client-secret: "YOUR_GITHUB_CLIENT_SECRET"
```

### 3. The Security Filter Chain
You must tell Spring Security to actually use the OAuth2 Login system.

```java
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.web.SecurityFilterChain;

@Configuration
public class SecurityConfig {

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/", "/public/**").permitAll() // Public pages
                .anyRequest().authenticated() // Everything else requires login
            )
            // The Magic Line! Enables the OAuth2 Dance!
            // If an unauthenticated user hits a protected page, 
            // Spring will auto-generate a login page with buttons for Google/GitHub!
            .oauth2Login(oauth2 -> oauth2
                .defaultSuccessUrl("/dashboard")
            );

        return http.build();
    }
}
```

### 4. Accessing the Logged-In User Data
Once Google redirects back to your app, Spring automatically parses the JWT ID Token, extracts the user's name and email, and stores it in the `SecurityContext`.

```java
import org.springframework.security.core.annotation.AuthenticationPrincipal;
import org.springframework.security.oauth2.core.user.OAuth2User;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class UserController {

    @GetMapping("/api/me")
    public String getProfile(
        // Inject the magic OIDC User object directly into your controller!
        @AuthenticationPrincipal OAuth2User principal
    ) {
        // The principal contains all the data Google sent us!
        String name = principal.getAttribute("name");
        String email = principal.getAttribute("email");
        
        return "Hello, " + name + "! Your Google email is: " + email;
    }
}
```

## REAL_WORLD
**OAuth2 Resource Server (Microservices)**
The code above is for an OAuth2 **Client** (an application with a frontend that redirects users to Google).
But what if you are building a backend Microservice architecture? You have a dedicated Authentication Server (like Keycloak), and 5 distinct Spring Boot APIs.
The APIs don't redirect users. They just receive HTTP requests containing an `Authorization: Bearer <token>` header from an Angular/React frontend.
In this case, your Spring Boot app acts as an **OAuth2 Resource Server**.
You change the dependency to `spring-boot-starter-oauth2-resource-server`.
In `application.yml`, you provide the URL of your Keycloak server:
`spring.security.oauth2.resourceserver.jwt.issuer-uri=https://my-keycloak.com/realms/myapp`
When a request arrives, Spring automatically downloads the public cryptographic keys from Keycloak, mathematically verifies the signature on the incoming JWT, and grants or denies access instantly!

## INTERVIEW
**Q: You implement "Login with Google" using Spring Security. It works perfectly. However, your application has a strict authorization model based on roles (e.g., `ROLE_ADMIN`, `ROLE_USER`). You have a method annotated with `@PreAuthorize("hasRole('ADMIN')")`. How do you map the generic data coming from Google into the specific Spring Security `GrantedAuthority` roles your application requires?**
A: **By implementing a custom `OAuth2UserService` (or mapping the authorities via the `GrantedAuthoritiesMapper`).**
By default, when a user logs in via Google, Spring Security grants them a single, generic authority called `OAUTH2_USER` (or `SCOPE_email`). It has no idea who is an "Admin" in your specific business logic.
To fix this, you must intercept the authentication flow right after Google returns the user data but before Spring saves it to the SecurityContext.
You implement the `OAuth2UserService<OidcUserRequest, OidcUser>` interface. Inside the `loadUser` method, you take the email address Google provided (`alice@example.com`), execute a database query to check your local `users` table to see if Alice is an Admin, and then manually attach the `SimpleGrantedAuthority("ROLE_ADMIN")` to the final `OidcUser` object returned to Spring Security. Now your `@PreAuthorize` annotations will work perfectly with external Google users!

## FEYNMAN CHECK

### Explain OAuth2 and OIDC in Spring Security Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **OAuth2 and OIDC in Spring Security** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about OAuth2 and OIDC in Spring Security, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does OAuth2 and OIDC in Spring Security solve? Why would JavaScript/web development be harder without it?**
> **A:** OAuth2 and OIDC in Spring Security solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about OAuth2 and OIDC in Spring Security?**
> **A:** The single most important thing is understanding the *mental model*: OAuth2 and OIDC in Spring Security operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with OAuth2 and OIDC in Spring Security and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which OAuth2 and OIDC in Spring Security operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does OAuth2 and OIDC in Spring Security interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** OAuth2 and OIDC in Spring Security interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of OAuth2 and OIDC in Spring Security to a senior engineer in two sentences, what would you say?**
> **A:** "OAuth2 and OIDC in Spring Security works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a OAuth2 and OIDC in Spring Security Utility

**Goal:** Implement a real, working OAuth2 and OIDC in Spring Security module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `oauth2spring` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir oauth2-spring-demo && cd oauth2-spring-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── OAuth2 and OIDC in Spring Security Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of OAuth2 and OIDC in Spring Security.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createOAuth2andOIDCinSpringSecurity(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[OAuth2 and OIDC in Spring Security] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual OAuth2 and OIDC in Spring Security logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[OAuth2 and OIDC in Spring Security] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[OAuth2 and OIDC in Spring Security] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createOAuth2andOIDCinSpringSecurity({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[OAuth2 and OIDC in Spring Security] Input cannot be null or undefined`);
    }
    const instance = createOAuth2andOIDCinSpringSecurity();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[OAuth2 and OIDC in Spring Security] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── OAuth2 and OIDC in Spring Security Tests ──');

test('initialises correctly', () => {
  const m = createOAuth2andOIDCinSpringSecurity();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createOAuth2andOIDCinSpringSecurity();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[OAuth2 and OIDC in Spring Security] Initialized with: hello world
[OAuth2 and OIDC in Spring Security] Processed: hello world
Result: hello world
[OAuth2 and OIDC in Spring Security] Destroyed.

── OAuth2 and OIDC in Spring Security Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define OAuth2 and OIDC in Spring Security in one sentence.**
<details><summary>Answer</summary>

OAuth2 and OIDC in Spring Security is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of OAuth2 and OIDC in Spring Security?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of OAuth2 and OIDC in Spring Security.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between OAuth2 and OIDC in Spring Security and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | OAuth2 and OIDC in Spring Security | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of OAuth2 and OIDC in Spring Security] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing OAuth2 and OIDC in Spring Security causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct OAuth2 and OIDC in Spring Security usage].
</details>

**Q6: What does OAuth2 and OIDC in Spring Security look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, OAuth2 and OIDC in Spring Security typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use OAuth2 and OIDC in Spring Security correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using OAuth2 and OIDC in Spring Security:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_oauth2_spring(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test OAuth2 and OIDC in Spring Security in isolation? What would you mock?**
<details><summary>Answer</summary>

To test OAuth2 and OIDC in Spring Security in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('OAuth2 and OIDC in Spring Security', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of OAuth2 and OIDC in Spring Security at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how OAuth2 and OIDC in Spring Security uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common OAuth2 and OIDC in Spring Security interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "OAuth2 and OIDC in Spring Security is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does OAuth2 and OIDC in Spring Security relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
OAuth2 and OIDC in Spring Security
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use OAuth2 and OIDC in Spring Security to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where OAuth2 and OIDC in Spring Security fits in the data/control flow.
2. **Choose** the right variant/pattern of OAuth2 and OIDC in Spring Security for the scale requirement.
3. **Handle** failure modes: what happens when OAuth2 and OIDC in Spring Security is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you OAuth2 and OIDC in Spring Security is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
