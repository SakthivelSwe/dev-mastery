---
slug: "spring-actuator"
title: "Spring Boot Actuator"
level: 5
---

## WHY
Deploying an application to production is not the end of your job. You then need to continuously monitor: Is the app healthy? Is the database connection pool exhausted? Is the memory about to run out? How many HTTP requests are being served per second?

**Spring Boot Actuator** is a production-ready sub-system built into Spring Boot that exposes all of this diagnostic information via HTTP endpoints, completely automatically, with zero custom code.

## THEORY
### The Actuator Endpoint System
Actuator endpoints are automatically generated and registered at application startup. Key endpoints include:

- `/actuator/health`: Reports the application's health status (UP/DOWN).
- `/actuator/info`: Shows application info (version, git commit).
- `/actuator/metrics`: Exposes hundreds of JVM and application metrics.
- `/actuator/env`: Shows all environment variables and config properties.
- `/actuator/loggers`: Lets you change log levels at runtime WITHOUT a restart!
- `/actuator/httptrace`: Shows the last 100 HTTP requests/responses.

## VISUALIZATION_CONFIG
```json
{
  "language": "yaml",
  "fileName": "application.yml",
  "steps": [
    {
      "title": "What is Actuator?",
      "description": "Spring Boot Actuator adds production-ready endpoints: /health, /metrics, /env, /info, /loggers. Zero code — just add the dependency.",
      "code": "# pom.xml\nspring-boot-starter-actuator\n\n# application.yml\nmanagement:\n  endpoints:\n    web:\n      exposure:\n        include: health,metrics,info,loggers",
      "diagram": {
        "kind": "boxes",
        "title": "Key actuator endpoints",
        "items": [
          {
            "label": "/actuator/health",
            "color": "#10b981",
            "highlight": true
          },
          {
            "label": "/actuator/metrics",
            "color": "#818cf8"
          },
          {
            "label": "/actuator/info",
            "color": "#818cf8"
          },
          {
            "label": "/actuator/loggers",
            "color": "#f59e0b"
          },
          {
            "label": "/actuator/env",
            "color": "#818cf8"
          }
        ]
      }
    },
    {
      "title": "/health endpoint",
      "description": "/actuator/health aggregates the health of all registered HealthIndicators (DB, Redis, disk space, custom). Returns UP or DOWN.",
      "code": "# application.yml\nmanagement:\n  endpoint:\n    health:\n      show-details: always\n# Returns:\n# { \"status\":\"UP\", \"components\":{\"db\":{\"status\":\"UP\"},...} }",
      "language": "yaml",
      "highlight": [
        5,
        7
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Health checks",
        "items": [
          {
            "label": "DataSourceHealthIndicator (DB)",
            "color": "#10b981",
            "highlight": true
          },
          {
            "label": "DiskSpaceHealthIndicator",
            "color": "#10b981"
          },
          {
            "label": "Custom HealthIndicator",
            "color": "#818cf8"
          },
          {
            "label": "Overall: UP if all UP",
            "color": "#10b981"
          }
        ]
      }
    },
    {
      "title": "/metrics with Micrometer",
      "description": "Micrometer is the metrics facade. Backed by Prometheus, Datadog, Graphite, etc. Counters, gauges, timers are auto-created by Spring Boot.",
      "code": "// Access via HTTP\n# GET /actuator/metrics/http.server.requests\n// Micrometer API for custom metrics\n@Autowired MeterRegistry registry;\nregistry.counter(\"orders.created\").increment();\nregistry.timer(\"orders.latency\").record(elapsed, MILLISECONDS);",
      "language": "java",
      "highlight": [
        5,
        6
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Micrometer metrics",
        "items": [
          {
            "label": "Counter",
            "color": "#818cf8"
          },
          {
            "label": "Gauge",
            "color": "#818cf8"
          },
          {
            "label": "Timer",
            "color": "#10b981",
            "highlight": true
          },
          {
            "label": "→ Prometheus / Grafana",
            "color": "#f59e0b"
          }
        ]
      }
    },
    {
      "title": "Custom health indicator",
      "description": "Implement HealthIndicator to add your own checks — e.g., verify an external API is reachable.",
      "code": "@Component\npublic class PaymentHealthIndicator implements HealthIndicator {\n    @Override\n    public Health health() {\n        try {\n            paymentClient.ping();\n            return Health.up().withDetail(\"gateway\", \"OK\").build();\n        } catch (Exception e) {\n            return Health.down().withDetail(\"error\", e.getMessage()).build();\n        }\n    }\n}",
      "language": "java",
      "highlight": [
        3,
        5,
        7,
        9
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "GET /actuator/health called",
            "done": true
          },
          {
            "label": "paymentClient.ping()",
            "done": true
          },
          {
            "label": "success → UP · failure → DOWN",
            "active": true
          }
        ]
      }
    },
    {
      "title": "Securing actuator endpoints",
      "description": "Never expose /actuator to the public internet. Restrict to an internal port or require authentication.",
      "code": "management:\n  server:\n    port: 8081       # separate port for internal access\n  endpoints:\n    web:\n      exposure:\n        include: \"health,metrics\"\n  endpoint:\n    health:\n      show-details: when-authorized",
      "language": "yaml",
      "highlight": [
        2,
        8,
        9,
        10
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Actuator security",
        "items": [
          {
            "label": "Internal port 8081",
            "color": "#10b981",
            "highlight": true
          },
          {
            "label": "show-details: when-authorized",
            "color": "#10b981"
          },
          {
            "label": "expose only: health, metrics",
            "color": "#818cf8"
          },
          {
            "label": "NEVER expose on public port without auth",
            "color": "#ef4444"
          }
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner (Setup)
```xml
<!-- pom.xml -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-actuator</artifactId>
</dependency>
```

```yaml
# application.yml
management:
  endpoints:
    web:
      exposure:
        # Expose all endpoints (RESTRICT this in production!)
        include: "health,info,metrics,loggers"
      base-path: /actuator
  endpoint:
    health:
      # Show full health details (DB connection, disk space, Redis, etc.)
      show-details: always
```

### Level 2 — Building Blocks (Health Checks)
The `/actuator/health` endpoint is automatically wired to check ALL configured integrations (PostgreSQL, Redis, Kafka, etc.).

```json
// GET /actuator/health
{
  "status": "UP",
  "components": {
    "db": {
      "status": "UP",
      "details": { "database": "PostgreSQL", "validationQuery": "isValid()" }
    },
    "redis": {
      "status": "UP",
      "details": { "version": "7.0.5" }
    },
    "diskSpace": {
      "status": "UP",
      "details": { "total": 107374182400, "free": 50890653696, "threshold": 10485760 }
    }
  }
}
```

### Level 3 — Intermediate (Custom Health Indicators)
```java
import org.springframework.boot.actuate.health.*;
import org.springframework.stereotype.Component;

// Spring auto-registers this as a health check!
@Component
public class ExternalApiHealthIndicator implements HealthIndicator {

    private final RestTemplate restTemplate;

    @Override
    public Health health() {
        try {
            // Call the external payment API gateway's ping endpoint
            restTemplate.getForObject("https://api.stripe.com/v1/ping", String.class);
            
            return Health.up()
                    .withDetail("service", "Stripe Payment Gateway")
                    .withDetail("status", "Reachable")
                    .build();
        } catch (Exception ex) {
            return Health.down()
                    .withDetail("service", "Stripe Payment Gateway")
                    .withException(ex)
                    .build();
        }
    }
}
```

### Level 4 — Advanced (Custom Metrics with Micrometer)
Spring Boot uses **Micrometer** as its metrics library (analogous to SLF4J for metrics). You can record custom business metrics (like "number of orders placed") that flow directly into Prometheus and are visualized in Grafana!

```java
import io.micrometer.core.instrument.*;
import org.springframework.stereotype.Service;

@Service
public class OrderService {

    private final Counter ordersPlacedCounter;
    private final Timer orderProcessingTimer;

    // Inject the MeterRegistry (the Micrometer hub)
    public OrderService(MeterRegistry registry) {
        // 1. A Counter that increments with every successful order
        this.ordersPlacedCounter = Counter.builder("orders.placed.total")
                .description("Total number of successfully placed orders")
                .tag("region", "us-east-1")
                .register(registry);

        // 2. A Timer to measure order processing latency
        this.orderProcessingTimer = Timer.builder("orders.processing.time")
                .description("Time to process an order end-to-end")
                .register(registry);
    }

    public Order placeOrder(OrderRequest req) {
        // Record the order processing time automatically
        return orderProcessingTimer.record(() -> {
            Order order = /* ... processing logic ... */ new Order();
            ordersPlacedCounter.increment(); // Increment the business metric
            return order;
        });
    }
}
```

## REAL_WORLD
**The Modern Observability Stack (The "Goldilocks" Setup):**
1. **Spring Boot Actuator** — Exposes metrics, health, and info endpoints.
2. **Prometheus** — Scrapes the `/actuator/prometheus` endpoint every 15 seconds and stores the time-series metrics data.
3. **Grafana** — Provides beautiful real-time dashboards built on top of Prometheus data.
4. **Loki** — Aggregates log streams from all service instances.
5. **Tempo** — Collects distributed traces (with Micrometer Tracing / OpenTelemetry).

This stack is completely open-source and universally used in the Enterprise.

## INTERVIEW
**Q: How do you secure Actuator endpoints in production?**
A: You should never expose `/actuator/env` (which shows all environment variables including secrets!) to the public internet. Best practices:
1. Move actuator to a different, internal-only port: `management.server.port=8081`.
2. Restrict endpoint exposure to only `health` and `info` for external access.
3. Secure the admin actuator endpoints with Spring Security, requiring `ROLE_ADMIN`.

**Q: What is the difference between Micrometer and Spring Boot Actuator?**
A: **Micrometer** is the standalone metrics facade library (analogous to SLF4J for logging). It abstracts the metrics API from specific monitoring systems (Prometheus, Datadog, Cloudwatch). **Spring Boot Actuator** is the Spring Boot integration that auto-configures Micrometer, wires it to JVM metrics, and exposes the HTTP endpoints. Actuator depends on Micrometer; they work together.

## FEYNMAN CHECK

### Explain Spring Boot Actuator Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Spring Boot Actuator** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Spring Boot Actuator, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Spring Boot Actuator solve? Why would JavaScript/web development be harder without it?**
> **A:** Spring Boot Actuator solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Spring Boot Actuator?**
> **A:** The single most important thing is understanding the *mental model*: Spring Boot Actuator operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Spring Boot Actuator and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Spring Boot Actuator operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Spring Boot Actuator interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Spring Boot Actuator interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Spring Boot Actuator to a senior engineer in two sentences, what would you say?**
> **A:** "Spring Boot Actuator works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Spring Boot Actuator Utility

**Goal:** Implement a real, working Spring Boot Actuator module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `springactuator` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir spring-actuator-demo && cd spring-actuator-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Spring Boot Actuator Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Spring Boot Actuator.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createSpringBootActuator(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Spring Boot Actuator] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Spring Boot Actuator logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Spring Boot Actuator] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Spring Boot Actuator] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createSpringBootActuator({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Spring Boot Actuator] Input cannot be null or undefined`);
    }
    const instance = createSpringBootActuator();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Spring Boot Actuator] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Spring Boot Actuator Tests ──');

test('initialises correctly', () => {
  const m = createSpringBootActuator();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createSpringBootActuator();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Spring Boot Actuator] Initialized with: hello world
[Spring Boot Actuator] Processed: hello world
Result: hello world
[Spring Boot Actuator] Destroyed.

── Spring Boot Actuator Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Spring Boot Actuator in one sentence.**
<details><summary>Answer</summary>

Spring Boot Actuator is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Spring Boot Actuator?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Spring Boot Actuator.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Spring Boot Actuator and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Spring Boot Actuator | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Spring Boot Actuator] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Spring Boot Actuator causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Spring Boot Actuator usage].
</details>

**Q6: What does Spring Boot Actuator look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Spring Boot Actuator typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Spring Boot Actuator correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Spring Boot Actuator:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_spring_actuator(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Spring Boot Actuator in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Spring Boot Actuator in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Spring Boot Actuator', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Spring Boot Actuator at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Spring Boot Actuator uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Spring Boot Actuator interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Spring Boot Actuator is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Spring Boot Actuator relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Spring Boot Actuator
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Spring Boot Actuator to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Spring Boot Actuator fits in the data/control flow.
2. **Choose** the right variant/pattern of Spring Boot Actuator for the scale requirement.
3. **Handle** failure modes: what happens when Spring Boot Actuator is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Spring Boot Actuator is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
