---
slug: "resilience4j-spring"
title: "Resilience4j (Circuit Breakers)"
level: 5
---

## WHY
In a monolithic application, if a Java function calls another Java function, it works 100% of the time.
In a microservices architecture, the `OrderService` must make an HTTP network call to the `PaymentService`. Networks fail. The Payment Service might be overloaded and taking 15 seconds to respond. 
If 1,000 users try to place an order, the `OrderService` will spawn 1,000 threads, each waiting 15 seconds for the dead Payment Service. The `OrderService` will exhaust its thread pool, run out of memory, and crash!
This is a **Cascading Failure**. One dead microservice takes down the entire system.
**Resilience4j** is a fault-tolerance library that prevents this. By implementing a **Circuit Breaker Pattern**, your application will instantly detect that the Payment Service is failing, "trip the circuit," and immediately return fallback responses to the users, allowing the Order Service to survive and the Payment Service time to recover!

## THEORY
### The Circuit Breaker State Machine
A Circuit Breaker sits between your Service and the remote API, monitoring every call. It has three states:
1. **CLOSED (Normal):** Everything is fine. Requests flow through normally.
2. **OPEN (Tripped):** If the failure rate exceeds a threshold (e.g., 50% of requests fail or timeout), the circuit "opens". It physically blocks all requests, instantly throwing an error or executing a fallback method *without* ever attempting the network call!
3. **HALF-OPEN (Testing):** After a cooldown period (e.g., 30 seconds), it allows a limited number of test requests through. If they succeed, it closes the circuit (Recovery!). If they fail, it opens it again.

## VISUALIZATION_CONFIG
```json
{
  "language": "java",
  "fileName": "Resilience.java",
  "steps": [
    {
      "title": "Circuit Breaker pattern",
      "description": "A circuit breaker monitors for failures. After a threshold, it OPENS — calls fail fast without hitting the broken service. After a reset timeout, it goes HALF-OPEN to test.",
      "code": "@CircuitBreaker(name=\"paymentService\", fallbackMethod=\"paymentFallback\")\npublic PaymentResult charge(Order order) {\n    return paymentClient.charge(order.total());\n}\npublic PaymentResult paymentFallback(Order order, Throwable t) {\n    return PaymentResult.queued(order.id());\n}",
      "highlight": [
        1,
        5,
        6
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "CLOSED: normal operation",
            "done": true
          },
          {
            "label": "failures > threshold (50% in 10s)",
            "done": true
          },
          {
            "label": "OPEN: fail-fast with fallback",
            "active": true
          },
          {
            "label": "after 60s: HALF-OPEN → test real call"
          }
        ]
      }
    },
    {
      "title": "Retry — with exponential backoff",
      "description": "@Retry retries on exception with configurable attempts and back-off. Use randomized jitter to avoid thundering herd.",
      "code": "@Retry(name=\"externalApi\", fallbackMethod=\"fallback\")\n@CircuitBreaker(name=\"externalApi\")\npublic Data fetchData(String id) {\n    return externalApi.get(id);\n}",
      "highlight": [
        1,
        2
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Retry + backoff",
        "items": [
          {
            "label": "attempt 1: immediate",
            "color": "#818cf8"
          },
          {
            "label": "attempt 2: 1s (+ jitter)",
            "color": "#f59e0b"
          },
          {
            "label": "attempt 3: 2s (+ jitter)",
            "color": "#f59e0b"
          },
          {
            "label": "all failed → fallback",
            "color": "#ef4444",
            "highlight": true
          }
        ]
      }
    },
    {
      "title": "RateLimiter",
      "description": "@RateLimiter limits requests per time window. Returns RequestNotPermitted exception (handled by fallback) when exceeded.",
      "code": "@RateLimiter(name=\"searchApi\", fallbackMethod=\"fallbackSearch\")\npublic List<Result> search(String q) {\n    return externalSearch.query(q);\n}\npublic List<Result> fallbackSearch(String q, RequestNotPermitted e) {\n    return searchCache.get(q);  // serve stale cache\n}",
      "highlight": [
        1,
        5,
        6
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "request arrives",
            "done": true
          },
          {
            "label": "has remaining quota? → proceed",
            "done": true
          },
          {
            "label": "quota exceeded → RequestNotPermitted",
            "active": true
          },
          {
            "label": "fallbackSearch() → return stale cache"
          }
        ]
      }
    },
    {
      "title": "Bulkhead — isolate failures",
      "description": "@Bulkhead limits concurrent calls to a service. Prevents a slow dependency from consuming all thread pool slots.",
      "code": "@Bulkhead(name=\"inventoryService\", type=Bulkhead.Type.THREADPOOL)\n@TimeLimiter(name=\"inventoryService\")\npublic CompletableFuture<Stock> getStock(Long productId) {\n    return CompletableFuture.supplyAsync(() -> inventoryApi.get(productId));\n}",
      "highlight": [
        1,
        2,
        4
      ],
      "diagram": {
        "kind": "threadpool",
        "capacity": 6,
        "workers": [
          {
            "name": "inv-worker-1",
            "state": "RUNNABLE"
          },
          {
            "name": "inv-worker-2",
            "state": "RUNNABLE"
          },
          {
            "name": "inv-worker-3",
            "state": "WAITING",
            "note": "idle"
          }
        ],
        "queue": [
          {
            "label": "T4"
          },
          {
            "label": "T5"
          }
        ]
      }
    },
    {
      "title": "application.yml configuration",
      "description": "Configure thresholds in application.yml. Different settings per named instance.",
      "code": "resilience4j:\n  circuitbreaker:\n    instances:\n      paymentService:\n        sliding-window-size: 10\n        failure-rate-threshold: 50\n        wait-duration-in-open-state: 60s\n  retry:\n    instances:\n      externalApi:\n        max-attempts: 3\n        wait-duration: 1s",
      "language": "yaml",
      "highlight": [
        4,
        5,
        6,
        7,
        9,
        10,
        11
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Config per instance",
        "items": [
          {
            "label": "paymentService CB: 50% failure → open",
            "color": "#10b981",
            "highlight": true
          },
          {
            "label": "externalApi retry: 3 attempts × 1s wait",
            "color": "#818cf8"
          }
        ]
      }
    }
  ]
}
```

## CODE

Resilience4j integrates beautifully with Spring Boot via annotations (AOP).

### 1. The Dependency
Add the Spring Boot starter for Resilience4j.

```xml
<dependency>
    <groupId>io.github.resilience4j</groupId>
    <artifactId>resilience4j-spring-boot3</artifactId>
</dependency>
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-aop</artifactId> <!-- Required for annotations! -->
</dependency>
```

### 2. Configuration (application.yml)
We define the exact mathematical rules for when the circuit should trip.

```yaml
resilience4j:
  circuitbreaker:
    instances:
      paymentService: # The specific name of this circuit breaker
        slidingWindowSize: 10 # Monitor the last 10 requests
        failureRateThreshold: 50 # If 50% of them fail (5 requests), trip the circuit!
        waitDurationInOpenState: 10s # Wait 10 seconds before entering HALF-OPEN state
        permittedNumberOfCallsInHalfOpenState: 3 # Allow 3 test requests
```

### 3. Using the @CircuitBreaker Annotation
We wrap the dangerous network call in our Service layer with the annotation, and provide a **Fallback Method**.

```java
import io.github.resilience4j.circuitbreaker.annotation.CircuitBreaker;
import org.springframework.stereotype.Service;
import org.springframework.web.client.RestTemplate;

@Service
public class OrderService {

    private final RestTemplate restTemplate = new RestTemplate();

    // 1. name = "paymentService" links it to the YAML config!
    // 2. fallbackMethod defines what to execute if the remote server fails, 
    //    OR if the circuit is currently OPEN!
    @CircuitBreaker(name = "paymentService", fallbackMethod = "paymentFallback")
    public String processPayment(String orderId) {
        
        System.out.println("Attempting physical network call to Payment API...");
        
        // This is a dangerous network call. It might timeout or return 500!
        return restTemplate.getForObject("http://payment-service/api/charge", String.class);
    }

    // The Fallback Method! 
    // It MUST have the exact same return type and parameters, plus the Exception.
    public String paymentFallback(String orderId, Exception e) {
        System.out.println("Payment API failed or Circuit is OPEN! Executing Fallback!");
        
        // Return a safe, graceful degradation response instead of crashing!
        return "Payment service is currently degraded. Your order " + orderId + " has been saved to a queue and will be processed later.";
    }
}
```

## REAL_WORLD
**Beyond Circuit Breakers (Retries and Bulkheads)**
Resilience4j provides several other critical fault-tolerance mechanisms that you can stack together!
- **`@Retry`:** If a network call fails, don't give up immediately! Maybe it was a minor blip. `@Retry(name = "myService")` tells Spring to automatically try the exact same HTTP call 3 times before finally throwing an exception or opening the Circuit Breaker.
- **`@RateLimiter`:** Protects *your* server by rejecting requests if too many users hit a specific method per second.
- **`@Bulkhead`:** Limits the maximum number of concurrent threads executing a specific method. If you have a thread pool of 100, and a heavy report generation method takes all 100 threads, the rest of your app dies. A Bulkhead restricts that specific method to a maximum of 20 threads, guaranteeing the other 80 threads are always available for standard user requests!

## INTERVIEW
**Q: You implement a Circuit Breaker on a method that calls a highly unreliable 3rd-party API. The API is so terrible that it takes exactly 25 seconds to respond, but it *does* eventually respond with a 200 OK. After monitoring your system, you notice your Circuit Breaker NEVER trips into the OPEN state, but your Spring Boot application still completely crashes due to thread pool exhaustion. Why didn't the Circuit Breaker protect you?**
A: **Circuit Breakers only trip on Exceptions, not Timeouts! You must configure a `TimeLimiter`.**
By default, the `RestTemplate` (and standard Java HTTP clients) have infinite (or incredibly long) timeouts. 
If the 3rd-party API takes 25 seconds to respond with a 200 OK, there is no Java Exception thrown! Because there is no Exception, the Resilience4j Circuit Breaker views the request as a 100% successful operation, so the Failure Rate remains at 0%.
Meanwhile, 1,000 user threads pile up, waiting 25 seconds each, and the application crashes.
*The Fix:* You must always pair a Circuit Breaker with strict Timeouts!
You can configure the timeout directly on the `RestTemplate` factory, or you can stack Resilience4j's `@TimeLimiter` annotation on top of the `@CircuitBreaker`. 
A TimeLimiter mathematically enforces that if the method takes longer than 2 seconds, it forcefully interrupts the thread and throws a `TimeoutException`. *That* exception will then be caught by the Circuit Breaker, registering as a failure, tripping the circuit, and instantly saving the entire application!

## FEYNMAN CHECK

### Explain Resilience4j (Circuit Breakers) Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Resilience4j (Circuit Breakers)** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Resilience4j (Circuit Breakers), they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Resilience4j (Circuit Breakers) solve? Why would JavaScript/web development be harder without it?**
> **A:** Resilience4j (Circuit Breakers) solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Resilience4j (Circuit Breakers)?**
> **A:** The single most important thing is understanding the *mental model*: Resilience4j (Circuit Breakers) operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Resilience4j (Circuit Breakers) and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Resilience4j (Circuit Breakers) operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Resilience4j (Circuit Breakers) interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Resilience4j (Circuit Breakers) interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Resilience4j (Circuit Breakers) to a senior engineer in two sentences, what would you say?**
> **A:** "Resilience4j (Circuit Breakers) works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Resilience4j (Circuit Breakers) Utility

**Goal:** Implement a real, working Resilience4j (Circuit Breakers) module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `resilience4jspring` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir resilience4j-spring-demo && cd resilience4j-spring-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Resilience4j (Circuit Breakers) Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Resilience4j (Circuit Breakers).
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createResilience4j(CircuitBreakers)(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Resilience4j (Circuit Breakers)] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Resilience4j (Circuit Breakers) logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Resilience4j (Circuit Breakers)] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Resilience4j (Circuit Breakers)] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createResilience4j(CircuitBreakers)({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Resilience4j (Circuit Breakers)] Input cannot be null or undefined`);
    }
    const instance = createResilience4j(CircuitBreakers)();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Resilience4j (Circuit Breakers)] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Resilience4j (Circuit Breakers) Tests ──');

test('initialises correctly', () => {
  const m = createResilience4j(CircuitBreakers)();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createResilience4j(CircuitBreakers)();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Resilience4j (Circuit Breakers)] Initialized with: hello world
[Resilience4j (Circuit Breakers)] Processed: hello world
Result: hello world
[Resilience4j (Circuit Breakers)] Destroyed.

── Resilience4j (Circuit Breakers) Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Resilience4j (Circuit Breakers) in one sentence.**
<details><summary>Answer</summary>

Resilience4j (Circuit Breakers) is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Resilience4j (Circuit Breakers)?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Resilience4j (Circuit Breakers).**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Resilience4j (Circuit Breakers) and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Resilience4j (Circuit Breakers) | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Resilience4j (Circuit Breakers)] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Resilience4j (Circuit Breakers) causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Resilience4j (Circuit Breakers) usage].
</details>

**Q6: What does Resilience4j (Circuit Breakers) look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Resilience4j (Circuit Breakers) typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Resilience4j (Circuit Breakers) correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Resilience4j (Circuit Breakers):
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_resilience4j_spring(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Resilience4j (Circuit Breakers) in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Resilience4j (Circuit Breakers) in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Resilience4j (Circuit Breakers)', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Resilience4j (Circuit Breakers) at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Resilience4j (Circuit Breakers) uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Resilience4j (Circuit Breakers) interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Resilience4j (Circuit Breakers) is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Resilience4j (Circuit Breakers) relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Resilience4j (Circuit Breakers)
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Resilience4j (Circuit Breakers) to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Resilience4j (Circuit Breakers) fits in the data/control flow.
2. **Choose** the right variant/pattern of Resilience4j (Circuit Breakers) for the scale requirement.
3. **Handle** failure modes: what happens when Resilience4j (Circuit Breakers) is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Resilience4j (Circuit Breakers) is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
