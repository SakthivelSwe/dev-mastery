---
slug: "github-basics"
title: "GitHub Basics"
level: 2
---

## WHY
Git is a distributed version control tool. GitHub is a cloud platform built on top of Git that adds collaboration features: repository hosting, code review (Pull Requests), issue tracking, project management, CI/CD (GitHub Actions), and a social layer for open-source contribution.
Understanding GitHub is essential — it's where virtually all modern software development collaboration happens, and your GitHub profile is your portfolio to the world.

## THEORY
### Git vs GitHub
- **Git:** The version control tool (free, open-source, runs on your machine)
- **GitHub:** A cloud service that hosts Git repositories + collaboration tools
- **Alternatives:** GitLab (self-hostable), Bitbucket (Atlassian)

GitHub is not Git — it's a company (owned by Microsoft) that built a product on top of Git.

## VISUALIZATION_CONFIG
```json
{ "component": "ArchitectureVisualizer", "state": "github-basics" }
```

## CODE

### Essential GitHub Concepts
```bash
# Repository (Repo): Your project + its Git history
# Public repos: Visible to everyone on the internet
# Private repos: Only you and collaborators can see

# Forking: Create your own copy of someone else's public repo
# Used for: Contributing to open-source projects
# Your fork: github.com/yourusername/original-project

# Stars: Bookmark/upvote a repo (like a 'like' button)
# Watching: Get notifications for a repo's activity
```

### GitHub CLI (Essential Tool)
```bash
# Install GitHub CLI
# Mac: brew install gh
# Windows: winget install GitHub.cli

# Authenticate
gh auth login

# Create a new repository on GitHub (from your local project)
gh repo create my-project --public --source=. --push

# Clone a repo
gh repo clone facebook/react

# Open current repo in browser
gh repo view --web

# Create an issue
gh issue create --title "Login button not working on mobile" \
  --body "Steps to reproduce: 1. Go to /login 2. On mobile viewport..."

# List open issues
gh issue list

# Create a PR
gh pr create --title "Fix mobile login button" --body "..."

# Review a PR
gh pr review 45 --approve
gh pr review 45 --request-changes --body "Please add tests"

# Merge a PR
gh pr merge 45 --squash --delete-branch
```

### GitHub Repository Structure
```
github.com/user/repo/
├── Code tab          # Browse files, navigate code
├── Issues tab        # Bug reports, feature requests
├── Pull Requests tab # Code review workflow
├── Actions tab       # CI/CD pipelines
├── Projects tab      # Kanban boards, project management
├── Wiki tab          # Documentation
├── Settings tab      # Collaborators, branch protection, secrets

# Special files GitHub auto-renders:
├── README.md         # Project description (shows on repo homepage)
├── CONTRIBUTING.md   # How to contribute guide
├── CODE_OF_CONDUCT.md # Community guidelines
├── LICENSE           # Open-source license
├── .github/
│   ├── ISSUE_TEMPLATE/ # Template for new issues
│   ├── PULL_REQUEST_TEMPLATE.md
│   └── workflows/    # GitHub Actions CI/CD
```

## REAL_WORLD
**GitHub for Open Source Contribution**
```bash
# Contributing to an open-source project:

# 1. Fork the repository (on GitHub.com — click Fork button)

# 2. Clone YOUR fork
git clone git@github.com:yourusername/react.git

# 3. Add the ORIGINAL as upstream
git remote add upstream git@github.com:facebook/react.git

# 4. Create a branch for your fix
git checkout -b fix/typo-in-readme

# 5. Make your change and commit
git commit -m "Fix typo in CONTRIBUTING.md"

# 6. Push to YOUR fork
git push origin fix/typo-in-readme

# 7. Create a PR from YOUR fork to the ORIGINAL repo (on GitHub.com)
gh pr create --repo facebook/react --title "Fix typo" --body "..."

# 8. The maintainers review and (hopefully) merge!
```

## INTERVIEW
**Q: What is the difference between Forking a repository and Cloning it?**

A: Both copy a repository, but they operate differently:

**Cloning** (`git clone`) downloads the repository to your local machine. It's a local copy. You can push changes only if you have write access to the original repository.

**Forking** is a GitHub-level operation (not a Git concept). It creates a copy of the repository under YOUR GitHub account. You have full write access to your fork. The original repository shows that yours is a "fork." You can submit Pull Requests from your fork to the original.

**When to use each:**
- **Clone only:** You have write access to the repo (it's your team's project) — just clone and work directly
- **Fork first:** Contributing to someone else's public repo (open source) where you don't have write access — fork, clone YOUR fork, make changes, then PR to the original

## FEYNMAN CHECK

### Explain GitHub Basics Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **GitHub Basics** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about GitHub Basics, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does GitHub Basics solve? Why would JavaScript/web development be harder without it?**
> **A:** GitHub Basics solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about GitHub Basics?**
> **A:** The single most important thing is understanding the *mental model*: GitHub Basics operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with GitHub Basics and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which GitHub Basics operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does GitHub Basics interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** GitHub Basics interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of GitHub Basics to a senior engineer in two sentences, what would you say?**
> **A:** "GitHub Basics works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a GitHub Basics Utility

**Goal:** Implement a real, working GitHub Basics module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `githubbasics` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir github-basics-demo && cd github-basics-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── GitHub Basics Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of GitHub Basics.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createGitHubBasics(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[GitHub Basics] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual GitHub Basics logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[GitHub Basics] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[GitHub Basics] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createGitHubBasics({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[GitHub Basics] Input cannot be null or undefined`);
    }
    const instance = createGitHubBasics();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[GitHub Basics] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── GitHub Basics Tests ──');

test('initialises correctly', () => {
  const m = createGitHubBasics();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createGitHubBasics();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[GitHub Basics] Initialized with: hello world
[GitHub Basics] Processed: hello world
Result: hello world
[GitHub Basics] Destroyed.

── GitHub Basics Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define GitHub Basics in one sentence.**
<details><summary>Answer</summary>

GitHub Basics is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of GitHub Basics?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of GitHub Basics.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between GitHub Basics and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | GitHub Basics | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of GitHub Basics] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing GitHub Basics causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct GitHub Basics usage].
</details>

**Q6: What does GitHub Basics look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, GitHub Basics typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use GitHub Basics correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using GitHub Basics:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_github_basics(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test GitHub Basics in isolation? What would you mock?**
<details><summary>Answer</summary>

To test GitHub Basics in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('GitHub Basics', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of GitHub Basics at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how GitHub Basics uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common GitHub Basics interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "GitHub Basics is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does GitHub Basics relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
GitHub Basics
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use GitHub Basics to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where GitHub Basics fits in the data/control flow.
2. **Choose** the right variant/pattern of GitHub Basics for the scale requirement.
3. **Handle** failure modes: what happens when GitHub Basics is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you GitHub Basics is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
