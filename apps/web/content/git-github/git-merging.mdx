---
slug: "git-merging"
title: "Git Merging"
level: 2
---

## WHY
Branches let you work in isolation. But eventually, your feature needs to join the main codebase. **Git Merge** combines the work from one branch into another.
Understanding merge types and how to resolve conflicts is essential — merge conflicts are one of the most common and dreaded moments in collaborative development, but they're straightforward once you understand them.

## THEORY
### Types of Merges
- **Fast-Forward Merge:** If the target branch hasn't diverged (no new commits on main since you branched), Git simply moves the branch pointer forward. No merge commit created. Linear history.
- **Three-Way Merge:** If both branches have new commits, Git finds the common ancestor and creates a new "merge commit" combining both branches. History is non-linear but accurate.
- **Squash Merge:** All commits from the feature branch are squashed into ONE commit and applied to main. Clean history, but you lose individual commit history.

## VISUALIZATION_CONFIG
```json
{ "component": "GitVisualizer", "state": "merge-types" }
```

## CODE

### Basic Merging
```bash
# Merge feature/auth into main
git checkout main
git merge feature/auth

# If no conflicts: auto-merged!
# Git opens editor for merge commit message (for 3-way merge)

# Fast-forward (no extra commit created):
git merge --ff-only feature/auth

# Always create a merge commit (even if fast-forward is possible):
git merge --no-ff feature/auth

# Squash: combine all feature commits into one
git merge --squash feature/auth
git commit -m "Add user authentication feature"
```

### Resolving Merge Conflicts
```bash
git merge feature/auth
# CONFLICT (content): Merge conflict in src/api/user.js
# Automatic merge failed; fix conflicts and then commit the result.

# Open the conflicted file — Git adds markers:
cat src/api/user.js

# <<<<<<< HEAD (your current branch - main)
# function getUser() { return db.findUser(); }
# =======
# function getUser(id) { return cache.get(id) || db.findUser(id); }
# >>>>>>> feature/auth (incoming branch)

# Choose what you want to keep:
# Option A: Keep main's version (delete everything else)
# Option B: Keep feature's version (delete everything else)
# Option C: Combine both (the most common choice!)
# function getUser(id) { return cache.get(id) || db.findUser(id); }

# After resolving ALL conflicts, stage and commit:
git add src/api/user.js
git commit -m "Merge feature/auth into main"
# Or use a merge tool for a visual editor:
git mergetool  # Opens VS Code or another configured diff tool
```

### Checking Merge Status
```bash
# See all unresolved conflicts
git status

# See what conflicts look like diff-style
git diff

# See merged branches (safe to delete)
git branch --merged

# See unmerged branches (NOT safe to delete — still have unique commits)
git branch --no-merged
```

### Aborting a Merge
```bash
# Made a mess? Start over!
git merge --abort
# Returns everything to pre-merge state
```

## REAL_WORLD
**Merge Strategy: Squash vs Regular Merge**

| Strategy | Use When |
|---|---|
| Fast-forward | Clean feature branches, linear history preferred |
| Regular merge | Want to preserve when features were integrated |
| Squash merge | Messy WIP commits, want clean main branch history |

GitHub Pull Request merging options match these: "Create a merge commit", "Squash and merge", "Rebase and merge".

Most teams pick **one strategy** and enforce it through GitHub branch protection rules to keep history consistent.

## INTERVIEW
**Q: During a merge, you have a conflict in a binary file (like an image). Git shows the file as conflicted but you can't see diff markers. How do you resolve it?**
A: Binary files can't be merged by Git — it can't do line-by-line diffing on binary content. Git marks the file as conflicted but shows the entire file content from both sides.

You must manually choose which version to keep:

```bash
# Keep YOUR version (main branch's version):
git checkout HEAD -- assets/logo.png

# Keep THEIR version (the branch you're merging):
git checkout MERGE_HEAD -- assets/logo.png

# Then stage it:
git add assets/logo.png
git commit -m "Merge: keep new logo design from feature branch"
```

If you need to actually combine binary files (e.g., merging two Photoshop files), that's a design/content management workflow problem — not something Git can solve. In practice, binary files that change frequently should be managed in a separate system (Git LFS for large files, or a design tool like Figma that handles versioning natively).

## FEYNMAN CHECK

### Explain Git Merging Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Git Merging** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Git Merging, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Git Merging solve? Why would JavaScript/web development be harder without it?**
> **A:** Git Merging solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Git Merging?**
> **A:** The single most important thing is understanding the *mental model*: Git Merging operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Git Merging and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Git Merging operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Git Merging interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Git Merging interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Git Merging to a senior engineer in two sentences, what would you say?**
> **A:** "Git Merging works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Git Merging Utility

**Goal:** Implement a real, working Git Merging module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `gitmerging` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir git-merging-demo && cd git-merging-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Git Merging Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Git Merging.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createGitMerging(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Git Merging] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Git Merging logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Git Merging] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Git Merging] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createGitMerging({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Git Merging] Input cannot be null or undefined`);
    }
    const instance = createGitMerging();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Git Merging] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Git Merging Tests ──');

test('initialises correctly', () => {
  const m = createGitMerging();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createGitMerging();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Git Merging] Initialized with: hello world
[Git Merging] Processed: hello world
Result: hello world
[Git Merging] Destroyed.

── Git Merging Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Git Merging in one sentence.**
<details><summary>Answer</summary>

Git Merging is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Git Merging?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Git Merging.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Git Merging and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Git Merging | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Git Merging] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Git Merging causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Git Merging usage].
</details>

**Q6: What does Git Merging look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Git Merging typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Git Merging correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Git Merging:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_git_merging(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Git Merging in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Git Merging in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Git Merging', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Git Merging at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Git Merging uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Git Merging interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Git Merging is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Git Merging relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Git Merging
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Git Merging to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Git Merging fits in the data/control flow.
2. **Choose** the right variant/pattern of Git Merging for the scale requirement.
3. **Handle** failure modes: what happens when Git Merging is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Git Merging is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
