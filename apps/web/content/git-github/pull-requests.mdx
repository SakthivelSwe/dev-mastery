---
slug: "pull-requests"
title: "Pull Requests"
level: 3
---

## WHY
Without Pull Requests (PRs), developers push directly to `main` and hope for the best. Broken code goes straight to production. No one reviews code for security vulnerabilities, bugs, or design problems before they're deployed.
**Pull Requests** are the standard mechanism for collaborative code review. A developer finishes a feature on a branch, opens a PR, and asks teammates to review the code before it's merged. This catches bugs, improves code quality, shares knowledge, and maintains a clear audit trail of all changes.

## THEORY
### Pull Request Workflow
1. Developer creates a feature branch and commits changes
2. Developer pushes branch to GitHub and opens a PR
3. Reviewers are notified and examine the diff
4. Reviewers leave comments, request changes, or approve
5. Developer makes requested changes and pushes more commits
6. Once approved (and CI passes), the PR is merged to main

### PR vs MR
GitHub calls them **Pull Requests (PRs)**, GitLab calls them **Merge Requests (MRs)**. Same concept.

## VISUALIZATION_CONFIG
```json
{ "component": "ArchitectureVisualizer", "state": "pr-workflow" }
```

## CODE

### Creating a PR via GitHub CLI
```bash
# Install GitHub CLI
# brew install gh  (Mac)
# winget install GitHub.cli  (Windows)

# Authenticate
gh auth login

# Push your branch
git push origin feature/user-auth

# Create a PR
gh pr create \
  --title "Add OAuth2 Google Login" \
  --body "This PR implements Google OAuth2 login using Spring Security.

## What Changed
- Added oauth2-spring boot starter
- Configured Google provider in SecurityConfig
- Added UserController to expose /api/me endpoint

## Testing
- Manual: Verified login flow with test Google account
- Unit tests: Added SecurityConfig tests

Closes #142" \
  --reviewer alice,bob \
  --assignee me \
  --label "feature,authentication"

# List open PRs
gh pr list

# Check PR status (CI checks, reviews)
gh pr status

# Merge the PR (after approval)
gh pr merge 47 --squash --delete-branch

# View PR in browser
gh pr view 47 --web
```

### The Perfect PR Description
```markdown
## Description
Brief explanation of what this PR does and WHY it was needed.

## Changes Made
- [ ] Added OAuth2 Google provider configuration
- [ ] Created UserController with /api/me endpoint
- [ ] Added SecurityFilterChain configuration

## Screenshots (for UI changes)
Before: [screenshot]
After: [screenshot]

## How to Test
1. Configure Google OAuth2 credentials in application.yml
2. Start the application
3. Navigate to /api/me without a token — should return 401
4. Complete Google OAuth2 flow
5. Navigate to /api/me with token — should return user profile

## Related Issues
Closes #142
Depends on #138

## Checklist
- [x] Tests added/updated
- [x] Documentation updated
- [x] Backwards compatible
- [ ] Breaking change (describe migration path)
```

### Branch Protection Rules (Enforcing PR Process)
```
# GitHub Settings → Branches → Branch protection rules → Add rule

Branch name pattern: main

✅ Require a pull request before merging
  ✅ Require approvals: 1
  ✅ Dismiss stale pull request approvals when new commits are pushed
✅ Require status checks to pass before merging
  ✅ Require branches to be up to date
  Status checks: CI Pipeline, Build
✅ Restrict who can push to matching branches
✅ Do not allow bypassing the above settings
```

With these rules, **nobody** (not even repo owners) can push directly to main without a PR.

## REAL_WORLD
**Draft PRs for Early Feedback**
If you want early feedback before the feature is complete:
- Open the PR as a **Draft** on GitHub
- Reviewers know it's not ready for final review
- You get early architectural feedback
- CI runs on every commit so you see test results early
- When ready: convert Draft → Ready for Review

```bash
gh pr create --draft --title "WIP: Add payment gateway"
```

## INTERVIEW
**Q: A senior developer reviews your PR and requests 15 changes. You believe 13 of them are valid, but 2 are subjective style preferences that you disagree with. How do you handle this professionally?**
A: 

1. **Address the 13 valid changes** with actual code commits and mark each comment as "Resolved."

2. **For the 2 subjective disagreements**, engage professionally in the PR comment thread:
   - Acknowledge the reviewer's perspective: "I understand why you prefer X approach"
   - Clearly explain your reasoning: "I chose Y because [specific technical reason]"
   - Propose compromise if possible: "Would approach Z work for you?"
   - Ask for a team standard: "Should we add this to our style guide so everyone follows the same pattern?"
   - Escalate to a third-party for an objective opinion if needed

3. **Never merge without resolving all comments**, even if you disagree. Either change the code or get explicit acknowledgment that the reviewer is OK with your alternative approach (mark as "Resolved" with the reviewer's blessing).

The goal of code review is to improve the code AND maintain team relationships. Being right but adversarial is worse than being collaborative.

## FEYNMAN CHECK

### Explain Pull Requests Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Pull Requests** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Pull Requests, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Pull Requests solve? Why would JavaScript/web development be harder without it?**
> **A:** Pull Requests solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Pull Requests?**
> **A:** The single most important thing is understanding the *mental model*: Pull Requests operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Pull Requests and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Pull Requests operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Pull Requests interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Pull Requests interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Pull Requests to a senior engineer in two sentences, what would you say?**
> **A:** "Pull Requests works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Pull Requests Utility

**Goal:** Implement a real, working Pull Requests module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `pullrequests` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir pull-requests-demo && cd pull-requests-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Pull Requests Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Pull Requests.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createPullRequests(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Pull Requests] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Pull Requests logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Pull Requests] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Pull Requests] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createPullRequests({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Pull Requests] Input cannot be null or undefined`);
    }
    const instance = createPullRequests();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Pull Requests] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Pull Requests Tests ──');

test('initialises correctly', () => {
  const m = createPullRequests();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createPullRequests();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Pull Requests] Initialized with: hello world
[Pull Requests] Processed: hello world
Result: hello world
[Pull Requests] Destroyed.

── Pull Requests Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Pull Requests in one sentence.**
<details><summary>Answer</summary>

Pull Requests is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Pull Requests?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Pull Requests.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Pull Requests and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Pull Requests | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Pull Requests] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Pull Requests causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Pull Requests usage].
</details>

**Q6: What does Pull Requests look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Pull Requests typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Pull Requests correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Pull Requests:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_pull_requests(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Pull Requests in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Pull Requests in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Pull Requests', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Pull Requests at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Pull Requests uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Pull Requests interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Pull Requests is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Pull Requests relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Pull Requests
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Pull Requests to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Pull Requests fits in the data/control flow.
2. **Choose** the right variant/pattern of Pull Requests for the scale requirement.
3. **Handle** failure modes: what happens when Pull Requests is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Pull Requests is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
