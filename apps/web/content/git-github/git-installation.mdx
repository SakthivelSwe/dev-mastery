---
slug: "git-installation"
title: "Git Installation & Setup"
level: 1
---

## WHY
Before using Git, you need it installed and properly configured. The initial setup — especially configuring your identity and SSH keys for GitHub — saves countless authentication headaches later.

## THEORY
### Global vs Local Configuration
Git config has three levels:
- **System:** Applies to all users on the machine (`/etc/gitconfig`)
- **Global:** Applies to all repositories for your user (`~/.gitconfig`)
- **Local:** Applies to a specific repository (`.git/config`)
Local overrides Global, which overrides System.

## VISUALIZATION_CONFIG
```json
{ "component": "TerminalVisualizer", "state": "git-setup" }
```

## CODE

### Installation
```bash
# Mac
brew install git

# Ubuntu/Debian
sudo apt update && sudo apt install git

# Windows: Download from https://git-scm.com/

# Verify
git --version  # git version 2.43.0
```

### Essential Configuration
```bash
# Identity (required for commits — use your real name and email!)
git config --global user.name "Alice Smith"
git config --global user.email "alice@example.com"

# Set default editor
git config --global core.editor "code --wait"     # VS Code
git config --global core.editor "vim"              # Vim
git config --global core.editor "nano"             # Nano (beginner-friendly)

# Set default branch name to 'main' (modern standard)
git config --global init.defaultBranch main

# Better diff visualization
git config --global core.autocrlf input  # Mac/Linux
git config --global core.autocrlf true   # Windows

# View all configuration
git config --list --global
```

### SSH Key Setup for GitHub (One-Time)
```bash
# Generate SSH key pair
ssh-keygen -t ed25519 -C "alice@example.com"
# Press Enter for default location (~/.ssh/id_ed25519)
# Enter a passphrase (recommended!)

# Start the SSH agent
eval "$(ssh-agent -s)"

# Add key to agent
ssh-add ~/.ssh/id_ed25519

# Copy public key (add this to GitHub Settings → SSH Keys)
cat ~/.ssh/id_ed25519.pub
# ssh-ed25519 AAAAC3NzaC1lZDI1NTE5... alice@example.com

# Test connection
ssh -T git@github.com
# Hi alice! You've successfully authenticated...
```

### .gitignore Best Practices
```bash
# .gitignore file in project root
# Never commit these:
node_modules/
.env
.env.local
.env.production
*.log
.DS_Store          # Mac system file
target/            # Java/Maven build output
build/
dist/
*.class
.idea/             # IntelliJ IDEA settings (debated — some teams commit)
.vscode/           # VS Code settings (debated)

# Create global gitignore for OS/editor files
git config --global core.excludesfile ~/.gitignore_global
echo ".DS_Store" >> ~/.gitignore_global
echo "Thumbs.db" >> ~/.gitignore_global
echo ".idea/" >> ~/.gitignore_global
```

## REAL_WORLD
**SSH vs HTTPS for Remote Operations**
When cloning/pushing to GitHub, you can use SSH or HTTPS:

```bash
# HTTPS (simpler, uses username/password or token)
git clone https://github.com/user/repo.git

# SSH (more secure, uses key pair — no password needed!)
git clone git@github.com:user/repo.git
```

In 2021, GitHub deprecated password authentication for HTTPS. Now HTTPS requires a **Personal Access Token (PAT)** — a long random string that replaces your password.

Most developers prefer SSH for its one-time setup (add key once, never authenticate again) vs HTTPS which requires managing tokens.

## INTERVIEW
**Q: Your team uses Git with both Windows and Mac developers. Sometimes files show up as modified even when nothing has changed. What's likely causing this?**
A: **Line ending differences (CRLF vs LF)**.

Windows uses `\r\n` (CRLF) for line endings. Mac/Linux uses `\n` (LF). When a Mac developer commits a file with LF endings, a Windows developer's editor saves it with CRLF — Git sees every line as changed, even though the visible content is identical.

**Fix with `.gitattributes`** (the correct team-wide solution, not individual `core.autocrlf`):
```
# .gitattributes (committed to the repository)
* text=auto         # Git normalizes line endings automatically
*.sh text eol=lf    # Shell scripts: always LF (Linux servers need this)
*.bat text eol=crlf # Batch files: always CRLF (Windows)
*.png binary        # Binary files: no line ending conversion
```

Or configure each developer's Git:
- Windows: `git config --global core.autocrlf true` (CRLF on checkout, LF on commit)
- Mac/Linux: `git config --global core.autocrlf input` (LF on commit, untouched on checkout)

## FEYNMAN CHECK

### Explain Git Installation & Setup Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Git Installation & Setup** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Git Installation & Setup, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Git Installation & Setup solve? Why would JavaScript/web development be harder without it?**
> **A:** Git Installation & Setup solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Git Installation & Setup?**
> **A:** The single most important thing is understanding the *mental model*: Git Installation & Setup operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Git Installation & Setup and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Git Installation & Setup operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Git Installation & Setup interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Git Installation & Setup interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Git Installation & Setup to a senior engineer in two sentences, what would you say?**
> **A:** "Git Installation & Setup works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Git Installation & Setup Utility

**Goal:** Implement a real, working Git Installation & Setup module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `gitinstallation` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir git-installation-demo && cd git-installation-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Git Installation & Setup Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Git Installation & Setup.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createGitInstallation&Setup(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Git Installation & Setup] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Git Installation & Setup logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Git Installation & Setup] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Git Installation & Setup] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createGitInstallation&Setup({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Git Installation & Setup] Input cannot be null or undefined`);
    }
    const instance = createGitInstallation&Setup();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Git Installation & Setup] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Git Installation & Setup Tests ──');

test('initialises correctly', () => {
  const m = createGitInstallation&Setup();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createGitInstallation&Setup();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Git Installation & Setup] Initialized with: hello world
[Git Installation & Setup] Processed: hello world
Result: hello world
[Git Installation & Setup] Destroyed.

── Git Installation & Setup Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Git Installation & Setup in one sentence.**
<details><summary>Answer</summary>

Git Installation & Setup is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Git Installation & Setup?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Git Installation & Setup.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Git Installation & Setup and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Git Installation & Setup | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Git Installation & Setup] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Git Installation & Setup causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Git Installation & Setup usage].
</details>

**Q6: What does Git Installation & Setup look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Git Installation & Setup typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Git Installation & Setup correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Git Installation & Setup:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_git_installation(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Git Installation & Setup in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Git Installation & Setup in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Git Installation & Setup', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Git Installation & Setup at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Git Installation & Setup uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Git Installation & Setup interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Git Installation & Setup is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Git Installation & Setup relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Git Installation & Setup
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Git Installation & Setup to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Git Installation & Setup fits in the data/control flow.
2. **Choose** the right variant/pattern of Git Installation & Setup for the scale requirement.
3. **Handle** failure modes: what happens when Git Installation & Setup is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Git Installation & Setup is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
