---
slug: "code-review"
title: "Code Review Best Practices"
level: 3
---

## WHY
Code review is more than finding bugs — it's the primary mechanism for knowledge sharing, maintaining code quality standards, onboarding new developers, and preventing security vulnerabilities from reaching production.
Done well, code review improves both the code AND the team. Done poorly, it creates friction, slows development, and damages morale.

## THEORY
### The Goals of Code Review
1. **Defect detection:** Catch bugs, logic errors, security issues before production
2. **Knowledge sharing:** Reviewers learn what changed; authors learn from feedback
3. **Standards enforcement:** Consistent patterns, architecture alignment
4. **Mentoring:** Senior developers guide junior developers through PR feedback

## VISUALIZATION_CONFIG
```json
{ "component": "ArchitectureVisualizer", "state": "code-review-process" }
```

## CODE

### What to Look for When Reviewing
```
REVIEWER CHECKLIST:
□ Logic: Does the code actually do what it claims to do?
□ Edge cases: What happens with null, empty list, 0, -1?
□ Security: SQL injection, XSS, exposed secrets, authentication holes?
□ Performance: O(n²) where O(n) is possible? N+1 queries? Missing indexes?
□ Tests: Are critical paths tested? Edge cases tested?
□ Error handling: What happens when things fail? Are exceptions handled?
□ Naming: Are variables/functions named clearly and consistently?
□ Documentation: Complex logic needs comments. Public APIs need docs.
□ Dependencies: Is a new library necessary? Is it maintained and trusted?
□ Breaking changes: Does this change break existing API consumers?
```

### GitHub Code Review Features
```bash
# Review workflow on GitHub:

# 1. See the full diff
gh pr view 47 --web     # Opens in browser

# 2. Leave inline comments on specific lines
# (Click the '+' button next to a line in the GitHub diff view)

# 3. Suggest specific code changes (author can apply with one click!)
# In a review comment, use a suggestion block:
# ```suggestion
# const user = await db.users.findUnique({ where: { id } });
# ```

# 4. Submit review
gh pr review 47 --approve --body "LGTM! Great work on the caching."
gh pr review 47 --request-changes --body "Please add input validation for the email field."
gh pr review 47 --comment --body "Have you considered using a Set here for O(1) lookups?"
```

### Writing Constructive Review Comments
```
BAD: "This is wrong."
BAD: "Why did you do this???"
BAD: "I would never write it this way."

GOOD: "This might cause an issue with empty arrays. Consider adding: if (!arr.length) return []"
GOOD: "Question: why did we choose LinkedHashMap here instead of HashMap? Documenting the reasoning would help future readers."
GOOD: "Suggestion: extracting this into a separate method would make it easier to test in isolation. Not a blocker, just a thought!"
GOOD: "nit: Minor style preference — consider renaming `x` to `userId` for clarity."

GOOD PREFIXES (make intent clear):
"nit:" - Minor, non-blocking stylistic preference
"question:" - Genuine question, not necessarily a change request
"blocker:" - Must be addressed before merge
"suggestion:" - Take it or leave it
"thinking out loud:" - Not a change request, just sharing context
```

## REAL_WORLD
**Review Size Guidelines**
Research shows:
- Optimal PR size for effective review: **200-400 lines**
- At 500+ lines: Reviewers start rubber-stamping (approving without really reviewing)
- At 1000+ lines: The PR is too large to review effectively — should be split

**How to split large PRs:**
1. Refactor separately from feature addition
2. Database migration PR → Feature PR
3. Tests PR → Implementation PR (though usually combined)
4. Split by layer: API changes → UI changes

## INTERVIEW
**Q: You're reviewing a PR that implements a new feature. You notice the code works correctly, but uses a completely different pattern than the rest of the codebase (e.g., uses a different error handling approach, different state management). How do you handle this?**

A: The approach depends on whether the new pattern is genuinely better or just different.

**If the new pattern is better:**
```
"I notice this PR introduces a new error handling pattern using Result types. 
I think this is actually an improvement over our current approach! Before we 
merge, can we: 1) Document this as our new standard, 2) Consider creating a 
ticket to migrate existing code gradually? I'll approve this PR, but I want 
to make sure we're intentional about adopting this pattern team-wide."
```

**If it's just different (no clear improvement):**
```
"This works correctly, but I notice we're using a different error handling 
approach than the rest of the codebase. To maintain consistency, could you 
align with the pattern in UserService.java? If you think this pattern is 
better, let's discuss it in our next team meeting and decide as a team before 
adopting it."
```

The key is separating "this is wrong" from "this is inconsistent." Consistency is valuable. But don't reject genuinely better approaches just because they're new — bring them through the proper channel for team decision-making.

## FEYNMAN CHECK

### Explain Code Review Best Practices Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Code Review Best Practices** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Code Review Best Practices, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Code Review Best Practices solve? Why would JavaScript/web development be harder without it?**
> **A:** Code Review Best Practices solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Code Review Best Practices?**
> **A:** The single most important thing is understanding the *mental model*: Code Review Best Practices operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Code Review Best Practices and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Code Review Best Practices operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Code Review Best Practices interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Code Review Best Practices interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Code Review Best Practices to a senior engineer in two sentences, what would you say?**
> **A:** "Code Review Best Practices works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Code Review Best Practices Utility

**Goal:** Implement a real, working Code Review Best Practices module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `codereview` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir code-review-demo && cd code-review-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Code Review Best Practices Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Code Review Best Practices.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createCodeReviewBestPractices(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Code Review Best Practices] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Code Review Best Practices logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Code Review Best Practices] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Code Review Best Practices] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createCodeReviewBestPractices({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Code Review Best Practices] Input cannot be null or undefined`);
    }
    const instance = createCodeReviewBestPractices();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Code Review Best Practices] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Code Review Best Practices Tests ──');

test('initialises correctly', () => {
  const m = createCodeReviewBestPractices();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createCodeReviewBestPractices();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Code Review Best Practices] Initialized with: hello world
[Code Review Best Practices] Processed: hello world
Result: hello world
[Code Review Best Practices] Destroyed.

── Code Review Best Practices Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Code Review Best Practices in one sentence.**
<details><summary>Answer</summary>

Code Review Best Practices is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Code Review Best Practices?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Code Review Best Practices.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Code Review Best Practices and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Code Review Best Practices | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Code Review Best Practices] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Code Review Best Practices causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Code Review Best Practices usage].
</details>

**Q6: What does Code Review Best Practices look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Code Review Best Practices typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Code Review Best Practices correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Code Review Best Practices:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_code_review(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Code Review Best Practices in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Code Review Best Practices in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Code Review Best Practices', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Code Review Best Practices at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Code Review Best Practices uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Code Review Best Practices interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Code Review Best Practices is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Code Review Best Practices relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Code Review Best Practices
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Code Review Best Practices to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Code Review Best Practices fits in the data/control flow.
2. **Choose** the right variant/pattern of Code Review Best Practices for the scale requirement.
3. **Handle** failure modes: what happens when Code Review Best Practices is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Code Review Best Practices is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
