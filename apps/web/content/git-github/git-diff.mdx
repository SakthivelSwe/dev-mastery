---
slug: "git-diff"
title: "Git Diff"
level: 1
---

## WHY
Before committing changes, you want to review exactly what you've changed. `git diff` shows you line-by-line differences between any two states of your code — working directory vs staged, staged vs committed, or any two commits.
Understanding diff output is fundamental to reviewing your own work and understanding others' changes.

## THEORY
### Reading Diff Output
Git diffs use a unified diff format:
- Lines starting with `-` (red): Lines removed
- Lines starting with `+` (green): Lines added
- Lines with no prefix: Context lines (unchanged, shown for context)

## VISUALIZATION_CONFIG
```json
{ "component": "CodeVisualizer", "state": "git-diff" }
```

## CODE

### diff Variations
```bash
# Compare working directory to staging area (unstaged changes)
git diff

# Compare staging area to last commit (what will be committed)
git diff --staged
git diff --cached   # Same thing

# Compare working directory to last commit (all changes)
git diff HEAD

# Compare two commits
git diff a3f82b7 d4e5f6c

# Compare two branches
git diff main feature/payment

# Diff a specific file only
git diff src/api/user.js
git diff HEAD -- src/api/user.js

# Summary (just changed files, no line-by-line)
git diff --stat
# src/api/user.js  | 15 +++++++---
# src/model/User.java | 5 +++--
# 2 files changed, 15 insertions(+), 5 deletions(-)

# Word-level diff (more granular than line-level)
git diff --word-diff
```

### Reading a Diff
```diff
diff --git a/src/api/user.js b/src/api/user.js
index 3f4a5b6..7c8d9e0 100644
--- a/src/api/user.js      ← The "before" file
+++ b/src/api/user.js      ← The "after" file
@@ -15,10 +15,12 @@     ← Line numbers: @@ before lines @@ after lines
 function getUser(id) {
-  return db.findById(id);
+  const cached = cache.get(`user:${id}`);
+  if (cached) return cached;
+  const user = await db.findById(id);
+  cache.set(`user:${id}`, user, 3600);
+  return user;
 }
```

### Practical diff Workflow
```bash
# Before committing, always review your changes!
git diff --staged   # Review what you're about to commit

# If the diff is long, use less/more:
git diff | less

# Use a visual diff tool (VS Code):
git config --global diff.tool vscode
git config --global difftool.vscode.cmd 'code --wait --diff $LOCAL $REMOTE'
git difftool        # Opens VS Code with side-by-side diff!
```

## REAL_WORLD
**Reviewing Before a PR**
```bash
# Before creating a PR, compare your branch to main:
git diff main..feature/payment

# See which files changed (for a quick overview):
git diff main..feature/payment --stat

# Diff from the branching point (ignoring other changes to main):
git diff main...feature/payment  # Note: three dots!
```

## INTERVIEW
**Q: What's the difference between `git diff main..feature` and `git diff main...feature` (two dots vs three dots)?**

A: 
- **Two dots (`..`):** Shows all differences between the tip of `main` and the tip of `feature`. This includes any commits that were added to `main` AFTER you branched off.

- **Three dots (`...`):** Shows differences between the COMMON ANCESTOR of both branches and the tip of `feature`. This is the "changes you made on your feature branch" — it excludes any changes that were added to `main` after you branched.

Example:
```
main:     A → B → C → D (D added after feature was created)
feature:  A → B → E → F

git diff main..feature  → Shows: remove D's changes, add E+F's changes
git diff main...feature → Shows: only E+F's changes (what YOU wrote)
```

For reviewing a PR's changes, `...` is what you want — it shows what the feature branch actually contributed, without noise from main's changes.

## FEYNMAN CHECK

### Explain Git Diff Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Git Diff** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Git Diff, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Git Diff solve? Why would JavaScript/web development be harder without it?**
> **A:** Git Diff solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Git Diff?**
> **A:** The single most important thing is understanding the *mental model*: Git Diff operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Git Diff and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Git Diff operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Git Diff interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Git Diff interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Git Diff to a senior engineer in two sentences, what would you say?**
> **A:** "Git Diff works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Git Diff Utility

**Goal:** Implement a real, working Git Diff module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `gitdiff` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir git-diff-demo && cd git-diff-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Git Diff Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Git Diff.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createGitDiff(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Git Diff] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Git Diff logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Git Diff] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Git Diff] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createGitDiff({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Git Diff] Input cannot be null or undefined`);
    }
    const instance = createGitDiff();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Git Diff] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Git Diff Tests ──');

test('initialises correctly', () => {
  const m = createGitDiff();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createGitDiff();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Git Diff] Initialized with: hello world
[Git Diff] Processed: hello world
Result: hello world
[Git Diff] Destroyed.

── Git Diff Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Git Diff in one sentence.**
<details><summary>Answer</summary>

Git Diff is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Git Diff?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Git Diff.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Git Diff and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Git Diff | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Git Diff] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Git Diff causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Git Diff usage].
</details>

**Q6: What does Git Diff look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Git Diff typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Git Diff correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Git Diff:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_git_diff(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Git Diff in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Git Diff in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Git Diff', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Git Diff at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Git Diff uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Git Diff interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Git Diff is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Git Diff relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Git Diff
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Git Diff to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Git Diff fits in the data/control flow.
2. **Choose** the right variant/pattern of Git Diff for the scale requirement.
3. **Handle** failure modes: what happens when Git Diff is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Git Diff is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
