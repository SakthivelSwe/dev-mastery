---
slug: "git-workflows"
title: "Git Workflows"
level: 3
---

## WHY
Having branches isn't enough — your team needs a shared agreement on HOW to use them. Without a workflow, every developer branches and merges differently, creating chaos and frequent broken builds.
**Git Workflows** are standardized branching strategies that define rules for when to create branches, how to name them, and how/when to merge them. The right workflow reduces merge conflicts, enables parallel development, and keeps releases predictable.

## THEORY
### The Main Git Workflows

**1. GitHub Flow** — Simple, recommended for most teams:
- One long-lived branch: `main`
- Feature branches → PR → merge to main → deploy

**2. Git Flow** — Complex, for teams with scheduled releases:
- Long-lived: `main`, `develop`
- Short-lived: `feature/*`, `release/*`, `hotfix/*`

**3. Trunk-Based Development (TBD)** — Extreme CI/CD:
- Everyone commits to `main` (or very short-lived branches of 1-2 days)
- Feature flags for incomplete features

## VISUALIZATION_CONFIG
```json
{ "component": "GitVisualizer", "state": "git-workflows" }
```

## CODE

### GitHub Flow (Recommended for Most Teams)
```bash
# The entire workflow in 7 steps:

# 1. Update main
git checkout main && git pull origin main

# 2. Create feature branch
git checkout -b feature/stripe-payment

# 3. Work and commit
git add . && git commit -m "Add Stripe SDK integration"
git add . && git commit -m "Add payment processing service"

# 4. Push branch
git push origin feature/stripe-payment

# 5. Open PR → Code Review → CI passes

# 6. Merge (via GitHub UI or CLI)
gh pr merge --squash --delete-branch

# 7. Deploy main (automatic via CI/CD)

# REPEAT for every feature!
```

### Git Flow (Complex Scheduled Releases)
```bash
# Initialize git flow (install: brew install git-flow)
git flow init

# Start a feature
git flow feature start user-authentication
# Creates branch: feature/user-authentication from develop

# Finish the feature (merges to develop, deletes feature branch)
git flow feature finish user-authentication

# Start a release
git flow release start 1.2.0
# Creates branch: release/1.2.0 from develop

# On release branch: only bug fixes, version bumps, docs
git commit -m "Bump version to 1.2.0"
git commit -m "Fix last-minute checkout bug"

# Finish release (merges to both main AND develop, creates tag)
git flow release finish 1.2.0

# Start a hotfix (for critical production bugs)
git flow hotfix start payment-security-patch
# Creates branch: hotfix/payment-security-patch from main!

git flow hotfix finish payment-security-patch
# Merges to both main AND develop, creates tag
```

### Trunk-Based Development (TBD)
```bash
# In TBD, you either commit directly to main, or use very short branches
# Feature flags are used to hide incomplete features

git checkout main
git pull origin main
git checkout -b feature/new-recommendation-engine  # Max 1-2 days!

# ... write code with feature flag ...
if (process.env.ENABLE_NEW_RECOMMENDATIONS === 'true') {
  return newRecommendationEngine.getSuggestions(user);
}
return legacyEngine.getSuggestions(user);

git commit -m "Add new recommendation engine (behind feature flag)"
git push origin feature/new-recommendation-engine

# Create PR → merge to main TODAY or tomorrow
# Feature is deployed but hidden by flag
# Enable flag gradually: 1% → 10% → 100% of users
```

## REAL_WORLD
**Choosing the Right Workflow**

| Team Size | Release Cadence | Recommended Workflow |
|---|---|---|
| 1-5 devs | Continuous (multiple times/day) | GitHub Flow or TBD |
| 5-20 devs | Weekly | GitHub Flow |
| 20+ devs | Scheduled (biweekly/monthly) | Git Flow |
| Any size | Continuous with feature flags | Trunk-Based Development |

**Most modern teams** (including Google, Facebook, Netflix) use Trunk-Based Development or GitHub Flow. Git Flow has fallen out of favor because long-lived branches cause "integration hell" — merging a 3-week-old branch to main can cause dozens of conflicts.

## INTERVIEW
**Q: Your team currently uses Git Flow with 2-week release cycles. The CEO wants to move to deploying 10 times per day. What workflow changes would you recommend and what technical practices must the team adopt?**
A: Moving from Git Flow to Continuous Deployment requires:

**Workflow Change:** Adopt **Trunk-Based Development**:
- Delete `develop` branch — everyone pushes to `main` (or ultra-short feature branches of 1-2 days)
- Delete `release` branches — releases happen automatically from `main`
- Hotfixes become just regular commits with immediate deployment

**Technical Requirements (the hard part):**
1. **Feature Flags:** Every incomplete feature must be behind a feature flag. You deploy code, but the feature is hidden until the flag is enabled.
2. **Comprehensive CI:** Tests must pass in < 10 minutes (fast feedback). Slow tests kill CD.
3. **Automated Testing:** 80%+ code coverage with automated integration/E2E tests so you trust the pipeline.
4. **Database Migrations:** Must be backward-compatible. Cannot break the current running version. Use expand-contract pattern.
5. **Observability:** Real-time monitoring and instant rollback capability (canary deployments, feature flag kill switches).
6. **Deployment Pipeline:** Automated blue-green or canary deployments — no manual deployment steps.

This is a 6-12 month transformation, not a weekend project.

## FEYNMAN CHECK

### Explain Git Workflows Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Git Workflows** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Git Workflows, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Git Workflows solve? Why would JavaScript/web development be harder without it?**
> **A:** Git Workflows solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Git Workflows?**
> **A:** The single most important thing is understanding the *mental model*: Git Workflows operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Git Workflows and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Git Workflows operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Git Workflows interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Git Workflows interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Git Workflows to a senior engineer in two sentences, what would you say?**
> **A:** "Git Workflows works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Git Workflows Utility

**Goal:** Implement a real, working Git Workflows module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `gitworkflows` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir git-workflows-demo && cd git-workflows-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Git Workflows Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Git Workflows.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createGitWorkflows(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Git Workflows] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Git Workflows logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Git Workflows] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Git Workflows] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createGitWorkflows({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Git Workflows] Input cannot be null or undefined`);
    }
    const instance = createGitWorkflows();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Git Workflows] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Git Workflows Tests ──');

test('initialises correctly', () => {
  const m = createGitWorkflows();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createGitWorkflows();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Git Workflows] Initialized with: hello world
[Git Workflows] Processed: hello world
Result: hello world
[Git Workflows] Destroyed.

── Git Workflows Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Git Workflows in one sentence.**
<details><summary>Answer</summary>

Git Workflows is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Git Workflows?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Git Workflows.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Git Workflows and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Git Workflows | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Git Workflows] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Git Workflows causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Git Workflows usage].
</details>

**Q6: What does Git Workflows look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Git Workflows typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Git Workflows correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Git Workflows:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_git_workflows(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Git Workflows in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Git Workflows in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Git Workflows', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Git Workflows at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Git Workflows uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Git Workflows interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Git Workflows is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Git Workflows relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Git Workflows
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Git Workflows to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Git Workflows fits in the data/control flow.
2. **Choose** the right variant/pattern of Git Workflows for the scale requirement.
3. **Handle** failure modes: what happens when Git Workflows is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Git Workflows is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
