---
slug: "kadane-algorithm"
title: "Kadane's Algorithm"
level: 3
---

## WHY
In 1984, Jay Kadane formulated a brilliant algorithm that solves the "Maximum Subarray Problem" in blazing fast $O(N)$ time. 
Given an array of numbers (including negatives), find the contiguous subarray that has the largest mathematical sum. A naive approach (checking every possible subarray) requires $O(N^2)$ time. **Kadane's Algorithm** proves that you only need to iterate through the array exactly once, making a simple, greedy mathematical decision at every single step.

It is a masterpiece of Dynamic Programming and a highly frequent question in technical interviews.

## THEORY
### The Core Philosophy
As you iterate through the array, at each index, you ask yourself one simple question:
**"Am I better off adding the current number to the existing running total, OR should I throw away the past entirely and start a brand new subarray starting with just this number?"**

If the running total ever drops below zero, it is mathematically poisoning your future sums. You should instantly reset the running total to 0.

## VISUALIZATION_CONFIG
```json
{
  "steps": [
    {
      "title": "Maximum Subarray Sum",
      "description": "Kadane's: track running max ending at each index — reset if negative.",
      "code": "// LC 53: Maximum Subarray\nfunction maxSubArray(nums) {\n  let currMax = nums[0], globalMax = nums[0];\n  for (let i = 1; i < nums.length; i++) {\n    currMax = Math.max(nums[i], currMax + nums[i]);\n    globalMax = Math.max(globalMax, currMax);\n  }\n  return globalMax;\n}\n// currMax at i = max subarray sum ending at i\n// Either start fresh at nums[i] or extend prev\n// O(n) time, O(1) space",
      "highlight": [
        3,
        4,
        5,
        6,
        7,
        11,
        12,
        13
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "currMax = max ending here"
          },
          {
            "label": "Start fresh or extend"
          },
          {
            "label": "globalMax tracks answer"
          },
          {
            "label": "Kadane's greedy"
          },
          {
            "label": "O(n)"
          }
        ]
      }
    },
    {
      "title": "Maximum Product Subarray",
      "description": "Track both max and min (negatives can flip).",
      "code": "// LC 152: Maximum Product Subarray\nfunction maxProduct(nums) {\n  let maxSoFar = nums[0];\n  let currMax = nums[0], currMin = nums[0];\n  for (let i = 1; i < nums.length; i++) {\n    const n = nums[i];\n    const tempMax = Math.max(n, currMax * n, currMin * n);\n    currMin = Math.min(n, currMax * n, currMin * n);\n    currMax = tempMax;\n    maxSoFar = Math.max(maxSoFar, currMax);\n  }\n  return maxSoFar;\n}\n// Key: negative × negative min = max\n// Track BOTH max and min",
      "highlight": [
        3,
        4,
        5,
        6,
        7,
        8,
        9,
        10,
        11,
        14,
        15
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Track currMax and currMin"
          },
          {
            "label": "Negatives flip signs"
          },
          {
            "label": "max = max(n, max*n, min*n)"
          },
          {
            "label": "Similarly for min"
          },
          {
            "label": "Track global max"
          }
        ]
      }
    },
    {
      "title": "Best Time to Buy and Sell Stock",
      "description": "Track min price so far — max profit = current - min so far.",
      "code": "// LC 121: Best Time to Buy and Sell Stock\nfunction maxProfit(prices) {\n  let minPrice = Infinity;\n  let maxProfit = 0;\n  for (const p of prices) {\n    minPrice = Math.min(minPrice, p);\n    maxProfit = Math.max(maxProfit, p - minPrice);\n  }\n  return maxProfit;\n}\n// One transaction only\n// LC 122: unlimited transactions — greedy sum ups\nfunction maxProfitII(prices) {\n  let profit = 0;\n  for (let i = 1; i < prices.length; i++) {\n    if (prices[i] > prices[i-1]) profit += prices[i] - prices[i-1];\n  }\n  return profit;\n}",
      "highlight": [
        3,
        4,
        5,
        6,
        7,
        8,
        9,
        13,
        14,
        15,
        16,
        17
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Track min price"
          },
          {
            "label": "profit = curr - min"
          },
          {
            "label": "Update max profit"
          },
          {
            "label": "One transaction O(n)"
          },
          {
            "label": "Multiple: sum ups"
          }
        ]
      }
    },
    {
      "title": "Maximum Sum Circular Subarray",
      "description": "Circular variant — either normal max OR total - min subarray.",
      "code": "// LC 918: Maximum Sum Circular Subarray\nfunction maxSubarraySumCircular(nums) {\n  let total = 0, currMax = 0, currMin = 0;\n  let maxSum = -Infinity, minSum = Infinity;\n  for (const n of nums) {\n    total += n;\n    currMax = Math.max(currMax + n, n);\n    maxSum = Math.max(maxSum, currMax);\n    currMin = Math.min(currMin + n, n);\n    minSum = Math.min(minSum, currMin);\n  }\n  // If all negative, don't use circular formula\n  return maxSum > 0 ? Math.max(maxSum, total - minSum) : maxSum;\n}",
      "highlight": [
        3,
        4,
        5,
        6,
        7,
        8,
        9,
        10,
        13,
        14
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Kadane for max"
          },
          {
            "label": "Also Kadane for min"
          },
          {
            "label": "Circular = total - minSubarray"
          },
          {
            "label": "Take max of both"
          },
          {
            "label": "Handle all-negative case"
          }
        ]
      }
    },
    {
      "title": "Maximum Absolute Sum",
      "description": "Track both maxSum and minSum — answer is max of |max|, |min|.",
      "code": "// LC 1749: Maximum Absolute Sum of Any Subarray\nfunction maxAbsoluteSum(nums) {\n  let currMax = 0, currMin = 0;\n  let maxSum = 0, minSum = 0;\n  for (const n of nums) {\n    currMax = Math.max(currMax + n, n);\n    maxSum = Math.max(maxSum, currMax);\n    currMin = Math.min(currMin + n, n);\n    minSum = Math.min(minSum, currMin);\n  }\n  return Math.max(maxSum, -minSum);\n}\n// |x| = max(x, -x) → run Kadane twice",
      "highlight": [
        3,
        4,
        5,
        6,
        7,
        8,
        9,
        10,
        11,
        13
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Kadane max"
          },
          {
            "label": "Kadane min"
          },
          {
            "label": "|abs| = max(max, -min)"
          },
          {
            "label": "Two passes"
          },
          {
            "label": "O(n)"
          }
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner (Maximum Subarray)
*Problem: Given an integer array `nums`, find the contiguous subarray (containing at least one number) which has the largest sum and return its sum.*

```javascript
function maxSubArray(nums) {
    // Initialize both to the first element to handle arrays with all negative numbers!
    let max_so_far = nums[0];
    let current_max = nums[0];
    
    for (let i = 1; i < nums.length; i++) {
        // The core Kadane decision:
        // Do I attach to the previous sum, or do I start fresh?
        current_max = Math.max(nums[i], current_max + nums[i]);
        
        // Did we just beat the all-time high score?
        max_so_far = Math.max(max_so_far, current_max);
    }
    
    return max_so_far;
}

console.log(maxSubArray([-2, 1, -3, 4, -1, 2, 1, -5, 4]));
// Output: 6 (The subarray is [4, -1, 2, 1])
```

### Level 2 — Building Blocks (Returning the Subarray Elements)
What if the interviewer asks you to return the actual subarray itself, not just the sum? We need to track the indices!

```javascript
function findMaxSubArrayElements(nums) {
    let max_so_far = nums[0];
    let current_max = nums[0];
    
    let start_idx = 0;
    let end_idx = 0;
    let temp_start_idx = 0; // Tracks the start of the CURRENT running total
    
    for (let i = 1; i < nums.length; i++) {
        // If starting fresh is better than adding to the current sum...
        if (nums[i] > current_max + nums[i]) {
            current_max = nums[i];
            temp_start_idx = i; // Reset the starting pointer!
        } else {
            current_max += nums[i];
        }
        
        // If we beat the high score, officially record the indices!
        if (current_max > max_so_far) {
            max_so_far = current_max;
            start_idx = temp_start_idx;
            end_idx = i;
        }
    }
    
    // Return the sliced array using the recorded indices
    return nums.slice(start_idx, end_idx + 1);
}
```

### Level 3 — Intermediate (Maximum Product Subarray)
*Problem: Find the contiguous subarray that has the largest PRODUCT.*
*(Warning: `[-2, 3, -4]` has a max sum of `3`, but a max product of `24` because two negatives multiply to make a massive positive! Kadane's algorithm must be modified).*

Because a negative number instantly flips the sign, we must track BOTH the Maximum Product *and* the Minimum Product (because the minimum negative could become a massive positive on the very next loop!).

```javascript
function maxProduct(nums) {
    if (nums.length === 0) return 0;
    
    let max_so_far = nums[0];
    let current_max = nums[0];
    let current_min = nums[0];
    
    for (let i = 1; i < nums.length; i++) {
        const num = nums[i];
        
        // If the current number is negative, it will FLIP the max and min!
        // So we swap them!
        if (num < 0) {
            let temp = current_max;
            current_max = current_min;
            current_min = temp;
        }
        
        // The standard Kadane decision
        current_max = Math.max(num, current_max * num);
        current_min = Math.min(num, current_min * num);
        
        max_so_far = Math.max(max_so_far, current_max);
    }
    
    return max_so_far;
}
```

### Level 4 — Advanced (Maximum Circular Subarray)
*Problem: The array is circular (the last element connects to the first). Find the maximum subarray sum.*
*Trick:* The max subarray is either a normal subarray (found by standard Kadane) OR it wraps around the edges. If it wraps around the edges, it means the *middle* of the array is the Minimum Subarray! 

```javascript
function maxSubarraySumCircular(nums) {
    let total_sum = 0;
    
    let max_so_far = nums[0];
    let current_max = 0;
    
    let min_so_far = nums[0];
    let current_min = 0;
    
    for (let i = 0; i < nums.length; i++) {
        const num = nums[i];
        total_sum += num;
        
        // Standard Kadane for MAX
        current_max = Math.max(num, current_max + num);
        max_so_far = Math.max(max_so_far, current_max);
        
        // Standard Kadane for MIN
        current_min = Math.min(num, current_min + num);
        min_so_far = Math.min(min_so_far, current_min);
    }
    
    // Edge case: If all numbers are negative, total_sum == min_so_far!
    // Wrapping around would yield 0, which is incorrect. Just return max_so_far.
    if (max_so_far < 0) {
        return max_so_far;
    }
    
    // The answer is either the normal MAX, or the Total Sum minus the normal MIN
    return Math.max(max_so_far, total_sum - min_so_far);
}
```

## REAL_WORLD
**Computer Vision (Finding the brightest spot)**
Kadane's Algorithm is heavily used in 2D image processing to find the brightest rectangular section of an image (e.g., detecting the sun or a lightbulb in a photograph). By converting the image pixels to a 2D matrix of light intensities, and adapting Kadane's algorithm to 2D (often combined with the Prefix Sum matrix pattern), computer vision systems can identify the brightest sub-matrix in $O(N^3)$ time instead of an impossible $O(N^6)$ brute force time.

## INTERVIEW
**Q: I initialized `current_max = 0` and `max_so_far = 0` before the loop. The algorithm worked for most test cases but failed when the array contained ONLY negative numbers (e.g. `[-5, -2, -9]`). It returned 0 instead of -2. Why?**
A: Because an empty subarray has a sum of 0, if you initialize your variables to 0, you are implicitly telling the algorithm: "If everything is negative, just pick nothing (0)." If the problem explicitly states that a subarray MUST contain at least one element, you must initialize your max variables to the **first element in the array** (`nums[0]`) or `-Infinity`. This forces the algorithm to pick the "least bad" negative number rather than defaulting to 0.

## FEYNMAN CHECK

### Explain Kadane's Algorithm Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Kadane's Algorithm** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Kadane's Algorithm, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Kadane's Algorithm solve? Why would JavaScript/web development be harder without it?**
> **A:** Kadane's Algorithm solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Kadane's Algorithm?**
> **A:** The single most important thing is understanding the *mental model*: Kadane's Algorithm operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Kadane's Algorithm and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Kadane's Algorithm operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Kadane's Algorithm interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Kadane's Algorithm interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Kadane's Algorithm to a senior engineer in two sentences, what would you say?**
> **A:** "Kadane's Algorithm works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Kadane's Algorithm Utility

**Goal:** Implement a real, working Kadane's Algorithm module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `kadanealgorithm` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir kadane-algorithm-demo && cd kadane-algorithm-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Kadane's Algorithm Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Kadane's Algorithm.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createKadane'sAlgorithm(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Kadane's Algorithm] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Kadane's Algorithm logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Kadane's Algorithm] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Kadane's Algorithm] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createKadane'sAlgorithm({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Kadane's Algorithm] Input cannot be null or undefined`);
    }
    const instance = createKadane'sAlgorithm();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Kadane's Algorithm] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Kadane's Algorithm Tests ──');

test('initialises correctly', () => {
  const m = createKadane'sAlgorithm();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createKadane'sAlgorithm();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Kadane's Algorithm] Initialized with: hello world
[Kadane's Algorithm] Processed: hello world
Result: hello world
[Kadane's Algorithm] Destroyed.

── Kadane's Algorithm Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Kadane's Algorithm in one sentence.**
<details><summary>Answer</summary>

Kadane's Algorithm is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Kadane's Algorithm?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Kadane's Algorithm.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Kadane's Algorithm and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Kadane's Algorithm | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Kadane's Algorithm] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Kadane's Algorithm causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Kadane's Algorithm usage].
</details>

**Q6: What does Kadane's Algorithm look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Kadane's Algorithm typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Kadane's Algorithm correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Kadane's Algorithm:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_kadane_algorithm(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Kadane's Algorithm in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Kadane's Algorithm in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Kadane's Algorithm', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Kadane's Algorithm at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Kadane's Algorithm uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Kadane's Algorithm interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Kadane's Algorithm is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Kadane's Algorithm relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Kadane's Algorithm
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Kadane's Algorithm to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Kadane's Algorithm fits in the data/control flow.
2. **Choose** the right variant/pattern of Kadane's Algorithm for the scale requirement.
3. **Handle** failure modes: what happens when Kadane's Algorithm is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Kadane's Algorithm is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
