---
slug: "hashmap-frequency-pattern"
title: "Hashmap / Frequency Counting Pattern"
level: 2
---

## WHY
Imagine you are given an array of 100,000 numbers and asked to find the most frequent element. A naive approach would be a nested loop: for every number, scan the entire array again to count how many times it appears. This results in $O(N^2)$ time complexity, which is disastrously slow and will instantly fail any coding interview.

The **Hashmap Frequency Pattern** eliminates the inner loop. By utilizing a Hash Map (or Object/Dictionary) to store the frequency of elements as we traverse the array exactly once, we drop the time complexity to a blistering $O(N)$.

## THEORY
### The Time-Space Tradeoff
A Hash Map provides $O(1)$ average time complexity for both insertions and lookups. 
This pattern is the ultimate example of the **Time-Space Tradeoff** in computer science: we gladly sacrifice $O(N)$ auxiliary memory (to store the Hash Map) in order to achieve $O(N)$ time complexity. Memory is cheap; execution time is expensive.

## VISUALIZATION_CONFIG
```json
{
  "steps": [
    {
      "title": "Two Sum (Hashmap)",
      "description": "Classic use of hashmap — O(n) lookup of complement.",
      "code": "// LC 1: Two Sum\nfunction twoSum(nums, target) {\n  const seen = new Map(); // value → index\n  for (let i = 0; i < nums.length; i++) {\n    const complement = target - nums[i];\n    if (seen.has(complement)) {\n      return [seen.get(complement), i];\n    }\n    seen.set(nums[i], i);\n  }\n  return [];\n}\n// O(n) time, O(n) space\n// Trades space for time vs O(n²) brute force",
      "highlight": [
        3,
        4,
        5,
        6,
        7,
        8,
        9
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Iterate nums"
          },
          {
            "label": "complement = target - n"
          },
          {
            "label": "Complement in map?"
          },
          {
            "label": "Return pair"
          },
          {
            "label": "Otherwise add current"
          }
        ]
      }
    },
    {
      "title": "Group Anagrams",
      "description": "Group strings by sorted character key.",
      "code": "// LC 49: Group Anagrams\nfunction groupAnagrams(strs) {\n  const groups = new Map();\n  for (const s of strs) {\n    const key = s.split('').sort().join('');\n    if (!groups.has(key)) groups.set(key, []);\n    groups.get(key).push(s);\n  }\n  return [...groups.values()];\n}\n\n// Faster key: character frequency\nfunction groupAnagramsFast(strs) {\n  const groups = new Map();\n  for (const s of strs) {\n    const count = new Array(26).fill(0);\n    for (const c of s) count[c.charCodeAt(0) - 97]++;\n    const key = count.join(',');\n    if (!groups.has(key)) groups.set(key, []);\n    groups.get(key).push(s);\n  }\n  return [...groups.values()];\n}",
      "highlight": [
        4,
        5,
        6,
        7,
        14,
        15,
        16,
        17,
        18,
        19
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Compute canonical key"
          },
          {
            "label": "Sort chars or count freq"
          },
          {
            "label": "Group by key"
          },
          {
            "label": "Return values"
          },
          {
            "label": "O(n × k log k)"
          }
        ]
      }
    },
    {
      "title": "Subarray Sum Equals K",
      "description": "Prefix sum + hashmap — O(n) counting subarrays with target sum.",
      "code": "// LC 560: Subarray Sum Equals K\nfunction subarraySum(nums, k) {\n  const map = new Map([[0, 1]]); // prefixSum → count\n  let sum = 0, count = 0;\n  for (const n of nums) {\n    sum += n;\n    if (map.has(sum - k)) count += map.get(sum - k);\n    map.set(sum, (map.get(sum) || 0) + 1);\n  }\n  return count;\n}\n// Key insight: subarray sum = k means prefixSum[j] - prefixSum[i] = k\n// Look for prefixSum[i] = prefixSum[j] - k in the map",
      "highlight": [
        3,
        4,
        5,
        6,
        7,
        8,
        12,
        13
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Track running sum"
          },
          {
            "label": "sum - k in map?"
          },
          {
            "label": "Add its count"
          },
          {
            "label": "Update map"
          },
          {
            "label": "O(n) total"
          }
        ]
      }
    },
    {
      "title": "Longest Consecutive Sequence",
      "description": "Hashset — for each number without predecessor, expand forward.",
      "code": "// LC 128: Longest Consecutive Sequence\nfunction longestConsecutive(nums) {\n  const set = new Set(nums);\n  let longest = 0;\n  for (const n of set) {\n    if (!set.has(n - 1)) { // start of sequence\n      let curr = n;\n      let len = 1;\n      while (set.has(curr + 1)) {\n        curr++;\n        len++;\n      }\n      longest = Math.max(longest, len);\n    }\n  }\n  return longest;\n}\n// O(n) — each number visited at most twice\n// Sort would be O(n log n)",
      "highlight": [
        3,
        5,
        6,
        7,
        8,
        9,
        10,
        11,
        12
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "HashSet of nums"
          },
          {
            "label": "Find sequence starts (no n-1)"
          },
          {
            "label": "Expand forward while +1 exists"
          },
          {
            "label": "Track longest"
          },
          {
            "label": "O(n)"
          }
        ]
      }
    },
    {
      "title": "LRU Cache",
      "description": "HashMap + doubly linked list = O(1) get and put.",
      "code": "// LC 146: LRU Cache\nclass LRUCache {\n  constructor(capacity) {\n    this.capacity = capacity;\n    this.map = new Map(); // preserves insertion order!\n  }\n  get(key) {\n    if (!this.map.has(key)) return -1;\n    const val = this.map.get(key);\n    this.map.delete(key);\n    this.map.set(key, val); // move to end (most recent)\n    return val;\n  }\n  put(key, val) {\n    if (this.map.has(key)) this.map.delete(key);\n    else if (this.map.size >= this.capacity) {\n      const oldest = this.map.keys().next().value;\n      this.map.delete(oldest);\n    }\n    this.map.set(key, val);\n  }\n}\n// JS Map preserves insertion order → simulates DLL for free!",
      "highlight": [
        4,
        5,
        9,
        10,
        11,
        15,
        16,
        17,
        18,
        19,
        22,
        23
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Map preserves order"
          },
          {
            "label": "get: move to end"
          },
          {
            "label": "put: evict oldest if full"
          },
          {
            "label": "O(1) all operations"
          },
          {
            "label": "JS Map = free DLL"
          }
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner (Counting Frequencies)
The core foundation of this pattern is building the frequency map.
*Problem: Given an array of integers, return a map of how many times each integer appears.*

```javascript
function buildFrequencyMap(arr) {
    const map = {};
    
    for (let i = 0; i < arr.length; i++) {
        const num = arr[i];
        
        // If the number is already in the map, increment its count
        if (map[num] !== undefined) {
            map[num]++;
        } else {
            // Otherwise, initialize its count to 1
            map[num] = 1;
        }
    }
    
    return map;
}

console.log(buildFrequencyMap([1, 2, 2, 3, 1, 1])); 
// Output: { '1': 3, '2': 2, '3': 1 }
```
*(Pro-tip: In JavaScript, you can write this much cleaner: `map[num] = (map[num] || 0) + 1;`)*

### Level 2 — Building Blocks (Two Sum)
The most famous LeetCode problem of all time (Problem #1) is perfectly solved using a Hash Map.
*Problem: Given an array of numbers and a `target`, return the indices of the two numbers that add up to the target.*

```javascript
function twoSum(nums, target) {
    // Map stores { number: index }
    const map = new Map();
    
    for (let i = 0; i < nums.length; i++) {
        const currentNum = nums[i];
        // What number do we mathematically NEED to hit the target?
        const complement = target - currentNum;
        
        // Did we see the complement earlier in the array?
        if (map.has(complement)) {
            return [map.get(complement), i];
        }
        
        // If not, record the current number and its index for future reference
        map.set(currentNum, i);
    }
    
    return [];
}
```

### Level 3 — Intermediate (Valid Anagram)
*Problem: Given two strings `s` and `t`, return true if `t` is an anagram of `s` (contains the exact same characters in any order).*

```javascript
function isAnagram(s, t) {
    if (s.length !== t.length) return false;
    
    const countMap = {};
    
    // Step 1: Build the frequency map for the first string
    for (let char of s) {
        countMap[char] = (countMap[char] || 0) + 1;
    }
    
    // Step 2: "Spend" the characters using the second string
    for (let char of t) {
        // If the character doesn't exist, or we ran out of them, it's not an anagram!
        if (!countMap[char]) {
            return false;
        }
        countMap[char]--;
    }
    
    return true;
}
```

### Level 4 — Advanced (Subarray Sum Equals K)
This problem combines the Prefix Sum pattern with the Hashmap pattern!
*Problem: Given an array of integers and an integer `k`, return the total number of continuous subarrays whose sum equals `k`.*

```javascript
function subarraySum(nums, k) {
    let count = 0;
    let currentSum = 0;
    
    // Map stores { prefixSum: frequency }
    // Initialize with { 0: 1 } to handle subarrays that start at index 0
    const prefixMap = new Map();
    prefixMap.set(0, 1);
    
    for (let i = 0; i < nums.length; i++) {
        currentSum += nums[i];
        
        // If (currentSum - k) exists in the map, it means there is a previous 
        // prefix sum that we can chop off to leave exactly 'k' remaining!
        const difference = currentSum - k;
        if (prefixMap.has(difference)) {
            count += prefixMap.get(difference);
        }
        
        // Add the current sum to the map
        prefixMap.set(currentSum, (prefixMap.get(currentSum) || 0) + 1);
    }
    
    return count;
}
```

## REAL_WORLD
**Detecting Fraudulent Activity**
Imagine you work for a bank processing millions of credit card transactions per hour. You need to alert the security team if the exact same credit card number is used more than 5 times within a one-minute window. 

Sorting the transactions by credit card number would take $O(N \log N)$ time, introducing massive latency to the real-time pipeline. By feeding the transactions into an in-memory Hash Map (or a Redis cache) and incrementing the frequency counter in $O(1)$ time, the system can instantly trigger an alert the millisecond a card hits a frequency of 5.

## INTERVIEW
**Q: When should I use a JavaScript `Map` vs a standard JavaScript Object `{}` for frequency counting?**
A: You should almost always use a `Map` in an interview for three reasons:
1. **Key Types:** Standard Objects convert all keys to strings (e.g., the number `5` becomes `"5"`). A `Map` preserves the exact data type.
2. **Iteration:** A `Map` is strictly ordered by insertion time and is easily iterable using `for...of`.
3. **Prototype Pollution:** Standard Objects inherit built-in methods (like `.toString`). If an array contains the string `"toString"`, doing `map["toString"]++` on a standard Object will crash your algorithm! A `Map` is a pure data structure immune to this.

## FEYNMAN CHECK

### Explain Hashmap / Frequency Counting Pattern Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Hashmap / Frequency Counting Pattern** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Hashmap / Frequency Counting Pattern, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Hashmap / Frequency Counting Pattern solve? Why would JavaScript/web development be harder without it?**
> **A:** Hashmap / Frequency Counting Pattern solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Hashmap / Frequency Counting Pattern?**
> **A:** The single most important thing is understanding the *mental model*: Hashmap / Frequency Counting Pattern operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Hashmap / Frequency Counting Pattern and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Hashmap / Frequency Counting Pattern operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Hashmap / Frequency Counting Pattern interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Hashmap / Frequency Counting Pattern interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Hashmap / Frequency Counting Pattern to a senior engineer in two sentences, what would you say?**
> **A:** "Hashmap / Frequency Counting Pattern works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Hashmap / Frequency Counting Pattern Utility

**Goal:** Implement a real, working Hashmap / Frequency Counting Pattern module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `hashmapfrequencypattern` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir hashmap-frequency-pattern-demo && cd hashmap-frequency-pattern-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Hashmap / Frequency Counting Pattern Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Hashmap / Frequency Counting Pattern.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createHashmap/FrequencyCountingPattern(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Hashmap / Frequency Counting Pattern] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Hashmap / Frequency Counting Pattern logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Hashmap / Frequency Counting Pattern] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Hashmap / Frequency Counting Pattern] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createHashmap/FrequencyCountingPattern({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Hashmap / Frequency Counting Pattern] Input cannot be null or undefined`);
    }
    const instance = createHashmap/FrequencyCountingPattern();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Hashmap / Frequency Counting Pattern] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Hashmap / Frequency Counting Pattern Tests ──');

test('initialises correctly', () => {
  const m = createHashmap/FrequencyCountingPattern();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createHashmap/FrequencyCountingPattern();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Hashmap / Frequency Counting Pattern] Initialized with: hello world
[Hashmap / Frequency Counting Pattern] Processed: hello world
Result: hello world
[Hashmap / Frequency Counting Pattern] Destroyed.

── Hashmap / Frequency Counting Pattern Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Hashmap / Frequency Counting Pattern in one sentence.**
<details><summary>Answer</summary>

Hashmap / Frequency Counting Pattern is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Hashmap / Frequency Counting Pattern?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Hashmap / Frequency Counting Pattern.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Hashmap / Frequency Counting Pattern and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Hashmap / Frequency Counting Pattern | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Hashmap / Frequency Counting Pattern] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Hashmap / Frequency Counting Pattern causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Hashmap / Frequency Counting Pattern usage].
</details>

**Q6: What does Hashmap / Frequency Counting Pattern look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Hashmap / Frequency Counting Pattern typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Hashmap / Frequency Counting Pattern correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Hashmap / Frequency Counting Pattern:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_hashmap_frequency_pattern(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Hashmap / Frequency Counting Pattern in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Hashmap / Frequency Counting Pattern in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Hashmap / Frequency Counting Pattern', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Hashmap / Frequency Counting Pattern at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Hashmap / Frequency Counting Pattern uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Hashmap / Frequency Counting Pattern interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Hashmap / Frequency Counting Pattern is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Hashmap / Frequency Counting Pattern relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Hashmap / Frequency Counting Pattern
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Hashmap / Frequency Counting Pattern to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Hashmap / Frequency Counting Pattern fits in the data/control flow.
2. **Choose** the right variant/pattern of Hashmap / Frequency Counting Pattern for the scale requirement.
3. **Handle** failure modes: what happens when Hashmap / Frequency Counting Pattern is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Hashmap / Frequency Counting Pattern is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
