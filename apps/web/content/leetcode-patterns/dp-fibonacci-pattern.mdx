---
slug: "dp-fibonacci-pattern"
title: "DP: The Fibonacci Pattern"
level: 4
---

## WHY
The Fibonacci sequence ($0, 1, 1, 2, 3, 5, 8...$) is the "Hello World" of Dynamic Programming. 
Every algorithm student is taught to solve it recursively: `fib(n) = fib(n-1) + fib(n-2)`. 

While this mathematically works, it is computationally disastrous. To calculate `fib(5)`, the computer recalculates `fib(2)` three separate times. For `fib(50)`, it performs trillions of redundant calculations, completely freezing the browser. By applying the **Fibonacci DP Pattern**, we learn how to eliminate redundant work and solve sequential stepping problems in $O(N)$ time.

## THEORY
### Overlapping Subproblems
Dynamic Programming requires two things:
1. **Optimal Substructure:** The main problem can be solved by combining the solutions to smaller subproblems.
2. **Overlapping Subproblems:** The exact same subproblems are asked multiple times. 

If a problem asks you to calculate the $N^{th}$ step based on the $(N-1)$ and $(N-2)$ steps, it belongs to the Fibonacci family.

## VISUALIZATION_CONFIG
```json
{
  "steps": [
    {
      "title": "DP Fundamentals",
      "description": "Dynamic Programming = recursion + memoization or iteration with a table. Overlapping subproblems + optimal substructure.",
      "code": "// Fibonacci: 3 versions\n\n// 1. Naive recursion: O(2^n) 💥\nfunction fibSlow(n) {\n  if (n < 2) return n;\n  return fibSlow(n-1) + fibSlow(n-2);\n}\n\n// 2. Top-down memoization: O(n)\nfunction fibMemo(n, memo = {}) {\n  if (n < 2) return n;\n  if (memo[n]) return memo[n];\n  return memo[n] = fibMemo(n-1, memo) + fibMemo(n-2, memo);\n}\n\n// 3. Bottom-up: O(n) time, O(1) space\nfunction fib(n) {\n  let a = 0, b = 1;\n  for (let i = 2; i <= n; i++) [a, b] = [b, a + b];\n  return n < 2 ? n : b;\n}",
      "highlight": [
        4,
        5,
        6,
        10,
        11,
        12,
        13,
        18,
        19,
        20
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "Naive: exponential",
          "Memoize: O(n)",
          "Bottom-up: O(1) space",
          "Optimal",
          "Same time"
        ]
      }
    },
    {
      "title": "Climbing Stairs",
      "description": "Ways to reach top with 1 or 2 steps = Fibonacci sequence.",
      "code": "// LC 70: Climbing Stairs\nfunction climbStairs(n) {\n  if (n < 3) return n;\n  let prev2 = 1, prev1 = 2;\n  for (let i = 3; i <= n; i++) {\n    const curr = prev1 + prev2;\n    prev2 = prev1;\n    prev1 = curr;\n  }\n  return prev1;\n}\n\n// Recurrence: dp[i] = dp[i-1] + dp[i-2]\n// Base: dp[1]=1, dp[2]=2\n// n=5: 1,2,3,5,8 → 8 ways",
      "highlight": [
        3,
        4,
        5,
        6,
        7,
        12,
        13,
        14
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "dp[i] = dp[i-1] + dp[i-2]",
          "From steps of 1 or 2",
          "Same as Fibonacci",
          "O(1) space",
          "O(n) time"
        ]
      }
    },
    {
      "title": "House Robber",
      "description": "Cannot rob adjacent houses — choose max between (rob current + skip prev) or (skip current).",
      "code": "// LC 198: House Robber\nfunction rob(nums) {\n  let prev2 = 0, prev1 = 0;\n  for (const n of nums) {\n    const curr = Math.max(prev1, prev2 + n);\n    prev2 = prev1;\n    prev1 = curr;\n  }\n  return prev1;\n}\n\n// Recurrence: dp[i] = max(dp[i-1], dp[i-2] + nums[i])\n// LC 213 House Robber II: circular — run twice\n// max(rob(0..n-2), rob(1..n-1))",
      "highlight": [
        3,
        4,
        5,
        6,
        7,
        12,
        13
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "dp[i] = max(dp[i-1], dp[i-2]+n)",
          "Skip current",
          "Or rob + skip prev",
          "Max carries forward",
          "O(1) space"
        ]
      }
    },
    {
      "title": "Min Cost Climbing Stairs",
      "description": "Reach top with minimum cost — choose min of (1 step, 2 steps).",
      "code": "// LC 746: Min Cost Climbing Stairs\nfunction minCostClimbingStairs(cost) {\n  let prev2 = 0, prev1 = 0;\n  for (let i = 2; i <= cost.length; i++) {\n    const curr = Math.min(\n      prev1 + cost[i - 1],\n      prev2 + cost[i - 2]\n    );\n    prev2 = prev1;\n    prev1 = curr;\n  }\n  return prev1;\n}\n\n// Recurrence: dp[i] = min(dp[i-1] + cost[i-1], dp[i-2] + cost[i-2])",
      "highlight": [
        3,
        4,
        5,
        6,
        7,
        8,
        14
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "dp[i] = min path to top",
          "Cost of last step +",
          "min(prev1, prev2)",
          "Reach step n",
          "Total min cost"
        ]
      }
    },
    {
      "title": "Decode Ways",
      "description": "Count ways to decode a digit string — like Fibonacci with constraints.",
      "code": "// LC 91: Decode Ways\nfunction numDecodings(s) {\n  if (s[0] === '0') return 0;\n  let prev2 = 1, prev1 = 1;\n  for (let i = 1; i < s.length; i++) {\n    let curr = 0;\n    if (s[i] !== '0') curr += prev1;\n    const two = parseInt(s.slice(i-1, i+1));\n    if (two >= 10 && two <= 26) curr += prev2;\n    prev2 = prev1;\n    prev1 = curr;\n  }\n  return prev1;\n}\n\n// dp[i] = dp[i-1] (if s[i] != '0') + dp[i-2] (if s[i-1..i] in 10..26)",
      "highlight": [
        3,
        5,
        6,
        7,
        8,
        9,
        14,
        15
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          "One char valid? +dp[i-1]",
          "Two chars in [10,26]? +dp[i-2]",
          "Sum ways",
          "Fibonacci variant",
          "O(1) space"
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner (Climbing Stairs)
*Problem: You are climbing a staircase. It takes `n` steps to reach the top. Each time you can either climb 1 or 2 steps. In how many distinct ways can you climb to the top?*

```javascript
function climbStairs(n) {
    // Edge cases
    if (n === 1) return 1;
    if (n === 2) return 2;
    
    // dp[i] represents the number of ways to reach step 'i'
    const dp = new Array(n + 1).fill(0);
    
    // Base cases (You can hardcode the first few steps)
    dp[1] = 1;
    dp[2] = 2;
    
    for (let i = 3; i <= n; i++) {
        // The core Fibonacci logic:
        // To get to step 3, you either stepped from step 2, or you jumped from step 1!
        dp[i] = dp[i - 1] + dp[i - 2];
    }
    
    return dp[n];
}
```

### Level 2 — Building Blocks (Space Optimization)
If you look closely at Level 1, to calculate `dp[10]`, we ONLY need `dp[9]` and `dp[8]`. We do not care about `dp[1]` or `dp[2]` anymore. 
Creating a massive array of size $N$ consumes $O(N)$ memory. We can optimize this to $O(1)$ constant space by just using two variables!

```javascript
function climbStairsOptimized(n) {
    if (n === 1) return 1;
    if (n === 2) return 2;
    
    let twoStepsBehind = 1; // dp[1]
    let oneStepBehind = 2;  // dp[2]
    
    for (let i = 3; i <= n; i++) {
        let current = oneStepBehind + twoStepsBehind;
        
        // Shift the pointers forward for the next loop iteration!
        twoStepsBehind = oneStepBehind;
        oneStepBehind = current;
    }
    
    return oneStepBehind;
}
```

### Level 3 — Intermediate (House Robber)
*Problem: You are a professional robber. Each house has a certain amount of money. The only constraint is that you cannot rob two adjacent houses. Find the maximum amount of money you can rob tonight.*
*(Example: `[1, 2, 3, 1]` -> Rob house 1 and 3 -> `1 + 3 = 4`)*

This is Fibonacci with a twist. The decision isn't just adding the previous two steps; it requires picking the Maximum value!

```javascript
function rob(nums) {
    if (nums.length === 0) return 0;
    if (nums.length === 1) return nums[0];
    
    // dp[i] represents the max money we can have when standing at house 'i'
    const dp = new Array(nums.length).fill(0);
    
    // Base Cases
    dp[0] = nums[0];
    dp[1] = Math.max(nums[0], nums[1]);
    
    for (let i = 2; i < nums.length; i++) {
        // The Decision:
        // Option 1: Rob the current house (must add money from 2 houses ago)
        // Option 2: Skip the current house (keep the money from the house right next door)
        dp[i] = Math.max(
            nums[i] + dp[i - 2], 
            dp[i - 1]
        );
    }
    
    return dp[nums.length - 1];
}
```

### Level 4 — Advanced (Decode Ways)
*Problem: A message containing letters from A-Z can be encoded into numbers (`A -> 1`, `B -> 2`, ... `Z -> 26`). Given a string of digits, return the number of ways to decode it.*
*(Example: `"226"` can be `"BZ" (2, 26)`, `"VF" (22, 6)`, or `"BBF" (2, 2, 6)`. Answer: 3)*

```javascript
function numDecodings(s) {
    // If the string starts with a '0', it's instantly invalid (no letter maps to 0)
    if (s == null || s.length === 0 || s[0] === '0') return 0;
    
    const n = s.length;
    // dp[i] represents the number of ways to decode a string of length 'i'
    const dp = new Array(n + 1).fill(0);
    
    // Base cases
    dp[0] = 1; // There is 1 way to decode an empty string
    dp[1] = 1; // We already checked that s[0] isn't '0'
    
    for (let i = 2; i <= n; i++) {
        // Single digit decoding (e.g. "6" -> "F")
        // Get the character at the current position (i-1)
        const singleDigit = parseInt(s.substring(i - 1, i));
        if (singleDigit >= 1 && singleDigit <= 9) {
            // We can form a valid letter! Inherit the branches from 1 step back.
            dp[i] += dp[i - 1];
        }
        
        // Double digit decoding (e.g. "26" -> "Z")
        const doubleDigit = parseInt(s.substring(i - 2, i));
        if (doubleDigit >= 10 && doubleDigit <= 26) {
            // We can form a valid letter! Inherit the branches from 2 steps back.
            dp[i] += dp[i - 2];
        }
    }
    
    return dp[n];
}
```

## REAL_WORLD
**Algorithmic Trading & State Machines**
In quantitative finance, algorithms decide whether to BUY, SELL, or HOLD a stock on any given day based on complex historical states. A variation of this DP pattern (State Machine DP, like the famous "Best Time to Buy and Sell Stock with Cooldown" problem) is exactly how basic algorithmic trading systems map out the maximum possible profit trajectory over a massive time series of price data.

## INTERVIEW
**Q: If I use recursion with Memoization (Top-Down) instead of a `for` loop (Bottom-Up Tabulation), is it worse?**
A: **Technically, yes, but in an interview, no.** 
Top-Down Recursion + Memoization has the exact same Big-O Time and Space complexity ($O(N)$) as Tabulation. However, recursion requires the Operating System to add a new frame to the Call Stack for every single function call. For $N = 100,000$, this will trigger a "Maximum Call Stack Size Exceeded" error (Stack Overflow) in languages like JavaScript and Python. Tabulation (the `for` loop) operates entirely on the heap, avoiding call stack limits entirely. 
That being said, if recursion makes more sense to your brain during a stressful interview, *always* write the Memoized recursive solution first. It proves you understand the DP logic. You can optimize it to Tabulation if you have time remaining.

## FEYNMAN CHECK

### Explain DP: The Fibonacci Pattern Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **DP: The Fibonacci Pattern** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about DP: The Fibonacci Pattern, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does DP: The Fibonacci Pattern solve? Why would JavaScript/web development be harder without it?**
> **A:** DP: The Fibonacci Pattern solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about DP: The Fibonacci Pattern?**
> **A:** The single most important thing is understanding the *mental model*: DP: The Fibonacci Pattern operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with DP: The Fibonacci Pattern and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which DP: The Fibonacci Pattern operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does DP: The Fibonacci Pattern interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** DP: The Fibonacci Pattern interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of DP: The Fibonacci Pattern to a senior engineer in two sentences, what would you say?**
> **A:** "DP: The Fibonacci Pattern works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a DP: The Fibonacci Pattern Utility

**Goal:** Implement a real, working DP: The Fibonacci Pattern module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `dpfibonaccipattern` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir dp-fibonacci-pattern-demo && cd dp-fibonacci-pattern-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── DP: The Fibonacci Pattern Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of DP: The Fibonacci Pattern.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createDP:TheFibonacciPattern(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[DP: The Fibonacci Pattern] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual DP: The Fibonacci Pattern logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[DP: The Fibonacci Pattern] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[DP: The Fibonacci Pattern] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createDP:TheFibonacciPattern({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[DP: The Fibonacci Pattern] Input cannot be null or undefined`);
    }
    const instance = createDP:TheFibonacciPattern();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[DP: The Fibonacci Pattern] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── DP: The Fibonacci Pattern Tests ──');

test('initialises correctly', () => {
  const m = createDP:TheFibonacciPattern();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createDP:TheFibonacciPattern();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[DP: The Fibonacci Pattern] Initialized with: hello world
[DP: The Fibonacci Pattern] Processed: hello world
Result: hello world
[DP: The Fibonacci Pattern] Destroyed.

── DP: The Fibonacci Pattern Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define DP: The Fibonacci Pattern in one sentence.**
<details><summary>Answer</summary>

DP: The Fibonacci Pattern is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of DP: The Fibonacci Pattern?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of DP: The Fibonacci Pattern.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between DP: The Fibonacci Pattern and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | DP: The Fibonacci Pattern | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of DP: The Fibonacci Pattern] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing DP: The Fibonacci Pattern causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct DP: The Fibonacci Pattern usage].
</details>

**Q6: What does DP: The Fibonacci Pattern look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, DP: The Fibonacci Pattern typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use DP: The Fibonacci Pattern correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using DP: The Fibonacci Pattern:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_dp_fibonacci_pattern(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test DP: The Fibonacci Pattern in isolation? What would you mock?**
<details><summary>Answer</summary>

To test DP: The Fibonacci Pattern in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('DP: The Fibonacci Pattern', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of DP: The Fibonacci Pattern at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how DP: The Fibonacci Pattern uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common DP: The Fibonacci Pattern interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "DP: The Fibonacci Pattern is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does DP: The Fibonacci Pattern relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
DP: The Fibonacci Pattern
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use DP: The Fibonacci Pattern to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where DP: The Fibonacci Pattern fits in the data/control flow.
2. **Choose** the right variant/pattern of DP: The Fibonacci Pattern for the scale requirement.
3. **Handle** failure modes: what happens when DP: The Fibonacci Pattern is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you DP: The Fibonacci Pattern is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
