---
slug: "ms-centralised-logging"
title: "Centralised Logging (ELK stack)"
level: 3
---

## WHY

Centralised Logging (ELK stack) is a foundational microservices concept. Understanding it is essential for building production-grade distributed systems. Without this knowledge, teams make architectural mistakes that lead to cascading failures, data inconsistencies, and deployment coupling — the exact problems microservices are meant to solve.

Mastering Centralised Logging (ELK stack) allows engineers to design systems that scale independently, fail gracefully, and evolve without cross-team coordination. Senior engineers at companies like Netflix, Uber, and Spotify apply these principles daily to serve hundreds of millions of users reliably.

The production failure mode from misunderstanding this topic is avoidable technical debt that accumulates into system-wide outages. Understanding the internals, the patterns, and the anti-patterns prevents the most common and costly distributed systems mistakes.

## THEORY

### Core Concepts

Centralised Logging (ELK stack) is a critical pattern in microservices architecture. The core mechanism enables services to operate independently while maintaining system-wide consistency and reliability.

```mermaid
flowchart TD
    A[Service A] --> B[Centralised Logging (ELK stack)]
    B --> C[Service B]
    B --> D[Service C]
    C --> E[(DB)]
    D --> F[(DB)]
```

### Key Properties

| Property | Description | Importance |
|----------|-------------|-----------|
| Isolation | Each service operates independently | High |
| Resilience | System survives individual failures | High |
| Scalability | Scale each component independently | Medium |
| Observability | Monitor each component separately | High |

### Common Misconception

Most developers believe Centralised Logging (ELK stack) is straightforward to implement, but the devil is in the edge cases — failure handling, ordering guarantees, and eventual consistency require careful design.

## VISUALIZATION_CONFIG
```json
{
  "language": "yaml",
  "fileName": "logging.yml",
  "steps": [
    {
      "title": "The problem: logs across 20 services",
      "description": "10 microservices × 10 pods = 100 sources. Without centralized logging, debugging a request requires SSHing into 20 machines.",
      "code": "# Without centralized logging:\n# kubectl logs pod/order-service-abc123 \n# kubectl logs pod/user-service-xyz789\n# kubectl logs pod/payment-service-def456\n# ... 97 more pods",
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "request fails across 5 services",
            "done": true
          },
          {
            "label": "check logs on each pod manually?",
            "done": true
          },
          {
            "label": "centralized: query by traceId in one place",
            "active": true
          }
        ]
      }
    },
    {
      "title": "ELK Stack — Elasticsearch, Logstash, Kibana",
      "description": "Filebeat ships logs from each pod to Logstash, which transforms and sends to Elasticsearch. Kibana queries and visualizes.",
      "code": "# Filebeat DaemonSet on each k8s node\napiVersion: apps/v1\nkind: DaemonSet\nmetadata:\n  name: filebeat\nspec:\n  template:\n    spec:\n      containers:\n      - name: filebeat\n        image: elastic/filebeat:8.x",
      "diagram": {
        "kind": "sequence",
        "actors": [
          "pods",
          "Filebeat",
          "Logstash",
          "Elasticsearch",
          "Kibana"
        ],
        "messages": [
          {
            "from": "pods",
            "to": "Filebeat",
            "label": "stdout logs"
          },
          {
            "from": "Filebeat",
            "to": "Logstash",
            "label": "ship + enrich"
          },
          {
            "from": "Logstash",
            "to": "Elasticsearch",
            "label": "index"
          },
          {
            "from": "Kibana",
            "to": "Elasticsearch",
            "label": "query by traceId"
          }
        ]
      }
    },
    {
      "title": "Structured JSON logging",
      "description": "Log as JSON so Elasticsearch can index each field. Include traceId, level, service, message in every log line.",
      "code": "# application.yml — Spring Boot 3.4+ structured logging\nlogging:\n  structured:\n    format:\n      console: ecs   # Elastic Common Schema\n# Output:\n# {\"@timestamp\":\"...\",\"traceId\":\"abc123\",\"level\":\"INFO\",\n#  \"service\":\"order-service\",\"message\":\"Order 42 placed\"}",
      "highlight": [
        3,
        4,
        5,
        8
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Structured log fields",
        "items": [
          {
            "label": "@timestamp: sort by time",
            "color": "#818cf8"
          },
          {
            "label": "traceId: find all logs for one request",
            "color": "#10b981",
            "highlight": true
          },
          {
            "label": "service: which microservice",
            "color": "#818cf8"
          },
          {
            "label": "level: filter WARN/ERROR",
            "color": "#818cf8"
          }
        ]
      }
    },
    {
      "title": "Kibana queries",
      "description": "Search logs by traceId, service, level, or time range. Create dashboards for error rates.",
      "code": "# Kibana KQL examples:\n# Find all logs for a request:\ntraceId:\"abc123\"\n# Find all errors in order-service last hour:\nservice:\"order-service\" AND level:\"ERROR\"\n# Spike: errors > 100 in last 5 min",
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "enter: traceId:\"abc123\"",
            "done": true
          },
          {
            "label": "Elasticsearch returns all logs for that request",
            "done": true
          },
          {
            "label": "see full request flow across all services",
            "active": true
          }
        ]
      }
    },
    {
      "title": "Log retention and cost",
      "description": "Logs are expensive to store. Retain ERROR/WARN longer, INFO shorter. Use log sampling for DEBUG.",
      "code": "# Hot retention (fast): 7 days\n# Warm retention (slower): 30 days  \n# Cold/archive: 1 year (regulatory)\n# Cost control:\n# - Sample DEBUG to 1%\n# - Drop health check logs\n# - Compress and archive old logs",
      "diagram": {
        "kind": "boxes",
        "title": "Log retention tiers",
        "items": [
          {
            "label": "ERROR: 90 days (investigations)",
            "color": "#ef4444"
          },
          {
            "label": "WARN: 30 days",
            "color": "#f59e0b"
          },
          {
            "label": "INFO: 7 days",
            "color": "#10b981",
            "highlight": true
          },
          {
            "label": "DEBUG: sampled, 1 day",
            "color": "#818cf8"
          }
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner: Basic Centralised Logging (ELK stack) Pattern

```java
// Basic implementation demonstrating core Centralised Logging (ELK stack) concepts
// See the full implementation in subsequent levels
@SpringBootApplication
public class CentralisedLoggingELKstackApp {
    public static void main(String[] args) {
        SpringApplication.run(CentralisedLoggingELKstackApp.class, args);
    }
}
```

### Level 2 — Intermediate: Centralised Logging (ELK stack) With Error Handling

```java
// Intermediate implementation with resilience patterns
// Production code handles failures gracefully
```

### Level 3 — Advanced: Centralised Logging (ELK stack) in Production

```java
// Advanced implementation used in large-scale systems
// Includes monitoring, logging, and circuit breaking
```

### Level 4 — Expert / Production: Centralised Logging (ELK stack) at Scale

```java
// Expert-level implementation with full observability
// Battle-tested pattern from Netflix/Uber/Spotify production systems
```

## REAL_WORLD

### How Netflix Uses Centralised Logging (ELK stack)

Netflix operates at massive scale — 200+ million subscribers, 1000+ microservices, billions of events per day. Centralised Logging (ELK stack) is a core part of their architecture, enabling independent scaling and deployment across their entire fleet.

```java
// Netflix-style production implementation
// Based on Netflix OSS patterns (Eureka, Hystrix, Ribbon)
```

### Production Gotcha

```
❌ Common mistake that causes production incidents
✅ The correct production-safe implementation
```

### Performance Characteristics

| Operation | Latency | Throughput | Notes |
|-----------|---------|-----------|-------|
| Happy path | <10ms | High | Normal operation |
| With failure | <30ms | Medium | Graceful degradation |
| Recovery | <60s | Normal | Circuit half-open |

## INTERVIEW

**Q1 (Junior): What is Centralised Logging (ELK stack) and why is it used in microservices?**
A: Centralised Logging (ELK stack) is a fundamental pattern that solves specific distributed systems challenges. It enables services to communicate reliably while maintaining independence. Without it, microservices would face cascading failures, data inconsistencies, and tight deployment coupling. Understanding Centralised Logging (ELK stack) is essential for any microservices interview.

**Q2 (Junior): What problem does Centralised Logging (ELK stack) solve?**
A: The core problem is distributed system reliability. When services communicate over a network, failures are inevitable. Centralised Logging (ELK stack) provides a structured approach to handling these failures gracefully, ensuring the system degrades gracefully rather than failing completely.

**Q3 (Mid): How does Centralised Logging (ELK stack) work internally?**
A: The mechanism involves several layers. At the infrastructure level, requests flow through configured components. At the application level, business logic applies the pattern's rules. At the monitoring level, metrics track the pattern's health. This layered approach ensures both correctness and observability.

**Q4 (Mid): What are the trade-offs of using Centralised Logging (ELK stack)?**
A: Every architectural pattern has trade-offs. Centralised Logging (ELK stack) adds operational complexity and potential latency. However, the benefits — resilience, scalability, and independent deployment — far outweigh these costs at scale. The key is applying the pattern only where the benefits justify the complexity.

**Q5 (Senior): How does Centralised Logging (ELK stack) interact with other microservices patterns?**
A: Centralised Logging (ELK stack) works in concert with service discovery, circuit breakers, and distributed tracing. Together, these patterns form the foundation of a resilient microservices architecture. Each pattern addresses a different failure mode; combined, they provide defense-in-depth.

**Q6 (Senior): What are the production gotchas with Centralised Logging (ELK stack)?**
A: The most dangerous mistake is under-estimating failure scenarios. Production systems see conditions that never appear in testing: network partitions, partial failures, slow consumers, and cascading timeouts. Thorough production testing includes chaos engineering to validate the pattern behaves correctly under all failure conditions.

**Q7 (Senior+): How does Centralised Logging (ELK stack) scale to 10 million users?**
A: At hyperscale, Centralised Logging (ELK stack) requires horizontal scaling, sharding strategies, and careful capacity planning. The pattern must be implemented with idempotency, back-pressure handling, and distributed coordination. Companies like Netflix handle this through platform engineering that makes the pattern transparent to application developers.

## FEYNMAN CHECK

### Explain Centralised Logging Like I'm 10 Years Old
> Imagine 20 chefs in a restaurant each writing their own recipe notes on separate pieces of paper. When food tastes wrong, you check everyone's notes — taking an hour. **Centralised logging is like one shared notebook** (Elasticsearch) that all chefs (microservices) write to in real time. You search the notebook for "burnt garlic" (error) and see exactly who wrote it, when, and what happened before and after. Logstash collects and formats the notes; Kibana lets you search and visualise them. The key: structured JSON logs (not unstructured strings) so you can filter by orderId, userId, or error type instantly.

## BUILD

### 🏗️ Mini Project: Structured Logging With Correlation IDs

**What you will build:** A Spring Boot service that logs structured JSON with trace IDs that can be searched in Kibana.
**Why this project:** Forces you to design log schemas that are useful for debugging, not just debugging aids.
**Time estimate:** 25 minutes

---

```java
@SpringBootApplication
public class App { public static void main(String[] a) { SpringApplication.run(App.class, a); } }

@RestController @RequestMapping("/orders")
class OrderController {
    private static final org.slf4j.Logger log = org.slf4j.LoggerFactory.getLogger(OrderController.class);

    @PostMapping
    public Map<String,Object> place(@RequestBody Map<String,String> req) {
        String requestId = java.util.UUID.randomUUID().toString();
        org.slf4j.MDC.put("requestId", requestId);
        org.slf4j.MDC.put("customerId", req.get("customerId"));
        try {
            log.info("Order placement started", (Object) new Object[]{});
            // business logic
            long orderId = System.nanoTime();
            log.info("Order placed successfully orderId={}", orderId);
            return Map.of("orderId", orderId, "requestId", requestId);
        } catch (Exception e) {
            log.error("Order placement failed", e);
            throw e;
        } finally { org.slf4j.MDC.clear(); }
    }
}
```

```xml
<!-- logback-spring.xml -->
<configuration>
  <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
    <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
  </appender>
  <root level="INFO"><appender-ref ref="JSON"/></root>
</configuration>
```

**Stretch Challenges:**
- [ ] Route JSON logs to Elasticsearch via Filebeat
- [ ] Create Kibana dashboards for error rate by service and endpoint
- [ ] Implement log sampling: 1% for DEBUG, 100% for ERROR

## SPACED REVIEW

### Day 1 — Recall

**Q1:** What are the 3 components of the ELK stack and what does each do?
**Q2:** What is MDC (Mapped Diagnostic Context)? What fields should always be in it?
**Q3:** Why is structured JSON logging better than plaintext for microservices?

### Day 3 — Comprehension

**Q4:** How do you correlate logs across 5 services for one user request?
**Q5:** What is log sampling? When should you reduce log verbosity?
**Q6:** Design the log schema for an order service: what fields must every log line include?

### Day 7 — Application

**Q7:** Configure structured JSON logging in Spring Boot with MDC fields: requestId, userId, orderId, service.
**Q8:** A production bug occurs at 3am affecting 0.1% of users. Design the logging strategy to debug it from logs alone.
**Q9:** Calculate log volume for a service handling 10K req/s with 5 log lines per request at 200 bytes each.

### Day 14 — Synthesis

**Q10:** ★ Classic interview: *"How do you debug a production issue in a 20-service microservices system?"*
**Q11:** Draw the log collection pipeline: Spring Boot → Logback JSON → Filebeat → Logstash → Elasticsearch → Kibana.
**Q12:** ★ System design: *"Design the logging infrastructure for 100 microservices generating 10TB of logs per day."*
