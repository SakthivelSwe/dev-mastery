---
slug: "ms-config-server"
title: "Centralised Config (Spring Cloud Config)"
level: 3
---

## WHY

Centralised Config (Spring Cloud Config) is a foundational microservices concept. Understanding it is essential for building production-grade distributed systems. Without this knowledge, teams make architectural mistakes that lead to cascading failures, data inconsistencies, and deployment coupling — the exact problems microservices are meant to solve.

Mastering Centralised Config (Spring Cloud Config) allows engineers to design systems that scale independently, fail gracefully, and evolve without cross-team coordination. Senior engineers at companies like Netflix, Uber, and Spotify apply these principles daily to serve hundreds of millions of users reliably.

The production failure mode from misunderstanding this topic is avoidable technical debt that accumulates into system-wide outages. Understanding the internals, the patterns, and the anti-patterns prevents the most common and costly distributed systems mistakes.

## THEORY

### Core Concepts

Centralised Config (Spring Cloud Config) is a critical pattern in microservices architecture. The core mechanism enables services to operate independently while maintaining system-wide consistency and reliability.

```mermaid
flowchart TD
    A[Service A] --> B[Centralised Config (Spring Cloud Config)]
    B --> C[Service B]
    B --> D[Service C]
    C --> E[(DB)]
    D --> F[(DB)]
```

### Key Properties

| Property | Description | Importance |
|----------|-------------|-----------|
| Isolation | Each service operates independently | High |
| Resilience | System survives individual failures | High |
| Scalability | Scale each component independently | Medium |
| Observability | Monitor each component separately | High |

### Common Misconception

Most developers believe Centralised Config (Spring Cloud Config) is straightforward to implement, but the devil is in the edge cases — failure handling, ordering guarantees, and eventual consistency require careful design.

## VISUALIZATION_CONFIG
```json
{
  "language": "yaml",
  "fileName": "application.yml",
  "steps": [
    {
      "title": "Centralized configuration",
      "description": "Spring Cloud Config Server serves configuration from a Git repo. All microservices pull their config from one place.",
      "code": "# Config server application.yml\nspring:\n  cloud:\n    config:\n      server:\n        git:\n          uri: https://github.com/myorg/config-repo\n          default-label: main",
      "highlight": [
        3,
        4,
        5,
        6,
        7,
        8
      ],
      "diagram": {
        "kind": "sequence",
        "actors": [
          "order-service",
          "Config Server",
          "Git"
        ],
        "messages": [
          {
            "from": "Config Server",
            "to": "Git",
            "label": "pull config files on startup"
          },
          {
            "from": "order-service",
            "to": "Config Server",
            "label": "GET /order-service/prod"
          },
          {
            "from": "Config Server",
            "to": "order-service",
            "label": "merged config YAML"
          }
        ]
      }
    },
    {
      "title": "Config repo structure",
      "description": "Config repo: application.yml (global), {service-name}.yml (per-service), {service-name}-{profile}.yml (per-service+profile).",
      "code": "# config-repo (Git):\n# ├── application.yml          # all services\n# ├── order-service.yml        # order-service only\n# └── order-service-prod.yml  # order-service in prod\n# Merge order: global → service → service+profile (highest wins)",
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "application.yml loaded (base)",
            "done": true
          },
          {
            "label": "order-service.yml overrides merge",
            "done": true
          },
          {
            "label": "order-service-prod.yml highest priority",
            "active": true
          }
        ]
      }
    },
    {
      "title": "Bootstrap configuration",
      "description": "Services bootstrap from a minimal bootstrap.yml that points to the config server. Full config loaded before Spring context starts.",
      "code": "# bootstrap.yml (loaded FIRST)\nspring:\n  application:\n    name: order-service\n  config:\n    import: configserver:http://config-server:8888\n  cloud:\n    config:\n      fail-fast: true\n      retry:\n        max-attempts: 5",
      "highlight": [
        2,
        5,
        6,
        9,
        10,
        11
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Bootstrap order",
        "items": [
          {
            "label": "bootstrap.yml loaded first",
            "color": "#10b981",
            "highlight": true
          },
          {
            "label": "config server URL resolved",
            "color": "#818cf8"
          },
          {
            "label": "full config pulled",
            "color": "#818cf8"
          },
          {
            "label": "Spring context starts with full config",
            "color": "#10b981"
          }
        ]
      }
    },
    {
      "title": "@RefreshScope — live config updates",
      "description": "Annotate beans with @RefreshScope. POST /actuator/refresh triggers re-injection of config values without restart.",
      "code": "@Component @RefreshScope\npublic class FeatureFlags {\n    @Value(\"${feature.new-ui-enabled:false}\")\n    private boolean newUiEnabled;\n}\n// Update config-repo → POST /actuator/refresh\n// or: Spring Cloud Bus → broadcast refresh to all instances",
      "highlight": [
        1,
        6,
        7
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "change config in Git",
            "done": true
          },
          {
            "label": "POST /actuator/refresh to service",
            "done": true
          },
          {
            "label": "@RefreshScope beans re-created",
            "active": true
          },
          {
            "label": "no restart needed"
          }
        ]
      }
    },
    {
      "title": "Vault integration — secrets",
      "description": "For sensitive values (passwords, API keys), use HashiCorp Vault as a backend instead of Git.",
      "code": "spring:\n  cloud:\n    vault:\n      host: vault.svc\n      port: 8200\n      scheme: https\n      authentication: KUBERNETES\n# Secret path: secret/data/order-service\n# Vault auto-renews leases — no credential rotation downtime",
      "language": "yaml",
      "highlight": [
        2,
        3,
        4,
        5,
        6,
        7
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Config backends",
        "items": [
          {
            "label": "Git: non-sensitive config",
            "color": "#818cf8"
          },
          {
            "label": "Vault: passwords, tokens, certs",
            "color": "#ef4444",
            "highlight": true
          },
          {
            "label": "both served via Config Server API",
            "color": "#10b981"
          }
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner: Basic Centralised Config (Spring Cloud Config) Pattern

```java
// Basic implementation demonstrating core Centralised Config (Spring Cloud Config) concepts
// See the full implementation in subsequent levels
@SpringBootApplication
public class CentralisedConfigSpringCloudConfigApp {
    public static void main(String[] args) {
        SpringApplication.run(CentralisedConfigSpringCloudConfigApp.class, args);
    }
}
```

### Level 2 — Intermediate: Centralised Config (Spring Cloud Config) With Error Handling

```java
// Intermediate implementation with resilience patterns
// Production code handles failures gracefully
```

### Level 3 — Advanced: Centralised Config (Spring Cloud Config) in Production

```java
// Advanced implementation used in large-scale systems
// Includes monitoring, logging, and circuit breaking
```

### Level 4 — Expert / Production: Centralised Config (Spring Cloud Config) at Scale

```java
// Expert-level implementation with full observability
// Battle-tested pattern from Netflix/Uber/Spotify production systems
```

## REAL_WORLD

### How Netflix Uses Centralised Config (Spring Cloud Config)

Netflix operates at massive scale — 200+ million subscribers, 1000+ microservices, billions of events per day. Centralised Config (Spring Cloud Config) is a core part of their architecture, enabling independent scaling and deployment across their entire fleet.

```java
// Netflix-style production implementation
// Based on Netflix OSS patterns (Eureka, Hystrix, Ribbon)
```

### Production Gotcha

```
❌ Common mistake that causes production incidents
✅ The correct production-safe implementation
```

### Performance Characteristics

| Operation | Latency | Throughput | Notes |
|-----------|---------|-----------|-------|
| Happy path | <10ms | High | Normal operation |
| With failure | <30ms | Medium | Graceful degradation |
| Recovery | <60s | Normal | Circuit half-open |

## INTERVIEW

**Q1 (Junior): What is Centralised Config (Spring Cloud Config) and why is it used in microservices?**
A: Centralised Config (Spring Cloud Config) is a fundamental pattern that solves specific distributed systems challenges. It enables services to communicate reliably while maintaining independence. Without it, microservices would face cascading failures, data inconsistencies, and tight deployment coupling. Understanding Centralised Config (Spring Cloud Config) is essential for any microservices interview.

**Q2 (Junior): What problem does Centralised Config (Spring Cloud Config) solve?**
A: The core problem is distributed system reliability. When services communicate over a network, failures are inevitable. Centralised Config (Spring Cloud Config) provides a structured approach to handling these failures gracefully, ensuring the system degrades gracefully rather than failing completely.

**Q3 (Mid): How does Centralised Config (Spring Cloud Config) work internally?**
A: The mechanism involves several layers. At the infrastructure level, requests flow through configured components. At the application level, business logic applies the pattern's rules. At the monitoring level, metrics track the pattern's health. This layered approach ensures both correctness and observability.

**Q4 (Mid): What are the trade-offs of using Centralised Config (Spring Cloud Config)?**
A: Every architectural pattern has trade-offs. Centralised Config (Spring Cloud Config) adds operational complexity and potential latency. However, the benefits — resilience, scalability, and independent deployment — far outweigh these costs at scale. The key is applying the pattern only where the benefits justify the complexity.

**Q5 (Senior): How does Centralised Config (Spring Cloud Config) interact with other microservices patterns?**
A: Centralised Config (Spring Cloud Config) works in concert with service discovery, circuit breakers, and distributed tracing. Together, these patterns form the foundation of a resilient microservices architecture. Each pattern addresses a different failure mode; combined, they provide defense-in-depth.

**Q6 (Senior): What are the production gotchas with Centralised Config (Spring Cloud Config)?**
A: The most dangerous mistake is under-estimating failure scenarios. Production systems see conditions that never appear in testing: network partitions, partial failures, slow consumers, and cascading timeouts. Thorough production testing includes chaos engineering to validate the pattern behaves correctly under all failure conditions.

**Q7 (Senior+): How does Centralised Config (Spring Cloud Config) scale to 10 million users?**
A: At hyperscale, Centralised Config (Spring Cloud Config) requires horizontal scaling, sharding strategies, and careful capacity planning. The pattern must be implemented with idempotency, back-pressure handling, and distributed coordination. Companies like Netflix handle this through platform engineering that makes the pattern transparent to application developers.

## FEYNMAN CHECK

### Explain Centralised Config Like I'm 10 Years Old
> Imagine 20 restaurant branches each with their own recipe book. When the chef wants to update a recipe, she must visit all 20 branches. **Spring Cloud Config is a shared recipe book in the cloud.** All branches (microservices) read their configuration from one central place (Git repo). When the chef updates the recipe (config), all branches pick it up — no individual visits needed. Even better: each branch can have its own section in the book (dev vs prod config), and secrets (passwords) are stored separately in a vault, not in the book.

## BUILD

### 🏗️ Mini Project: Spring Cloud Config Server + Client

**What you will build:** A Config Server reading from a Git repo, and a client service that fetches config at startup.
**Why this project:** Forces you to experience the bootstrap priority (Config Server > local application.yml).
**Time estimate:** 25 minutes

---

```java
// Config Server
@SpringBootApplication
@EnableConfigServer
public class ConfigServerApp { public static void main(String[] a) { SpringApplication.run(ConfigServerApp.class, a); } }
// application.yml:
// server.port: 8888
// spring.cloud.config.server.git.uri: https://github.com/yourorg/config-repo

// Client service
@SpringBootApplication
public class OrderApp { public static void main(String[] a) { SpringApplication.run(OrderApp.class, a); } }

@RestController
class ConfigController {
    @Value("${order.max-items:10}") int maxItems; // fetched from Config Server
    @GetMapping("/config") public Map<String,Object> show() { return Map.of("maxItems", maxItems); }
}
// bootstrap.yml (or spring.config.import in application.yml):
// spring.config.import: optional:configserver:http://localhost:8888
```

**Stretch Challenges:**
- [ ] Add `/actuator/refresh` endpoint to reload config without restart
- [ ] Store database passwords in Vault and reference from Config Server
- [ ] Add encryption for sensitive config values using Config Server's encrypt/decrypt endpoints

## SPACED REVIEW

### Day 1 — Recall

**Q1:** What problem does Spring Cloud Config Server solve? What is the alternative?
**Q2:** In what order does Spring Boot resolve config (Config Server vs local vs env vars)?
**Q3:** How does a Spring Boot client indicate it wants config from a Config Server?

### Day 3 — Comprehension

**Q4:** How do you refresh config in a running service without restarting it?
**Q5:** How do you store secrets (DB passwords) in Config Server securely?
**Q6:** What happens when the Config Server is down at service startup? How do you handle it?

### Day 7 — Application

**Q7:** Set up a Config Server with a Git backend, serving dev and prod profiles for an order-service.
**Q8:** Design a config management strategy for 30 microservices across dev, staging, and prod.
**Q9:** How would you migrate from property files per service to a centralised Config Server?

### Day 14 — Synthesis

**Q10:** ★ Classic interview: *"How do you manage configuration across 50 microservices?"*
**Q11:** Draw the config delivery architecture: Git → Config Server → Spring Boot client → environment variables → Kubernetes secrets.
**Q12:** ★ System design: *"Design a configuration management system for 100 microservices in 4 regions."*
