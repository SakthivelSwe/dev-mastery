---
slug: "docker-networking"
title: "Docker Networking"
level: 2
---

## WHY
Containers are isolated by default — they can't talk to each other or the outside world unless you explicitly configure networking.
When you run a web app container that needs to talk to a database container, you need to connect them. When you want users on the internet to access your container, you need to expose ports.
**Docker Networking** is the mechanism that controls how containers communicate with each other, with the host, and with the external network.

## THEORY
### Docker Network Drivers
1. **bridge (default):** Creates a virtual network on the host. Containers on the same bridge network can communicate using their container names as hostnames. The default network for containers without a specified network.
2. **host:** Removes network isolation — container uses the host's network directly. No port mapping needed, but no isolation. Linux only.
3. **overlay:** Connects containers across multiple Docker hosts (for Docker Swarm / distributed systems).
4. **none:** Completely disables networking. For maximum isolation or batch jobs that don't need network.

### Custom Networks (Best Practice)
The **default bridge network** has a limitation: containers can't resolve each other by name. You must create a **custom bridge network** to enable automatic DNS resolution between containers.

## VISUALIZATION_CONFIG
```json
{ "component": "ArchitectureVisualizer", "state": "docker-networking" }
```

## CODE

### Creating and Using Custom Networks
```bash
# Create a custom bridge network
docker network create my-app-network

# Run a Postgres container on this network
docker run -d \
  --name postgres-db \
  --network my-app-network \
  -e POSTGRES_PASSWORD=secret \
  postgres:15

# Run your API container on the SAME network
docker run -d \
  --name api \
  --network my-app-network \
  -p 8080:8080 \
  my-api-image

# Inside the API container, 'postgres-db' is a valid hostname!
# Connection string: jdbc:postgresql://postgres-db:5432/mydb
```

### Port Publishing
```bash
# -p hostPort:containerPort
# Publish container port 3000 as port 8080 on the host
docker run -p 8080:3000 my-app

# Bind to a specific host IP (only accept connections from localhost)
docker run -p 127.0.0.1:8080:3000 my-app

# Publish to a RANDOM available port
docker run -P my-app  # Docker picks a random available port
docker port my-app    # Show what port was assigned
```

### Inspecting Networks
```bash
# List all Docker networks
docker network ls
# NETWORK ID     NAME               DRIVER    SCOPE
# abc123         bridge             bridge    local
# def456         host               host      local
# ghi789         my-app-network     bridge    local

# Inspect a network to see connected containers
docker network inspect my-app-network

# Connect a running container to a network
docker network connect my-app-network existing-container

# Disconnect
docker network disconnect my-app-network existing-container
```

### Inter-Container Communication
```bash
# Two containers on the same custom network can ping each other by name!
docker run -it --rm --network my-app-network alpine sh

# Inside the alpine container:
ping postgres-db   # Works! Docker DNS resolves 'postgres-db' to the container's IP
wget http://api:8080/health  # Works! 'api' resolves to the API container
```

## REAL_WORLD
**Multi-Tier Network Architecture**
Production applications should have isolated networks for each tier:

```yaml
# docker-compose.yml
services:
  nginx:          # Public-facing reverse proxy
    networks: [frontend]
    ports: ["80:80", "443:443"]

  api:            # Application tier - bridges both networks
    networks: [frontend, backend]

  db:             # Database - ONLY on backend network, not accessible from internet!
    networks: [backend]

networks:
  frontend:       # Public network: nginx → api
  backend:        # Private network: api → db (db not exposed to internet!)
```

This architecture ensures the database is never directly accessible from the public internet — it's on a completely separate Docker network that only the API can reach.

## INTERVIEW
**Q: You run two containers without specifying a network. Both are on the default `bridge` network. Why can't they communicate using container names? And how do you fix it?**

A: The **default bridge network** (`bridge`) uses static IP assignments but does NOT enable automatic **DNS-based service discovery** between containers. Containers on the default bridge network can only reach each other via IP address (found with `docker inspect`), which is fragile because IPs change.

The fix is to create a **custom bridge network**:
```bash
docker network create mynet
docker run --network mynet --name db postgres
docker run --network mynet --name api my-api
```

On a custom bridge network, Docker runs an embedded DNS server that resolves container names to their IPs. So `api` can now connect to `db` using the hostname `db` — the DNS resolution is automatic. This is also what Docker Compose does automatically: it creates a custom network for all services in the compose file, which is why Compose services can reference each other by service name.

## FEYNMAN CHECK

### Explain Docker Networking Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Docker Networking** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Docker Networking, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Docker Networking solve? Why would JavaScript/web development be harder without it?**
> **A:** Docker Networking solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Docker Networking?**
> **A:** The single most important thing is understanding the *mental model*: Docker Networking operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Docker Networking and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Docker Networking operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Docker Networking interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Docker Networking interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Docker Networking to a senior engineer in two sentences, what would you say?**
> **A:** "Docker Networking works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Docker Networking Utility

**Goal:** Implement a real, working Docker Networking module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `dockernetworking` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir docker-networking-demo && cd docker-networking-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Docker Networking Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Docker Networking.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createDockerNetworking(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Docker Networking] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Docker Networking logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Docker Networking] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Docker Networking] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createDockerNetworking({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Docker Networking] Input cannot be null or undefined`);
    }
    const instance = createDockerNetworking();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Docker Networking] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Docker Networking Tests ──');

test('initialises correctly', () => {
  const m = createDockerNetworking();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createDockerNetworking();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Docker Networking] Initialized with: hello world
[Docker Networking] Processed: hello world
Result: hello world
[Docker Networking] Destroyed.

── Docker Networking Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Docker Networking in one sentence.**
<details><summary>Answer</summary>

Docker Networking is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Docker Networking?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Docker Networking.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Docker Networking and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Docker Networking | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Docker Networking] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Docker Networking causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Docker Networking usage].
</details>

**Q6: What does Docker Networking look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Docker Networking typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Docker Networking correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Docker Networking:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_docker_networking(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Docker Networking in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Docker Networking in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Docker Networking', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Docker Networking at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Docker Networking uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Docker Networking interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Docker Networking is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Docker Networking relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Docker Networking
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Docker Networking to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Docker Networking fits in the data/control flow.
2. **Choose** the right variant/pattern of Docker Networking for the scale requirement.
3. **Handle** failure modes: what happens when Docker Networking is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Docker Networking is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
