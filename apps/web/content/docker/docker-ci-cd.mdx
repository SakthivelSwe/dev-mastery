---
slug: "docker-ci-cd"
title: "Docker in CI/CD"
level: 4
---

## WHY
Docker is the backbone of modern CI/CD pipelines. By packaging your application and its environment together, Docker ensures that the artifact tested in CI is exactly what gets deployed to production — no "it worked in CI but fails in production" surprises.
Every major CI/CD platform (GitHub Actions, GitLab CI, Jenkins, CircleCI) has first-class Docker support. Understanding how to integrate Docker into your pipeline is essential for modern software delivery.

## THEORY
### The Docker CI/CD Flow
1. **Developer pushes code to Git**
2. **CI triggers:** Build Docker image from Dockerfile
3. **Run tests:** Inside a Docker container (consistent environment)
4. **Security scan:** Check image for vulnerabilities (Trivy, Snyk)
5. **Push image:** To a registry (GHCR, ECR, Docker Hub) with a unique tag (git SHA)
6. **Deploy:** Update production to use the new image tag

### Benefits
- **Reproducible:** Same Docker image runs in CI and production
- **Isolated:** Tests run in a clean, consistent environment every time
- **Artifact-based:** The image is the deployable artifact — no "build on the server"
- **Rollback:** Any previous image tag can be redeployed instantly

## VISUALIZATION_CONFIG
```json
{ "component": "ArchitectureVisualizer", "state": "docker-cicd-pipeline" }
```

## CODE

### GitHub Actions Full Pipeline
```yaml
# .github/workflows/ci-cd.yml
name: Build, Test, and Deploy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Build with Docker BuildKit (faster, better caching)
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Login (only on push to main, not PRs)
      - name: Login to GitHub Container Registry
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Build image (and push only on main branch)
      - name: Build and Push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: ${{ github.event_name == 'push' }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          # Cache from GitHub Actions cache for faster builds!
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # Run tests INSIDE the built container
      - name: Run tests
        run: |
          docker run --rm \
            -e NODE_ENV=test \
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }} \
            npm test

      # Security scan
      - name: Vulnerability Scan with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          format: 'table'
          exit-code: '1'           # Fail the pipeline on CRITICAL vulnerabilities!
          severity: 'CRITICAL,HIGH'

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Deploy to Production
        run: |
          # SSH to production server and pull new image
          ssh -i ${{ secrets.SSH_KEY }} user@production-server \
            "docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }} && \
             docker stop api && docker rm api && \
             docker run -d --name api \
               --restart=unless-stopped \
               -p 8080:8080 \
               ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}"
```

### GitLab CI with Docker
```yaml
# .gitlab-ci.yml
stages:
  - build
  - test
  - deploy

variables:
  IMAGE_TAG: $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA

build:
  stage: build
  image: docker:24
  services:
    - docker:dind   # Docker-in-Docker
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build -t $IMAGE_TAG .
    - docker push $IMAGE_TAG

test:
  stage: test
  script:
    - docker run --rm $IMAGE_TAG npm test

deploy:
  stage: deploy
  only:
    - main
  script:
    - docker pull $IMAGE_TAG
    - docker service update --image $IMAGE_TAG my-app  # Docker Swarm
```

## REAL_WORLD
**Caching in CI for Speed**
Docker build caches are lost between CI runs by default (each run starts fresh). Use registry-based caching:

```yaml
- name: Build with registry cache
  uses: docker/build-push-action@v5
  with:
    cache-from: type=registry,ref=ghcr.io/myorg/myapp:cache
    cache-to: type=registry,ref=ghcr.io/myorg/myapp:cache,mode=max
```

This stores the build cache in the registry and restores it on the next run. Result: `npm install` steps are cached across pipeline runs — builds that took 8 minutes now take 90 seconds.

## INTERVIEW
**Q: Your CI/CD pipeline builds a Docker image and runs tests. The pipeline takes 12 minutes — 8 of which are just `npm install`. How do you optimize this?**
A: The root cause is that `npm install` is not being cached between pipeline runs. Three solutions:

1. **Layer cache ordering:** Ensure `package*.json` is copied and `npm install` runs BEFORE `COPY . .`. This way, npm install is only re-executed when `package.json` changes, not when any source file changes.

2. **Registry-based layer caching:** Use `cache-from: type=registry` in GitHub Actions build-push-action to persist the Docker layer cache in the container registry between pipeline runs.

3. **BuildKit inline cache:** Use `--build-arg BUILDKIT_INLINE_CACHE=1` to embed cache metadata in the pushed image, allowing `cache-from: image-name:latest` to restore layers.

With proper caching, `npm install` only runs when `package.json` actually changes. Most builds will skip it entirely, reducing the pipeline from 12 minutes to 3-4 minutes.

## FEYNMAN CHECK

### Explain Docker in CI/CD Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Docker in CI/CD** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Docker in CI/CD, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Docker in CI/CD solve? Why would JavaScript/web development be harder without it?**
> **A:** Docker in CI/CD solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Docker in CI/CD?**
> **A:** The single most important thing is understanding the *mental model*: Docker in CI/CD operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Docker in CI/CD and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Docker in CI/CD operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Docker in CI/CD interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Docker in CI/CD interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Docker in CI/CD to a senior engineer in two sentences, what would you say?**
> **A:** "Docker in CI/CD works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Docker in CI/CD Utility

**Goal:** Implement a real, working Docker in CI/CD module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `dockercicd` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir docker-ci-cd-demo && cd docker-ci-cd-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Docker in CI/CD Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Docker in CI/CD.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createDockerinCI/CD(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Docker in CI/CD] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Docker in CI/CD logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Docker in CI/CD] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Docker in CI/CD] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createDockerinCI/CD({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Docker in CI/CD] Input cannot be null or undefined`);
    }
    const instance = createDockerinCI/CD();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Docker in CI/CD] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Docker in CI/CD Tests ──');

test('initialises correctly', () => {
  const m = createDockerinCI/CD();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createDockerinCI/CD();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Docker in CI/CD] Initialized with: hello world
[Docker in CI/CD] Processed: hello world
Result: hello world
[Docker in CI/CD] Destroyed.

── Docker in CI/CD Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Docker in CI/CD in one sentence.**
<details><summary>Answer</summary>

Docker in CI/CD is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Docker in CI/CD?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Docker in CI/CD.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Docker in CI/CD and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Docker in CI/CD | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Docker in CI/CD] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Docker in CI/CD causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Docker in CI/CD usage].
</details>

**Q6: What does Docker in CI/CD look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Docker in CI/CD typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Docker in CI/CD correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Docker in CI/CD:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_docker_ci_cd(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Docker in CI/CD in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Docker in CI/CD in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Docker in CI/CD', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Docker in CI/CD at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Docker in CI/CD uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Docker in CI/CD interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Docker in CI/CD is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Docker in CI/CD relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Docker in CI/CD
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Docker in CI/CD to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Docker in CI/CD fits in the data/control flow.
2. **Choose** the right variant/pattern of Docker in CI/CD for the scale requirement.
3. **Handle** failure modes: what happens when Docker in CI/CD is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Docker in CI/CD is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
