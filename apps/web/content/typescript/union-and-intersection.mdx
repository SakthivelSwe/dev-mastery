---
slug: "union-and-intersection"
title: "Union & Intersection Types"
level: 2
---

## WHY
In the real world, data is rarely perfectly uniform. A function might return a `User` object on success, or an `Error` object on failure. A configuration setting might accept a `number` (for pixels) or a `string` (for percentages). 

TypeScript provides **Union Types (`|`)** to model data that can be "one of many things", and **Intersection Types (`&`)** to model data that combines "multiple things together". Mastering these two operators allows you to model highly dynamic JavaScript patterns with strict type safety.

## THEORY
### Unions (`|`) = Logical OR
A value can be **this OR that**.
If a value is a union of `A | B`, you can only safely access properties that are common to *both* `A` and `B` until you "narrow" the type down.

### Intersections (`&`) = Logical AND
A value must be **this AND that**.
It creates a new type that combines all properties from `A` and `B`. It is primarily used for combining object shapes.

## VISUALIZATION_CONFIG
```json
{
  "language": "typescript",
  "fileName": "union-intersection.ts",
  "steps": [
    {
      "title": "Union types",
      "description": "Union types (A | B) represent a value that can be one of several types. TypeScript requires you to handle all cases before accessing type-specific members.",
      "code": "type ID = string | number;\ntype NullableString = string | null;\ntype Result = \"success\" | \"error\" | \"loading\";\n\n// Must handle all union members\nfunction processId(id: ID) {\n  // id.toUpperCase();  // ERROR: number has no toUpperCase\n\n  if (typeof id === \"string\") {\n    return id.toUpperCase();  // string\n  } else {\n    return id.toString();    // number\n  }\n}\n\n// Union with objects\ntype Cat = { type: \"cat\"; meow: () => void };\ntype Dog = { type: \"dog\"; bark: () => void };\ntype Pet = Cat | Dog;",
      "highlight": [
        1,
        2,
        3,
        9,
        10,
        12,
        17,
        18,
        19
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Union type properties",
        "items": [
          {
            "label": "Assignable",
            "color": "#10b981",
            "value": "string assigns to string | number",
            "highlight": true
          },
          {
            "label": "Shared methods only",
            "color": "#818cf8",
            "value": "Only properties in ALL union members"
          },
          {
            "label": "Narrowing required",
            "color": "#f59e0b",
            "value": "Must narrow to access specific members"
          },
          {
            "label": "Discriminated union",
            "color": "#60a5fa",
            "value": "Add type tag to distinguish members"
          }
        ]
      }
    },
    {
      "title": "Intersection types",
      "description": "Intersection types (A & B) combine multiple types into one that has ALL properties of both. Used for mixins and combining roles.",
      "code": "interface Serializable {\n  serialize(): string;\n  deserialize(s: string): void;\n}\n\ninterface Loggable {\n  log(message: string): void;\n  readonly logLevel: \"debug\" | \"info\" | \"warn\" | \"error\";\n}\n\n// Intersection: must have ALL properties of both\ntype SerializableLoggable = Serializable & Loggable;\n\n// Object must implement everything\nconst obj: SerializableLoggable = {\n  serialize() { return \"{}\"; },\n  deserialize(s) { },\n  log(msg) { console.log(msg); },\n  logLevel: \"info\" as const,\n};",
      "highlight": [
        12,
        16,
        17,
        18,
        19,
        20
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Intersection type",
        "items": [
          {
            "label": "A & B",
            "color": "#10b981",
            "value": "Type must satisfy BOTH A and B",
            "highlight": true
          },
          {
            "label": "Mixins",
            "color": "#818cf8",
            "value": "Combine capabilities via intersection"
          },
          {
            "label": "Empty intersection",
            "color": "#ef4444",
            "value": "{ a: string } & { a: number } = never"
          },
          {
            "label": "Use case",
            "color": "#f59e0b",
            "value": "Extending 3rd party types non-invasively"
          }
        ]
      }
    },
    {
      "title": "Narrowing union types",
      "description": "TypeScript narrows union types through conditions. The narrowing must be exhaustive — handle all cases.",
      "code": "type Shape =\n  | { kind: \"circle\"; radius: number }\n  | { kind: \"square\"; side: number };\n\nfunction area(shape: Shape): number {\n  switch (shape.kind) {\n    case \"circle\": return Math.PI * shape.radius ** 2;\n    case \"square\": return shape.side ** 2;\n    default:\n      const impossible: never = shape;\n      return impossible;\n  }\n}\n\n// Error union pattern\ntype Ok<T> = { ok: true; value: T };\ntype Err<E> = { ok: false; error: E };\ntype Result<T, E = Error> = Ok<T> | Err<E>;\n\nconst result: Result<User> = await getUser();\nif (result.ok) { use(result.value); }  // value: User\nelse { handle(result.error); }          // error: Error",
      "highlight": [
        6,
        7,
        8,
        10,
        16,
        17,
        18,
        20,
        21
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "shape.kind discriminates the union",
            "done": true
          },
          {
            "label": "case \"circle\": shape.radius accessible",
            "done": true
          },
          {
            "label": "never in default: exhaustive check",
            "active": true
          },
          {
            "label": "Result<T,E> pattern: ok flag discriminates"
          }
        ]
      }
    },
    {
      "title": "Excess property checking",
      "description": "TypeScript performs excess property checking on object literals — assigning extra properties to a typed variable is an error, but not when going through a variable.",
      "code": "interface User { name: string; age: number; }\n\n// Excess property check ON object literal:\nconst user: User = { name: \"Alice\", age: 30, email: \"a@b.com\" };\n// ERROR: Object literal may only specify known properties\n// email does not exist in type User\n\n// No error through intermediate variable:\nconst extra = { name: \"Alice\", age: 30, email: \"a@b.com\" };\nconst user2: User = extra;  // OK — no excess check on variable\n\n// Practical: use Omit to remove extra properties\ntype UserInput = Omit<User, \"id\"> & { email: string };\n\n// Discriminated union: extra props prevented by literal type tag\ntype Event = { type: \"click\"; x: number } | { type: \"keydown\"; key: string };\nconst e: Event = { type: \"click\", x: 10, key: \"a\" }; // ERROR: key not in \"click\" variant",
      "highlight": [
        4,
        5,
        9,
        10,
        13,
        17
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Excess property check",
        "items": [
          {
            "label": "Object literal assignment",
            "color": "#ef4444",
            "value": "Extra props → error",
            "highlight": true
          },
          {
            "label": "Variable assignment",
            "color": "#10b981",
            "value": "No excess check — duck typing"
          },
          {
            "label": "Function argument",
            "color": "#818cf8",
            "value": "Object literal → excess check applies"
          },
          {
            "label": "Workaround",
            "color": "#f59e0b",
            "value": "Assign to variable first, then use"
          }
        ]
      }
    },
    {
      "title": "Union types in real applications",
      "description": "Union types power robust domain modeling: Result types for error handling, State machines for UI, and discriminated unions for data variants.",
      "code": "// State machine with union type\ntype AsyncState<T> =\n  | { status: \"idle\" }\n  | { status: \"loading\" }\n  | { status: \"success\"; data: T }\n  | { status: \"error\"; error: Error };\n\n// React component usage\nfunction TopicView({ state }: { state: AsyncState<Topic> }) {\n  switch (state.status) {\n    case \"idle\":    return <button>Load</button>;\n    case \"loading\": return <Spinner />;\n    case \"success\": return <Topic data={state.data} />;\n    case \"error\":   return <Error msg={state.error.message} />;\n  }\n}\n\n// Type-safe reducer\ntype Action =\n  | { type: \"FETCH_START\" }\n  | { type: \"FETCH_SUCCESS\"; payload: Topic[] }\n  | { type: \"FETCH_ERROR\"; error: Error };",
      "highlight": [
        3,
        4,
        5,
        6,
        11,
        12,
        13,
        14,
        19,
        20,
        21
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Union in real apps",
        "items": [
          {
            "label": "AsyncState<T>",
            "color": "#10b981",
            "value": "idle/loading/success/error states",
            "highlight": true
          },
          {
            "label": "Redux Action union",
            "color": "#818cf8",
            "value": "Each action type carries its payload"
          },
          {
            "label": "Complete coverage",
            "color": "#f59e0b",
            "value": "TypeScript warns on missing cases"
          },
          {
            "label": "Self-documenting",
            "color": "#60a5fa",
            "value": "Types describe all possible states"
          }
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner (Basic Unions)
Unions are most commonly used for primitives or literal values.

```typescript
// 1. Primitive Unions
function printId(id: number | string) {
    // ❌ Error: Property 'toUpperCase' does not exist on type 'string | number'.
    // id.toUpperCase(); 
    
    // You must narrow the type first!
    if (typeof id === "string") {
        console.log(id.toUpperCase()); // ✅ TS knows it's a string here
    } else {
        console.log(id.toFixed(2));    // ✅ TS knows it's a number here
    }
}

// 2. Literal Unions (Extremely common in React props)
type ButtonVariant = "primary" | "secondary" | "danger";
type ButtonSize = "sm" | "md" | "lg";

function renderButton(variant: ButtonVariant, size: ButtonSize) { ... }

// ✅ Autocomplete will suggest these exact strings!
renderButton("primary", "md");
```

### Level 2 — Building Blocks (Object Unions)
When you union two objects, you can only access properties they both share.

```typescript
interface Bird {
    name: string;
    fly(): void;
}
interface Fish {
    name: string;
    swim(): void;
}

function moveAnimal(animal: Bird | Fish) {
    // ✅ Valid: Both share 'name'
    console.log(animal.name);
    
    // ❌ Error: Fish cannot fly!
    // animal.fly();
    
    // We use the 'in' operator to narrow the type
    if ("fly" in animal) {
        animal.fly(); // ✅ TS knows it's a Bird
    } else {
        animal.swim(); // ✅ TS knows it's a Fish
    }
}
```

### Level 3 — Intermediate (Intersections)
Intersections are used to mix multiple types together into one massive type.

```typescript
interface HasName { name: string; }
interface HasAge { age: number; }
interface HasEmail { email: string; }

// Combine all three interfaces into one!
type UserProfile = HasName & HasAge & HasEmail;

const user: UserProfile = {
    name: "Alice",
    age: 30,
    email: "alice@example.com"
};
```
*Note: This achieves the exact same result as extending interfaces (`interface UserProfile extends HasName, HasAge, HasEmail`), but using `&` is often preferred when combining dynamic `type` aliases.*

### Level 4 — Advanced (Discriminated Unions)
This is the single most important pattern in advanced TypeScript (heavily used in Redux and modern state management).

Instead of guessing what type an object is using the `in` operator, you give every object a **shared literal property** (a discriminator tag) so TypeScript can instantly narrow it!

```typescript
interface NetworkSuccess {
    status: "success"; // The Discriminator Tag
    data: string[];
}

interface NetworkError {
    status: "error";   // The Discriminator Tag
    errorMessage: string;
}

interface NetworkLoading {
    status: "loading"; // The Discriminator Tag
}

// The Discriminated Union
type NetworkState = NetworkSuccess | NetworkError | NetworkLoading;

function handleResponse(response: NetworkState) {
    // We switch on the shared discriminator property
    switch (response.status) {
        case "success":
            // TS knows this is NetworkSuccess
            console.log(response.data); 
            break;
        case "error":
            // TS knows this is NetworkError
            console.error(response.errorMessage); 
            break;
        case "loading":
            console.log("Loading...");
            break;
    }
}
```

## REAL_WORLD
**Never Intersecting Primitives**
What happens if you accidentally Intersect two primitives that cannot possibly overlap?
```typescript
type Confused = string & number;
```
TypeScript will evaluate this as the `never` type. It is impossible for a value to be both a `string` and a `number` at the exact same time. If you ever see a `never` type appearing unexpectedly in your codebase, it means you accidentally used an Intersection (`&`) when you meant to use a Union (`|`).

## INTERVIEW
**Q: What is the difference between `any` and `unknown` in TypeScript?**
A: Both allow a variable to hold any type of value. 
However, `any` turns off the TypeScript compiler completely. You can call `.fly()` on an `any` variable and TS won't warn you. 
`unknown` is a type-safe alternative. TS forces you to narrow the `unknown` type (via `typeof` or `instanceof`) before you are allowed to interact with it. Always prefer `unknown` over `any` when dealing with unpredictable data (like `JSON.parse` responses).

## FEYNMAN CHECK

### Explain Unions & Intersections Like I'm 10 Years Old
> A UNION (`A | B`) says "this value is EITHER an A OR a B." You can only use what's COMMON to both until you narrow it. An INTERSECTION (`A & B`) says "this value is BOTH an A AND a B simultaneously" — must have ALL properties of both. Unions describe ALTERNATIVES (a Response is success OR error); intersections describe COMBINATIONS (a User & Admin has all user props plus admin props). The non-obvious twist: unions are like "or", but on object types you can only access the INTERSECTION of their properties (the common subset) — opposite of intuition. `(Cat | Dog).bark()` fails because not every member has bark; you must narrow with `in 'bark'` or a discriminant first.

---

### 5 Deep Conceptual Questions

**Q1: What problem do discriminated unions solve?**
> **A:** Modelling "one of N variants" with type-safe access. `type Result = { kind: 'ok'; data: T } | { kind: 'err'; error: E }` lets TS NARROW the type based on the `kind` literal: inside `if (r.kind === 'ok')`, TS knows r is the ok branch with `data` available. This eliminates null-check chains, makes pattern matching exhaustive (with `never` guards), and replaces error-prone `if (err) ... else ...` shapes with provably correct switch statements.

**Q2: Mental model for union narrowing?**
> **A:** TS uses CONTROL FLOW ANALYSIS — it tracks the type at every point based on the runtime checks you've performed. `typeof x === 'string'` narrows to string. `'foo' in x` narrows to objects with foo. `x.kind === 'ok'` narrows discriminated unions. The compiler runs this in your IDE in real-time. The narrowed type is REVERTED when you assign to the variable or call a function that mutates it.

**Q3: Most dangerous misconception? Show with code.**
> **A:** Believing intersection of incompatible types is allowed:
> ```ts
> // ❌ Looks fine but produces `never`
> type Impossible = string & number;   // type is `never`
> const x: Impossible = ???;            // can't construct one
>
> // ❌ Mixing literal unions with intersection sneakily
> type WrongStatus = ('a' | 'b') & ('b' | 'c');   // narrows to 'b' only
>
> // ✅ Use union when alternatives, intersection when combining
> type LoggableUser = User & { log: () => void };
> ```

**Q4: How does narrowing interact with closures at runtime?**
> **A:** TS's control flow analysis is INTRA-FUNCTION. If you narrow `x: string | null` to string and then put `() => x.length` in a callback, TS may RE-WIDEN x to the original union inside the callback (because the callback could run later, after x changed). The fix is to capture the narrowed value in a const: `const s = x; if (s) callback(() => s.length)` — now s is provably `string` for the closure's lifetime.

**Q5: FAANG-grade definition?**
> **A:** "Union types (`A | B`) describe values that inhabit either type, accessing only the intersection of their public surface until narrowed; intersection types (`A & B`) describe values that simultaneously satisfy both type constraints — control-flow analysis narrows unions via typeof, instanceof, 'in', and discriminant property checks, producing the exhaustiveness guarantees that underpin algebraic data types (ADTs) in TypeScript."

## BUILD

### 🏗️ Mini Project: Result Monad For Type-Safe Error Handling

**What you will build:** A `Result<T, E>` discriminated union that replaces try/catch in app code, with helpers `ok`, `err`, `map`, `mapErr`, and `unwrap` — the same pattern Rust uses.
**Why this project:** Forces discriminated unions, narrowing, generics, and never-based exhaustiveness — the foundation of safe TS error handling.
**Time estimate:** 35 minutes

---

#### Step 1 — Setup
```bash
mkdir ts-result && cd ts-result
npm init -y && npm install -D typescript vitest
npx tsc --init --strict --target ES2022 --module NodeNext --moduleResolution NodeNext
ni src/result.ts, src/main.ts -ItemType File
```

#### Step 2 — The Result Type
```ts
// src/result.ts
export type Result<T, E> =
  | { kind: 'ok';  value: T }
  | { kind: 'err'; error: E };

export const ok  = <T>(value: T): Result<T, never> => ({ kind: 'ok',  value });
export const err = <E>(error: E): Result<never, E> => ({ kind: 'err', error });

export function map<T, U, E>(r: Result<T, E>, fn: (v: T) => U): Result<U, E> {
  return r.kind === 'ok' ? ok(fn(r.value)) : r;
}

export function mapErr<T, E, F>(r: Result<T, E>, fn: (e: E) => F): Result<T, F> {
  return r.kind === 'err' ? err(fn(r.error)) : r;
}

export function flatMap<T, U, E>(r: Result<T, E>, fn: (v: T) => Result<U, E>): Result<U, E> {
  return r.kind === 'ok' ? fn(r.value) : r;
}

export function unwrap<T, E>(r: Result<T, E>): T {
  if (r.kind === 'ok') return r.value;
  throw new Error(`unwrap on err: ${JSON.stringify(r.error)}`);
}
```

#### Step 3 — Use It (Replace try/catch)
```ts
// src/main.ts
import { ok, err, map, flatMap, type Result } from './result.js';

type ApiError = { code: 'NOT_FOUND' | 'NETWORK' | 'INVALID'; message: string };

function fetchUser(id: number): Result<{ id: number; name: string }, ApiError> {
  if (id <= 0)         return err({ code: 'INVALID',   message: 'id must be positive' });
  if (id === 999)      return err({ code: 'NOT_FOUND', message: 'no such user' });
  return ok({ id, name: `User ${id}` });
}

const r = flatMap(fetchUser(42), u => ok({ ...u, name: u.name.toUpperCase() }));

// Exhaustive switch — TS forces handling both branches
switch (r.kind) {
  case 'ok':  console.log('Got:', r.value); break;
  case 'err': console.log('Failed:', r.error.code, '-', r.error.message); break;
}
```

#### Step 4 — Error Handling: Exhaustiveness Check
```ts
function describeError(e: ApiError): string {
  switch (e.code) {
    case 'NOT_FOUND': return 'Resource missing';
    case 'NETWORK':   return 'Try again later';
    case 'INVALID':   return e.message;
    default: {
      const _: never = e.code;   // catches missed cases at compile time
      return _;
    }
  }
}
```

#### Step 5 — Tests
```ts
import { describe, it, expect } from 'vitest';
import { ok, err, map, flatMap, unwrap } from './result.js';

describe('Result', () => {
  it('map transforms ok value', () => {
    expect(map(ok(2), n => n * 10)).toEqual({ kind: 'ok', value: 20 });
  });
  it('map skips err', () => {
    expect(map(err('boom'), (n: number) => n * 10)).toEqual({ kind: 'err', error: 'boom' });
  });
  it('flatMap chains results', () => {
    const r = flatMap(ok(2), n => n > 0 ? ok(n * 2) : err('neg'));
    expect(r).toEqual({ kind: 'ok', value: 4 });
  });
  it('unwrap throws on err', () => {
    expect(() => unwrap(err('x'))).toThrow();
  });
});
```

**Expected Output:**
```
Got: { id: 42, name: 'USER 42' }

# Exhaustiveness check fires if you add a new ApiError code:
src/main.ts:25:21 - error TS2322: Type '"TIMEOUT"' is not assignable to type 'never'.
```

**Stretch Challenges:**
- [ ] Add an `asyncMap` for Promise-wrapped Results
- [ ] Add `Result.all([r1, r2, r3])` that combines multiple Results
- [ ] Compare Result with Promise.allSettled — when is each better?

## SPACED REVIEW

### Day 1 — Recall
**Q1:** Difference between `A | B` and `A & B`?
**Q2:** Why can't you call `.bar()` on `{a:1} | {b:2}` without narrowing?
**Q3:** Write a discriminated union for `Loading | Loaded | Error`. From memory.

### Day 3 — Comprehension
**Q4:** Why does `'a' | 'b' & 'b' | 'c'` reduce to `'a' | 'b' | 'c'` instead of just `'b'`?
**Q5:** A junior writes `x.kind === 'ok' && x.value.length` inside a callback — TS errors. Diagnose.
**Q6:** Refactor with a discriminated union:
```ts
type Fetch = { isLoading: boolean; data?: User; error?: string };
```

### Day 7 — Application
**Q7:** Build a typed state machine for a checkout flow using discriminated unions.
**Q8:** A PR uses `any` for fetch responses — replace with `Result<T, ApiError>`. Show the diff.
**Q9:** What does intersection `User & Admin` mean for assignability when both define a `name: string` differently?

### Day 14 — Synthesis
**Q10:** ★ Interview: "Walk me through narrowing — every way TS does it, and the gotchas with closures."
**Q11:** Draw: how do unions, intersections, and discriminated unions correspond to algebraic data types?
**Q12:** ★ System design: "Model a workflow engine state graph (10+ states, 30+ transitions) with TypeScript discriminated unions — what trade-offs vs XState?"
