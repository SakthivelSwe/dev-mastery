---
slug: "enums-ts"
title: "TypeScript Enums (And Why To Avoid Them)"
level: 2
---

## WHY
In many strictly typed languages (like Java or C#), Enums (Enumerations) are a fundamental way to define a set of named constants. TypeScript introduced Enums to bring this feature to JavaScript developers. However, Enums in TypeScript are highly controversial, often considered a design mistake, and modern TypeScript development heavily favors Union Types instead.

## THEORY
### What is an Enum?
An Enum allows you to define a collection of related values that can be numeric or string-based.

**The Problem with TypeScript Enums:**
TypeScript is fundamentally designed as a *Type-Level* language. It provides types that completely disappear when the code is compiled to JavaScript (Erasure). Enums violate this principle. Enums are a *Runtime* feature. When you compile a TypeScript Enum, the compiler actually injects complex, self-invoking JavaScript objects into your runtime code. This increases bundle size, complicates minification, and breaks standard JavaScript idioms.

## VISUALIZATION_CONFIG
```json
{
  "language": "typescript",
  "fileName": "enums.ts",
  "steps": [
    {
      "title": "Numeric and string enums",
      "description": "Enums define named constants. Numeric enums auto-increment from 0. String enums have explicit values. Use const enums for zero-cost abstractions.",
      "code": "// Numeric enum (auto-increment)\nenum Direction { North, South, East, West }  // 0,1,2,3\nenum Priority { Low = 1, Medium = 5, High = 10 }  // custom start\n\n// String enum (no reverse mapping)\nenum Status { Active = \"ACTIVE\", Inactive = \"INACTIVE\", Pending = \"PENDING\" }\n\n// Usage\nconst dir: Direction = Direction.North;  // dir = 0\nconst status: Status = Status.Active;    // status = \"ACTIVE\"\n\n// Const enum: inlined at compile time (no runtime object)\nconst enum Color { Red, Green, Blue }\nconst c = Color.Red;  // compiled to: const c = 0;",
      "highlight": [
        2,
        3,
        6,
        9,
        10,
        13,
        14
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Enum types",
        "items": [
          {
            "label": "Numeric enum",
            "color": "#10b981",
            "value": "Auto-increment + reverse mapping",
            "highlight": true
          },
          {
            "label": "String enum",
            "color": "#818cf8",
            "value": "No reverse mapping — safer"
          },
          {
            "label": "const enum",
            "color": "#f59e0b",
            "value": "Inlined at compile time — no runtime"
          },
          {
            "label": "Heterogeneous",
            "color": "#60a5fa",
            "value": "Mixed numeric+string — avoid"
          }
        ]
      }
    },
    {
      "title": "Enum patterns and alternatives",
      "description": "Enums are useful but have quirks. Modern TypeScript often uses union types or as const objects as safer alternatives.",
      "code": "// Enum quirk: numeric enums accept any number\nenum Level { One = 1, Two = 2, Three = 3 }\nconst bad: Level = 99;  // NO ERROR with numeric enum!\n\n// Alternative 1: Union of literals (most common)\ntype Level = 1 | 2 | 3;\nconst good: Level = 99;  // ERROR: 99 not assignable\n\n// Alternative 2: as const object (readable keys + values)\nconst Levels = { One: 1, Two: 2, Three: 3 } as const;\ntype Level = typeof Levels[keyof typeof Levels];  // 1 | 2 | 3\n\n// When to use enums:\n// - Need runtime object (Object.values, Object.keys)\n// - Existing codebase uses enums\n// - Bidirectional numeric mapping needed",
      "highlight": [
        3,
        6,
        7,
        10,
        11,
        14,
        15,
        16
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Numeric enum: accepts out-of-range numbers (quirk)",
            "done": true
          },
          {
            "label": "String union: exact values enforced",
            "done": true
          },
          {
            "label": "as const object: values + type derived together",
            "active": true
          },
          {
            "label": "Prefer unions for pure type usage"
          }
        ]
      }
    },
    {
      "title": "Enum in switch statements",
      "description": "Enums are great for exhaustive switch statements. TypeScript can detect if you forget a case using the never type.",
      "code": "enum Status { Active, Inactive, Pending }\n\nfunction handleStatus(status: Status): string {\n  switch (status) {\n    case Status.Active:   return \"Processing\";\n    case Status.Inactive: return \"Skipping\";\n    case Status.Pending:  return \"Waiting\";\n    default:\n      // Exhaustiveness check\n      const _exhaustive: never = status;\n      throw new Error(`Unhandled status: ${_exhaustive}`);\n  }\n}\n\n// If you add Status.Archived without a case:\n// TypeScript ERROR: Type \"Archived\" not assignable to type \"never\"",
      "highlight": [
        5,
        6,
        7,
        10,
        11,
        15,
        16
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Exhaustive checks",
        "items": [
          {
            "label": "const _: never = value",
            "color": "#10b981",
            "value": "Catches missing cases at compile time",
            "highlight": true
          },
          {
            "label": "TypeScript error",
            "color": "#818cf8",
            "value": "New enum value = compile error without case"
          },
          {
            "label": "Default throw",
            "color": "#f59e0b",
            "value": "Runtime safety net"
          },
          {
            "label": "Discriminated unions",
            "color": "#60a5fa",
            "value": "Same pattern for union type switches"
          }
        ]
      }
    },
    {
      "title": "Enum bitflags",
      "description": "Numeric enum values can be powers of 2 to use bitwise operations as compact flags. Combine multiple options with | and check with &.",
      "code": "const enum Permission {\n  None   = 0,\n  Read   = 1 << 0,  // 1\n  Write  = 1 << 1,  // 2\n  Delete = 1 << 2,  // 4\n  Admin  = Read | Write | Delete  // 7\n}\n\nlet userPerms = Permission.Read | Permission.Write;  // 3\n\n// Check permission\nfunction hasPermission(perms: Permission, check: Permission): boolean {\n  return (perms & check) === check;\n}\n\nhasPermission(userPerms, Permission.Read);   // true\nhasPermission(userPerms, Permission.Delete); // false",
      "highlight": [
        3,
        4,
        5,
        6,
        9,
        13,
        16,
        17
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Bitflag pattern",
        "items": [
          {
            "label": "Powers of 2 values",
            "color": "#10b981",
            "value": "1, 2, 4, 8 — each bit is one flag",
            "highlight": true
          },
          {
            "label": "Combine with |",
            "color": "#818cf8",
            "value": "Read | Write = 3 (both flags)"
          },
          {
            "label": "Check with &",
            "color": "#f59e0b",
            "value": "perms & Read === Read → has Read"
          },
          {
            "label": "Compact storage",
            "color": "#60a5fa",
            "value": "32 flags in one number"
          }
        ]
      }
    },
    {
      "title": "Enum vs discriminated union",
      "description": "For complex data structures, discriminated unions (tagged unions) are often better than enums — they carry data with each variant.",
      "code": "// Enum approach: type + separate data\nenum ResultType { Success, Error, Loading }\ninterface Result { type: ResultType; data?: unknown; error?: Error; }\n\n// Discriminated union: type + data together\ntype Result<T> =\n  | { type: \"success\"; data: T }\n  | { type: \"error\"; error: Error }\n  | { type: \"loading\" };\n\n// TypeScript narrows in switch\nfunction handleResult<T>(result: Result<T>) {\n  switch (result.type) {\n    case \"success\": console.log(result.data);  // data: T\n    case \"error\":   console.log(result.error); // error: Error\n    case \"loading\": console.log(\"Loading...\");\n  }\n}",
      "highlight": [
        6,
        7,
        8,
        9,
        14,
        15,
        16
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Discriminated unions",
        "items": [
          {
            "label": "Literal type tag",
            "color": "#10b981",
            "value": "type: \"success\" | \"error\" | \"loading\"",
            "highlight": true
          },
          {
            "label": "Each variant carries its data",
            "color": "#818cf8",
            "value": "success has data, error has error"
          },
          {
            "label": "TypeScript narrows",
            "color": "#f59e0b",
            "value": "In case \"success\": knows data exists"
          },
          {
            "label": "Better than enums",
            "color": "#60a5fa",
            "value": "No separate data field needed"
          }
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner (Numeric Enums)
By default, Enums are numeric and auto-incrementing starting from 0.

```typescript
// Defining a Numeric Enum
enum Direction {
  Up,    // 0
  Down,  // 1
  Left,  // 2
  Right  // 3
}

// Usage
function move(dir: Direction) {
  if (dir === Direction.Up) {
    console.log("Moving up!");
  }
}

move(Direction.Up);

// THE DANGER OF NUMERIC ENUMS:
// This is perfectly valid TypeScript and will NOT throw an error!
move(99); 
```
Wait, what? Yes. Prior to TypeScript 5.0, numeric enums were wildly unsafe. You could pass any random number to a function expecting a numeric enum, and the compiler would not stop you.

### Level 2 — Building Blocks (String Enums)
String enums solve the safety issue of numeric enums.

```typescript
enum UserRole {
  Admin = "ADMIN",
  Editor = "EDITOR",
  Viewer = "VIEWER"
}

function checkAccess(role: UserRole) {
  if (role === UserRole.Admin) {
    console.log("Full Access");
  }
}

// ✅ Safe: Only Enum members are allowed
checkAccess(UserRole.Admin);

// ❌ Error: Argument of type '"ADMIN"' is not assignable to parameter of type 'UserRole'.
checkAccess("ADMIN"); 
```
While safer, this creates an annoying DX (Developer Experience). Even if the string is identical to the enum value, you *must* import the Enum object and use it. You can't just pass the string literal.

### Level 3 — Intermediate (The Modern Alternative: Union Types)
This is the industry standard for modern TypeScript. Instead of Enums, we use **String Literal Union Types**.

```typescript
// Define a Union Type of exact string literals
type ModernUserRole = "ADMIN" | "EDITOR" | "VIEWER";

function checkAccessModern(role: ModernUserRole) {
  if (role === "ADMIN") {
    console.log("Full Access");
  }
}

// ✅ Perfect Developer Experience!
// The IDE will autocomplete "ADMIN", "EDITOR", or "VIEWER" when you type the quote!
checkAccessModern("ADMIN");

// ❌ TypeScript catches the error immediately!
checkAccessModern("SUPER_USER"); 
```
**Benefits of Union Types:**
1. Zero runtime overhead (compiles to absolutely nothing).
2. No need to import an Enum object into every file just to pass a value.
3. Perfect IDE autocomplete.

### Level 4 — Advanced (Const Assertions for Iteration)
The one thing Enums do better than Union Types is iteration. You can't use `Object.values()` on a Union Type because it doesn't exist at runtime.
If you need a list of values at runtime *and* a strict type, use an **Object with `as const`**.

```typescript
// 1. Define a standard JavaScript object, but freeze it with 'as const'
const Roles = {
  Admin: "ADMIN",
  Editor: "EDITOR",
  Viewer: "VIEWER"
} as const;

// 2. Extract the TYPE from the object's values dynamically!
// RoleType becomes: "ADMIN" | "EDITOR" | "VIEWER"
type RoleType = typeof Roles[keyof typeof Roles];

// Now you have the best of both worlds!
// You have a strict type for your functions:
function assignRole(role: RoleType) {}

// AND you have a runtime object you can iterate over to build UI dropdowns:
Object.values(Roles).forEach(role => {
  console.log(`<option value="${role}">${role}</option>`);
});
```

## REAL_WORLD
**When SHOULD you use Enums?**
If you are building an SDK that heavily interfaces with a backend written in Java or C#, and that backend uses numeric bitwise flags (e.g., `FilePermission.Read = 1, FilePermission.Write = 2, FilePermission.Execute = 4`), TypeScript Enums are actually the perfect tool for handling bitwise operations and mapping to those external systems. For standard web application state, stick to Union Types.

## INTERVIEW
**Q: What is a `const enum`?**
A: A `const enum` is a special TypeScript feature designed to fix the bundle bloat of standard Enums. When you compile a `const enum`, it completely erases the Enum object and inlines the raw values directly into the compiled JavaScript. 

```typescript
const enum Status { Active = 1, Inactive = 0 }
const userStatus = Status.Active;

// Compiles to pure JS:
const userStatus = 1;
```
However, `const enum` causes massive problems with modern build tools (like Babel, SWC, and esbuild) because these tools compile files one at a time and don't perform whole-program type analysis. Because of this, the official TypeScript team recommends avoiding `const enum` entirely in modern projects.

## FEYNMAN CHECK

### Explain Enums Like I'm 10 Years Old
> Enums are NAMED CONSTANTS grouped together. `enum Status { Active, Inactive, Banned }` creates Status.Active = 0, Status.Inactive = 1, Status.Banned = 2 by default (numeric). `enum Status { Active = 'active' }` gives string values you control. Enums ARE one of the few TS features that emit RUNTIME code (a JS object) — unlike pure types which vanish. This makes them controversial: bigger bundles, harder to tree-shake, weird semantics. The modern alternative is `const Status = { Active: 'active' } as const` plus `type Status = typeof Status[keyof typeof Status]` — zero runtime cost, better type narrowing, easier to refactor. The TypeScript team now recommends literal unions over enums for most cases.

---

### 5 Deep Conceptual Questions

**Q1: Why do most modern TS codebases avoid enums?**
> **A:** Three reasons: (1) Runtime cost — enums emit a JS object that bundlers struggle to tree-shake. (2) Numeric enums allow assigning arbitrary numbers (`status = 99` is allowed even though no member equals 99) — broken type safety. (3) String literal unions (`type Status = 'active' | 'banned'`) give you the same compile-time safety with zero runtime, full tree-shaking, and better autocomplete. Const enums are a third option (inlined at compile time) but break isolatedModules.

**Q2: Mental model: enum vs const object vs literal union?**
> **A:** Literal unions (`'a' | 'b'`) are the purest types — zero runtime, no namespace. Const objects (`as const`) give you both the type and a runtime lookup. Enums give you a runtime two-way mapping (numeric reverse-lookup) but cost bundle size. Pick literal union by default; const object when you need a runtime values collection; enum only for backward compat or numeric flag enums.

**Q3: Most dangerous misconception? Show with code.**
> **A:** Believing numeric enums are type-safe:
> ```ts
> // ❌ Any number is assignable to a numeric enum
> enum Status { Active = 0, Banned = 1 }
> const s: Status = 99;   // 💀 no error — silent corruption
>
> // ✅ String enum or literal union
> enum Status2 { Active = 'active', Banned = 'banned' }
> const s2: Status2 = 'unknown';   // ❌ now TS rejects
>
> type StatusU = 'active' | 'banned';
> const s3: StatusU = 'unknown';   // ❌ rejected
> ```

**Q4: How does `const enum` differ from regular enum at compile time?**
> **A:** A `const enum` is INLINED — calls to `MyEnum.Active` are replaced with the literal value during compilation, and no runtime object is emitted. Zero runtime cost. The downside: it breaks under `isolatedModules: true` (required by esbuild, swc, ts-loader's transpileOnly) because each file is compiled independently and can't see the enum's values. Modern build tooling forces you off const enums into literal unions or as-const objects.

**Q5: FAANG-grade definition?**
> **A:** "TypeScript enums are runtime-emitted named-constant collections with numeric reverse-mapping for numeric variants — providing both compile-time type-safety and runtime lookup, but at the cost of bundle size, broken tree-shaking, and unsafe numeric assignability — for which modern codebases substitute either literal-union types (compile-time only, zero runtime) or as-const objects (typed runtime values with full type safety)."

## BUILD

### 🏗️ Mini Project: Migrate an Enum to `as const` And Compare Outputs

**What you will build:** Define the same Status concept three ways (numeric enum, string enum, as-const object), compile each, and inspect the JS output to see bundle-size and tree-shaking differences.
**Why this project:** Forces you to SEE why enums lose to literal unions in modern bundlers.
**Time estimate:** 25 minutes

---

#### Step 1 — Setup
```bash
mkdir ts-enums && cd ts-enums
npm init -y && npm install -D typescript
npx tsc --init --strict --target ES2022 --module NodeNext --moduleResolution NodeNext
ni src/numeric.ts, src/string-enum.ts, src/as-const.ts, src/main.ts -ItemType File
```

#### Step 2 — Three Versions of the Same Idea
```ts
// src/numeric.ts
export enum StatusNumeric { Active, Inactive, Banned }

// src/string-enum.ts
export enum StatusString {
  Active   = 'active',
  Inactive = 'inactive',
  Banned   = 'banned',
}

// src/as-const.ts
export const Status = {
  Active:   'active',
  Inactive: 'inactive',
  Banned:   'banned',
} as const;
export type Status = typeof Status[keyof typeof Status];   // 'active'|'inactive'|'banned'
```

#### Step 3 — Usage Comparison
```ts
// src/main.ts
import { StatusNumeric } from './numeric.js';
import { StatusString }  from './string-enum.js';
import { Status }        from './as-const.js';

// Numeric enum (DANGEROUS):
const a: StatusNumeric = 999;       // ❌❌❌ NO type error!
console.log(StatusNumeric.Active);  // 0
console.log(StatusNumeric[0]);      // 'Active' — reverse mapping (bundle cost)

// String enum (better):
const b: StatusString = 'active';
// const c: StatusString = 'unknown';   // ❌ TS error

// as-const literal union (best):
const d: Status = 'active';
// const e: Status = 'unknown';         // ❌ TS error
```

#### Step 4 — Error Handling: Exhaustive Switch
```ts
function describe(s: Status): string {
  switch (s) {
    case 'active':   return 'User is active';
    case 'inactive': return 'User is dormant';
    case 'banned':   return 'User is banned';
    default: {
      const _: never = s;
      return _;
    }
  }
}
console.log(describe(Status.Active));
```

#### Step 5 — Tests + Bundle Size Inspection
```bash
npx tsc
# Inspect outputs:
cat dist/numeric.js      # ~200 bytes — emits a full Status object with reverse mapping
cat dist/string-enum.js  # ~150 bytes — Status object without reverse mapping
cat dist/as-const.js     # ~100 bytes — just a frozen-ish object
```
```ts
import { describe, it, expect } from 'vitest';
import { Status } from './as-const.js';

describe('Status as const', () => {
  it('values are literal strings', () => {
    expect(Status.Active).toBe('active');
  });
  it('type narrows correctly', () => {
    const s: Status = 'banned';
    expect(s).toBe('banned');
  });
});
```

**Expected Output:**
```
0
Active
User is active

# bundle sizes (gzip):
numeric.js:     ~80 B  (has reverse mapping table)
string-enum.js: ~60 B
as-const.js:    ~40 B  (best, fully tree-shakable)
```

**Stretch Challenges:**
- [ ] Try `const enum Status` and observe the inlined output — then enable isolatedModules and watch it break
- [ ] Build a numeric flag enum (bitwise — e.g. permissions) and compare to a Set<Permission>
- [ ] Migrate a real project's enums to as-const and measure bundle delta

## SPACED REVIEW

### Day 1 — Recall
**Q1:** What's the default starting value of a numeric enum?
**Q2:** Why are enums controversial in modern TS codebases?
**Q3:** Write a Status using `as const` + literal-union type. From memory.

### Day 3 — Comprehension
**Q4:** Why does `const x: NumericEnum = 999` not error?
**Q5:** What does `const enum` do, and why is it incompatible with isolatedModules?
**Q6:** Refactor this numeric enum to a typed literal-union:
```ts
enum LogLevel { Debug, Info, Warn, Error }
function log(level: LogLevel, msg: string) { /* */ }
```

### Day 7 — Application
**Q7:** Build a permissions system using a bitwise number enum (read=1, write=2, admin=4) and compare with a Set<Permission>.
**Q8:** A PR adds a string enum to a library. Explain what consumers must do to use it (tree-shaking risk).
**Q9:** What is the file-size cost per enum member at scale (1000 enums)?

### Day 14 — Synthesis
**Q10:** ★ Interview: "When would you choose enum vs literal union vs as-const object?"
**Q11:** Draw: bundle output diff for each of the three Status forms.
**Q12:** ★ System design: "Audit a 100k-line TS app for enum usage and plan migration to literal unions. What tooling, what risks?"
