---
slug: "window-functions"
title: "Window Functions"
level: 3
---

## WHY
`GROUP BY` aggregations collapse multiple rows into one. If you want the average salary per department, `GROUP BY department` gives you one row per department. But what if you want to see each individual employee AND their department average salary on the same row? With `GROUP BY`, you'd have to run two separate queries and join them.

**Window Functions** solve this. They perform calculations across a "window" of rows related to the current row WITHOUT collapsing those rows. Every row remains in the output, but each row gains a new computed column derived from the aggregate calculation across the window.

## THEORY
### The `OVER()` Clause
The key difference between a regular aggregate and a window function is the `OVER()` clause. Appending `OVER()` to an aggregate function transforms it into a window function.

- `SUM(salary) OVER(PARTITION BY department)` — Calculates the total salary for each department AND displays it next to every individual employee row.
- `RANK() OVER(ORDER BY salary DESC)` — Ranks every employee by salary without collapsing the rows.

## VISUALIZATION_CONFIG
```json
{ "component": "CodeRunner", "state": "sql-window-functions" }
```

## CODE

### Level 1 — Beginner (Basic Aggregation over a Window)
```sql
-- WITHOUT Window Function: You get the average, but lose individual rows
SELECT department, AVG(salary) AS dept_avg
FROM employees
GROUP BY department;

-- WITH Window Function: Individual rows PRESERVED + dept avg!
SELECT
    name,
    department,
    salary,
    AVG(salary) OVER(PARTITION BY department) AS dept_avg_salary,
    salary - AVG(salary) OVER(PARTITION BY department) AS deviation_from_avg
FROM employees
ORDER BY department, salary DESC;

/* Output:
name     | department | salary | dept_avg_salary | deviation_from_avg
---------|------------|--------|-----------------|-------------------
Alice    | Engineering| 120000 | 100000          | +20000
Bob      | Engineering| 90000  | 100000          | -10000
Charlie  | Engineering| 90000  | 100000          | -10000
Dave     | Marketing  | 70000  | 65000           | +5000
Eve      | Marketing  | 60000  | 65000           | -5000
*/
```

### Level 2 — Building Blocks (Ranking Functions)
```sql
-- RANK() vs DENSE_RANK() vs ROW_NUMBER()
SELECT
    name,
    department,
    salary,
    
    -- ROW_NUMBER: Unique, sequential number for every row (no ties)
    ROW_NUMBER() OVER(PARTITION BY department ORDER BY salary DESC) AS row_num,
    
    -- RANK: Ties get the same rank, next rank SKIPS numbers (1, 1, 3)
    RANK() OVER(PARTITION BY department ORDER BY salary DESC) AS salary_rank,
    
    -- DENSE_RANK: Ties get the same rank, but next rank does NOT skip (1, 1, 2)
    DENSE_RANK() OVER(PARTITION BY department ORDER BY salary DESC) AS dense_rank
FROM employees;

-- Practical use: Get the TOP earner in each department
SELECT name, department, salary
FROM (
    SELECT
        name, department, salary,
        RANK() OVER(PARTITION BY department ORDER BY salary DESC) AS r
    FROM employees
) ranked
WHERE r = 1;
```

### Level 3 — Intermediate (LAG and LEAD — Accessing Adjacent Rows)
```sql
-- LAG: Access data from a PREVIOUS row in the result set
-- LEAD: Access data from a NEXT row in the result set

-- Month-over-month revenue comparison
SELECT
    month_year,
    total_revenue,
    LAG(total_revenue) OVER(ORDER BY month_year) AS prev_month_revenue,
    total_revenue - LAG(total_revenue, 1, 0) OVER(ORDER BY month_year) AS revenue_change,
    ROUND(
        (total_revenue - LAG(total_revenue) OVER(ORDER BY month_year))
        / LAG(total_revenue) OVER(ORDER BY month_year) * 100, 2
    ) AS growth_percentage
FROM monthly_revenue
ORDER BY month_year;
```

### Level 4 — Advanced (Frames — ROWS BETWEEN)
Window Functions can define a sliding "Frame" around the current row to create Moving Averages and Running Totals.

```sql
-- RUNNING TOTAL (cumulative sum)
SELECT
    order_date,
    daily_sales,
    SUM(daily_sales) OVER(
        ORDER BY order_date
        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW  -- All rows up to current
    ) AS cumulative_sales
FROM daily_sales_summary;

-- 7-DAY MOVING AVERAGE (smooths out noise in time-series data)
SELECT
    order_date,
    daily_sales,
    ROUND(AVG(daily_sales) OVER(
        ORDER BY order_date
        ROWS BETWEEN 6 PRECEDING AND CURRENT ROW  -- Current row + previous 6 rows
    ), 2) AS seven_day_moving_avg
FROM daily_sales_summary;
```

## REAL_WORLD
Window functions are essential for building **Analytical Dashboards and Business Intelligence (BI) reports** without pulling data into application memory.

Rather than fetching all 1 million sales records into Node.js/Python and calculating rankings, averages, and trends in code, you push the computation down to the database engine. PostgreSQL can process millions of rows and compute window functions in a fraction of a second using optimized C code, returning only the pre-computed final table to your application.

## INTERVIEW
**Q: What is the execution order of SQL clauses? Where do Window Functions execute?**
A: SQL executes clauses in this order: `FROM` → `WHERE` → `GROUP BY` → `HAVING` → **Window Functions (inside SELECT)** → `ORDER BY` → `LIMIT`. Because Window Functions execute after `WHERE` and `GROUP BY` but before `ORDER BY`, you cannot filter by a window function result in a `WHERE` clause. You must wrap it in a subquery or CTE.

**Q: Can you combine a `GROUP BY` with Window Functions in the same query?**
A: Yes! First, the `GROUP BY` collapses the data. Then, the Window Function operates on the collapsed, aggregated result set. However, you can no longer reference individual pre-aggregated columns.

**Q: What is the performance difference between `ROW_NUMBER()`, `RANK()`, and `DENSE_RANK()`?**
A: They all have essentially the same performance since they all require sorting the window. The choice between them is purely semantic — based on how you want ties to be handled in the ranking output.

## FEYNMAN CHECK

### Explain Window Functions Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Window Functions** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Window Functions, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Window Functions solve? Why would JavaScript/web development be harder without it?**
> **A:** Window Functions solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Window Functions?**
> **A:** The single most important thing is understanding the *mental model*: Window Functions operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Window Functions and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Window Functions operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Window Functions interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Window Functions interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Window Functions to a senior engineer in two sentences, what would you say?**
> **A:** "Window Functions works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Window Functions Utility

**Goal:** Implement a real, working Window Functions module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `windowfunctions` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir window-functions-demo && cd window-functions-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Window Functions Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Window Functions.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createWindowFunctions(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Window Functions] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Window Functions logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Window Functions] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Window Functions] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createWindowFunctions({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Window Functions] Input cannot be null or undefined`);
    }
    const instance = createWindowFunctions();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Window Functions] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Window Functions Tests ──');

test('initialises correctly', () => {
  const m = createWindowFunctions();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createWindowFunctions();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Window Functions] Initialized with: hello world
[Window Functions] Processed: hello world
Result: hello world
[Window Functions] Destroyed.

── Window Functions Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Window Functions in one sentence.**
<details><summary>Answer</summary>

Window Functions is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Window Functions?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Window Functions.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Window Functions and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Window Functions | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Window Functions] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Window Functions causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Window Functions usage].
</details>

**Q6: What does Window Functions look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Window Functions typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Window Functions correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Window Functions:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_window_functions(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Window Functions in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Window Functions in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Window Functions', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Window Functions at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Window Functions uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Window Functions interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Window Functions is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Window Functions relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Window Functions
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Window Functions to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Window Functions fits in the data/control flow.
2. **Choose** the right variant/pattern of Window Functions for the scale requirement.
3. **Handle** failure modes: what happens when Window Functions is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Window Functions is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
