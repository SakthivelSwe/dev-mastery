---
slug: "error-handling-api"
title: "API Error Handling & Standards"
level: 3
---

## WHY
If your API crashes because a user submitted an invalid email address, and your backend returns a generic HTTP `500 Internal Server Error` with a 200-line Java StackTrace in the body, you have failed as an API designer.

The frontend developer (or third-party client) parsing your response has no idea *why* it failed. Was it a network glitch? Bad input? A database timeout? 
**API Error Handling** is the art of returning predictable, structured, and actionable error messages using standard HTTP status codes. Proper error handling turns an API from a frustrating black box into a joyful developer experience.

## THEORY
### HTTP Status Code Categories
The foundation of REST error handling is returning the correct HTTP status code. 
- **2xx (Success):** Everything worked perfectly.
- **4xx (Client Error):** The frontend messed up. (e.g., Bad syntax, unauthorized, resource not found). The client should *not* retry the exact same request because it will fail again.
- **5xx (Server Error):** The backend messed up. (e.g., Database crashed, unhandled exception). The client *can* retry later, hoping the server has recovered.

### RFC 7807: Problem Details for HTTP APIs
Instead of inventing your own custom JSON error format, the industry standard is RFC 7807 (`application/problem+json`). It defines a standardized JSON structure for returning errors.

## VISUALIZATION_CONFIG
```json
{ "component": "ArchitectureVisualizer", "state": "api-error-handling" }
```

## CODE

### Level 1 — Beginner (Status Codes)
Learn the core 4xx codes. Returning a `400 Bad Request` for an authentication failure is a rookie mistake.

```json
// HTTP 400 Bad Request
// The client sent malformed JSON or failed basic validation (e.g., age = -5).

// HTTP 401 Unauthorized
// The client provided NO authentication token, or an EXPIRED token.
// (Think: "Who are you?")

// HTTP 403 Forbidden
// The client provided a VALID token, but they do not have PERMISSION.
// (e.g., A standard user trying to access the /admin dashboard).
// (Think: "I know who you are, but you aren't allowed to do this.")

// HTTP 404 Not Found
// The requested resource does not exist (e.g., /users/9999).

// HTTP 429 Too Many Requests
// Rate limiting! The client is sending too many requests too quickly.
```

### Level 2 — Building Blocks (RFC 7807 Format)
Instead of a random error string, return a structured RFC 7807 response.

```json
// Response Header: Content-Type: application/problem+json
// HTTP Status: 400 Bad Request

{
  // A URI reference that identifies the problem type. 
  // (Usually links to your API documentation!)
  "type": "https://api.mycompany.com/errors/insufficient-funds",
  
  // A short, human-readable summary of the problem type
  "title": "Insufficient Funds",
  
  // The HTTP status code (mirrored here for convenience)
  "status": 400,
  
  // A human-readable explanation specific to THIS occurrence of the problem
  "detail": "Your current balance is $30, but the item costs $50.",
  
  // A URI identifying the specific occurrence of the problem (for logging/debugging)
  "instance": "/account/12345/transactions/trace-987"
}
```

### Level 3 — Intermediate (Field-Level Validation Errors)
What happens if the user submits a registration form, but the password is too short AND the email is already taken? 
You should not return just one error. You must return a list of all validation failures so the frontend can highlight the specific input boxes in red!
RFC 7807 allows you to add custom extension fields (like `invalidParams`).

```json
// Response Header: Content-Type: application/problem+json
// HTTP Status: 400 Bad Request

{
  "type": "https://api.mycompany.com/errors/validation-failed",
  "title": "Validation Failed",
  "status": 400,
  "detail": "The request payload failed validation. See 'invalidParams' for details.",
  
  // CUSTOM FIELD: Array of specific field errors!
  "invalidParams": [
    {
      "field": "email",
      "reason": "Email address 'john@com' is missing a valid TLD."
    },
    {
      "field": "password",
      "reason": "Password must be at least 8 characters long."
    }
  ]
}
```

### Level 4 — Advanced (Global Exception Handlers)
If your API has 100 endpoints, you DO NOT want to write `try/catch` blocks in every single controller to format these JSON responses manually.
Modern backend frameworks (Express, Spring Boot, ASP.NET) support **Global Exception Handlers**.

```java
// Spring Boot Example (@ControllerAdvice)
@RestControllerAdvice
public class GlobalExceptionHandler {

    // 1. Catch ALL exceptions of a specific type (e.g., UserNotFoundException)
    @ExceptionHandler(UserNotFoundException.class)
    public ResponseEntity<ProblemDetail> handleUserNotFound(UserNotFoundException ex) {
        
        // 2. Automatically format it into an RFC 7807 ProblemDetail object!
        ProblemDetail problem = ProblemDetail.forStatusAndDetail(
            HttpStatus.NOT_FOUND, 
            ex.getMessage()
        );
        problem.setType(URI.create("https://api.mycompany.com/errors/not-found"));
        problem.setTitle("Resource Not Found");
        
        // 3. Return the standardized JSON
        return ResponseEntity.status(HttpStatus.NOT_FOUND).body(problem);
    }
    
    // Catch a generic Exception (The dreaded HTTP 500)
    @ExceptionHandler(Exception.class)
    public ResponseEntity<ProblemDetail> handleGenericError(Exception ex) {
        // DANGER: NEVER expose the StackTrace to the client in production!
        // It exposes your internal architecture to hackers.
        // Log the StackTrace internally, but return a generic message to the user!
        log.error("Unhandled Exception: ", ex);
        
        ProblemDetail problem = ProblemDetail.forStatusAndDetail(
            HttpStatus.INTERNAL_SERVER_ERROR, 
            "An unexpected internal server error occurred. Please try again later."
        );
        return ResponseEntity.status(500).body(problem);
    }
}
```

## REAL_WORLD
**Stripe API Errors**
Stripe is widely considered to have the best developer experience (DX) in the world. A massive part of this is their error handling. 
If your credit card charge fails, Stripe doesn't just return `400 Bad Request`. They return a highly structured error object with a `code` (e.g., `card_declined`), a `decline_code` (e.g., `insufficient_funds`), and a human-readable `message` that the developer can display directly to the end-user. 
They also guarantee that their Error JSON schema will *never* change and provides a unique `request_id` in every single response. If a developer contacts Stripe Support, they just paste the `request_id`, and Stripe engineers can instantly find the exact internal logs for that exact failure.

## INTERVIEW
**Q: I am building an API. If the user searches for a specific ID (`GET /users/999`) and it doesn't exist, I return `404 Not Found`. However, if the user searches a list (`GET /users?status=active`) and there are no active users, should I return `404 Not Found` or `200 OK`?**
A: **You should return `200 OK` with an empty array `[]`.**
A `404 Not Found` means the *Resource URI* itself does not exist. The collection `/users` absolutely exists; it just happens to have zero elements in it at this moment. 
Returning `404` for an empty array is a major anti-pattern. It forces the frontend developer to write a massive `try/catch` block just to handle an empty list, whereas returning `[]` with a `200 OK` allows the frontend to simply loop over the array (which will execute zero times safely) without throwing any JavaScript errors.

## FEYNMAN CHECK

### Explain API Error Handling & Standards Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **API Error Handling & Standards** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about API Error Handling & Standards, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does API Error Handling & Standards solve? Why would JavaScript/web development be harder without it?**
> **A:** API Error Handling & Standards solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about API Error Handling & Standards?**
> **A:** The single most important thing is understanding the *mental model*: API Error Handling & Standards operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with API Error Handling & Standards and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which API Error Handling & Standards operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does API Error Handling & Standards interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** API Error Handling & Standards interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of API Error Handling & Standards to a senior engineer in two sentences, what would you say?**
> **A:** "API Error Handling & Standards works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a API Error Handling & Standards Utility

**Goal:** Implement a real, working API Error Handling & Standards module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `errorhandlingapi` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir error-handling-api-demo && cd error-handling-api-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── API Error Handling & Standards Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of API Error Handling & Standards.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createAPIErrorHandling&Standards(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[API Error Handling & Standards] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual API Error Handling & Standards logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[API Error Handling & Standards] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[API Error Handling & Standards] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createAPIErrorHandling&Standards({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[API Error Handling & Standards] Input cannot be null or undefined`);
    }
    const instance = createAPIErrorHandling&Standards();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[API Error Handling & Standards] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── API Error Handling & Standards Tests ──');

test('initialises correctly', () => {
  const m = createAPIErrorHandling&Standards();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createAPIErrorHandling&Standards();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[API Error Handling & Standards] Initialized with: hello world
[API Error Handling & Standards] Processed: hello world
Result: hello world
[API Error Handling & Standards] Destroyed.

── API Error Handling & Standards Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define API Error Handling & Standards in one sentence.**
<details><summary>Answer</summary>

API Error Handling & Standards is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of API Error Handling & Standards?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of API Error Handling & Standards.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between API Error Handling & Standards and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | API Error Handling & Standards | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of API Error Handling & Standards] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing API Error Handling & Standards causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct API Error Handling & Standards usage].
</details>

**Q6: What does API Error Handling & Standards look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, API Error Handling & Standards typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use API Error Handling & Standards correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using API Error Handling & Standards:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_error_handling_api(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test API Error Handling & Standards in isolation? What would you mock?**
<details><summary>Answer</summary>

To test API Error Handling & Standards in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('API Error Handling & Standards', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of API Error Handling & Standards at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how API Error Handling & Standards uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common API Error Handling & Standards interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "API Error Handling & Standards is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does API Error Handling & Standards relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
API Error Handling & Standards
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use API Error Handling & Standards to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where API Error Handling & Standards fits in the data/control flow.
2. **Choose** the right variant/pattern of API Error Handling & Standards for the scale requirement.
3. **Handle** failure modes: what happens when API Error Handling & Standards is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you API Error Handling & Standards is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
