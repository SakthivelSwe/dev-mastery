---
slug: "authorization-api"
title: "API Authorization (RBAC & ABAC)"
level: 3
---

## WHY
Once your API knows *who* a user is (Authentication), it must determine *what* they are allowed to do. If Alice logs into your HR system, she shouldn't be able to view Bob's salary just because she is authenticated. A failure in Authorization (AuthZ) leads to IDOR (Insecure Direct Object Reference) vulnerabilities, which are consistently ranked as the #1 most critical API security flaw by OWASP.

## THEORY
### Role-Based Access Control (RBAC)
The most common authorization pattern. Users are assigned predefined **Roles** (e.g., `Admin`, `Editor`, `Viewer`). The API checks the user's role before allowing them to execute an endpoint.

### Attribute-Based Access Control (ABAC)
RBAC is often too rigid. What if an `Editor` is only allowed to edit an article *if they are the original author*? The API must evaluate the specific **Attributes** of the resource (the article's `authorId`) against the attributes of the user (the user's `id`).

## VISUALIZATION_CONFIG
```json
{
  "language": "http",
  "fileName": "authz.http",
  "steps": [
    {
      "title": "RBAC — Role-Based Access Control",
      "description": "Users are assigned roles. Roles have permissions. Simple, works well for most apps with a few clear roles (admin, editor, viewer).",
      "code": "# User → Role → Permissions\nUser Alice → Role: admin → [read:*, write:*, delete:*]\nUser Bob   → Role: editor → [read:*, write:topics]\nUser Carol → Role: viewer → [read:*]\n\n# Check in middleware\nfunction requireRole(role: string) {\n  return (req, res, next) => {\n    if (req.user.role !== role) return res.status(403).json({error: \"Forbidden\"});\n    next();\n  };\n}\n\napp.delete(\"/topics/:id\", requireRole(\"admin\"), deleteTopic);\napp.post(\"/topics\", requireRole(\"editor\"), createTopic);\n\n# Spring Security\n@PreAuthorize(\"hasRole('ADMIN')\")\npublic void deleteTopic(String id) { ... }",
      "highlight": [
        2,
        3,
        4,
        7,
        8,
        9,
        10,
        13,
        14,
        17,
        18
      ],
      "diagram": {
        "kind": "boxes",
        "title": "RBAC benefits",
        "items": [
          {
            "label": "Simple mental model",
            "color": "#10b981",
            "value": "User → Role → Permissions",
            "highlight": true
          },
          {
            "label": "Easy to implement",
            "color": "#818cf8",
            "value": "Middleware / annotation check"
          },
          {
            "label": "Scales up to ~20 roles",
            "color": "#f59e0b",
            "value": "Beyond that: ABAC"
          },
          {
            "label": "Rigid",
            "color": "#ef4444",
            "value": "Can't express \"only own resources\""
          }
        ]
      }
    },
    {
      "title": "ABAC — Attribute-Based Access Control",
      "description": "Decisions based on attributes of user, resource, action, and context. More flexible than RBAC. Rules like \"editors can update topics they created\".",
      "code": "# ABAC: policy considers multiple attributes\n// Attributes: user, resource, action, environment\n\nasync function canUpdateTopic(user, topic) {\n  // Rule 1: admins can update anything\n  if (user.role === \"admin\") return true;\n\n  // Rule 2: authors can update their own drafts\n  if (topic.authorId === user.id && topic.status === \"draft\") return true;\n\n  // Rule 3: editors can update in their domain\n  if (user.role === \"editor\" && user.domains.includes(topic.domain)) return true;\n\n  return false;\n}\n\n# OPA (Open Policy Agent) — externalized ABAC engine\npackage authz\nallow {\n  input.user.role == \"admin\"\n}\nallow {\n  input.action == \"update\"\n  input.resource.author == input.user.id\n  input.resource.status == \"draft\"\n}",
      "highlight": [
        4,
        5,
        6,
        7,
        8,
        9,
        10,
        11,
        12,
        18,
        19,
        20,
        21,
        22,
        23,
        24,
        25
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "Combine attributes: user + resource + action",
            "done": true
          },
          {
            "label": "Evaluate policy rules",
            "done": true
          },
          {
            "label": "Flexible: \"own drafts\", \"in their team\"",
            "active": true
          },
          {
            "label": "Externalize with OPA for large systems"
          }
        ]
      }
    },
    {
      "title": "Scopes and OAuth permissions",
      "description": "OAuth scopes limit what a token can do. Clients request scopes; users approve. Fine-grained access delegation for third-party integrations.",
      "code": "# Client requests specific scopes\nGET https://accounts.google.com/o/oauth2/auth?\n  scope=read:topics+write:progress\n\n# User consent screen\n\"DevMastery wants to:\"\n\"- Read your topics\"\n\"- Update your progress\"\n[Allow] [Deny]\n\n# Access token limited to approved scopes\nGET /api/topics\nAuthorization: Bearer eyJ...\n→ 200 OK (scope: read:topics ✓)\n\nDELETE /api/topics/42\nAuthorization: Bearer eyJ...\n→ 403 Forbidden (no delete:topics scope)\n\n# Scope naming conventions\nread:resource\nwrite:resource\nadmin:resource",
      "highlight": [
        3,
        7,
        8,
        9,
        14,
        17,
        18,
        21,
        22,
        23,
        24
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Scopes design",
        "items": [
          {
            "label": "Least privilege",
            "color": "#10b981",
            "value": "Request only needed scopes",
            "highlight": true
          },
          {
            "label": "Composable",
            "color": "#818cf8",
            "value": "read:topics write:progress"
          },
          {
            "label": "User-visible",
            "color": "#f59e0b",
            "value": "Shown on consent screen"
          },
          {
            "label": "Verb:resource",
            "color": "#60a5fa",
            "value": "Naming convention: read:x, write:x"
          }
        ]
      }
    },
    {
      "title": "Row-level security",
      "description": "Users see only their own data. Enforce at the query level (WHERE user_id = ?) or at DB level (PostgreSQL RLS, Supabase policies).",
      "code": "# Application-level: filter in query\nasync function getUserOrders(userId: string) {\n  return db.query(\"SELECT * FROM orders WHERE user_id = $1\", [userId]);\n}\n\n# PostgreSQL Row-Level Security\nALTER TABLE orders ENABLE ROW LEVEL SECURITY;\n\nCREATE POLICY user_orders_policy ON orders\n  FOR ALL\n  USING (user_id = current_setting('app.user_id')::uuid);\n\n# Supabase RLS\nCREATE POLICY \"Users see own topics\" ON topics\n  FOR SELECT\n  USING (auth.uid() = author_id);\n\nCREATE POLICY \"Public topics visible to all\" ON topics\n  FOR SELECT\n  USING (is_published = true);",
      "highlight": [
        2,
        3,
        7,
        8,
        9,
        10,
        11,
        13,
        14,
        15,
        16,
        18,
        19,
        20
      ],
      "diagram": {
        "kind": "flow",
        "steps": [
          {
            "label": "App tier: WHERE user_id = ?",
            "done": true
          },
          {
            "label": "DB tier: RLS enforces on every query",
            "done": true
          },
          {
            "label": "RLS: safety net if app forgets filter",
            "active": true
          },
          {
            "label": "Supabase: policies use auth.uid()"
          }
        ]
      }
    },
    {
      "title": "Multi-tenant authorization",
      "description": "SaaS apps have multiple customers (tenants). Isolate data by tenant. Every query includes tenant_id. Never allow cross-tenant access.",
      "code": "# Multi-tenant patterns:\n\n# 1. Row-level with tenant_id column\nCREATE TABLE topics (\n  id UUID PRIMARY KEY,\n  tenant_id UUID NOT NULL,\n  title TEXT NOT NULL,\n  ...\n);\nCREATE INDEX ON topics(tenant_id);\n\n# Every query filters by tenant\nSELECT * FROM topics WHERE tenant_id = $1 AND ...;\n\n# 2. Schema per tenant (PostgreSQL)\nSET search_path TO tenant_123, public;\nSELECT * FROM topics;  # queries tenant_123.topics\n\n# 3. Database per tenant (isolation, but ops cost)\n\n# Middleware injects tenant\nfunction tenantMiddleware(req, res, next) {\n  req.tenantId = req.user.tenantId;\n  next();\n}\n\n# Every service call receives tenantId\nfindTopics({ tenantId, filter });  # cannot access other tenants",
      "highlight": [
        3,
        4,
        5,
        6,
        7,
        8,
        9,
        10,
        12,
        13,
        15,
        16,
        17,
        19,
        22,
        23,
        24,
        27,
        28
      ],
      "diagram": {
        "kind": "boxes",
        "title": "Multi-tenancy models",
        "items": [
          {
            "label": "Shared DB, tenant_id column",
            "color": "#10b981",
            "value": "Most common — cheap, needs discipline",
            "highlight": true
          },
          {
            "label": "Schema per tenant",
            "color": "#818cf8",
            "value": "Better isolation, some ops overhead"
          },
          {
            "label": "DB per tenant",
            "color": "#f59e0b",
            "value": "Maximum isolation, high ops cost"
          },
          {
            "label": "Tenant in ALL queries",
            "color": "#ef4444",
            "value": "Forgetting once = data leak"
          }
        ]
      }
    }
  ]
}
```

## CODE

### Level 1 — Beginner (Simple RBAC)
In many frameworks (like Spring Boot or NestJS), RBAC is enforced using simple decorators or annotations directly on the controller methods.

```java
@RestController
@RequestMapping("/api/reports")
public class ReportController {

    // Any authenticated user can read the reports
    @GetMapping
    @PreAuthorize("hasRole('USER')")
    public List<Report> getAllReports() { ... }

    // ONLY Admins can delete reports!
    @DeleteMapping("/{id}")
    @PreAuthorize("hasRole('ADMIN')")
    public void deleteReport(@PathVariable String id) { ... }
}
```

### Level 2 — Building Blocks (The IDOR Vulnerability)
Imagine a simple banking API where a user fetches their account details.

```javascript
// ❌ BAD: A classic IDOR Vulnerability
app.get('/api/accounts/:accountId', (req, res) => {
    // 1. We extract the account ID from the URL (/api/accounts/999)
    const targetAccountId = req.params.accountId;
    
    // 2. We fetch the account from the DB
    const account = db.accounts.findById(targetAccountId);
    
    // 3. We return it!
    res.json(account);
});
```
**Why is this a critical vulnerability?**
Alice logs in. Her account ID is `123`. The frontend fetches `/api/accounts/123`. 
Alice opens Postman, changes the URL to `/api/accounts/999`, and sends the request. Because the API never checked if Alice actually *owns* account `999`, it returns Bob's banking details to Alice!

### Level 3 — Intermediate (Fixing IDOR with ABAC)
To fix IDOR, the API must perform Attribute-Based Authorization. It must compare the currently authenticated user's ID against the owner of the resource.

```javascript
// ✅ GOOD: Enforcing Ownership (ABAC)
app.get('/api/accounts/:accountId', (req, res) => {
    const targetAccountId = req.params.accountId;
    
    // 1. Who is making this request? (Extracted securely from their JWT)
    const currentUserId = req.user.id;
    
    // 2. Fetch the requested account
    const account = db.accounts.findById(targetAccountId);
    
    // 3. AUTHORIZATION CHECK!
    // Does the current user own this account? Are they a super admin?
    if (account.ownerId !== currentUserId && req.user.role !== 'ADMIN') {
        // Return 403 Forbidden!
        return res.status(403).json({ error: "Access Denied." });
    }
    
    res.json(account);
});
```

### Level 4 — Advanced (Externalized Authorization - OPA)
If you have 50 different microservices, writing `if (user.id == resource.ownerId)` inside every single service leads to massive duplication. If the business changes the rules (e.g., "Admins can no longer view private accounts on weekends"), you have to update and deploy 50 microservices.

Modern enterprise architecture uses **Externalized Authorization**. 
Tools like **OPA (Open Policy Agent)** run as a standalone sidecar next to your microservices. You write your authorization rules in a special policy language (Rego) and load them into OPA.

When a request hits your microservice, the microservice pauses and asks OPA: "User 123 wants to read Article 99. Allowed?" OPA evaluates the global policies and returns a simple `True` or `False`. Your microservices no longer contain *any* authorization logic!

## REAL_WORLD
**401 Unauthorized vs 403 Forbidden**
Many junior developers mix up these HTTP status codes.
- **401 Unauthorized:** Actually means *Unauthenticated*. It means "I don't know who you are. Please log in." (The client forgot to send a JWT, or it expired).
- **403 Forbidden:** Actually means *Unauthorized*. It means "I know exactly who you are, Alice, but you do not have permission to delete this file." (The client sent a valid JWT, but failed the RBAC/ABAC check).

## INTERVIEW
**Q: How do you handle authorization when a user requests a list of 10,000 items, but they are only allowed to see 50 of them? You can't fetch 10,000 items into memory and run an `if` check on every single one.**
A: This is a classic problem known as **Data Filtering Authorization**. You must push the authorization logic down into the database query itself. Instead of fetching all items and filtering in memory, the Business Layer injects the user's ID directly into the SQL `WHERE` clause (e.g., `SELECT * FROM articles WHERE is_published = true OR author_id = ?`). The database does the heavy lifting, ensuring the user only ever receives the exact records they are authorized to see.

## FEYNMAN CHECK

### Explain API Authorization (RBAC & ABAC) Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **API Authorization (RBAC & ABAC)** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about API Authorization (RBAC & ABAC), they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does API Authorization (RBAC & ABAC) solve? Why would JavaScript/web development be harder without it?**
> **A:** API Authorization (RBAC & ABAC) solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about API Authorization (RBAC & ABAC)?**
> **A:** The single most important thing is understanding the *mental model*: API Authorization (RBAC & ABAC) operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with API Authorization (RBAC & ABAC) and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which API Authorization (RBAC & ABAC) operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does API Authorization (RBAC & ABAC) interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** API Authorization (RBAC & ABAC) interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of API Authorization (RBAC & ABAC) to a senior engineer in two sentences, what would you say?**
> **A:** "API Authorization (RBAC & ABAC) works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a API Authorization (RBAC & ABAC) Utility

**Goal:** Implement a real, working API Authorization (RBAC & ABAC) module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `authorizationapi` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir authorization-api-demo && cd authorization-api-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── API Authorization (RBAC & ABAC) Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of API Authorization (RBAC & ABAC).
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createAPIAuthorization(RBAC&ABAC)(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[API Authorization (RBAC & ABAC)] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual API Authorization (RBAC & ABAC) logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[API Authorization (RBAC & ABAC)] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[API Authorization (RBAC & ABAC)] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createAPIAuthorization(RBAC&ABAC)({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[API Authorization (RBAC & ABAC)] Input cannot be null or undefined`);
    }
    const instance = createAPIAuthorization(RBAC&ABAC)();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[API Authorization (RBAC & ABAC)] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── API Authorization (RBAC & ABAC) Tests ──');

test('initialises correctly', () => {
  const m = createAPIAuthorization(RBAC&ABAC)();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createAPIAuthorization(RBAC&ABAC)();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[API Authorization (RBAC & ABAC)] Initialized with: hello world
[API Authorization (RBAC & ABAC)] Processed: hello world
Result: hello world
[API Authorization (RBAC & ABAC)] Destroyed.

── API Authorization (RBAC & ABAC) Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define API Authorization (RBAC & ABAC) in one sentence.**
<details><summary>Answer</summary>

API Authorization (RBAC & ABAC) is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of API Authorization (RBAC & ABAC)?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of API Authorization (RBAC & ABAC).**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between API Authorization (RBAC & ABAC) and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | API Authorization (RBAC & ABAC) | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of API Authorization (RBAC & ABAC)] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing API Authorization (RBAC & ABAC) causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct API Authorization (RBAC & ABAC) usage].
</details>

**Q6: What does API Authorization (RBAC & ABAC) look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, API Authorization (RBAC & ABAC) typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use API Authorization (RBAC & ABAC) correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using API Authorization (RBAC & ABAC):
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_authorization_api(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test API Authorization (RBAC & ABAC) in isolation? What would you mock?**
<details><summary>Answer</summary>

To test API Authorization (RBAC & ABAC) in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('API Authorization (RBAC & ABAC)', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of API Authorization (RBAC & ABAC) at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how API Authorization (RBAC & ABAC) uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common API Authorization (RBAC & ABAC) interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "API Authorization (RBAC & ABAC) is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does API Authorization (RBAC & ABAC) relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
API Authorization (RBAC & ABAC)
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use API Authorization (RBAC & ABAC) to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where API Authorization (RBAC & ABAC) fits in the data/control flow.
2. **Choose** the right variant/pattern of API Authorization (RBAC & ABAC) for the scale requirement.
3. **Handle** failure modes: what happens when API Authorization (RBAC & ABAC) is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you API Authorization (RBAC & ABAC) is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
