---
slug: "pg-statistics"
title: "PostgreSQL Statistics"
level: 3
---

## WHY
The PostgreSQL Query Planner is like a GPS navigation system. To find the fastest route, it needs an accurate map. PostgreSQL Statistics *are* that map. If the statistics are outdated or inaccurate, the planner might choose a terrible query plan (e.g., doing a sequential scan on a 100GB table instead of using an index), bringing your database to a halt.

## THEORY
### What are Statistics?
PostgreSQL maintains a system catalog (`pg_statistic`) that stores statistical data about the contents of tables and indexes. This includes:
- **Most Common Values (MCV):** A list of the most frequent values in a column.
- **Histograms:** The distribution of data across bounds.
- **Correlation:** How strongly the physical order of rows on disk matches the logical order of a column.
- **Distinct Count:** How many unique values exist in a column.

The `ANALYZE` command is responsible for sampling a random subset of rows from a table and updating these statistics. `AUTOVACUUM` runs `ANALYZE` automatically in the background.

## VISUALIZATION_CONFIG
```json
{ "component": "ArchitectureVisualizer", "state": "pg-statistics" }
```

## CODE

### Manually Updating Statistics
If you just inserted 5 million rows into a table, the auto-analyzer might not have run yet. It is a good practice to manually run ANALYZE.

```sql
-- Analyze a specific table
ANALYZE users;

-- Analyze the entire database
ANALYZE;

-- Run VACUUM and ANALYZE together (very common)
VACUUM ANALYZE orders;
```

### Viewing Statistics
The `pg_statistic` table is hard to read. PostgreSQL provides a human-readable view called `pg_stats`.

```sql
-- View statistics for the 'status' column in the 'orders' table
SELECT 
  null_frac, 
  n_distinct, 
  most_common_vals, 
  most_common_freqs 
FROM pg_stats 
WHERE tablename = 'orders' AND attname = 'status';
```

### Tuning Statistics Targets
By default, PostgreSQL samples 30,000 rows (based on `default_statistics_target = 100`) to generate the histogram and MCV list. For extremely large or highly skewed tables, this sample size might be too small, leading to bad query plans.

```sql
-- Increase the statistics target for a specific column (Max is 10000)
ALTER TABLE large_events ALTER COLUMN event_type SET STATISTICS 1000;

-- You MUST run ANALYZE after changing the target
ANALYZE large_events;
```

## REAL_WORLD
**The "Out of Bounds" Date Problem**
A classic statistics problem occurs with time-series data. Imagine an `orders` table. Statistics show the maximum `created_at` date is '2023-10-01' (because ANALYZE last ran yesterday).

Today is '2023-10-02'. A query asks for:
`SELECT * FROM orders WHERE created_at = '2023-10-02'`

The Query Planner looks at the statistics, sees the max date is yesterday, and estimates that exactly **0 rows** will match. Because it thinks 0 rows will match, it might choose a Nested Loop Join or ignore an index. In reality, there are 10,000 orders from today, and the query takes 5 minutes instead of 5 milliseconds!

**Solution:** Increase the auto-analyze frequency for that specific table so statistics are updated more often.

## INTERVIEW
**Q: You have a query `SELECT * FROM users WHERE city = 'London' AND status = 'ACTIVE'`. Both columns have indexes, but the query is incredibly slow. PostgreSQL is heavily underestimating the number of rows returned. How do you fix this?**

A: You are experiencing a **Cross-Column Correlation** problem.

PostgreSQL statistics assume that columns are completely independent. It multiplies the probability of `city = 'London'` by the probability of `status = 'ACTIVE'`. But in reality, certain statuses might be highly correlated with certain cities.

**The Fix:** Create **Extended Statistics**. This tells PostgreSQL to analyze the relationship *between* the two columns.

```sql
CREATE STATISTICS st_users_city_status (dependencies) 
ON city, status FROM users;

ANALYZE users;
```
Now the planner knows how often 'London' and 'ACTIVE' appear *together*, resulting in an accurate row estimate and a much faster query plan.

## FEYNMAN CHECK

### Explain PostgreSQL Statistics Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **PostgreSQL Statistics** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about PostgreSQL Statistics, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does PostgreSQL Statistics solve? Why would JavaScript/web development be harder without it?**
> **A:** PostgreSQL Statistics solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about PostgreSQL Statistics?**
> **A:** The single most important thing is understanding the *mental model*: PostgreSQL Statistics operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with PostgreSQL Statistics and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which PostgreSQL Statistics operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does PostgreSQL Statistics interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** PostgreSQL Statistics interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of PostgreSQL Statistics to a senior engineer in two sentences, what would you say?**
> **A:** "PostgreSQL Statistics works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a PostgreSQL Statistics Utility

**Goal:** Implement a real, working PostgreSQL Statistics module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `pgstatistics` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir pg-statistics-demo && cd pg-statistics-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── PostgreSQL Statistics Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of PostgreSQL Statistics.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createPostgreSQLStatistics(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[PostgreSQL Statistics] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual PostgreSQL Statistics logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[PostgreSQL Statistics] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[PostgreSQL Statistics] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createPostgreSQLStatistics({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[PostgreSQL Statistics] Input cannot be null or undefined`);
    }
    const instance = createPostgreSQLStatistics();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[PostgreSQL Statistics] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── PostgreSQL Statistics Tests ──');

test('initialises correctly', () => {
  const m = createPostgreSQLStatistics();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createPostgreSQLStatistics();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[PostgreSQL Statistics] Initialized with: hello world
[PostgreSQL Statistics] Processed: hello world
Result: hello world
[PostgreSQL Statistics] Destroyed.

── PostgreSQL Statistics Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define PostgreSQL Statistics in one sentence.**
<details><summary>Answer</summary>

PostgreSQL Statistics is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of PostgreSQL Statistics?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of PostgreSQL Statistics.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between PostgreSQL Statistics and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | PostgreSQL Statistics | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of PostgreSQL Statistics] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing PostgreSQL Statistics causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct PostgreSQL Statistics usage].
</details>

**Q6: What does PostgreSQL Statistics look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, PostgreSQL Statistics typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use PostgreSQL Statistics correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using PostgreSQL Statistics:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_pg_statistics(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test PostgreSQL Statistics in isolation? What would you mock?**
<details><summary>Answer</summary>

To test PostgreSQL Statistics in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('PostgreSQL Statistics', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of PostgreSQL Statistics at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how PostgreSQL Statistics uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common PostgreSQL Statistics interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "PostgreSQL Statistics is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does PostgreSQL Statistics relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
PostgreSQL Statistics
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use PostgreSQL Statistics to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where PostgreSQL Statistics fits in the data/control flow.
2. **Choose** the right variant/pattern of PostgreSQL Statistics for the scale requirement.
3. **Handle** failure modes: what happens when PostgreSQL Statistics is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you PostgreSQL Statistics is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
