---
slug: "pg-vacuum"
title: "Vacuum and Autovacuum"
level: 3
---

## WHY
Because PostgreSQL uses MVCC (Multi-Version Concurrency Control), when you run an `UPDATE` or `DELETE` statement, the database does not actually delete the old row from the hard drive. It simply marks it as a "Dead Tuple". 

If you update a row 10,000 times, you will have 1 active row and 10,000 dead rows sitting on your hard drive, consuming massive amounts of storage and destroying query performance. The `VACUUM` process is the garbage collector of PostgreSQL. If it is not configured correctly, your database will eventually slow to a halt and die.

## THEORY
### What Vacuum Actually Does
A standard `VACUUM` does **not** shrink the size of the database file on the hard drive.
It simply scans the table pages, finds the Dead Tuples, and marks that exact physical space as "Available for Re-use" in the Free Space Map (FSM). The next time you `INSERT` a row, Postgres will write it into that empty hole instead of appending it to the end of the file.

### Autovacuum
In the early days of Postgres, DBAs had to set up cron jobs to run `VACUUM` every night. Today, Postgres uses an **Autovacuum Daemon**. It constantly monitors tables in the background, and when a table accumulates too many dead tuples (usually 20% of the table size), it automatically triggers a vacuum on that specific table.

## VISUALIZATION_CONFIG
```json
{ "component": "ArchitectureVisualizer", "state": "postgres-vacuum" }
```

## CODE

### Level 1 — Beginner (Manual Vacuuming)
While Autovacuum handles 99% of workloads, you can manually trigger a vacuum if you know you just performed a massive bulk delete.

```sql
-- Vacuum a specific table (Safe to run in production, does not lock the table)
VACUUM users;

-- Vacuum and update statistics for the query planner
VACUUM ANALYZE users;

-- Extremely verbose output showing exactly how many dead tuples were found
VACUUM VERBOSE users;
```

### Level 2 — Building Blocks (Vacuum Full)
If a standard `VACUUM` doesn't shrink the file size, what happens if you delete 1 billion rows and want to reclaim that 500GB of hard drive space back to the operating system?

You must run a **VACUUM FULL**. 
A `VACUUM FULL` physically rewrites the entire table from scratch, perfectly packing the live tuples together and deleting the old bloated file.

**WARNING:** `VACUUM FULL` requires an **Exclusive Lock** on the table. No one can read or write to the table while it runs. If it takes 3 hours to rewrite the 500GB table, your application is down for 3 hours. Never run `VACUUM FULL` in production without a planned maintenance window!

```sql
-- DANGER: Locks the table completely!
VACUUM FULL users;
```

### Level 3 — Intermediate (Tuning Autovacuum)
The default Autovacuum settings in Postgres are incredibly conservative so they don't impact server performance. For a massive database, they are often too slow, meaning tables bloat faster than the Autovacuum can clean them.

You can tune Autovacuum in `postgresql.conf`:
```ini
# By default, autovacuum kicks in when 20% of the tuples are dead.
# For a 100-million row table, that means it waits until 20 million rows are dead!
# That is too much bloat. We lower the scale factor to 5% or 2%.
autovacuum_vacuum_scale_factor = 0.05

# How many background worker threads can run vacuum simultaneously? (Default is 3)
autovacuum_max_workers = 5

# The maximum cost before a vacuum worker forces itself to sleep (to avoid CPU spiking).
# Default is 200. Increase it to make vacuum run much faster and aggressively.
autovacuum_vacuum_cost_limit = 2000
```

### Level 4 — Advanced (Transaction ID Wraparound)
This is the most terrifying event in PostgreSQL.
Postgres gives every transaction a sequential ID (XID). It uses these IDs to determine which rows are visible to which transactions (MVCC). 

The XID is a 32-bit integer, meaning it maxes out at **2.1 billion**.
What happens when you hit 2.1 billion transactions? The ID wraps around back to 0. Suddenly, Postgres looks at a row from yesterday (ID 2.1B) and compares it to a transaction today (ID 1). It thinks the row from yesterday is in the *future*, and makes it invisible to everyone. The entire database appears to have deleted all of its data!

**The Savior (Vacuum Freeze):**
When Autovacuum scans a table, it looks for very old rows. It replaces their XID with a special "Frozen" XID, meaning "This row is permanently visible to everyone forever." This prevents wraparound.

If your database is processing massive amounts of transactions and the Autovacuum is too slow to freeze old rows, Postgres will completely shut down and refuse to accept any new queries to protect your data from Wraparound. You must manually boot it in single-user mode and run a desperate vacuum.

## REAL_WORLD
**Tuning per-table**
You shouldn't always change Autovacuum settings globally in `postgresql.conf`. If you have a specific `sessions` table that gets updated aggressively and constantly bloats, you can change the autovacuum rules for *just that table*!

```sql
-- Tell autovacuum to trigger on the sessions table when 
-- only 1% of the rows are dead!
ALTER TABLE sessions SET (autovacuum_vacuum_scale_factor = 0.01);
```

## INTERVIEW
**Q: I have a massive table. I deleted half the rows, but the database file on disk didn't shrink. I know I shouldn't use `VACUUM FULL` because it locks the table. How can I safely reclaim the disk space in production without downtime?**
A: You use an open-source extension called **`pg_repack`**. It creates a brand new physical table in the background, copies all the live data into it, tracks any real-time changes using triggers, and then executes a millisecond swap of the table names at the very end. It achieves the exact same result as `VACUUM FULL` but requires almost zero locking, allowing your application to stay completely online!

## FEYNMAN CHECK

### Explain Vacuum and Autovacuum Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Vacuum and Autovacuum** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Vacuum and Autovacuum, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Vacuum and Autovacuum solve? Why would JavaScript/web development be harder without it?**
> **A:** Vacuum and Autovacuum solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Vacuum and Autovacuum?**
> **A:** The single most important thing is understanding the *mental model*: Vacuum and Autovacuum operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Vacuum and Autovacuum and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Vacuum and Autovacuum operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Vacuum and Autovacuum interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Vacuum and Autovacuum interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Vacuum and Autovacuum to a senior engineer in two sentences, what would you say?**
> **A:** "Vacuum and Autovacuum works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Vacuum and Autovacuum Utility

**Goal:** Implement a real, working Vacuum and Autovacuum module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `pgvacuum` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir pg-vacuum-demo && cd pg-vacuum-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Vacuum and Autovacuum Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Vacuum and Autovacuum.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createVacuumandAutovacuum(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Vacuum and Autovacuum] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Vacuum and Autovacuum logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Vacuum and Autovacuum] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Vacuum and Autovacuum] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createVacuumandAutovacuum({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Vacuum and Autovacuum] Input cannot be null or undefined`);
    }
    const instance = createVacuumandAutovacuum();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Vacuum and Autovacuum] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Vacuum and Autovacuum Tests ──');

test('initialises correctly', () => {
  const m = createVacuumandAutovacuum();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createVacuumandAutovacuum();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Vacuum and Autovacuum] Initialized with: hello world
[Vacuum and Autovacuum] Processed: hello world
Result: hello world
[Vacuum and Autovacuum] Destroyed.

── Vacuum and Autovacuum Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Vacuum and Autovacuum in one sentence.**
<details><summary>Answer</summary>

Vacuum and Autovacuum is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Vacuum and Autovacuum?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Vacuum and Autovacuum.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Vacuum and Autovacuum and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Vacuum and Autovacuum | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Vacuum and Autovacuum] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Vacuum and Autovacuum causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Vacuum and Autovacuum usage].
</details>

**Q6: What does Vacuum and Autovacuum look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Vacuum and Autovacuum typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Vacuum and Autovacuum correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Vacuum and Autovacuum:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_pg_vacuum(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Vacuum and Autovacuum in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Vacuum and Autovacuum in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Vacuum and Autovacuum', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Vacuum and Autovacuum at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Vacuum and Autovacuum uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Vacuum and Autovacuum interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Vacuum and Autovacuum is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Vacuum and Autovacuum relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Vacuum and Autovacuum
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Vacuum and Autovacuum to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Vacuum and Autovacuum fits in the data/control flow.
2. **Choose** the right variant/pattern of Vacuum and Autovacuum for the scale requirement.
3. **Handle** failure modes: what happens when Vacuum and Autovacuum is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Vacuum and Autovacuum is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
