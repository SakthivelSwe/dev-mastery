---
slug: "daemonsets"
title: "DaemonSets"
level: 4
---

## WHY
Some infrastructure concerns need to run on EVERY node in your cluster — log collectors, monitoring agents, security scanners, and network plugins. If you use a Deployment, you have no guarantee that every node runs the workload.
**DaemonSets** ensure exactly ONE Pod runs on every Node in the cluster. When a new Node is added, DaemonSet automatically deploys a Pod to it. When a Node is removed, the Pod is garbage collected.

## THEORY
### DaemonSet vs Deployment
- **Deployment:** Run N replicas, distributed as scheduler decides. Best for stateless apps.
- **DaemonSet:** Run one Pod on EVERY node. Best for node-level agents.

### Common DaemonSet Use Cases
- **Log collection:** Fluentd, Filebeat (tails logs from each node's filesystem)
- **Monitoring agents:** Prometheus Node Exporter, Datadog Agent
- **Network plugins:** CNI plugins (Calico, Flannel run as DaemonSets)
- **Security:** Falco (runtime security), Sysdig
- **Storage:** GlusterFS or Ceph storage nodes

## VISUALIZATION_CONFIG
```json
{ "component": "ArchitectureVisualizer", "state": "daemonset" }
```

## CODE

### DaemonSet YAML
```yaml
# Log collection DaemonSet using Fluentd
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: fluentd
  namespace: logging
spec:
  selector:
    matchLabels:
      name: fluentd
  template:
    metadata:
      labels:
        name: fluentd
    spec:
      # Allow scheduling on control-plane nodes too (optional)
      tolerations:
        - key: node-role.kubernetes.io/control-plane
          effect: NoSchedule
      
      containers:
        - name: fluentd
          image: fluent/fluentd-kubernetes-daemonset:v1.16
          env:
            - name: FLUENT_ELASTICSEARCH_HOST
              value: elasticsearch.logging.svc.cluster.local
          resources:
            limits:
              memory: 200Mi
            requests:
              cpu: 100m
              memory: 200Mi
          volumeMounts:
            # Mount the node's log directory to collect all container logs!
            - name: varlog
              mountPath: /var/log
            - name: varlibdockercontainers
              mountPath: /var/lib/docker/containers
              readOnly: true
      
      volumes:
        - name: varlog
          hostPath:          # Access the actual Node's filesystem!
            path: /var/log
        - name: varlibdockercontainers
          hostPath:
            path: /var/lib/docker/containers
```

### DaemonSet Commands
```bash
# Apply DaemonSet
kubectl apply -f fluentd-daemonset.yml

# View DaemonSet status
kubectl get daemonsets -n logging
# NAME      DESIRED   CURRENT   READY   UP-TO-DATE   AVAILABLE   NODE SELECTOR
# fluentd   3         3         3       3            3           <none>
# DESIRED = number of nodes!

# View Pods (one per node!)
kubectl get pods -n logging -o wide
# NAME            READY   STATUS    NODE
# fluentd-abc123  1/1     Running   worker-01
# fluentd-def456  1/1     Running   worker-02
# fluentd-ghi789  1/1     Running   master-01

# Add a new node → DaemonSet automatically adds a Pod!
```

### Targeting Specific Nodes with Node Selectors
```yaml
# Only run on nodes labeled 'role=worker' (not control-plane)
spec:
  template:
    spec:
      nodeSelector:
        role: worker
```

## REAL_WORLD
**Prometheus Node Exporter as a DaemonSet**
Node Exporter exposes hardware and OS-level metrics from every node (CPU, memory, disk, network) for Prometheus to scrape.

```yaml
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: node-exporter
spec:
  template:
    spec:
      hostNetwork: true    # Use node's network namespace (not Pod's)
      hostPID: true        # Access host's process list for metrics
      containers:
        - name: node-exporter
          image: prom/node-exporter:v1.7.0
          ports:
            - containerPort: 9100
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
```

## INTERVIEW
**Q: You add a new node to your Kubernetes cluster. Which workloads automatically get scheduled on the new node, and which don't?**

A: 
**Automatically scheduled on new node:**
- **DaemonSet Pods:** The DaemonSet controller immediately schedules a Pod on every new node. Zero action required.

**NOT automatically moved:**
- **Deployment/ReplicaSet Pods:** Existing running Pods do NOT automatically migrate to new nodes. The scheduler only places NEW Pods (from scale-out, rolling updates, or Pod restarts) on the new node. Existing Pods stay where they are unless killed and rescheduled.

**To rebalance:** You can use the **Descheduler** (a separate Kubernetes tool) to automatically evict and reschedule Pods to achieve better distribution across nodes. Or manually cordon old nodes (`kubectl cordon node-name`) and drain them (`kubectl drain node-name`) to force rescheduling.

This is by design — unnecessarily moving running Pods would cause brief downtime and is disruptive. DaemonSets need immediate presence on every node, so they're the exception.

## FEYNMAN CHECK

### Explain DaemonSets Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **DaemonSets** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about DaemonSets, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does DaemonSets solve? Why would JavaScript/web development be harder without it?**
> **A:** DaemonSets solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about DaemonSets?**
> **A:** The single most important thing is understanding the *mental model*: DaemonSets operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with DaemonSets and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which DaemonSets operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does DaemonSets interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** DaemonSets interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of DaemonSets to a senior engineer in two sentences, what would you say?**
> **A:** "DaemonSets works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a DaemonSets Utility

**Goal:** Implement a real, working DaemonSets module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `daemonsets` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir daemonsets-demo && cd daemonsets-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── DaemonSets Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of DaemonSets.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createDaemonSets(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[DaemonSets] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual DaemonSets logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[DaemonSets] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[DaemonSets] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createDaemonSets({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[DaemonSets] Input cannot be null or undefined`);
    }
    const instance = createDaemonSets();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[DaemonSets] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── DaemonSets Tests ──');

test('initialises correctly', () => {
  const m = createDaemonSets();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createDaemonSets();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[DaemonSets] Initialized with: hello world
[DaemonSets] Processed: hello world
Result: hello world
[DaemonSets] Destroyed.

── DaemonSets Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define DaemonSets in one sentence.**
<details><summary>Answer</summary>

DaemonSets is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of DaemonSets?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of DaemonSets.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between DaemonSets and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | DaemonSets | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of DaemonSets] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing DaemonSets causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct DaemonSets usage].
</details>

**Q6: What does DaemonSets look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, DaemonSets typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use DaemonSets correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using DaemonSets:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_daemonsets(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test DaemonSets in isolation? What would you mock?**
<details><summary>Answer</summary>

To test DaemonSets in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('DaemonSets', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of DaemonSets at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how DaemonSets uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common DaemonSets interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "DaemonSets is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does DaemonSets relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
DaemonSets
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use DaemonSets to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where DaemonSets fits in the data/control flow.
2. **Choose** the right variant/pattern of DaemonSets for the scale requirement.
3. **Handle** failure modes: what happens when DaemonSets is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you DaemonSets is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
