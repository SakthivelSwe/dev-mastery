---
slug: "k8s-monitoring"
title: "Kubernetes Monitoring"
level: 4
---

## WHY
Running applications in Kubernetes without monitoring is flying blind. When something goes wrong — high latency, error spikes, OOM kills, pending Pods — you need to know immediately, not when a user reports it.
The **Prometheus + Grafana** stack is the de facto standard for Kubernetes monitoring: Prometheus scrapes and stores metrics; Grafana visualizes them. Combined with AlertManager, you get automated alerting when metrics cross thresholds.

## THEORY
### The Prometheus Architecture
- **Prometheus Server:** Scrapes HTTP `/metrics` endpoints from configured targets, stores time-series data
- **Node Exporter:** DaemonSet that exposes hardware/OS metrics from each Node
- **kube-state-metrics:** Exposes K8s object state (Pod status, Deployment replicas)
- **AlertManager:** Handles alerts: deduplication, grouping, routing to PagerDuty/Slack
- **Grafana:** Visualization dashboards that query Prometheus via PromQL

## VISUALIZATION_CONFIG
```json
{ "component": "ArchitectureVisualizer", "state": "k8s-monitoring" }
```

## CODE

### Installing Prometheus Stack with Helm
```bash
# The kube-prometheus-stack Helm chart installs EVERYTHING at once:
# Prometheus, Grafana, AlertManager, Node Exporter, kube-state-metrics

helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm repo update

helm install monitoring prometheus-community/kube-prometheus-stack \
  --namespace monitoring \
  --create-namespace \
  --set grafana.adminPassword=admin123

# Access Grafana UI
kubectl port-forward svc/monitoring-grafana -n monitoring 3000:80
# Open http://localhost:3000 (admin/admin123)

# Access Prometheus UI
kubectl port-forward svc/monitoring-kube-prometheus-prometheus -n monitoring 9090:9090
```

### Exposing Your Application's Metrics
```java
// Spring Boot: Add Actuator + Micrometer Prometheus
// pom.xml:
// spring-boot-starter-actuator
// micrometer-registry-prometheus

// application.yml:
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus  # Expose /actuator/prometheus
  metrics:
    export:
      prometheus:
        enabled: true
// GET /actuator/prometheus → Prometheus-format metrics!
```

```yaml
# ServiceMonitor: Tell Prometheus to scrape your app!
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: api-monitor
  namespace: production
  labels:
    release: monitoring  # Must match Prometheus operator selector!
spec:
  selector:
    matchLabels:
      app: api
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s  # Scrape every 30 seconds
```

### PromQL: Querying Metrics
```promql
# CPU usage of all API pods
rate(container_cpu_usage_seconds_total{pod=~"api-.*"}[5m])

# Memory usage by pod
container_memory_working_set_bytes{pod=~"api-.*"} / 1024 / 1024

# HTTP request rate
rate(http_server_requests_seconds_count{app="api"}[1m])

# HTTP error rate (5xx)
rate(http_server_requests_seconds_count{app="api", status=~"5.."}[5m])

# P95 latency
histogram_quantile(0.95, rate(http_server_requests_seconds_bucket{app="api"}[5m]))

# Pod restart count
kube_pod_container_status_restarts_total{namespace="production"}
```

### AlertManager Rules
```yaml
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: api-alerts
spec:
  groups:
    - name: api
      rules:
        - alert: HighErrorRate
          expr: rate(http_server_requests_seconds_count{status=~"5.."}[5m]) > 0.05
          for: 2m            # Must be true for 2 minutes before firing
          labels:
            severity: critical
          annotations:
            summary: "High error rate detected"
            description: "API error rate is {{ $value | humanize }} req/s"
        
        - alert: PodRestarting
          expr: kube_pod_container_status_restarts_total > 5
          for: 0m
          labels:
            severity: warning
          annotations:
            summary: "Pod {{ $labels.pod }} is restarting frequently"
```

## REAL_WORLD
**The Golden Signals**
Google's SRE book defines four golden signals to monitor for every service:
1. **Latency:** Time to serve a request (P50, P95, P99 percentiles)
2. **Traffic:** Requests per second
3. **Errors:** Rate of failed requests (5xx errors)
4. **Saturation:** How "full" the service is (CPU %, memory %, queue depth)

Every microservice should have a Grafana dashboard showing these four signals with alert thresholds.

## INTERVIEW
**Q: Your Kubernetes Pod is OOMKilled repeatedly. The current memory limit is 512Mi. How do you investigate the right memory limit to set?**

A: Multi-step investigation:

1. **Check current actual memory usage** in Prometheus/Grafana:
```promql
# Max memory usage of the pod over the last 7 days
max_over_time(container_memory_working_set_bytes{pod=~"api-.*"}[7d]) / 1024 / 1024
```

2. **Analyze the memory growth pattern:**
   - Gradual growth over hours → Memory leak (application bug)
   - Spike under load → Need more limit or fix the load pattern
   - Steady but above limit → Limit too low

3. **If it's a memory leak:** Fix the application. Increasing the limit just delays the next OOM kill.

4. **If the limit is genuinely too low:**
```promql
# Check p99 memory usage to set a comfortable limit
quantile_over_time(0.99, container_memory_working_set_bytes{pod=~"api-.*"}[7d]) / 1024 / 1024
```
Set the limit to 120-150% of the p99 usage. If p99 is 400Mi, set limit to 512Mi (128%).

5. **Use VPA for automatic right-sizing:**
```yaml
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: api-vpa
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: api
  updatePolicy:
    updateMode: Recommendation  # Don't auto-apply, just recommend
```
VPA observes actual usage and recommends the correct limit after 24-48 hours of data.

## FEYNMAN CHECK

### Explain Kubernetes Monitoring Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Kubernetes Monitoring** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Kubernetes Monitoring, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Kubernetes Monitoring solve? Why would JavaScript/web development be harder without it?**
> **A:** Kubernetes Monitoring solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Kubernetes Monitoring?**
> **A:** The single most important thing is understanding the *mental model*: Kubernetes Monitoring operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Kubernetes Monitoring and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Kubernetes Monitoring operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Kubernetes Monitoring interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Kubernetes Monitoring interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Kubernetes Monitoring to a senior engineer in two sentences, what would you say?**
> **A:** "Kubernetes Monitoring works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Kubernetes Monitoring Utility

**Goal:** Implement a real, working Kubernetes Monitoring module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `k8smonitoring` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir k8s-monitoring-demo && cd k8s-monitoring-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Kubernetes Monitoring Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Kubernetes Monitoring.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createKubernetesMonitoring(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Kubernetes Monitoring] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Kubernetes Monitoring logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Kubernetes Monitoring] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Kubernetes Monitoring] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createKubernetesMonitoring({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Kubernetes Monitoring] Input cannot be null or undefined`);
    }
    const instance = createKubernetesMonitoring();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Kubernetes Monitoring] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Kubernetes Monitoring Tests ──');

test('initialises correctly', () => {
  const m = createKubernetesMonitoring();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createKubernetesMonitoring();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Kubernetes Monitoring] Initialized with: hello world
[Kubernetes Monitoring] Processed: hello world
Result: hello world
[Kubernetes Monitoring] Destroyed.

── Kubernetes Monitoring Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Kubernetes Monitoring in one sentence.**
<details><summary>Answer</summary>

Kubernetes Monitoring is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Kubernetes Monitoring?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Kubernetes Monitoring.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Kubernetes Monitoring and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Kubernetes Monitoring | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Kubernetes Monitoring] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Kubernetes Monitoring causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Kubernetes Monitoring usage].
</details>

**Q6: What does Kubernetes Monitoring look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Kubernetes Monitoring typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Kubernetes Monitoring correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Kubernetes Monitoring:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_k8s_monitoring(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Kubernetes Monitoring in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Kubernetes Monitoring in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Kubernetes Monitoring', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Kubernetes Monitoring at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Kubernetes Monitoring uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Kubernetes Monitoring interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Kubernetes Monitoring is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Kubernetes Monitoring relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Kubernetes Monitoring
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Kubernetes Monitoring to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Kubernetes Monitoring fits in the data/control flow.
2. **Choose** the right variant/pattern of Kubernetes Monitoring for the scale requirement.
3. **Handle** failure modes: what happens when Kubernetes Monitoring is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Kubernetes Monitoring is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
