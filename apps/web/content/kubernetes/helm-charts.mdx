---
slug: "helm-charts"
title: "Helm Charts"
level: 5
---

## WHY
Deploying a production-grade application to Kubernetes requires many YAML files: Deployment, Service, ConfigMap, Secret, Ingress, HPA, RBAC roles, ServiceAccount... easily 10-20 YAML files per application.
Managing all these files manually — customizing them for different environments (dev, staging, prod), sharing them with other teams, versioning them, and rolling back to previous configurations — is an operational nightmare.
**Helm** is the package manager for Kubernetes. It bundles all YAML files for an application into a **Chart**, allows parameterization via templates, and treats deployments like versioned releases that can be installed, upgraded, and rolled back with single commands.

## THEORY
### Helm Concepts
- **Chart:** A collection of YAML templates + a `values.yml` file. Like an npm package for Kubernetes.
- **Release:** A specific instance of a Chart installed in a cluster (you can install the same chart multiple times with different configurations, creating multiple releases).
- **Repository:** A collection of Charts (like Docker Hub, but for Helm charts). Public repos: ArtifactHub, Bitnami, etc.
- **Values:** Configuration parameters that customize a Chart's templates.

## VISUALIZATION_CONFIG
```json
{ "component": "ArchitectureVisualizer", "state": "helm-chart-structure" }
```

## CODE

### Installing Applications with Helm
```bash
# Add a Helm chart repository
helm repo add bitnami https://charts.bitnami.com/bitnami
helm repo update

# Install PostgreSQL from the Bitnami chart
helm install my-postgres bitnami/postgresql \
  --set auth.postgresPassword=mysecretpassword \
  --set primary.persistence.size=20Gi

# Install cert-manager for TLS certificates
helm repo add jetstack https://charts.jetstack.io
helm install cert-manager jetstack/cert-manager \
  --namespace cert-manager \
  --create-namespace \
  --version v1.13.0 \
  --set installCRDs=true

# List all helm releases
helm list
# NAME          NAMESPACE   REVISION   STATUS    CHART
# my-postgres   default     1          deployed  postgresql-13.2.0

# Upgrade a release
helm upgrade my-postgres bitnami/postgresql --set auth.postgresPassword=newpassword

# Rollback to previous revision
helm rollback my-postgres 1

# Uninstall
helm uninstall my-postgres
```

### Creating Your Own Chart
```bash
# Scaffold a new chart
helm create my-api-chart

# Chart structure:
# my-api-chart/
# ├── Chart.yaml        # Chart metadata (name, version, description)
# ├── values.yaml       # Default configuration values
# ├── templates/        # YAML templates with Go templating
# │   ├── deployment.yaml
# │   ├── service.yaml
# │   ├── ingress.yaml
# │   └── _helpers.tpl  # Reusable template fragments
```

```yaml
# values.yaml (default values — teams override these)
image:
  repository: my-api
  tag: "1.0.0"
  pullPolicy: IfNotPresent

replicaCount: 1

service:
  type: ClusterIP
  port: 80
  targetPort: 3000

resources:
  limits:
    memory: 512Mi
    cpu: 500m
  requests:
    memory: 256Mi
    cpu: 250m

ingress:
  enabled: false
  host: ""
```

```yaml
# templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-deployment   # Release name injected!
spec:
  replicas: {{ .Values.replicaCount }}   # From values.yaml!
  template:
    spec:
      containers:
        - name: api
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          resources: {{- toYaml .Values.resources | nindent 12 }}
```

### Installing with Environment-Specific Values
```bash
# values.prod.yaml
replicaCount: 5
image:
  tag: "2.1.0"
resources:
  limits:
    memory: 2Gi
    cpu: "2"
ingress:
  enabled: true
  host: api.mycompany.com

# Install for production
helm upgrade --install my-api ./my-api-chart \
  -f values.yaml \
  -f values.prod.yaml \
  --namespace production \
  --create-namespace
```

## REAL_WORLD
**ArtifactHub: The Helm Chart Registry**
ArtifactHub (artifacthub.io) is the public registry for Helm charts. You can find production-ready charts for almost any software:
- **Monitoring:** Prometheus, Grafana, AlertManager
- **Ingress:** Nginx Ingress Controller, Traefik
- **Certificates:** cert-manager (Let's Encrypt)
- **Databases:** PostgreSQL, MySQL, Redis, MongoDB
- **CI/CD:** ArgoCD, Tekton, Jenkins

Instead of writing 500 lines of Kubernetes YAML to deploy Prometheus, you run: `helm install prometheus prometheus-community/kube-prometheus-stack`. Done.

## INTERVIEW
**Q: You want to deploy the same application to 3 environments (dev, staging, production) with different configurations (replica count, resource limits, image tags). How do you manage this with Helm without duplicating YAML files?**
A: Use **Helm's values override system** with environment-specific values files:

```
charts/
└── my-app/
    ├── Chart.yaml
    ├── templates/         # Single set of templates (no duplication!)
    ├── values.yaml        # Base/default values (committed to git)
    ├── values.dev.yaml    # Dev overrides: replicaCount=1, image.tag=latest
    ├── values.staging.yaml  # Staging: replicaCount=2, image.tag=rc-1.2.0
    └── values.prod.yaml   # Prod: replicaCount=10, image.tag=1.2.0
```

Deploy with:
```bash
helm upgrade --install myapp ./charts/my-app -f values.yaml -f values.${ENV}.yaml
```

Helm merges the values files (right-to-left priority), so environment-specific values override the base defaults. This gives you:
- **Single source of truth** for templates
- **Environment-specific configuration** without duplication
- **Version control** over both templates and configurations
- **Easy comparison** between environments (just diff the values files)

## FEYNMAN CHECK

### Explain Helm Charts Like I'm 10 Years Old
Imagine you have a magical LEGO set. Each LEGO brick knows exactly what it does and how it connects to other bricks. **Helm Charts** is like the instruction manual that tells you *which* bricks exist, *how* they snap together, and *what* you can build with them.

When a professional developer talks about Helm Charts, they are really talking about a precise set of rules the computer follows every single time — no surprises, no magic, just rules you can learn once and rely on forever.

---

### 5 Deep Conceptual Questions

**Q1: In your own words, what problem does Helm Charts solve? Why would JavaScript/web development be harder without it?**
> **A:** Helm Charts solves the problem of [describing the core purpose]. Without it, developers would need to write repetitive, error-prone boilerplate code every time they need this behaviour. It provides a standardised, predictable mechanism that the runtime engine optimises for performance.

**Q2: What is the single most important thing to remember about Helm Charts?**
> **A:** The single most important thing is understanding the *mental model*: Helm Charts operates on [core abstraction]. Once you internalize this, every API, every edge case, and every debugging scenario becomes logical rather than mysterious.

**Q3: What is the most common mistake developers make with Helm Charts and why?**
> **A:** The most common mistake is misunderstanding the *timing* or *scope* in which Helm Charts operates. Developers assume [incorrect assumption], but the actual behaviour is [correct behaviour]. This leads to bugs that are hard to reproduce because the error only appears under specific conditions.

**Q4: How does Helm Charts interact with the JavaScript event loop / browser rendering pipeline?**
> **A:** Helm Charts interacts with the runtime at the [synchronous/asynchronous/compile-time] phase. Understanding whether its work happens on the call stack, in a microtask queue, or during a browser paint cycle determines when side effects become visible and how to sequence operations correctly.

**Q5: If you had to explain the internal mechanism of Helm Charts to a senior engineer in two sentences, what would you say?**
> **A:** "Helm Charts works by [mechanism step 1]. The engine then [mechanism step 2], which is why you observe [observable behaviour]."


## BUILD

### 🏗️ Mini Project: Build a Helm Charts Utility

**Goal:** Implement a real, working Helm Charts module from scratch — no libraries, pure javascript.

**What you'll build:** A self-contained `helmcharts` module that demonstrates every key concept from this topic in a runnable, testable way.

---

#### Step 1 — Set Up the Project Structure

```bash
mkdir helm-charts-demo && cd helm-charts-demo
touch index.js
```

#### Step 2 — Core Implementation

```javascript
// ── Helm Charts Core Implementation ──────────────────────────────────────────────
// This module demonstrates the fundamental concepts of Helm Charts.
// Study each section carefully — each one maps directly to the THEORY section above.

// 1. Basic setup / initialisation
function createHelmCharts(config = {}) {
  const defaults = {
    enabled: true,
    debug: false,
    ...config,
  };

  // Internal state (private via closure)
  let _state = null;

  // 2. Core logic
  function init(value) {
    _state = value;
    if (defaults.debug) console.log(`[Helm Charts] Initialized with:`, value);
    return _state;
  }

  // 3. Main operation
  function process(input) {
    if (!defaults.enabled) return null;
    // TODO: Replace this with the actual Helm Charts logic from the THEORY section
    const result = input;
    if (defaults.debug) console.log(`[Helm Charts] Processed:`, result);
    return result;
  }

  // 4. Cleanup / teardown
  function destroy() {
    _state = null;
    if (defaults.debug) console.log(`[Helm Charts] Destroyed.`);
  }

  return { init, process, destroy };
}

// ── Usage ─────────────────────────────────────────────────────────────────────
const instance = createHelmCharts({ debug: true });
instance.init('hello world');
const result = instance.process('hello world');
console.log('Result:', result);
instance.destroy();
```

#### Step 3 — Add Error Handling

```javascript
// Robust error handling for production use
function safeProcess(input) {
  try {
    if (input === null || input === undefined) {
      throw new TypeError(`[Helm Charts] Input cannot be null or undefined`);
    }
    const instance = createHelmCharts();
    return { success: true, data: instance.process(input) };
  } catch (err) {
    console.error(`[Helm Charts] Error:`, err.message);
    return { success: false, error: err.message };
  }
}

console.log(safeProcess('valid input'));   // { success: true, data: '...' }
console.log(safeProcess(null));            // { success: false, error: '...' }
```

#### Step 4 — Write Tests

```javascript
// Simple test runner (no dependencies needed)
function test(description, fn) {
  try {
    fn();
    console.log(`  ✅ PASS: ${description}`);
  } catch (e) {
    console.error(`  ❌ FAIL: ${description} — ${e.message}`);
  }
}

function assert(actual, expected, msg = '') {
  if (JSON.stringify(actual) !== JSON.stringify(expected)) {
    throw new Error(`Expected ${JSON.stringify(expected)} but got ${JSON.stringify(actual)}. ${msg}`);
  }
}

console.log('\n── Helm Charts Tests ──');

test('initialises correctly', () => {
  const m = createHelmCharts();
  assert(typeof m.init, 'function');
  assert(typeof m.process, 'function');
});

test('processes valid input', () => {
  const m = createHelmCharts();
  m.init('test');
  const out = m.process('test');
  assert(out !== null && out !== undefined, true, 'output should not be null');
});

test('handles null input gracefully', () => {
  const result = safeProcess(null);
  assert(result.success, false);
  assert(typeof result.error, 'string');
});

console.log('\n── All tests complete ──\n');
```

#### Step 5 — Challenge Extensions (stretch goals)

- [ ] Add a caching layer so repeated calls with the same input skip reprocessing
- [ ] Add TypeScript types / JSDoc annotations to every function
- [ ] Make it work asynchronously (return a Promise)
- [ ] Add a rate-limiter so `process()` can only be called N times per second
- [ ] Write a benchmark comparing your implementation to a naive alternative

---

**Expected Output:**
```
[Helm Charts] Initialized with: hello world
[Helm Charts] Processed: hello world
Result: hello world
[Helm Charts] Destroyed.

── Helm Charts Tests ──
  ✅ PASS: initialises correctly
  ✅ PASS: processes valid input
  ✅ PASS: handles null input gracefully

── All tests complete ──
```


## SPACED REVIEW

> **How to use:** Answer each question from memory before revealing the answer. The increasing difficulty across days mirrors the Ebbinghaus forgetting curve — reviewing at these intervals locks the concept into long-term memory.

---

### Day 1 — Recall (immediately after studying)

**Q1: Define Helm Charts in one sentence.**
<details><summary>Answer</summary>

Helm Charts is [the core definition restated in simple terms, connecting the mechanism to the observable behaviour].
</details>

**Q2: What are the two most important properties / characteristics of Helm Charts?**
<details><summary>Answer</summary>

1. **[Property 1]** — [why it matters]
2. **[Property 2]** — [why it matters]
</details>

**Q3: Write a 5-line code snippet that demonstrates the most basic usage of Helm Charts.**
<details><summary>Answer</summary>

Refer to the **Level 1** example in the CODE section above. The key lines are the ones that [describe what the critical lines do].
</details>

---

### Day 3 — Comprehension (deepen understanding)

**Q4: What is the difference between Helm Charts and [the closest related concept]?**
<details><summary>Answer</summary>

| Feature | Helm Charts | Related Concept |
|---------|---------|----------------|
| Purpose | [purpose of Helm Charts] | [purpose of related] |
| When to use | [use case] | [use case] |
| Performance | [characteristic] | [characteristic] |

The key distinction is [one-sentence summary of the difference].
</details>

**Q5: Describe a real-world scenario where misusing Helm Charts causes a bug.**
<details><summary>Answer</summary>

**Scenario:** A developer [describes common misuse pattern]. 

**The bug:** [What goes wrong — e.g., memory leak, stale data, race condition].

**The fix:** [How to correct it with correct Helm Charts usage].
</details>

**Q6: What does Helm Charts look like in a production codebase? Name one popular open-source library that relies on it heavily.**
<details><summary>Answer</summary>

In production, Helm Charts typically appears in [where/how]. A well-known example is **[library name]** which uses it to [specific purpose], which you can see in its source at [conceptual location].
</details>

---

### Day 7 — Application (use the knowledge)

**Q7: Refactor the following naive code to use Helm Charts correctly:**
```javascript
// Naive — do NOT do this:
function badExample() {
  // repetitive / inefficient / error-prone pattern
  const a = doThing(1);
  const b = doThing(2);
  const c = doThing(3);
  return [a, b, c];
}
```
<details><summary>Answer</summary>

```javascript
// Better — using Helm Charts:
function goodExample() {
  return [1, 2, 3].map(n => doThingWith_helm_charts(n));
}
```
The refactored version is more readable, less error-prone, and easier to extend.
</details>

**Q8: How would you unit-test Helm Charts in isolation? What would you mock?**
<details><summary>Answer</summary>

To test Helm Charts in isolation:
1. **Mock** any external dependencies (network calls, timers, DOM).
2. **Assert** on the public interface — inputs and outputs — not internal state.
3. **Edge cases to cover:** null input, empty input, boundary values, and error conditions.

```javascript
// Example test skeleton
describe('Helm Charts', () => {
  it('handles the happy path', () => { /* ... */ });
  it('throws on invalid input', () => { /* ... */ });
  it('cleans up resources after use', () => { /* ... */ });
});
```
</details>

**Q9: What is the performance cost of Helm Charts at scale? How would you optimise it?**
<details><summary>Answer</summary>

At scale, the main costs are:
- **Memory:** [how Helm Charts uses memory and how to reduce it]
- **CPU:** [computational complexity and how to reduce it]
- **Network/IO:** [if applicable]

**Optimisation strategies:**
1. [Strategy 1] — reduces [cost] by [mechanism]
2. [Strategy 2] — most effective when [condition]
3. [Strategy 3] — trade-off: [pro vs con]
</details>

---

### Day 14 — Synthesis & Interview Prep

**Q10: ★ Classic interview question: "[Most common Helm Charts interview question]"**
<details><summary>Answer</summary>

**Answer structure (use this in interviews):**

1. **Define it:** "Helm Charts is..."
2. **Explain the mechanism:** "Under the hood, it works by..."
3. **Give a concrete example:** "A classic case is..."
4. **Mention the gotcha:** "The subtle thing most developers miss is..."
5. **Close with best practice:** "In production, you should always..."

This structure takes 60–90 seconds and signals senior-level thinking.
</details>

**Q11: How does Helm Charts relate to other concepts in this learning path? Draw the mental map.**
<details><summary>Answer</summary>

```
Helm Charts
  ├── depends on ──► [Prerequisite concept A]
  ├── depends on ──► [Prerequisite concept B]
  ├── enables ─────► [Advanced concept C]
  └── pairs with ──► [Sibling concept D]
```

Understanding this graph tells you the order to learn/teach these topics and which bugs are *actually* caused by misunderstanding an upstream concept.
</details>

**Q12: ★ System design: "How would you use Helm Charts to build a [large-scale feature]?"**
<details><summary>Answer</summary>

**Approach:**
1. **Identify** where Helm Charts fits in the data/control flow.
2. **Choose** the right variant/pattern of Helm Charts for the scale requirement.
3. **Handle** failure modes: what happens when Helm Charts is unavailable or behaves unexpectedly?
4. **Monitor** it: what metric tells you Helm Charts is healthy in production?

This answer demonstrates that you can apply individual concepts to real architecture decisions — exactly what separates mid-level from senior engineers.
</details>
