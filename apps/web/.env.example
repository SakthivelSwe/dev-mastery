# ─── Backend API ──────────────────────────────────────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:8080

# ─── Code Execution (Judge0 via RapidAPI) ─────────────────────────────────
# ONE config — works identically in local dev AND production.
#
# Local:      copy these into apps/web/.env.local
# Production: add the same 3 vars to Cloudflare Pages
#             → Settings → Environment Variables → Production + Preview
#
# Get your key: https://rapidapi.com/dishis-technologies-judge0/api/judge029
JUDGE0_API_URL=https://judge029.p.rapidapi.com
JUDGE0_API_KEY=your_rapidapi_key_here
JUDGE0_API_HOST=judge029.p.rapidapi.com
