<?xml version="1.0" encoding="utf-8"?>
<!--
  Security hardening: HTTPS is enforced everywhere by default. Cleartext HTTP is
  permitted ONLY for the local development hosts (Android emulator loopback and
  localhost), never for production traffic. This prevents man-in-the-middle
  downgrade attacks against the Render/Cloudflare production endpoints.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>

