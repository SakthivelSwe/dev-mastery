module.exports = [
  {
    path: 'angular', slug: 'angular-pwa',
    steps: [
      { title: 'Adding PWA Support', description: 'One command adds Service Worker, Web App Manifest, and offline caching.', code: `# Add PWA\nng add @angular/pwa\n\n# Generated files:\n# ngsw-config.json    - caching strategy config\n# src/manifest.webmanifest - app name, icons, colors\n# src/app.config.ts   - provideServiceWorker() added\n\n// app.config.ts\nprovideServiceWorker('ngsw-worker.js', {\n  enabled: !isDevMode(),\n  registrationStrategy: 'registerWhenStable:30000'\n})`, highlight: [2, 11, 12, 13], diagram: { kind: 'flow', steps: ['ng add @angular/pwa', 'ngsw-worker.js', 'Service Worker registered', 'Offline caching', 'Installable'] } },
      { title: 'Caching Strategies', description: 'Configure what to cache — app shell, API responses, assets.', code: `// ngsw-config.json\n{\n  "assetGroups": [\n    {\n      "name": "app-shell",\n      "installMode": "prefetch",\n      "resources": { "files": ["/index.html", "/*.css", "/*.js"] }\n    },\n    {\n      "name": "assets",\n      "installMode": "lazy",\n      "resources": { "files": ["/assets/**", "/**/*.jpg"] }\n    }\n  ],\n  "dataGroups": [\n    {\n      "name": "api-cache",\n      "urls": ["/api/products"],\n      "cacheConfig": { "strategy": "freshness", "maxAge": "1h", "maxSize": 100 }\n    }\n  ]\n}`, highlight: [5, 6, 10, 11, 17, 18, 19], diagram: { kind: 'boxes', boxes: [{ id: 'pr', label: 'prefetch: on install', color: '#1e88e5' }, { id: 'lz', label: 'lazy: on first access', color: '#43a047' }, { id: 'fr', label: 'freshness: network first', color: '#fb8c00' }], connections: [] } },
      { title: 'Update Notifications', description: 'Detect and apply Service Worker updates with SwUpdate service.', code: `@Injectable({ providedIn: 'root' })\nexport class PwaUpdateService {\n  private swUpdate = inject(SwUpdate);\n  private snackBar = inject(MatSnackBar);\n\n  constructor() {\n    if (!this.swUpdate.isEnabled) return;\n\n    // Check for updates every 6 hours\n    interval(6 * 60 * 60 * 1000).subscribe(() =>\n      this.swUpdate.checkForUpdate()\n    );\n\n    // Notify user when update available\n    this.swUpdate.versionUpdates.pipe(\n      filter((e): e is VersionReadyEvent => e.type === 'VERSION_READY')\n    ).subscribe(() => {\n      this.snackBar.open('Update available', 'Refresh', { duration: 0 })\n        .onAction().subscribe(() => window.location.reload());\n    });\n  }\n}`, highlight: [9, 10, 15, 16, 17, 18], diagram: { kind: 'flow', steps: ['SW detects new version', 'VERSION_READY event', 'Snackbar shown', 'User clicks Refresh', 'New version active'] } },
      { title: 'Push Notifications', description: 'Angular PWA supports Web Push notifications via SwPush service.', code: `@Injectable({ providedIn: 'root' })\nexport class PushNotificationService {\n  private swPush = inject(SwPush);\n\n  async subscribe() {\n    const sub = await this.swPush.requestSubscription({\n      serverPublicKey: environment.vapidPublicKey\n    });\n    // Send subscription to backend\n    await this.http.post('/api/push/subscribe', sub).toPromise();\n  }\n\n  constructor() {\n    // Handle received notifications\n    this.swPush.messages.subscribe((msg: any) => {\n      console.log('Push received:', msg);\n    });\n    this.swPush.notificationClicks.subscribe(({ notification }) => {\n      window.open(notification.data.url);\n    });\n  }\n}`, highlight: [6, 7, 10, 15, 17, 18, 19], diagram: { kind: 'flow', steps: ['requestSubscription()', 'VAPID key', 'Sub sent to server', 'Server sends push', 'Browser shows notification'] } },
      { title: 'Offline Handling', description: 'Detect online/offline state and queue operations for sync.', code: `@Injectable({ providedIn: 'root' })\nexport class OfflineService {\n  isOnline = signal(navigator.onLine);\n  private pendingQueue: Action[] = [];\n\n  constructor() {\n    window.addEventListener('online',  () => {\n      this.isOnline.set(true);\n      this.flushQueue();\n    });\n    window.addEventListener('offline', () => this.isOnline.set(false));\n  }\n\n  queueAction(action: Action) {\n    if (this.isOnline()) {\n      return this.execute(action);\n    }\n    this.pendingQueue.push(action);\n  }\n\n  private async flushQueue() {\n    for (const action of this.pendingQueue) {\n      await this.execute(action);\n    }\n    this.pendingQueue = [];\n  }\n}`, highlight: [7, 8, 11, 14, 15, 17, 21, 22], diagram: { kind: 'flow', steps: ['Action dispatched', 'Online? Execute now', 'Offline? Queue it', 'Back online', 'Flush queue', 'All synced'] } }
    ]
  },
  {
    path: 'angular', slug: 'angular-universal',
    steps: [
      { title: 'SSR with Angular Universal', description: 'Angular Universal renders pages on the server for better SEO and initial load performance.', code: `# Add SSR\nng add @angular/ssr\n\n# Structure:\n# server.ts              - Express server\n# app.config.server.ts  - Server providers\n# app.config.ts         - Merged config\n\n// server.ts\nconst app = express();\n\napp.get('*', (req, res, next) => {\n  const { protocol, originalUrl, baseUrl, headers } = req;\n  commonEngine.render({\n    bootstrap, document, url: \`\${protocol}://\${headers.host}\${originalUrl}\`\n  }).then(html => res.send(html))\n    .catch(err => next(err));\n});`, highlight: [2, 10, 12, 13, 14, 15], diagram: { kind: 'flow', steps: ['Request arrives', 'Angular CommonEngine', 'Render component tree', 'HTML string', 'Send to browser', 'Hydrate'] } },
      { title: 'isPlatformBrowser Guard', description: 'SSR: some APIs don\'t exist on the server — guard browser-only code.', code: `import { isPlatformBrowser, isPlatformServer } from '@angular/common';\nimport { PLATFORM_ID } from '@angular/core';\n\n@Injectable({ providedIn: 'root' })\nexport class StorageService {\n  private platformId = inject(PLATFORM_ID);\n\n  get(key: string): string | null {\n    if (!isPlatformBrowser(this.platformId)) return null;\n    return localStorage.getItem(key); // only runs in browser\n  }\n\n  set(key: string, value: string): void {\n    if (!isPlatformBrowser(this.platformId)) return;\n    localStorage.setItem(key, value);\n  }\n}`, highlight: [6, 9, 14], diagram: { kind: 'flow', steps: ['SSR runs on Node', 'isPlatformBrowser?', 'false → skip localStorage', 'Browser hydrates', 'true → localStorage ok'] } },
      { title: 'Transfer State', description: 'TransferState avoids duplicate HTTP calls — server fetches data, transfers to browser.', code: `import { TransferState, makeStateKey } from '@angular/core';\n\nconst USERS_KEY = makeStateKey<User[]>('users');\n\n@Injectable({ providedIn: 'root' })\nexport class UserService {\n  private ts = inject(TransferState);\n  private http = inject(HttpClient);\n\n  getUsers(): Observable<User[]> {\n    const cached = this.ts.get(USERS_KEY, null);\n    if (cached) {\n      this.ts.remove(USERS_KEY);\n      return of(cached); // Browser: use server data\n    }\n    return this.http.get<User[]>('/api/users').pipe(\n      tap(users => this.ts.set(USERS_KEY, users)) // Server: store it\n    );\n  }\n}`, highlight: [3, 11, 12, 13, 16, 17], diagram: { kind: 'flow', steps: ['Server: HTTP fetch', 'ts.set() in HTML', 'Browser receives HTML', 'ts.get() reads', 'No second HTTP call'] } },
      { title: 'Meta Tags for SEO', description: 'Use Meta and Title services for dynamic SEO tags in SSR.', code: `import { Meta, Title } from '@angular/platform-browser';\n\n@Component({ ... })\nexport class ProductDetailComponent {\n  private meta = inject(Meta);\n  private title = inject(Title);\n  private router = inject(Router);\n\n  setMeta(product: Product) {\n    this.title.setTitle(\`\${product.name} | MyShop\`);\n    this.meta.updateTag({ name: 'description', content: product.description });\n    this.meta.updateTag({ property: 'og:title', content: product.name });\n    this.meta.updateTag({ property: 'og:image', content: product.imageUrl });\n    this.meta.updateTag({ property: 'og:url',\n      content: \`https://myshop.com\${this.router.url}\` });\n  }\n}`, highlight: [10, 11, 12, 13, 14, 15], diagram: { kind: 'flow', steps: ['Component activates', 'title.setTitle()', 'meta.updateTag()', 'SSR renders tags', 'Crawler sees tags'] } },
      { title: 'Hydration', description: 'Angular 17+ incremental hydration prevents flickering on client takeover.', code: `// app.config.ts\nexport const appConfig: ApplicationConfig = {\n  providers: [\n    provideClientHydration(\n      withEventReplay() // replay events during hydration\n    )\n  ]\n};\n\n// @defer with hydration trigger\n@Component({\n  template: \`\n    @defer (hydrate on viewport) {\n      <heavy-widget />\n    } @placeholder {\n      <lightweight-placeholder />\n    }\n  \`\n})`, highlight: [4, 5, 13, 15, 16], diagram: { kind: 'flow', steps: ['Server HTML sent', 'provideClientHydration()', 'DOM reused (no flash)', 'Events replayed', 'hydrate on viewport'] } }
    ]
  },
  {
    path: 'angular', slug: 'angular-compiler',
    steps: [
      { title: 'AOT vs JIT Compilation', description: 'AOT compiles templates at build time; JIT at runtime. AOT is default for production.', code: `// JIT (Just-in-Time): compile in browser\n// - Dev mode default (faster builds)\n// - Templates compiled at runtime\n// - Larger initial payload (compiler included)\n// - Slower startup\n\n// AOT (Ahead-of-Time): compile at build time\n// - Production default\n// - Templates validated at build time\n// - Smaller bundles (compiler excluded)\n// - Faster startup (pre-compiled)\n\n// Build commands\nng build              // JIT (dev)\nng build --prod       // AOT (production)`, highlight: [2, 3, 8, 9, 13, 14], diagram: { kind: 'boxes', boxes: [{ id: 'jit', label: 'JIT: fast build, slow run', color: '#fb8c00' }, { id: 'aot', label: 'AOT: slower build, fast run', color: '#43a047' }], connections: [] } },
      { title: 'Template Type Checking', description: 'Angular strict template checking catches type errors at compile time.', code: `// tsconfig.json\n{\n  "angularCompilerOptions": {\n    "strictTemplates": true,        // enable strict checking\n    "strictNullChecks": true,\n    "strictInjectionParameters": true,\n    "strictInputAccessModifiers": true\n  }\n}\n\n// Caught at compile time:\n@Component({\n  template: \`{{ user.nme }}\` // Error: Property 'nme' doesn't exist\n})\nexport class MyComponent {\n  user!: User; // { name: string }\n}`, highlight: [4, 5, 6, 7, 12], diagram: { kind: 'flow', steps: ['strictTemplates: true', 'Build-time check', 'Type error detected', 'Compile fails', 'Fix before deploy'] } },
      { title: 'Ivy Renderer', description: 'Ivy is Angular\'s current compilation and rendering engine — tree-shakeable, incremental.', code: `// Ivy advantages vs View Engine:\n// 1. Smaller bundles (tree-shaking)\n//    Only used components/pipes included\n\n// 2. Faster compilation\n//    Incremental compilation\n//    Each component compiled independently\n\n// 3. Better debugging\n//    Component instances accessible in DevTools\n//    ng.getComponent() in console\n\n// 4. Locality principle\n//    Component knows all it needs\n//    No global ngModule analysis needed\n\n// Check Ivy in production:\nconsole.log(??getAngularVersion());`, highlight: [2, 5, 8, 13], diagram: { kind: 'boxes', boxes: [{ id: 'ts', label: 'Tree-shakeable', color: '#43a047' }, { id: 'in', label: 'Incremental', color: '#1e88e5' }, { id: 'lo', label: 'Locality principle', color: '#fb8c00' }], connections: [] } },
      { title: 'Build Optimization', description: 'Angular CLI build optimizations: code splitting, tree-shaking, minification.', code: `// angular.json production config\n"configurations": {\n  "production": {\n    "budgets": [\n      { "type": "initial", "maximumWarning": "500kb", "maximumError": "1mb" }\n    ],\n    "outputHashing": "all",\n    "optimization": {\n      "scripts": true,\n      "styles": { "minify": true, "inlineCritical": true },\n      "fonts": { "inline": true }\n    },\n    "sourceMap": false\n  }\n}\n\n// Analyze bundle\nng build --stats-json && npx webpack-bundle-analyzer dist/stats.json`, highlight: [4, 5, 8, 9, 10, 11, 17], diagram: { kind: 'flow', steps: ['ng build --prod', 'AOT compile', 'Tree shake', 'Minify/uglify', 'Budget check', 'Optimized bundle'] } },
      { title: 'Custom Webpack Config', description: 'Extend the Angular CLI build with @angular-builders/custom-webpack.', code: `npm install @angular-builders/custom-webpack\n\n// angular.json\n"architect": {\n  "build": {\n    "builder": "@angular-builders/custom-webpack:browser",\n    "options": {\n      "customWebpackConfig": { "path": "./webpack.config.js" }\n    }\n  }\n}\n\n// webpack.config.js\nmodule.exports = {\n  plugins: [\n    new BundleAnalyzerPlugin(),\n    new CompressionPlugin(), // gzip\n  ],\n  resolve: {\n    alias: { '@app': path.resolve(__dirname, 'src/app') }\n  }\n}`, highlight: [6, 8, 15, 16, 19, 20], diagram: { kind: 'flow', steps: ['@angular-builders', 'custom webpack.config', 'Extends CLI webpack', 'Custom plugins/aliases', 'Build output'] } }
    ]
  }
];

