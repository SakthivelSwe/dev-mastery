module.exports = [
  { path: 'full-stack', slug: 'git-workflow', steps: [
    { title: 'Basic Git Commands', description: 'clone, add, commit, push, pull, branch, merge.', code: `# Clone repo\ngit clone https://github.com/user/repo.git\ncd repo\n\n# Create branch\ngit checkout -b feature/new-thing\n\n# Make changes, stage, commit\ngit add .\ngit commit -m "feat: add new thing"\n\n# Push\ngit push -u origin feature/new-thing\n\n# Update local main\ngit checkout main\ngit pull origin main\n\n# Rebase feature on latest main\ngit checkout feature/new-thing\ngit rebase main\n\n# Or merge main into feature\ngit merge main`, highlight: [2, 5, 8, 9, 12, 15, 16, 19, 20, 22, 23], diagram: { kind: 'flow', steps: ['clone → local', 'branch → isolate work', 'commit → snapshot', 'push → share', 'merge/rebase'] } },
    { title: 'Branching Strategies', description: 'Git Flow, GitHub Flow, trunk-based development.', code: `// GitHub Flow (simple)\n// main + short-lived feature branches\n// - Branch from main\n// - Work + push regularly\n// - PR → review → merge to main\n// - Deploy from main\n\n// Git Flow (complex)\n// main + develop + feature/release/hotfix\n// - Feature branches from develop\n// - Release branches for versioning\n// - Hotfix from main\n\n// Trunk-based (fastest)\n// Everyone commits to main directly\n// Feature flags for incomplete features\n// Continuous deployment\n// Great for high-trust teams\n\n// Pick based on:\n// - Team size\n// - Release cadence\n// - Testing maturity`, highlight: [1, 2, 3, 4, 5, 6, 8, 9, 10, 11, 12, 14, 15, 16, 17, 18], diagram: { kind: 'boxes', boxes: [{ id: 'g', label: 'GitHub Flow', color: '#1e88e5' }, { id: 'f', label: 'Git Flow', color: '#43a047' }, { id: 't', label: 'Trunk-based', color: '#fb8c00' }], connections: [] } },
    { title: 'Merge vs Rebase', description: 'Merge preserves history; rebase creates linear history.', code: `// Merge (default)\n// Preserves branch topology\ngit checkout main\ngit merge feature\n// Creates merge commit\n// History shows all commits + merge\n\n// Rebase (linear)\n// Replays commits on top of target\ngit checkout feature\ngit rebase main\ngit checkout main\ngit merge feature  // fast-forward\n// Linear history, no merge commit\n\n// Interactive rebase (rewrite history)\ngit rebase -i HEAD~5\n// Reorder, squash, edit, drop commits\n\n// NEVER rebase shared branches!\n// (rewrites hashes, breaks others' clones)\n\n// Squash merge (GitHub PR option)\n// Combine all feature commits into one`, highlight: [1, 2, 3, 4, 8, 9, 10, 11, 12, 13, 15, 16, 17, 18, 20, 21], diagram: { kind: 'boxes', boxes: [{ id: 'm', label: 'merge: branches preserved', color: '#1e88e5' }, { id: 'r', label: 'rebase: linear', color: '#43a047' }, { id: 's', label: 'squash: one commit', color: '#fb8c00' }], connections: [] } }
  ]},
  { path: 'full-stack', slug: 'version-control-basics', steps: [
    { title: 'Why Version Control?', description: 'Track changes, collaborate, revert bugs, branch experiments.', code: `// Benefits:\n// 1. History: who changed what, when, why\n// 2. Revert: bad code → git revert\n// 3. Blame: find introducer of bug\n// 4. Branching: isolate work\n// 5. Backup: repo on multiple machines\n// 6. Collaboration: parallel work\n\n// Git model:\n// - Repository: full history\n// - Commits: snapshots with parent refs\n// - Branches: pointers to commits\n// - Remotes: other copies (GitHub, GitLab)\n\n// Every clone is a full backup!`, highlight: [2, 3, 4, 5, 6, 7, 10, 11, 12, 13, 15], diagram: { kind: 'flow', steps: ['Work locally', 'git commit', 'git push', 'Remote synced', 'Team pulls'] } },
    { title: 'Commit Messages', description: 'Conventional Commits: type(scope): description.', code: `// Format:\n// <type>(<scope>): <subject>\n// \n// [optional body]\n//\n// [optional footer]\n\n// Types:\n// feat: new feature\n// fix: bug fix\n// docs: documentation\n// style: formatting\n// refactor: code restructure\n// test: tests\n// chore: build/tools\n// perf: performance\n\n// Examples:\n// feat(auth): add OAuth Google provider\n// fix(cart): prevent negative quantities\n// docs: update README installation steps\n\n// Breaking change\n// feat(api)!: rename user endpoint\n// BREAKING CHANGE: /users → /accounts`, highlight: [1, 2, 3, 4, 5, 6, 8, 9, 10, 11, 12, 13, 14, 15, 16, 18, 19, 20, 21, 22, 24, 25, 26], diagram: { kind: 'flow', steps: ['type(scope)', 'Subject imperative', 'Body: what + why', 'Breaking: !', 'Enables changelogs'] } },
    { title: '.gitignore Patterns', description: 'Exclude generated files, secrets, IDE files.', code: `# .gitignore\n\n# Dependencies\nnode_modules/\n.pnp/\n\n# Build outputs\ndist/\nbuild/\n*.log\n\n# Environment\n.env\n.env.local\n.env.*.local\n\n# IDE\n.vscode/\n.idea/\n*.swp\n\n# OS\n.DS_Store\nThumbs.db\n\n# Cache\n.next/\n.cache/\n.turbo/\n\n# Testing\ncoverage/\n\n# NEVER commit:\n# - Secrets, API keys\n# - node_modules (huge)\n# - Generated files`, highlight: [3, 4, 5, 7, 8, 9, 10, 12, 13, 14, 15, 17, 18, 19, 20, 22, 23, 24, 26, 27, 28, 29, 31, 32], diagram: { kind: 'boxes', boxes: [{ id: 'd', label: 'Dependencies', color: '#1e88e5' }, { id: 'b', label: 'Build output', color: '#43a047' }, { id: 'e', label: 'Env/secrets', color: '#e53935' }, { id: 'i', label: 'IDE/OS', color: '#fb8c00' }], connections: [] } }
  ]},
  { path: 'full-stack', slug: 'terminal-basics', steps: [
    { title: 'Essential Commands', description: 'Navigate, list, edit — the daily basics.', code: `# Navigate\npwd                  # print working directory\nls -la               # list all (with hidden)\ncd ~/projects        # change dir (~ = home)\ncd ..                # up one\ncd -                 # previous dir\n\n# Files\ntouch file.txt       # create empty\ncp source dest       # copy\ncp -r src/ dest/     # recursive\nmv old new           # move/rename\nrm file              # delete\nrm -rf dir/          # DELETE RECURSIVE (careful!)\n\n# View\ncat file.txt         # print all\nless file.txt        # paginated (q to quit)\nhead -n 20 file      # first 20 lines\ntail -f log.txt      # follow updates\n\n# Search\ngrep "pattern" file  # find text\nfind . -name "*.js"  # find files`, highlight: [2, 3, 4, 5, 6, 9, 10, 11, 12, 13, 14, 17, 18, 19, 20, 23, 24], diagram: { kind: 'flow', steps: ['pwd, ls, cd', 'touch, cp, mv, rm', 'cat, less, tail', 'grep, find', 'Terminal power'] } },
    { title: 'Piping and Redirection', description: 'Combine commands with | and > for powerful workflows.', code: `# Pipe: output of A → input of B\nls | wc -l           # count files\ncat log | grep error | head -20\n\n# Redirect to file\necho "hello" > out.txt       # overwrite\necho "world" >> out.txt      # append\ncommand 2> errors.log        # stderr\ncommand > all.log 2>&1        # both\n\n# Chain commands\ncmd1 && cmd2         # only if cmd1 succeeded\ncmd1 || cmd2         # only if cmd1 failed\ncmd1 ; cmd2          # both, regardless\n\n# Background\nnode server.js &     # run in background\njobs                 # list bg jobs\nfg %1                # bring to foreground\n\n# Variables\nexport API_KEY=abc\necho $API_KEY`, highlight: [2, 3, 6, 7, 8, 9, 11, 12, 13, 14, 17, 18, 19, 22, 23], diagram: { kind: 'flow', steps: ['A | B: pipe', '> overwrite', '>> append', '2>&1 stderr', '&& / || chain'] } },
    { title: 'Useful Tools', description: 'jq, curl, ssh, tmux, ripgrep — modern CLI must-haves.', code: `# HTTP client\ncurl https://api.example.com/users\ncurl -X POST -H "Content-Type: application/json" \\\n     -d '{"name":"Alice"}' https://api.example.com/users\n\n# JSON processing\ncurl https://api.example.com/users | jq '.[] | .name'\n\n# SSH\nssh user@server.com\nssh -i key.pem user@server.com\nscp file.tar.gz user@server:/tmp/\n\n# tmux (persistent sessions)\ntmux new -s work     # create session\n# Ctrl+B, D          # detach\ntmux attach -t work  # reattach\n\n# ripgrep (fast grep)\nrg "TODO" --type js\n\n# fzf (fuzzy finder)\ncd $(find . -type d | fzf)\n\n# htop (process viewer, better than top)`, highlight: [2, 3, 4, 7, 10, 11, 12, 15, 16, 17, 18, 21, 24, 26], diagram: { kind: 'boxes', boxes: [{ id: 'c', label: 'curl: HTTP', color: '#1e88e5' }, { id: 'j', label: 'jq: JSON', color: '#43a047' }, { id: 's', label: 'ssh: remote', color: '#fb8c00' }, { id: 't', label: 'tmux: sessions', color: '#8e24aa' }], connections: [] } }
  ]},
  { path: 'full-stack', slug: 'cloud-basics', steps: [
    { title: 'Cloud Providers', description: 'AWS, GCP, Azure — main hyperscalers.', code: `// AWS (Amazon Web Services)\n// EC2, S3, RDS, Lambda, ECS/EKS\n// Largest, most services, complex pricing\n\n// GCP (Google Cloud)\n// Compute Engine, Cloud Storage, BigQuery, GKE\n// Best for data/ML, K8s-native\n\n// Azure (Microsoft)\n// VMs, Blob Storage, SQL Database, AKS\n// Best for Microsoft ecosystem (AD, Windows)\n\n// Smaller options:\n// - DigitalOcean: simple, cheap\n// - Cloudflare: edge + Workers\n// - Fly.io: global apps in containers\n// - Vercel/Netlify: JAMstack\n// - Hetzner: cheap VPS Europe\n\n// Multi-cloud: hedge, avoid lock-in\n// But complexity multiplies!`, highlight: [1, 2, 3, 5, 6, 7, 9, 10, 11, 13, 14, 15, 16, 17, 18, 20, 21], diagram: { kind: 'boxes', boxes: [{ id: 'a', label: 'AWS', color: '#ff9900' }, { id: 'g', label: 'GCP', color: '#4285f4' }, { id: 'az', label: 'Azure', color: '#0078d4' }, { id: 'c', label: 'Cloudflare', color: '#f38020' }], connections: [] } },
    { title: 'Core Cloud Services', description: 'Compute, storage, database, networking, IAM.', code: `// Compute (run code)\n// AWS: EC2, Lambda, ECS, EKS, Fargate\n// GCP: Compute Engine, Cloud Run, GKE\n\n// Storage (files)\n// AWS: S3 (object), EBS (block), EFS (file)\n// GCP: Cloud Storage, Persistent Disk, Filestore\n\n// Database\n// AWS: RDS (SQL), DynamoDB (NoSQL), ElastiCache (Redis)\n// GCP: Cloud SQL, Firestore, Memorystore\n\n// Networking\n// VPC, subnets, security groups, load balancers\n// CloudFront (CDN), Route 53 (DNS)\n\n// IAM (identity + access)\n// Users, roles, policies\n// Least privilege principle\n\n// Managed services > self-hosted (usually)\n// Focus on your product, let cloud handle ops`, highlight: [1, 2, 3, 5, 6, 7, 9, 10, 11, 13, 14, 15, 17, 18, 19, 21, 22], diagram: { kind: 'boxes', boxes: [{ id: 'c', label: 'Compute', color: '#1e88e5' }, { id: 's', label: 'Storage', color: '#43a047' }, { id: 'd', label: 'Database', color: '#fb8c00' }, { id: 'n', label: 'Network', color: '#8e24aa' }], connections: [] } },
    { title: 'IaC and Cost Control', description: 'Infrastructure as Code (Terraform) + cost monitoring.', code: `# Terraform: declarative infrastructure\nresource "aws_instance" "web" {\n  ami           = "ami-0abc123"\n  instance_type = "t3.micro"\n  tags = { Name = "web-server" }\n}\n\nresource "aws_s3_bucket" "assets" {\n  bucket = "my-app-assets"\n}\n\n# Apply\nterraform plan\nterraform apply\n\n# Alternatives: Pulumi (real code), CDK (AWS)\n\n# Cost control:\n# - Set billing alerts\n# - Use spot/preemptible instances\n# - Auto-shutdown dev environments\n# - Right-size instances\n# - Delete unused resources\n# - Reserved instances for stable workloads\n# - Use tags for cost allocation`, highlight: [2, 3, 4, 5, 6, 8, 9, 10, 13, 14, 18, 19, 20, 21, 22, 23, 24, 25], diagram: { kind: 'flow', steps: ['Terraform HCL', 'terraform plan', 'terraform apply', 'Version-controlled infra', 'Reproducible'] } }
  ]},
  { path: 'full-stack', slug: 'monitoring-and-logging', steps: [
    { title: 'The Three Pillars', description: 'Metrics (numbers), Logs (events), Traces (request flows).', code: `// Metrics: numeric time series\n// - CPU %, memory, request rate, latency\n// - Aggregated (avg, p50, p95, p99)\n// - Tools: Prometheus, Datadog, CloudWatch\n\n// Logs: text records of events\n// - Structured (JSON) preferred\n// - Levels: DEBUG, INFO, WARN, ERROR\n// - Tools: ELK stack, Loki, Datadog\n\n// Traces: full request path across services\n// - Request A → Service B → DB → Cache → response\n// - Tools: Jaeger, Tempo, Datadog APM\n\n// Together: full observability\n// - Metrics alert → check logs → trace to root cause`, highlight: [1, 2, 3, 4, 6, 7, 8, 9, 11, 12, 13, 15, 16], diagram: { kind: 'boxes', boxes: [{ id: 'm', label: 'Metrics: numbers', color: '#1e88e5' }, { id: 'l', label: 'Logs: events', color: '#43a047' }, { id: 't', label: 'Traces: flows', color: '#fb8c00' }], connections: [] } },
    { title: 'Structured Logging', description: 'JSON logs are searchable, filterable, aggregatable.', code: `// Bad: unstructured\nconsole.log('User Alice logged in from IP 1.2.3.4');\n// Can't easily query by user or IP\n\n// Good: structured JSON\nlogger.info({\n  event: 'user.login',\n  userId: 'alice',\n  ip: '1.2.3.4',\n  timestamp: new Date().toISOString(),\n  requestId: 'req_abc123',\n});\n\n// In log aggregation (Loki, Elastic):\n// Query: event="user.login" AND ip="1.2.3.4"\n// Aggregate: count by userId\n// Alert: >10 failed logins per minute\n\n// Pino (fast JSON logger)\nimport pino from 'pino';\nconst logger = pino({\n  redact: ['password', 'token'],  // never log secrets!\n});`, highlight: [1, 2, 3, 5, 6, 7, 8, 9, 10, 11, 12, 14, 15, 16, 17, 19, 20, 21, 22, 23], diagram: { kind: 'flow', steps: ['Log JSON', 'Ship to aggregator', 'Query + filter', 'Alert on patterns', 'Debug fast'] } },
    { title: 'Alerting', description: 'Notify humans when things break — but not too often.', code: `// Good alerts:\n// - Actionable (need human intervention)\n// - Specific (what to fix)\n// - Rare (no false positives)\n\n// Bad alerts:\n// - Info notifications (no action needed)\n// - Duplicates\n// - Symptom, not root cause\n\n// SLI/SLO/SLA:\n// SLI: what you measure (99.9% uptime)\n// SLO: target you aim for (99.95%)\n// SLA: contract with customer (99.9%)\n\n// Error budget:\n// SLO 99.9% = 0.1% budget = 43 min/month\n// When exhausted, freeze deploys\n\n// Golden signals (Google SRE):\n// - Latency\n// - Traffic\n// - Errors\n// - Saturation\n\n// Alert on customer impact, not causes`, highlight: [1, 2, 3, 4, 6, 7, 8, 9, 11, 12, 13, 14, 16, 17, 18, 20, 21, 22, 23, 24, 26], diagram: { kind: 'flow', steps: ['SLI: measure', 'SLO: target', 'SLA: contract', 'Alert on SLO breach', 'Fix or freeze deploys'] } }
  ]}
];

