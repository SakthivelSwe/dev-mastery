module.exports = [
  {
    path: 'leetcode-patterns', slug: 'dp-subsequence-pattern',
    steps: [
      { title: 'Longest Common Subsequence', description: 'LCS: dp[i][j] = length of LCS of text1[0..i] and text2[0..j]. O(n×m).', code: `// LC 1143: Longest Common Subsequence\nfunction longestCommonSubsequence(text1, text2) {\n  const m = text1.length, n = text2.length;\n  const dp = Array.from({length: m+1}, () => new Array(n+1).fill(0));\n  for (let i = 1; i <= m; i++) {\n    for (let j = 1; j <= n; j++) {\n      if (text1[i-1] === text2[j-1]) {\n        dp[i][j] = dp[i-1][j-1] + 1;\n      } else {\n        dp[i][j] = Math.max(dp[i-1][j], dp[i][j-1]);\n      }\n    }\n  }\n  return dp[m][n];\n}`, highlight: [7, 8, 9, 10, 14], diagram: { kind: 'boxes', boxes: [{ id: 'm', label: 'Match: dp[i-1][j-1] + 1', color: '#43a047' }, { id: 'nm', label: 'No match: max(up, left)', color: '#e53935' }], connections: [] } },
      { title: 'Longest Increasing Subsequence', description: 'LIS: O(n²) DP or O(n log n) with binary search + patience piles.', code: `// LC 300: Longest Increasing Subsequence\n\n// O(n²) DP\nfunction lengthOfLIS(nums) {\n  const dp = new Array(nums.length).fill(1);\n  for (let i = 1; i < nums.length; i++) {\n    for (let j = 0; j < i; j++) {\n      if (nums[j] < nums[i]) dp[i] = Math.max(dp[i], dp[j] + 1);\n    }\n  }\n  return Math.max(...dp);\n}\n\n// O(n log n) with binary search\nfunction lengthOfLISFast(nums) {\n  const tails = [];\n  for (const n of nums) {\n    let lo = 0, hi = tails.length;\n    while (lo < hi) {\n      const mid = (lo + hi) >> 1;\n      if (tails[mid] < n) lo = mid + 1;\n      else hi = mid;\n    }\n    tails[lo] = n;\n  }\n  return tails.length;\n}`, highlight: [5, 6, 7, 8, 15, 16, 17, 18, 19, 20, 21, 22, 23], diagram: { kind: 'flow', steps: ['O(n²): compare each pair', 'OR', 'Binary search insertion', 'tails[] = smallest tail per length', 'Length = LIS'] } },
      { title: 'Edit Distance', description: 'Min operations (insert/delete/replace) to convert word1 to word2.', code: `// LC 72: Edit Distance\nfunction minDistance(word1, word2) {\n  const m = word1.length, n = word2.length;\n  const dp = Array.from({length: m+1}, () => new Array(n+1).fill(0));\n  for (let i = 0; i <= m; i++) dp[i][0] = i; // delete all\n  for (let j = 0; j <= n; j++) dp[0][j] = j; // insert all\n  for (let i = 1; i <= m; i++) {\n    for (let j = 1; j <= n; j++) {\n      if (word1[i-1] === word2[j-1]) {\n        dp[i][j] = dp[i-1][j-1]; // no op\n      } else {\n        dp[i][j] = 1 + Math.min(\n          dp[i-1][j],   // delete\n          dp[i][j-1],   // insert\n          dp[i-1][j-1]  // replace\n        );\n      }\n    }\n  }\n  return dp[m][n];\n}`, highlight: [5, 6, 9, 10, 12, 13, 14, 15, 20], diagram: { kind: 'flow', steps: ['Match: no op', 'Delete: dp[i-1][j] + 1', 'Insert: dp[i][j-1] + 1', 'Replace: dp[i-1][j-1] + 1', 'Take min'] } },
      { title: 'Distinct Subsequences', description: 'Count how many times pattern appears as subsequence of text.', code: `// LC 115: Distinct Subsequences\nfunction numDistinct(s, t) {\n  const m = s.length, n = t.length;\n  const dp = Array.from({length: m+1}, () => new Array(n+1).fill(0));\n  for (let i = 0; i <= m; i++) dp[i][0] = 1; // empty pattern\n  for (let i = 1; i <= m; i++) {\n    for (let j = 1; j <= n; j++) {\n      dp[i][j] = dp[i-1][j]; // skip s[i-1]\n      if (s[i-1] === t[j-1]) dp[i][j] += dp[i-1][j-1]; // match\n    }\n  }\n  return dp[m][n];\n}`, highlight: [5, 8, 9, 12], diagram: { kind: 'flow', steps: ['dp[i][0] = 1 (empty t)', 'Skip s[i-1]: dp[i-1][j]', 'Match: + dp[i-1][j-1]', 'Sum ways', 'Return dp[m][n]'] } },
      { title: 'Wildcard/Regex Matching', description: 'DP for regex-style pattern matching with * and ? wildcards.', code: `// LC 44: Wildcard Matching\nfunction isMatch(s, p) {\n  const m = s.length, n = p.length;\n  const dp = Array.from({length: m+1}, () => new Array(n+1).fill(false));\n  dp[0][0] = true;\n  for (let j = 1; j <= n; j++) {\n    if (p[j-1] === '*') dp[0][j] = dp[0][j-1];\n  }\n  for (let i = 1; i <= m; i++) {\n    for (let j = 1; j <= n; j++) {\n      if (p[j-1] === '*') {\n        // *: match empty (dp[i][j-1]) or one more (dp[i-1][j])\n        dp[i][j] = dp[i][j-1] || dp[i-1][j];\n      } else if (p[j-1] === '?' || p[j-1] === s[i-1]) {\n        dp[i][j] = dp[i-1][j-1];\n      }\n    }\n  }\n  return dp[m][n];\n}`, highlight: [5, 6, 7, 10, 11, 12, 13, 19], diagram: { kind: 'flow', steps: ['* : match empty or extend', '?: match one char', 'Exact match: consume both', 'dp[m][n] final', 'True/False'] } }
    ]
  },
  {
    path: 'leetcode-patterns', slug: 'dp-grid-pattern',
    steps: [
      { title: 'Unique Paths', description: 'Count paths in a grid from top-left to bottom-right — only move right or down.', code: `// LC 62: Unique Paths\nfunction uniquePaths(m, n) {\n  const dp = new Array(n).fill(1);\n  for (let i = 1; i < m; i++) {\n    for (let j = 1; j < n; j++) {\n      dp[j] += dp[j-1]; // from above + from left\n    }\n  }\n  return dp[n-1];\n}\n// Math: C(m+n-2, m-1)\n\n// LC 63 with obstacles:\n// dp[j] = 0 if grid[i][j] = 1 (obstacle)`, highlight: [3, 4, 5, 6, 9, 13, 14], diagram: { kind: 'flow', steps: ['dp[j] = paths to col j', 'Add from left dp[j-1]', 'Sweep row by row', 'O(n) space', 'dp[n-1] = answer'] } },
      { title: 'Minimum Path Sum', description: 'Find path with minimum sum from top-left to bottom-right.', code: `// LC 64: Minimum Path Sum\nfunction minPathSum(grid) {\n  const m = grid.length, n = grid[0].length;\n  const dp = new Array(n).fill(Infinity);\n  dp[0] = 0;\n  for (let i = 0; i < m; i++) {\n    dp[0] += grid[i][0]; // first column\n    for (let j = 1; j < n; j++) {\n      dp[j] = Math.min(dp[j], dp[j-1]) + grid[i][j];\n    }\n  }\n  return dp[n-1];\n}\n// dp[j] = min sum to reach (i, j)\n// = min(from above dp[j], from left dp[j-1]) + grid[i][j]`, highlight: [7, 8, 9, 12], diagram: { kind: 'flow', steps: ['1D dp row', 'dp[j] = min from above or left', '+ grid[i][j]', 'Sweep rows', 'dp[n-1] final'] } },
      { title: 'Maximal Square', description: 'Find largest square of 1s — dp[i][j] = side length of largest square ending at (i,j).', code: `// LC 221: Maximal Square\nfunction maximalSquare(matrix) {\n  const m = matrix.length, n = matrix[0].length;\n  const dp = new Array(n + 1).fill(0);\n  let maxSide = 0, prev = 0;\n  for (let i = 1; i <= m; i++) {\n    for (let j = 1; j <= n; j++) {\n      const temp = dp[j];\n      if (matrix[i-1][j-1] === '1') {\n        // Min of top, left, top-left + 1\n        dp[j] = Math.min(dp[j], dp[j-1], prev) + 1;\n        maxSide = Math.max(maxSide, dp[j]);\n      } else {\n        dp[j] = 0;\n      }\n      prev = temp;\n    }\n  }\n  return maxSide * maxSide;\n}`, highlight: [7, 8, 9, 10, 11, 12, 15, 19], diagram: { kind: 'flow', steps: ['dp[i][j] = square side ending here', 'Only if grid[i][j] = 1', 'Min of 3 neighbors + 1', 'Track max side', 'Return side²'] } },
      { title: 'Cherry Pickup (2 paths)', description: 'DP on multiple simultaneous paths — advanced grid DP.', code: `// LC 741: Cherry Pickup (down-right, then up-left)\n// Trick: run TWO parallel paths from (0,0) → (n-1,n-1)\n// State: (r1, c1, r2) — r2 = r1 + c1 - c2 (same steps)\n\nfunction cherryPickup(grid) {\n  const n = grid.length;\n  const memo = new Map();\n  const dp = (r1, c1, r2) => {\n    const c2 = r1 + c1 - r2;\n    if (r1 >= n || c1 >= n || r2 >= n || c2 >= n) return -Infinity;\n    if (grid[r1][c1] === -1 || grid[r2][c2] === -1) return -Infinity;\n    if (r1 === n-1 && c1 === n-1) return grid[r1][c1];\n    const key = \`\${r1},\${c1},\${r2}\`;\n    if (memo.has(key)) return memo.get(key);\n    let cherries = grid[r1][c1];\n    if (r1 !== r2) cherries += grid[r2][c2];\n    const best = Math.max(\n      dp(r1+1, c1, r2+1), dp(r1, c1+1, r2+1),\n      dp(r1+1, c1, r2),   dp(r1, c1+1, r2)\n    );\n    memo.set(key, cherries + best);\n    return cherries + best;\n  };\n  return Math.max(0, dp(0, 0, 0));\n}`, highlight: [7, 8, 9, 15, 16, 17, 18, 19, 20], diagram: { kind: 'flow', steps: ['Two paths simultaneously', 'Same steps: c2 = r1+c1-r2', 'Two moves each', '4 combinations', 'Max cherries'] } },
      { title: 'Dungeon Game (Reverse DP)', description: 'DP from bottom-right to top-left — knight needs min HP to reach princess alive.', code: `// LC 174: Dungeon Game\nfunction calculateMinimumHP(dungeon) {\n  const m = dungeon.length, n = dungeon[0].length;\n  // dp[i][j] = min HP needed at (i,j) to survive to princess\n  const dp = Array.from({length: m+1}, () => new Array(n+1).fill(Infinity));\n  dp[m][n-1] = dp[m-1][n] = 1;\n  for (let i = m-1; i >= 0; i--) {\n    for (let j = n-1; j >= 0; j--) {\n      const need = Math.min(dp[i+1][j], dp[i][j+1]) - dungeon[i][j];\n      dp[i][j] = Math.max(1, need);\n    }\n  }\n  return dp[0][0];\n}`, highlight: [5, 6, 7, 8, 9, 10, 13], diagram: { kind: 'flow', steps: ['DP from destination', 'Min HP at (i,j)', 'min(down, right) - room', 'At least 1 HP', 'dp[0][0] = answer'] } }
    ]
  },
  {
    path: 'leetcode-patterns', slug: 'dp-interval-pattern',
    steps: [
      { title: 'Interval DP Structure', description: 'dp[i][j] = optimal for range [i..j]. Fill by increasing length.', code: `// General interval DP template\nfor (let len = 2; len <= n; len++) {         // length\n  for (let i = 0; i + len - 1 < n; i++) {   // start\n    const j = i + len - 1;                   // end\n    for (let k = i; k < j; k++) {            // split point\n      dp[i][j] = optimum(dp[i][j],\n        dp[i][k] + dp[k+1][j] + costOfMerging(i, k, j));\n    }\n  }\n}\n\n// Use cases:\n// - Matrix chain multiplication\n// - Palindrome partitioning\n// - Optimal BST\n// - Burst balloons`, highlight: [2, 3, 4, 5, 6, 7], diagram: { kind: 'flow', steps: ['Iterate by length', 'For each start i, end j', 'Try every split k', 'Optimal cost', 'O(n³) typical'] } },
      { title: 'Matrix Chain Multiplication', description: 'Find optimal parenthesization to minimize scalar multiplications.', code: `// Matrix Chain Multiplication\nfunction matrixChain(dims) {\n  const n = dims.length - 1;\n  const dp = Array.from({length: n}, () => new Array(n).fill(0));\n  for (let len = 2; len <= n; len++) {\n    for (let i = 0; i + len - 1 < n; i++) {\n      const j = i + len - 1;\n      dp[i][j] = Infinity;\n      for (let k = i; k < j; k++) {\n        const cost = dp[i][k] + dp[k+1][j] + dims[i] * dims[k+1] * dims[j+1];\n        dp[i][j] = Math.min(dp[i][j], cost);\n      }\n    }\n  }\n  return dp[0][n-1];\n}\n// dims = [10, 30, 5, 60] → matrices A(10x30) B(30x5) C(5x60)\n// (AB)C: 10*30*5 + 10*5*60 = 4500\n// A(BC): 30*5*60 + 10*30*60 = 27000\n// Answer: 4500`, highlight: [5, 6, 7, 9, 10, 11, 17, 18, 19], diagram: { kind: 'flow', steps: ['For each chain length', 'Try each split point', 'Cost = left + right + merge', 'Take minimum', 'dp[0][n-1] answer'] } },
      { title: 'Burst Balloons', description: 'Last-balloon insight — think about which balloon bursts LAST in each range.', code: `// LC 312: Burst Balloons\nfunction maxCoins(nums) {\n  const arr = [1, ...nums, 1]; // pad with 1s\n  const n = arr.length;\n  const dp = Array.from({length: n}, () => new Array(n).fill(0));\n  for (let len = 2; len < n; len++) {\n    for (let i = 0; i + len < n; i++) {\n      const j = i + len;\n      for (let k = i + 1; k < j; k++) {\n        // k is LAST balloon to burst in (i, j)\n        const coins = arr[i] * arr[k] * arr[j] + dp[i][k] + dp[k][j];\n        dp[i][j] = Math.max(dp[i][j], coins);\n      }\n    }\n  }\n  return dp[0][n-1];\n}`, highlight: [3, 6, 7, 8, 9, 10, 11, 16], diagram: { kind: 'flow', steps: ['Pad with 1s', 'For each range (i,j)', 'Try each k as LAST', 'coins = arr[i]*arr[k]*arr[j]', 'Neighbors known when k bursts'] } },
      { title: 'Palindrome Partitioning II', description: 'Min cuts to partition string into palindromic substrings.', code: `// LC 132: Palindrome Partitioning II\nfunction minCut(s) {\n  const n = s.length;\n  const pal = Array.from({length: n}, () => new Array(n).fill(false));\n  const dp = new Array(n).fill(0);\n  for (let i = 0; i < n; i++) {\n    let min = i;\n    for (let j = 0; j <= i; j++) {\n      if (s[i] === s[j] && (i - j < 2 || pal[j+1][i-1])) {\n        pal[j][i] = true;\n        min = j === 0 ? 0 : Math.min(min, dp[j-1] + 1);\n      }\n    }\n    dp[i] = min;\n  }\n  return dp[n-1];\n}`, highlight: [4, 6, 8, 9, 10, 11, 14], diagram: { kind: 'flow', steps: ['Build palindrome table', 'For each end i', 'Try all starts j', 'If pal(j,i): update dp[i]', 'dp[n-1] = min cuts'] } },
      { title: 'Optimal BST', description: 'Construct BST from keys with given frequencies — minimize search cost.', code: `// Optimal BST\nfunction optimalBST(freq, n) {\n  const dp = Array.from({length: n+1}, () => new Array(n).fill(0));\n  // sum[i][j] = sum of freq[i..j]\n  for (let i = 0; i < n; i++) dp[i][i] = freq[i];\n  for (let len = 2; len <= n; len++) {\n    for (let i = 0; i + len - 1 < n; i++) {\n      const j = i + len - 1;\n      dp[i][j] = Infinity;\n      let sum = 0;\n      for (let s = i; s <= j; s++) sum += freq[s];\n      for (let r = i; r <= j; r++) {\n        const left = r > i ? dp[i][r-1] : 0;\n        const right = r < j ? dp[r+1][j] : 0;\n        dp[i][j] = Math.min(dp[i][j], left + right + sum);\n      }\n    }\n  }\n  return dp[0][n-1];\n}`, highlight: [5, 6, 7, 8, 12, 13, 14, 15], diagram: { kind: 'flow', steps: ['For each range', 'Try each root r', 'Left + right + sum', 'Min over roots', 'Optimal BST cost'] } }
    ]
  },
  {
    path: 'leetcode-patterns', slug: 'dp-palindrome-pattern',
    steps: [
      { title: 'Longest Palindromic Substring', description: 'Expand around center — O(n²) time, O(1) space.', code: `// LC 5: Longest Palindromic Substring\nfunction longestPalindrome(s) {\n  let start = 0, maxLen = 1;\n  const expand = (l, r) => {\n    while (l >= 0 && r < s.length && s[l] === s[r]) {\n      if (r - l + 1 > maxLen) {\n        start = l;\n        maxLen = r - l + 1;\n      }\n      l--; r++;\n    }\n  };\n  for (let i = 0; i < s.length; i++) {\n    expand(i, i);     // odd length\n    expand(i, i + 1); // even length\n  }\n  return s.substr(start, maxLen);\n}`, highlight: [4, 5, 6, 7, 8, 9, 13, 14, 15], diagram: { kind: 'flow', steps: ['For each center', 'Expand outward', 'Match? extend', 'Track max', 'Odd and even centers'] } },
      { title: 'Palindromic Substrings Count', description: 'Count all palindromic substrings — same expand technique.', code: `// LC 647: Palindromic Substrings\nfunction countSubstrings(s) {\n  let count = 0;\n  const expand = (l, r) => {\n    while (l >= 0 && r < s.length && s[l] === s[r]) {\n      count++;\n      l--; r++;\n    }\n  };\n  for (let i = 0; i < s.length; i++) {\n    expand(i, i);\n    expand(i, i + 1);\n  }\n  return count;\n}`, highlight: [4, 5, 6, 7, 8, 10, 11, 12], diagram: { kind: 'flow', steps: ['Each expansion = 1 palindrome', 'Odd length: (i, i)', 'Even length: (i, i+1)', 'Increment on match', 'Total count'] } },
      { title: 'Longest Palindromic Subsequence', description: 'LPS with DP — reverse and find LCS, or interval DP.', code: `// LC 516: Longest Palindromic Subsequence\nfunction longestPalindromeSubseq(s) {\n  const n = s.length;\n  const dp = Array.from({length: n}, () => new Array(n).fill(0));\n  for (let i = 0; i < n; i++) dp[i][i] = 1;\n  for (let len = 2; len <= n; len++) {\n    for (let i = 0; i + len - 1 < n; i++) {\n      const j = i + len - 1;\n      if (s[i] === s[j]) {\n        dp[i][j] = dp[i+1][j-1] + 2;\n      } else {\n        dp[i][j] = Math.max(dp[i+1][j], dp[i][j-1]);\n      }\n    }\n  }\n  return dp[0][n-1];\n}\n// Alt: LCS(s, reverse(s))`, highlight: [5, 6, 7, 8, 9, 10, 11, 12, 16, 17, 18], diagram: { kind: 'flow', steps: ['dp[i][j] = LPS in s[i..j]', 'Match ends: +2', 'No match: max(left, right)', 'Interval DP', 'dp[0][n-1]'] } },
      { title: 'Palindrome Partitioning (all)', description: 'Return all palindromic partitions — backtracking + palindrome check.', code: `// LC 131: Palindrome Partitioning\nfunction partition(s) {\n  const result = [];\n  const isPal = (l, r) => {\n    while (l < r) {\n      if (s[l++] !== s[r--]) return false;\n    }\n    return true;\n  };\n  const backtrack = (start, path) => {\n    if (start === s.length) {\n      result.push([...path]);\n      return;\n    }\n    for (let end = start; end < s.length; end++) {\n      if (isPal(start, end)) {\n        path.push(s.slice(start, end + 1));\n        backtrack(end + 1, path);\n        path.pop();\n      }\n    }\n  };\n  backtrack(0, []);\n  return result;\n}`, highlight: [10, 11, 12, 15, 16, 17, 18, 19], diagram: { kind: 'flow', steps: ['For each start', 'Try each end', 'Palindrome? recurse', 'Backtrack pop', 'All partitions'] } },
      { title: 'Manacher\'s Algorithm', description: 'Linear O(n) for longest palindromic substring — advanced technique.', code: `// Manacher's algorithm - O(n)\nfunction manacher(s) {\n  // Transform: "abc" → "^#a#b#c#$"\n  const t = '^#' + s.split('').join('#') + '#$';\n  const p = new Array(t.length).fill(0);\n  let c = 0, r = 0;\n  for (let i = 1; i < t.length - 1; i++) {\n    const mirror = 2 * c - i;\n    if (i < r) p[i] = Math.min(r - i, p[mirror]);\n    while (t[i + p[i] + 1] === t[i - p[i] - 1]) p[i]++;\n    if (i + p[i] > r) {\n      c = i;\n      r = i + p[i];\n    }\n  }\n  const maxLen = Math.max(...p);\n  const centerIdx = p.indexOf(maxLen);\n  const start = Math.floor((centerIdx - maxLen) / 2);\n  return s.substr(start, maxLen);\n}`, highlight: [4, 5, 6, 8, 9, 10, 11, 12, 13], diagram: { kind: 'flow', steps: ['Transform string (insert #)', 'p[i] = palindrome radius', 'Use mirror + expand', 'O(n) amortized', 'Extract answer'] } }
    ]
  },
  {
    path: 'leetcode-patterns', slug: 'dp-tree-pattern',
    steps: [
      { title: 'Tree DP: Diameter', description: 'DP on trees uses post-order: children\'s answers feed into parent\'s answer.', code: `// LC 543: Diameter of Binary Tree\nfunction diameterOfBinaryTree(root) {\n  let diameter = 0;\n\n  const depth = (node) => {\n    if (!node) return 0;\n    const left = depth(node.left);\n    const right = depth(node.right);\n    // At this node, longest path THROUGH here\n    diameter = Math.max(diameter, left + right);\n    // Return: depth of subtree\n    return 1 + Math.max(left, right);\n  };\n\n  depth(root);\n  return diameter;\n}`, highlight: [6, 7, 8, 10, 12, 15], diagram: { kind: 'flow', steps: ['Post-order DFS', 'left = depth(left)', 'right = depth(right)', 'Path through node: L+R', 'Track global max'] } },
      { title: 'House Robber III (Tree)', description: 'Cannot rob adjacent nodes — return (rob, notRob) tuple for each subtree.', code: `// LC 337: House Robber III\nfunction rob(root) {\n  const dfs = (node) => {\n    if (!node) return [0, 0]; // [rob, notRob]\n    const [lRob, lNot] = dfs(node.left);\n    const [rRob, rNot] = dfs(node.right);\n    return [\n      node.val + lNot + rNot,           // rob this: children can't be robbed\n      Math.max(lRob, lNot) + Math.max(rRob, rNot) // don't rob: max of each child\n    ];\n  };\n  const [rob, notRob] = dfs(root);\n  return Math.max(rob, notRob);\n}`, highlight: [3, 4, 5, 6, 8, 9, 10, 12, 13], diagram: { kind: 'flow', steps: ['Post-order return (rob, notRob)', 'Rob: node.val + child.notRob', 'notRob: max of child options', 'Max at root', 'O(n) time'] } },
      { title: 'Max Path Sum', description: 'Any node to any node path — track max at each node contributing to path.', code: `// LC 124: Binary Tree Maximum Path Sum\nfunction maxPathSum(root) {\n  let max = -Infinity;\n\n  const dfs = (node) => {\n    if (!node) return 0;\n    // Ignore negative subtrees\n    const left = Math.max(0, dfs(node.left));\n    const right = Math.max(0, dfs(node.right));\n    // Path THROUGH this node\n    max = Math.max(max, node.val + left + right);\n    // Return: single-side path (parent can only use one side)\n    return node.val + Math.max(left, right);\n  };\n\n  dfs(root);\n  return max;\n}`, highlight: [5, 6, 7, 8, 9, 10, 11, 12, 13], diagram: { kind: 'flow', steps: ['Post-order DFS', 'Ignore negative subtrees', 'Path through: v+L+R', 'Return single side', 'Max updates globally'] } },
      { title: 'Distribute Coins in Tree', description: 'Every node needs 1 coin — count moves needed. Bottom-up excess tracking.', code: `// LC 979: Distribute Coins in Binary Tree\nfunction distributeCoins(root) {\n  let moves = 0;\n\n  const dfs = (node) => {\n    if (!node) return 0;\n    const left = dfs(node.left);\n    const right = dfs(node.right);\n    moves += Math.abs(left) + Math.abs(right);\n    // Return excess/deficit\n    return node.val + left + right - 1;\n  };\n\n  dfs(root);\n  return moves;\n}\n// Insight: |excess of subtree| = moves through edge to parent`, highlight: [5, 6, 7, 8, 9, 10, 11, 15, 16], diagram: { kind: 'flow', steps: ['Post-order excess', 'Excess = val + subtrees - 1', 'moves += |excess|', 'Flow through edges', 'Total moves'] } },
      { title: 'Longest ZigZag Path', description: 'Alternate left-right children — DP returns two states per node.', code: `// LC 1372: Longest ZigZag Path in Binary Tree\nfunction longestZigZag(root) {\n  let max = 0;\n\n  const dfs = (node) => {\n    if (!node) return [-1, -1]; // [leftPath, rightPath]\n    const [lL, lR] = dfs(node.left);\n    const [rL, rR] = dfs(node.right);\n    // Going left from this node = 1 + right path of left child\n    const goLeft = lR + 1;\n    const goRight = rL + 1;\n    max = Math.max(max, goLeft, goRight);\n    return [goLeft, goRight];\n  };\n\n  dfs(root);\n  return max;\n}`, highlight: [5, 6, 7, 8, 9, 10, 11, 12, 13], diagram: { kind: 'flow', steps: ['Return [leftPath, rightPath]', 'Left path = 1 + child.rightPath', 'Right path = 1 + child.leftPath', 'ZigZag alternation', 'Track max'] } }
    ]
  }
];

