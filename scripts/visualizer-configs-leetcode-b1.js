module.exports = [
  {
    path: 'leetcode-patterns', slug: 'two-pointers-pattern',
    steps: [
      { title: 'Two Pointers Introduction', description: 'Use two indices moving through the array to reduce O(n²) to O(n) — one of the most common patterns.', code: `// Classic: Two Sum in sorted array\nfunction twoSum(nums, target) {\n  let left = 0, right = nums.length - 1;\n  while (left < right) {\n    const sum = nums[left] + nums[right];\n    if (sum === target) return [left, right];\n    if (sum < target) left++;\n    else right--;\n  }\n  return [];\n}`, highlight: [3, 4, 5, 6, 7, 8], diagram: { kind: 'flow', steps: ['sorted array', 'left=0, right=n-1', 'sum < target → left++', 'sum > target → right--', 'sum = target → found'] } },
      { title: 'Same Direction Two Pointers', description: 'Both pointers move in the same direction — used for in-place array modifications.', code: `// Remove duplicates from sorted array (LC 26)\nfunction removeDuplicates(nums) {\n  if (nums.length === 0) return 0;\n  let writeIdx = 1;\n  for (let readIdx = 1; readIdx < nums.length; readIdx++) {\n    if (nums[readIdx] !== nums[readIdx - 1]) {\n      nums[writeIdx] = nums[readIdx];\n      writeIdx++;\n    }\n  }\n  return writeIdx;\n}`, highlight: [4, 5, 6, 7, 8, 9, 10], diagram: { kind: 'flow', steps: ['read scans array', 'unique found?', 'write to writeIdx', 'writeIdx++', 'return writeIdx = new length'] } },
      { title: 'Three Sum', description: 'Sort array, fix one pointer, use two pointers for the remaining two — O(n²).', code: `function threeSum(nums) {\n  nums.sort((a, b) => a - b);\n  const result = [];\n  for (let i = 0; i < nums.length - 2; i++) {\n    if (i > 0 && nums[i] === nums[i-1]) continue; // skip duplicates\n    let left = i + 1, right = nums.length - 1;\n    while (left < right) {\n      const sum = nums[i] + nums[left] + nums[right];\n      if (sum === 0) {\n        result.push([nums[i], nums[left], nums[right]]);\n        while (nums[left] === nums[left+1]) left++;\n        while (nums[right] === nums[right-1]) right--;\n        left++; right--;\n      } else if (sum < 0) left++;\n      else right--;\n    }\n  }\n  return result;\n}`, highlight: [2, 4, 6, 7, 11, 12], diagram: { kind: 'flow', steps: ['Sort array', 'Fix nums[i]', 'Two pointers on rest', 'Skip duplicates', 'O(n²) total'] } },
      { title: 'Container With Most Water', description: 'Two pointers converge — always move the shorter side to try increasing area.', code: `function maxArea(height) {\n  let left = 0, right = height.length - 1;\n  let max = 0;\n  while (left < right) {\n    const h = Math.min(height[left], height[right]);\n    const w = right - left;\n    max = Math.max(max, h * w);\n    // Move the shorter side (only way to potentially grow area)\n    if (height[left] < height[right]) left++;\n    else right--;\n  }\n  return max;\n}`, highlight: [4, 5, 6, 7, 9, 10], diagram: { kind: 'flow', steps: ['left=0, right=n-1', 'area = min(h) × width', 'Move shorter side', 'Try larger height', 'Track max'] } },
      { title: 'When to Use Two Pointers', description: 'Signals: sorted input, pair/triplet search, in-place modification, palindrome check.', code: `// Palindrome check\nfunction isPalindrome(s) {\n  let left = 0, right = s.length - 1;\n  while (left < right) {\n    if (s[left] !== s[right]) return false;\n    left++; right--;\n  }\n  return true;\n}\n\n// Common LC problems:\n// 1. Two Sum II (sorted)\n// 15. 3Sum, 16. 3Sum Closest\n// 11. Container With Most Water\n// 42. Trapping Rain Water\n// 125. Valid Palindrome\n// 26. Remove Duplicates`, highlight: [3, 4, 5, 6, 12, 13, 14, 15, 16], diagram: { kind: 'boxes', boxes: [{ id: 's', label: 'Sorted array', color: '#1e88e5' }, { id: 'p', label: 'Palindrome', color: '#43a047' }, { id: 'i', label: 'In-place mod', color: '#fb8c00' }, { id: 'pt', label: 'Pair/triplet', color: '#8e24aa' }], connections: [] } }
    ]
  },
  {
    path: 'leetcode-patterns', slug: 'sliding-window-pattern',
    steps: [
      { title: 'Fixed-Size Sliding Window', description: 'Slide a window of fixed size k across the array — maintain running metric in O(1) per step.', code: `// Max sum of k consecutive elements (LC 643)\nfunction maxAverage(nums, k) {\n  let sum = 0;\n  for (let i = 0; i < k; i++) sum += nums[i];\n  let maxSum = sum;\n  for (let i = k; i < nums.length; i++) {\n    sum += nums[i] - nums[i - k]; // slide: add new, remove old\n    maxSum = Math.max(maxSum, sum);\n  }\n  return maxSum / k;\n}`, highlight: [4, 5, 7, 8, 9], diagram: { kind: 'flow', steps: ['Init sum of first k', 'Slide window', '+ nums[i], - nums[i-k]', 'Update max', 'O(n) total'] } },
      { title: 'Variable-Size Sliding Window', description: 'Expand right pointer, shrink left when constraint violated — pattern for min/max subarray problems.', code: `// Longest substring without repeating characters (LC 3)\nfunction lengthOfLongestSubstring(s) {\n  const seen = new Map();\n  let left = 0, maxLen = 0;\n  for (let right = 0; right < s.length; right++) {\n    if (seen.has(s[right]) && seen.get(s[right]) >= left) {\n      left = seen.get(s[right]) + 1; // shrink window\n    }\n    seen.set(s[right], right);\n    maxLen = Math.max(maxLen, right - left + 1);\n  }\n  return maxLen;\n}`, highlight: [4, 5, 6, 7, 9, 10], diagram: { kind: 'flow', steps: ['Expand right', 'Add to seen', 'Duplicate? shrink left', 'Update maxLen', 'Return maxLen'] } },
      { title: 'Minimum Window Substring', description: 'Classic hard sliding window — find smallest substring containing all chars of target.', code: `// LC 76: Minimum Window Substring\nfunction minWindow(s, t) {\n  const need = new Map();\n  for (const c of t) need.set(c, (need.get(c) || 0) + 1);\n  let matched = 0, left = 0, minLen = Infinity, start = 0;\n  const window = new Map();\n\n  for (let right = 0; right < s.length; right++) {\n    const c = s[right];\n    window.set(c, (window.get(c) || 0) + 1);\n    if (need.has(c) && window.get(c) === need.get(c)) matched++;\n\n    while (matched === need.size) {\n      if (right - left + 1 < minLen) {\n        minLen = right - left + 1;\n        start = left;\n      }\n      const leftC = s[left];\n      window.set(leftC, window.get(leftC) - 1);\n      if (need.has(leftC) && window.get(leftC) < need.get(leftC)) matched--;\n      left++;\n    }\n  }\n  return minLen === Infinity ? '' : s.substr(start, minLen);\n}`, highlight: [8, 9, 10, 11, 13, 14, 15, 20, 21], diagram: { kind: 'flow', steps: ['Expand right', 'Chars matched?', 'Shrink left', 'Update min', 'Continue'] } },
      { title: 'Sliding Window Maximum', description: 'Use a monotonic deque to find max of every k-sized window in O(n).', code: `// LC 239: Sliding Window Maximum\nfunction maxSlidingWindow(nums, k) {\n  const deque = []; // stores indices, values decreasing\n  const result = [];\n\n  for (let i = 0; i < nums.length; i++) {\n    // Remove indices out of window\n    while (deque.length && deque[0] < i - k + 1) deque.shift();\n    // Remove smaller values from back\n    while (deque.length && nums[deque[deque.length-1]] < nums[i]) deque.pop();\n    deque.push(i);\n    if (i >= k - 1) result.push(nums[deque[0]]);\n  }\n  return result;\n}`, highlight: [3, 7, 8, 9, 10, 11], diagram: { kind: 'flow', steps: ['Deque of indices', 'Remove out-of-window', 'Pop smaller from back', 'Push current', 'Front = window max'] } },
      { title: 'When to Use Sliding Window', description: 'Signals: subarray/substring, longest/shortest, k consecutive, all/any characters.', code: `// Fixed size: max sum of k, avg of k, negatives count\n// Variable size:\n//   - Longest substring with X property\n//   - Smallest substring containing Y\n//   - Subarray sum = target\n\n// Template:\nlet left = 0;\nfor (let right = 0; right < arr.length; right++) {\n  // 1. Expand: add arr[right] to window state\n  window.push(arr[right]);\n\n  // 2. Shrink: while constraint violated\n  while (constraintViolated()) {\n    window.remove(arr[left]);\n    left++;\n  }\n\n  // 3. Update answer\n  answer = Math.max(answer, right - left + 1);\n}`, highlight: [8, 9, 10, 12, 13, 14, 15, 19], diagram: { kind: 'flow', steps: ['Expand right', 'Update state', 'Shrink if invalid', 'Update answer', 'Repeat'] } }
    ]
  },
  {
    path: 'leetcode-patterns', slug: 'fast-slow-pointers',
    steps: [
      { title: 'Cycle Detection (Floyd)', description: 'Slow moves 1 step, fast moves 2 steps — if they meet, there\'s a cycle.', code: `// LC 141: Linked List Cycle\nfunction hasCycle(head) {\n  let slow = head, fast = head;\n  while (fast && fast.next) {\n    slow = slow.next;\n    fast = fast.next.next;\n    if (slow === fast) return true;\n  }\n  return false;\n}`, highlight: [3, 4, 5, 6, 7], diagram: { kind: 'flow', steps: ['slow +1', 'fast +2', 'gap closes by 1 each step', 'Meet inside cycle', 'Or fast → null (no cycle)'] } },
      { title: 'Find Cycle Start', description: 'After meeting, reset slow to head — meeting point is cycle entry.', code: `// LC 142: Cycle Start\nfunction detectCycle(head) {\n  let slow = head, fast = head;\n  // Phase 1: find meeting point\n  while (fast && fast.next) {\n    slow = slow.next;\n    fast = fast.next.next;\n    if (slow === fast) {\n      // Phase 2: find cycle start\n      slow = head;\n      while (slow !== fast) {\n        slow = slow.next;\n        fast = fast.next;\n      }\n      return slow; // = cycle start\n    }\n  }\n  return null;\n}`, highlight: [4, 5, 6, 8, 9, 10, 11, 12, 13, 14], diagram: { kind: 'flow', steps: ['Phase 1: meet in cycle', 'Reset slow → head', 'Both move at same speed', 'Meet at cycle entry', 'Math: 2(a+b) = a+b+kc → a = kc-b'] } },
      { title: 'Middle of Linked List', description: 'When fast reaches end, slow is at the middle.', code: `// LC 876: Middle of Linked List\nfunction middleNode(head) {\n  let slow = head, fast = head;\n  while (fast && fast.next) {\n    slow = slow.next;\n    fast = fast.next.next;\n  }\n  return slow; // middle (or second middle for even length)\n}\n\n// For odd:  [1,2,3,4,5] → 3\n// For even: [1,2,3,4] → 3 (second middle)`, highlight: [3, 4, 5, 6, 7], diagram: { kind: 'flow', steps: ['slow +1, fast +2', 'fast reaches end', 'slow at middle', 'Odd n: exact middle', 'Even n: second of two middles'] } },
      { title: 'Happy Number', description: 'Detect cycles in a number sequence — squared digit sums.', code: `// LC 202: Happy Number\nfunction isHappy(n) {\n  const squareSum = (num) => {\n    let sum = 0;\n    while (num > 0) {\n      const d = num % 10;\n      sum += d * d;\n      num = Math.floor(num / 10);\n    }\n    return sum;\n  };\n\n  let slow = n, fast = squareSum(n);\n  while (fast !== 1 && slow !== fast) {\n    slow = squareSum(slow);\n    fast = squareSum(squareSum(fast));\n  }\n  return fast === 1;\n}`, highlight: [13, 14, 15, 16, 18], diagram: { kind: 'flow', steps: ['Compute sum of squares', 'slow +1, fast +2', 'Meet? cycle (unhappy)', '1? happy', 'Cycle detection variant'] } },
      { title: 'When to Use Fast/Slow', description: 'Signals: linked list, cycle detection, finding middle, no extra space allowed.', code: `// Common problems:\n// LC 141. Linked List Cycle\n// LC 142. Linked List Cycle II\n// LC 202. Happy Number\n// LC 876. Middle of Linked List\n// LC 234. Palindrome Linked List (find middle + reverse)\n// LC 143. Reorder List\n// LC 287. Find Duplicate Number (treat array as linked list)\n\n// Pattern: O(1) space cycle/middle detection\n// Alternative: HashSet (O(n) space)`, highlight: [2, 3, 4, 5, 6, 7, 10, 11], diagram: { kind: 'boxes', boxes: [{ id: 'c', label: 'Cycle detection', color: '#1e88e5' }, { id: 'm', label: 'Middle node', color: '#43a047' }, { id: 'p', label: 'Palindrome', color: '#fb8c00' }, { id: 'd', label: 'Duplicate #', color: '#8e24aa' }], connections: [] } }
    ]
  },
  {
    path: 'leetcode-patterns', slug: 'merge-intervals-pattern',
    steps: [
      { title: 'Sort and Merge', description: 'Sort intervals by start, then merge overlapping ones — the foundation of the pattern.', code: `// LC 56: Merge Intervals\nfunction merge(intervals) {\n  intervals.sort((a, b) => a[0] - b[0]);\n  const result = [intervals[0]];\n  for (let i = 1; i < intervals.length; i++) {\n    const last = result[result.length - 1];\n    const curr = intervals[i];\n    if (curr[0] <= last[1]) {\n      // Overlap: extend end\n      last[1] = Math.max(last[1], curr[1]);\n    } else {\n      result.push(curr);\n    }\n  }\n  return result;\n}`, highlight: [3, 8, 9, 10, 11, 12], diagram: { kind: 'flow', steps: ['Sort by start', 'Compare with last merged', 'Overlap → extend end', 'No overlap → add new', 'O(n log n)'] } },
      { title: 'Insert Interval', description: 'Insert new interval into sorted list, merging as needed.', code: `// LC 57: Insert Interval\nfunction insert(intervals, newInterval) {\n  const result = [];\n  let i = 0;\n  // 1. Add non-overlapping before\n  while (i < intervals.length && intervals[i][1] < newInterval[0]) {\n    result.push(intervals[i++]);\n  }\n  // 2. Merge overlapping\n  while (i < intervals.length && intervals[i][0] <= newInterval[1]) {\n    newInterval[0] = Math.min(newInterval[0], intervals[i][0]);\n    newInterval[1] = Math.max(newInterval[1], intervals[i][1]);\n    i++;\n  }\n  result.push(newInterval);\n  // 3. Add non-overlapping after\n  while (i < intervals.length) result.push(intervals[i++]);\n  return result;\n}`, highlight: [6, 7, 10, 11, 12, 13, 15, 17], diagram: { kind: 'flow', steps: ['Before newInterval', 'Merge overlapping', 'Add merged', 'After newInterval', 'Result'] } },
      { title: 'Meeting Rooms', description: 'Count minimum meeting rooms needed — priority queue for active meetings.', code: `// LC 253: Meeting Rooms II\nfunction minMeetingRooms(intervals) {\n  intervals.sort((a, b) => a[0] - b[0]);\n  // Min-heap of end times\n  const heap = new MinHeap();\n  for (const [start, end] of intervals) {\n    if (heap.size() && heap.peek() <= start) {\n      heap.pop(); // meeting ended, room free\n    }\n    heap.push(end);\n  }\n  return heap.size();\n}\n\n// Alternative: event sweep\n// events: [(start, +1), (end, -1)]\n// sort, track max concurrent`, highlight: [3, 5, 7, 8, 9, 10, 12], diagram: { kind: 'flow', steps: ['Sort by start', 'Min-heap of ends', 'Start after earliest end?', 'Reuse room', 'Otherwise new room', 'Total rooms'] } },
      { title: 'Non-overlapping Intervals', description: 'Greedy: sort by end time, keep intervals with earliest end.', code: `// LC 435: Non-overlapping Intervals (min removals)\nfunction eraseOverlapIntervals(intervals) {\n  intervals.sort((a, b) => a[1] - b[1]); // sort by end!\n  let end = -Infinity, count = 0;\n  for (const [s, e] of intervals) {\n    if (s >= end) {\n      end = e; // keep this interval\n    } else {\n      count++; // must remove\n    }\n  }\n  return count;\n}\n\n// Greedy insight: earliest end = maximum room for future intervals`, highlight: [3, 4, 5, 6, 7, 8, 9], diagram: { kind: 'flow', steps: ['Sort by END time', 'Track last kept end', 'Non-overlapping? keep', 'Overlapping? remove', 'Min removals'] } },
      { title: 'Interval Intersection', description: 'Find intersection of two sorted interval lists — two pointers.', code: `// LC 986: Interval List Intersections\nfunction intervalIntersection(A, B) {\n  const result = [];\n  let i = 0, j = 0;\n  while (i < A.length && j < B.length) {\n    const start = Math.max(A[i][0], B[j][0]);\n    const end = Math.min(A[i][1], B[j][1]);\n    if (start <= end) result.push([start, end]);\n    // Advance the one that ends first\n    if (A[i][1] < B[j][1]) i++;\n    else j++;\n  }\n  return result;\n}`, highlight: [5, 6, 7, 8, 9, 10, 11], diagram: { kind: 'flow', steps: ['Two sorted lists', 'Compute overlap', 'max(starts), min(ends)', 'Valid? add', 'Advance smaller end'] } }
    ]
  },
  {
    path: 'leetcode-patterns', slug: 'cyclic-sort-pattern',
    steps: [
      { title: 'Cyclic Sort Basics', description: 'When array contains numbers 1..n, place each at its correct index in O(n).', code: `// Sort array containing 1 to n\nfunction cyclicSort(nums) {\n  let i = 0;\n  while (i < nums.length) {\n    const correctIdx = nums[i] - 1;\n    if (nums[i] !== nums[correctIdx]) {\n      // Swap to correct position\n      [nums[i], nums[correctIdx]] = [nums[correctIdx], nums[i]];\n    } else {\n      i++;\n    }\n  }\n  return nums;\n}`, highlight: [5, 6, 7, 8, 9, 10], diagram: { kind: 'flow', steps: ['nums[i] should be at i+1', 'Wrong? swap to correct pos', 'Correct? move i++', 'Each element swapped ≤ 1 time', 'O(n) total'] } },
      { title: 'Find Missing Number', description: 'Cyclic sort makes finding missing numbers trivial — find index where nums[i] ≠ i+1.', code: `// LC 268: Missing Number (0 to n)\nfunction missingNumber(nums) {\n  let i = 0;\n  while (i < nums.length) {\n    if (nums[i] < nums.length && nums[i] !== nums[nums[i]]) {\n      [nums[i], nums[nums[i]]] = [nums[nums[i]], nums[i]];\n    } else {\n      i++;\n    }\n  }\n  for (let j = 0; j < nums.length; j++) {\n    if (nums[j] !== j) return j; // first mismatch\n  }\n  return nums.length; // last number missing\n}`, highlight: [5, 6, 11, 12, 13, 14], diagram: { kind: 'flow', steps: ['Cyclic sort in place', 'Scan for nums[i] ≠ i', 'That i is missing', 'Or n at end', 'O(n) time, O(1) space'] } },
      { title: 'Find Duplicate', description: 'When 1..n has one duplicate, cyclic sort exposes it directly.', code: `// LC 287: Find Duplicate Number\nfunction findDuplicate(nums) {\n  let i = 0;\n  while (i < nums.length) {\n    if (nums[i] !== i + 1) {\n      const target = nums[i] - 1;\n      if (nums[i] !== nums[target]) {\n        [nums[i], nums[target]] = [nums[target], nums[i]];\n      } else {\n        return nums[i]; // duplicate found\n      }\n    } else {\n      i++;\n    }\n  }\n  return -1;\n}`, highlight: [5, 6, 7, 8, 9, 10], diagram: { kind: 'flow', steps: ['Cyclic sort', 'Duplicate collision', 'nums[i] = nums[target]', 'Cannot swap → duplicate', 'Return it'] } },
      { title: 'Find All Missing/Duplicates', description: 'Multiple missing or duplicate numbers in 1..n — cyclic sort then scan.', code: `// LC 448: Find All Missing Numbers\nfunction findMissing(nums) {\n  let i = 0;\n  while (i < nums.length) {\n    const j = nums[i] - 1;\n    if (nums[i] !== nums[j]) {\n      [nums[i], nums[j]] = [nums[j], nums[i]];\n    } else {\n      i++;\n    }\n  }\n  const missing = [];\n  for (let k = 0; k < nums.length; k++) {\n    if (nums[k] !== k + 1) missing.push(k + 1);\n  }\n  return missing;\n}`, highlight: [3, 4, 5, 6, 7, 12, 13, 14], diagram: { kind: 'flow', steps: ['Cyclic sort in place', 'Scan array', 'nums[i] ≠ i+1', 'i+1 is missing', 'Collect all'] } },
      { title: 'When to Use Cyclic Sort', description: 'Signals: numbers in range 1..n or 0..n-1, find missing/duplicate, O(1) space required.', code: `// Common problems:\n// LC 268: Missing Number\n// LC 287: Find Duplicate\n// LC 41:  First Missing Positive\n// LC 442: Find All Duplicates\n// LC 448: Find All Missing Numbers\n// LC 645: Set Mismatch\n\n// Key insight:\n// Range 1..n → each number has a UNIQUE correct index (num - 1)\n// Placing them in correct index reveals missing/duplicate\n// O(n) time, O(1) space (in-place)`, highlight: [2, 3, 4, 5, 6, 10, 11, 12], diagram: { kind: 'boxes', boxes: [{ id: 'r', label: 'Range 1..n', color: '#1e88e5' }, { id: 'i', label: 'In-place O(1)', color: '#43a047' }, { id: 'm', label: 'Missing/Duplicate', color: '#fb8c00' }], connections: [] } }
    ]
  }
];

