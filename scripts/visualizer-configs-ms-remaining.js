/* Microservices remaining 17 topics */
module.exports = { 'microservices': {

'ms-monolith-vs': {
  language:'java', fileName:'MonolithVsMs.java',
  steps:[
    { title:'Monolith — all-in-one', description:'A monolith packages the entire application into one deployable unit. Simple to develop, test, and deploy initially. Becomes a bottleneck at scale.',
      code:`// Monolith: single Spring Boot app\n// └── UserController, OrderController, PaymentController\n// └── UserService, OrderService, PaymentService\n// └── Single Postgres DB\n// Deploy: one fat jar`,
      diagram:{ kind:'boxes', title:'Monolith structure', items:[
        {label:'All code in one JAR', color:'#818cf8'},
        {label:'One DB schema', color:'#818cf8'},
        {label:'Deploy: one command', color:'#10b981', highlight:true},
        {label:'Scale: replicate entire app', color:'#f59e0b'},
      ]}},
    { title:'Monolith pain points at scale', description:'Deployment coupling: every change deploys everything. Scaling: can\'t scale one component. Team collision: merges and build conflicts.',
      code:`// Pain points:\n// - Ship new feature → redeploy ENTIRE app (risk!)\n// - Payment needs more CPU → must scale everything\n// - 50 devs all editing same codebase → conflicts`,
      diagram:{ kind:'flow', steps:[
        {label:'1 team → monolith works well', done:true},
        {label:'5+ teams → deployment friction', done:true},
        {label:'hotspot needs scaling → must scale all', active:true},
        {label:'tight coupling → slow releases'},
      ]}},
    { title:'Microservices — decomposed', description:'Each business capability becomes an independent service. Own codebase, own DB, own deployment pipeline. Teams can ship independently.',
      code:`// Microservices:\n// order-service: OrderController + OrderRepo + orders_db\n// user-service:  UserController  + UserRepo  + users_db\n// payment-service: PaymentController + payments_db\n// Each deployed independently, scaled independently`,
      diagram:{ kind:'boxes', title:'Microservices structure', items:[
        {label:'order-service (own deploy)', color:'#10b981'},
        {label:'user-service (own DB)', color:'#10b981'},
        {label:'payment-service (scales independently)', color:'#10b981', highlight:true},
        {label:'each team owns one service', color:'#818cf8'},
      ]}},
    { title:'Strangler Fig pattern — migration', description:'Migrate a monolith to microservices incrementally. Route traffic for one capability to a new service; keep the rest in the monolith.',
      code:`// 1. Identify a bounded context (e.g. payments)\n// 2. Build new payment-service\n// 3. API gateway routes /api/payments → new service\n// 4. Monolith still handles everything else\n// 5. Repeat for next bounded context`,
      diagram:{ kind:'flow', steps:[
        {label:'monolith handles everything', done:true},
        {label:'extract payment-service → route /payments there', done:true},
        {label:'extract user-service → route /users there', active:true},
        {label:'strangler fig: monolith shrinks over time'},
      ]}},
    { title:'When NOT to use microservices', description:'Don\'t start with microservices. Build a modular monolith first. Decompose when teams/deployments become bottlenecks.',
      code:`// Start: well-structured monolith (modular)\n// Split when:\n// - Different scaling requirements confirmed\n// - Teams blocked by deployment coupling\n// - Clear bounded contexts identified\n// NOT just because "microservices are trendy"`,
      diagram:{ kind:'boxes', title:'Decision guide', items:[
        {label:'< 5 developers: monolith', color:'#10b981'},
        {label:'10+ developers: consider modules', color:'#818cf8'},
        {label:'multiple teams: microservices viable', color:'#10b981', highlight:true},
      ]}},
  ],
},

'ms-pros-cons': {
  language:'java', fileName:'ProsCons.java',
  steps:[
    { title:'Pros: independent scaling', description:'Each service can be scaled independently based on its resource needs. Payment processing needs more CPU? Scale only payment-service.',
      code:`# Scale payment-service to 10 replicas (high traffic)\nkubectl scale deployment payment-service --replicas=10\n# user-service stays at 2 replicas (low traffic)\nkubectl scale deployment user-service --replicas=2`,
      diagram:{ kind:'boxes', title:'Independent scaling', items:[
        {label:'payment-service: 10 pods', color:'#10b981', highlight:true},
        {label:'user-service: 2 pods', color:'#818cf8'},
        {label:'total resource usage optimized', color:'#10b981'},
      ]}},
    { title:'Pros: technology diversity', description:'Each service can use the best technology for its use case. Analytics: Python. Recommendation: Rust. API: Java Spring Boot.',
      code:`// order-service:       Java Spring Boot + Postgres\n// recommendation-svc:  Python + Redis + ML model\n// analytics-svc:       Scala Spark + Parquet\n// notification-svc:    Go + SMTP\n// Each team chooses their best tool`,
      diagram:{ kind:'boxes', title:'Polyglot architecture', items:[
        {label:'Java: order + user services', color:'#818cf8'},
        {label:'Python: ML recommendation', color:'#818cf8'},
        {label:'Go: low-latency notification', color:'#10b981', highlight:true},
      ]}},
    { title:'Cons: distributed systems complexity', description:'Network calls fail. Services go down. Data is eventually consistent. Distributed transactions are hard. Debugging requires distributed tracing.',
      code:`// Problems you don\'t have in a monolith:\n// - Network timeouts: service B is slow\n// - Partial failures: order saved, payment failed\n// - Eventual consistency: user sees stale data\n// - 5 services to debug one bug`,
      diagram:{ kind:'boxes', title:'Complexity costs', items:[
        {label:'network timeouts', color:'#ef4444'},
        {label:'partial failures', color:'#ef4444'},
        {label:'eventual consistency', color:'#ef4444', highlight:true},
        {label:'distributed tracing required', color:'#f59e0b'},
      ]}},
    { title:'Cons: operational overhead', description:'Each service needs its own CI/CD pipeline, Dockerfile, health checks, logging, monitoring. Small teams often can\'t afford this overhead.',
      code:`// Per-service operational needs:\n// - Dockerfile + k8s manifests\n// - CI/CD pipeline\n// - Health checks + readiness probes\n// - Distributed tracing setup\n// - Log aggregation config\n// 5 services = 5× the operational work`,
      diagram:{ kind:'boxes', title:'Operational cost per service', items:[
        {label:'CI/CD pipeline', color:'#f59e0b'},
        {label:'monitoring + alerting', color:'#f59e0b'},
        {label:'k8s deployment manifests', color:'#f59e0b', highlight:true},
        {label:'× N services = N× cost', color:'#ef4444'},
      ]}},
    { title:'Net assessment', description:'Microservices pay off when: multiple teams > independent deployments > different scaling needs confirmed. Default: start with a modular monolith.',
      code:`// Use microservices when you have:\n// 1. Multiple independent teams\n// 2. Proven different scaling requirements\n// 3. Clear bounded contexts\n// 4. Operational maturity (DevOps, k8s)\n// Otherwise: well-structured monolith is faster to ship`,
      diagram:{ kind:'boxes', title:'Decision matrix', items:[
        {label:'multiple teams confirmed → consider', color:'#10b981'},
        {label:'scaling bottleneck confirmed → consider', color:'#10b981'},
        {label:'greenfield, small team → monolith', color:'#818cf8', highlight:true},
        {label:'uncertainty → start monolith, extract later', color:'#10b981'},
      ]}},
  ],
},

'ms-bounded-contexts': {
  language:'java', fileName:'BoundedContext.java',
  steps:[
    { title:'What is a bounded context?', description:'A bounded context is a logical boundary within which a particular domain model is consistent. "Order" means different things to billing vs. fulfillment — they are different models.',
      code:`// "Order" in billing context:\nclass Order { Long id; BigDecimal total; PaymentStatus status; }\n// "Order" in fulfillment context:\nclass Order { Long id; List<Item> items; WarehouseLocation origin; ShipmentStatus status; }`,
      diagram:{ kind:'boxes', title:'Bounded contexts', items:[
        {label:'billing context: Order has price/payment', color:'#818cf8'},
        {label:'fulfillment context: Order has items/shipment', color:'#10b981', highlight:true},
        {label:'same word, different models — that is OK', color:'#818cf8'},
      ]}},
    { title:'Context map — relationships', description:'How bounded contexts relate: partnership (teams collaborate), customer/supplier (upstream/downstream), conformist (downstream adopts upstream model).',
      code:`// Context map:\n// billing  ← (upstream/downstream) → fulfillment\n// billing publishes InvoiceCreated event\n// fulfillment translates to its own ShipmentApproved`,
      diagram:{ kind:'sequence', actors:['billing','translation layer','fulfillment'], messages:[
        {from:'billing', to:'translation layer', label:'InvoiceCreated event (billing model)'},
        {from:'translation layer', to:'fulfillment', label:'ShipmentApproved (fulfillment model)'},
      ]}},
    { title:'Anti-corruption layer', description:'When integrating with a legacy or external system, an Anti-Corruption Layer (ACL) translates between their model and yours.',
      code:`// External payment provider uses "charge" terminology\n// Our domain uses "payment" terminology\nclass PaymentAcl {\n    Payment fromCharge(ChargeResponse charge) {\n        return new Payment(charge.getChargeId(), charge.getAmount(), charge.getStatus());\n    }\n}`,
      highlight:[3,4,5,6],
      diagram:{ kind:'flow', steps:[
        {label:'external: ChargeResponse model', done:true},
        {label:'ACL: translate to our domain model', done:true},
        {label:'our service: works with Payment model', active:true},
        {label:'domain model stays clean, ACL absorbs friction'},
      ]}},
    { title:'Ubiquitous language per context', description:'Within each bounded context, all code, tests, and documentation use the same language as the domain experts. "Order" in billing = always billing\'s definition.',
      code:`// In billing context:\nbillingOrderRepo.findById(id);   // "billing order"\nbillingOrderService.invoice(order); // billing language\n// Never mix: billingOrderService.createFulfillmentShipment()\n// That belongs in the fulfillment context`,
      diagram:{ kind:'boxes', title:'Ubiquitous language', items:[
        {label:'billing: invoice, charge, payment', color:'#818cf8'},
        {label:'fulfillment: shipment, pick, pack, dispatch', color:'#10b981', highlight:true},
        {label:'each context has its own vocabulary', color:'#818cf8'},
      ]}},
    { title:'Bounded context = microservice boundary', description:'Bounded contexts often map 1:1 to microservices. They define natural deployment boundaries with minimal cross-context coupling.',
      code:`// billing BC → billing-service (microservice)\n// fulfillment BC → fulfillment-service\n// inventory BC → inventory-service\n// Each service: owns its model, its DB, its language`,
      diagram:{ kind:'boxes', title:'BC to microservice', items:[
        {label:'billing BC → billing-service', color:'#10b981'},
        {label:'fulfillment BC → fulfillment-service', color:'#10b981'},
        {label:'inventory BC → inventory-service', color:'#10b981', highlight:true},
        {label:'natural alignment of team + code + deploy', color:'#818cf8'},
      ]}},
  ],
},

'ms-inter-service-comm': {
  language:'java', fileName:'InterServiceComm.java',
  steps:[
    { title:'Synchronous: REST + Feign', description:'Service A calls Service B via HTTP and waits for the response. Simple, but creates coupling — if B is slow/down, A is affected.',
      code:`@FeignClient("user-service")\ninterface UserClient {\n    @GetMapping("/users/{id}")\n    UserDTO getUser(@PathVariable Long id);\n}\n// Spring Cloud OpenFeign: auto-implements HTTP client\n// Usage:\nUserDTO user = userClient.getUser(42);  // blocks until response`,
      highlight:[1,7,8],
      diagram:{ kind:'sequence', actors:['order-service','user-service'], messages:[
        {from:'order-service', to:'user-service', label:'GET /users/42'},
        {from:'user-service', to:'order-service', label:'200 { "id":42, "name":"Alice" }'},
      ]}},
    { title:'Synchronous: gRPC for internal services', description:'gRPC uses HTTP/2 + Protobuf. 3-10× faster than REST. Strong typing via .proto schema. Excellent for internal microservice calls.',
      code:`// user.proto\nservice UserService { rpc GetUser(UserRequest) returns (UserResponse); }\nmessage UserRequest { int64 id = 1; }\nmessage UserResponse { int64 id=1; string name=2; string email=3; }`,
      diagram:{ kind:'boxes', title:'gRPC vs REST', items:[
        {label:'REST: HTTP/1.1 + JSON, human-readable', color:'#818cf8'},
        {label:'gRPC: HTTP/2 + Protobuf, 3-10× faster', color:'#10b981', highlight:true},
        {label:'gRPC: streaming out of the box', color:'#10b981'},
      ]}},
    { title:'Asynchronous: Kafka events', description:'Service A publishes an event; Service B consumes it at its own pace. Decoupled, resilient. B can be down; events wait.',
      code:`// Producer\nkafkaTemplate.send("order-events", new OrderPlacedEvent(orderId));\n// Consumer (separate service)\n@KafkaListener(topics="order-events")\nvoid onOrderPlaced(OrderPlacedEvent e) { fulfillOrder(e.orderId()); }`,
      highlight:[2,5],
      diagram:{ kind:'sequence', actors:['order-service','Kafka','fulfillment-service'], messages:[
        {from:'order-service', to:'Kafka', label:'publish OrderPlaced'},
        {from:'Kafka', to:'fulfillment-service', label:'deliver OrderPlaced (when ready)'},
      ]}},
    { title:'Service mesh: Istio / Linkerd', description:'A service mesh handles cross-cutting communication: mTLS (encryption), retries, circuit breaking, observability — without code changes.',
      code:`# Istio sidecar (Envoy proxy) injected automatically\n# Handles:\n# - mTLS between all services\n# - Automatic retries\n# - Circuit breaking\n# - Request tracing\n# Your code sees: plain HTTP; Istio handles the rest`,
      diagram:{ kind:'boxes', title:'Service mesh capabilities', items:[
        {label:'mTLS: automatic encryption', color:'#10b981', highlight:true},
        {label:'retries + circuit breaking: no code change', color:'#10b981'},
        {label:'traffic management: canary, A/B', color:'#818cf8'},
        {label:'observability: traces, metrics auto', color:'#818cf8'},
      ]}},
    { title:'Choosing sync vs async', description:'Sync: when you need the result immediately (user lookup during order validation). Async: when the result isn\'t needed now (send email after order placed).',
      code:`// Sync: need result now\nUserDTO user = userClient.getUser(id);  // validate user exists\nif (!user.isActive()) throw new ForbiddenException();\n// Async: don\'t need result now\nkafkaTemplate.send("order-placed", event);  // notify 5 services`,
      diagram:{ kind:'boxes', title:'Sync vs Async choice', items:[
        {label:'need result immediately → sync (REST/gRPC)', color:'#818cf8'},
        {label:'fire and forget → async (Kafka)', color:'#10b981', highlight:true},
        {label:'default to async where possible', color:'#10b981'},
      ]}},
  ],
},

'ms-config-server': {
  language:'yaml', fileName:'application.yml',
  steps:[
    { title:'Centralized configuration', description:'Spring Cloud Config Server serves configuration from a Git repo. All microservices pull their config from one place.',
      code:`# Config server application.yml\nspring:\n  cloud:\n    config:\n      server:\n        git:\n          uri: https://github.com/myorg/config-repo\n          default-label: main`,
      highlight:[3,4,5,6,7,8],
      diagram:{ kind:'sequence', actors:['order-service','Config Server','Git'], messages:[
        {from:'Config Server', to:'Git', label:'pull config files on startup'},
        {from:'order-service', to:'Config Server', label:'GET /order-service/prod'},
        {from:'Config Server', to:'order-service', label:'merged config YAML'},
      ]}},
    { title:'Config repo structure', description:'Config repo: application.yml (global), {service-name}.yml (per-service), {service-name}-{profile}.yml (per-service+profile).',
      code:`# config-repo (Git):\n# ├── application.yml          # all services\n# ├── order-service.yml        # order-service only\n# └── order-service-prod.yml  # order-service in prod\n# Merge order: global → service → service+profile (highest wins)`,
      diagram:{ kind:'flow', steps:[
        {label:'application.yml loaded (base)', done:true},
        {label:'order-service.yml overrides merge', done:true},
        {label:'order-service-prod.yml highest priority', active:true},
      ]}},
    { title:'Bootstrap configuration', description:'Services bootstrap from a minimal bootstrap.yml that points to the config server. Full config loaded before Spring context starts.',
      code:`# bootstrap.yml (loaded FIRST)\nspring:\n  application:\n    name: order-service\n  config:\n    import: configserver:http://config-server:8888\n  cloud:\n    config:\n      fail-fast: true\n      retry:\n        max-attempts: 5`,
      highlight:[2,5,6,9,10,11],
      diagram:{ kind:'boxes', title:'Bootstrap order', items:[
        {label:'bootstrap.yml loaded first', color:'#10b981', highlight:true},
        {label:'config server URL resolved', color:'#818cf8'},
        {label:'full config pulled', color:'#818cf8'},
        {label:'Spring context starts with full config', color:'#10b981'},
      ]}},
    { title:'@RefreshScope — live config updates', description:'Annotate beans with @RefreshScope. POST /actuator/refresh triggers re-injection of config values without restart.',
      code:`@Component @RefreshScope\npublic class FeatureFlags {\n    @Value("\${feature.new-ui-enabled:false}")\n    private boolean newUiEnabled;\n}\n// Update config-repo → POST /actuator/refresh\n// or: Spring Cloud Bus → broadcast refresh to all instances`,
      highlight:[1,6,7],
      diagram:{ kind:'flow', steps:[
        {label:'change config in Git', done:true},
        {label:'POST /actuator/refresh to service', done:true},
        {label:'@RefreshScope beans re-created', active:true},
        {label:'no restart needed'},
      ]}},
    { title:'Vault integration — secrets', description:'For sensitive values (passwords, API keys), use HashiCorp Vault as a backend instead of Git.',
      code:`spring:\n  cloud:\n    vault:\n      host: vault.svc\n      port: 8200\n      scheme: https\n      authentication: KUBERNETES\n# Secret path: secret/data/order-service\n# Vault auto-renews leases — no credential rotation downtime`,
      language:'yaml', highlight:[2,3,4,5,6,7],
      diagram:{ kind:'boxes', title:'Config backends', items:[
        {label:'Git: non-sensitive config', color:'#818cf8'},
        {label:'Vault: passwords, tokens, certs', color:'#ef4444', highlight:true},
        {label:'both served via Config Server API', color:'#10b981'},
      ]}},
  ],
},

'ms-outbox-pattern': {
  language:'java', fileName:'Outbox.java',
  steps:[
    { title:'The dual-write problem', description:'Saving to DB and publishing to Kafka in the same operation can fail partially — DB saved but Kafka not published (or vice versa). The outbox pattern solves this.',
      code:`// BROKEN: dual-write\norderRepo.save(order);             // can succeed\nkafkaTemplate.send("events", event); // can fail → event lost!\n\n// OR: Kafka succeeds, DB fails → event without order in DB!`,
      diagram:{ kind:'flow', steps:[
        {label:'save order to DB ✓', done:true},
        {label:'publish to Kafka ✗ (network down)', done:true},
        {label:'order saved but event LOST', active:true},
        {label:'downstream services never notified → inconsistency'},
      ]}},
    { title:'Outbox table', description:'Write the event to an outbox table IN THE SAME TRANSACTION as the business data. Atomicity guaranteed by the DB transaction.',
      code:`// Same @Transactional:\ntransactionTemplate.execute(status -> {\n    orderRepo.save(order);\n    outboxRepo.save(new OutboxEvent(\n        "order-events",\n        objectMapper.writeValueAsString(new OrderPlacedEvent(order))\n    ));\n    return null;  // BOTH committed or BOTH rolled back\n});`,
      highlight:[2,3,4,5,6,7,8],
      diagram:{ kind:'boxes', title:'Outbox atomicity', items:[
        {label:'save order', color:'#818cf8'},
        {label:'save outbox record', color:'#818cf8'},
        {label:'BOTH in one transaction → atomic', color:'#10b981', highlight:true},
        {label:'Kafka publish is async/separate', color:'#818cf8'},
      ]}},
    { title:'Relay process', description:'A relay (poller or CDC listener) reads the outbox table and publishes to Kafka. Marks records as published after success.',
      code:`// Polling relay (simple)\n@Scheduled(fixedDelay = 1000)\nvoid relay() {\n    List<OutboxEvent> pending = outboxRepo.findByPublished(false);\n    for (OutboxEvent e : pending) {\n        kafkaTemplate.send(e.getTopic(), e.getPayload()).get();\n        outboxRepo.markPublished(e.getId());\n    }\n}`,
      highlight:[3,5,6,7],
      diagram:{ kind:'sequence', actors:['relay','outbox table','Kafka'], messages:[
        {from:'relay', to:'outbox table', label:'SELECT WHERE published=false'},
        {from:'relay', to:'Kafka', label:'send event payload'},
        {from:'relay', to:'outbox table', label:'UPDATE published=true'},
      ]}},
    { title:'Transactional outbox vs Debezium CDC', description:'Simple poller has at-least-once delivery. Debezium reads Postgres WAL (change data capture) — zero-latency, no polling, exact delivery.',
      code:`# Debezium connector config:\ndebezium.source.connector.class=PostgresConnector\ndebezium.source.database.hostname=postgres\ndebezium.source.table.include.list=public.outbox_events\n# Debezium reads WAL → publishes to Kafka\n# Much lower latency than polling`,
      diagram:{ kind:'boxes', title:'Relay approaches', items:[
        {label:'polling relay: simple, ~1s delay', color:'#818cf8'},
        {label:'Debezium CDC: real-time, reads WAL', color:'#10b981', highlight:true},
        {label:'both guarantee at-least-once delivery', color:'#818cf8'},
      ]}},
    { title:'Idempotent consumers', description:'The outbox guarantees at-least-once. Consumers MUST be idempotent — processing the same event twice has no additional effect.',
      code:`@KafkaListener(topics="order-events")\nvoid onOrderPlaced(OrderPlacedEvent e) {\n    // idempotency check: was this already processed?\n    if (processedEventRepo.exists(e.getEventId())) return;\n    fulfillOrder(e.getOrderId());\n    processedEventRepo.mark(e.getEventId());\n}`,
      highlight:[4,6],
      diagram:{ kind:'flow', steps:[
        {label:'event arrives', done:true},
        {label:'already processed? → skip', active:true},
        {label:'process + mark processed',},
        {label:'duplicate delivery has no effect'},
      ]}},
  ],
},

'ms-distributed-tx': {
  language:'java', fileName:'DistributedTx.java',
  steps:[
    { title:'Why 2PC fails in microservices', description:'Two-Phase Commit (2PC) requires all participants to hold locks until the coordinator decides. In microservices, this creates tight coupling, blocking, and can\'t tolerate network partitions.',
      code:`// 2PC fails in microservices:\n// Phase 1: Prepare — all services lock resources and vote\n// Phase 2: Commit — coordinator tells all to commit\n// Problem: if coordinator crashes, all services locked indefinitely`,
      diagram:{ kind:'boxes', title:'2PC problems', items:[
        {label:'locks held across network calls', color:'#ef4444', highlight:true},
        {label:'coordinator crash → deadlock', color:'#ef4444'},
        {label:'violates CAP: choose A or C in partition', color:'#ef4444'},
      ]}},
    { title:'Eventual consistency is the goal', description:'Accept that distributed systems can only guarantee eventual consistency. Design for it: make operations idempotent, use compensating transactions.',
      code:`// Instead of ACID:\n// - Accept: payment may show "processing" briefly\n// - Compensate: if payment fails, reverse order\n// - Communicate: "Your order is confirmed. Payment processing..."`,
      diagram:{ kind:'flow', steps:[
        {label:'order created → immediately visible', done:true},
        {label:'payment processing → async', done:true},
        {label:'payment success → order confirmed', active:true},
        {label:'payment fail → compensate: cancel order'},
      ]}},
    { title:'Saga pattern recap', description:'A saga is a sequence of local transactions, each publishing an event/message to trigger the next step. On failure, compensating transactions undo prior steps.',
      code:`// Forward path:\norderSaga.start → reserve inventory → charge payment → confirm order\n// Compensating path (on failure at step 3):\nrefund payment → release inventory → cancel order`,
      diagram:{ kind:'boxes', title:'Saga forward + compensation', items:[
        {label:'step 1: create order', color:'#818cf8'},
        {label:'step 2: reserve inventory', color:'#818cf8'},
        {label:'step 3 fail: refund → release → cancel', color:'#ef4444', highlight:true},
      ]}},
    { title:'Try-Confirm-Cancel (TCC)', description:'Each service exposes try (reserve), confirm (commit), cancel (rollback). Coordinator calls try on all, then confirm or cancel.',
      code:`// Coordinator:\ntry: inventoryService.reserve(orderId, items); // reservation\ntry: paymentService.hold(orderId, amount);     // hold funds\n// All tries succeed:\nconfirm: inventoryService.confirm(orderId);\nconfirm: paymentService.capture(orderId);\n// Any try fails: cancel all tries`,
      highlight:[2,3,5,6,7],
      diagram:{ kind:'sequence', actors:['coordinator','inventory','payment'], messages:[
        {from:'coordinator', to:'inventory', label:'try: reserve(orderId)'},
        {from:'coordinator', to:'payment', label:'try: hold(orderId)'},
        {from:'coordinator', to:'inventory', label:'confirm OR cancel'},
      ]}},
    { title:'When to accept eventual consistency', description:'Most user-facing operations can tolerate eventual consistency. Use immediate consistency only where legally or contractually required.',
      code:`// Eventual consistency acceptable:\n// - Order status update (show "processing" then "confirmed")\n// - Inventory count update (show -1, reconcile later)\n// - Notification sending\n// Need immediate consistency:\n// - Financial balance (regulatory requirement)`,
      diagram:{ kind:'boxes', title:'Consistency requirements', items:[
        {label:'eventual OK: orders, notifications', color:'#10b981'},
        {label:'strict: financial, legal compliance', color:'#ef4444', highlight:true},
        {label:'default: design for eventual consistency', color:'#10b981'},
      ]}},
  ],
},

'ms-rest-apis': {
  language:'java', fileName:'RestApis.java',
  steps:[
    { title:'REST principles in microservices', description:'Each resource has a URL. HTTP verbs map to CRUD. Stateless: no session on server. Cacheable responses where applicable.',
      code:`// Resource: /orders\nGET    /orders          // list\nGET    /orders/{id}     // get one\nPOST   /orders          // create\nPUT    /orders/{id}     // replace\nPATCH  /orders/{id}     // partial update\nDELETE /orders/{id}     // delete`,
      diagram:{ kind:'boxes', title:'REST verb mapping', items:[
        {label:'GET → Read (safe, idempotent)', color:'#10b981', highlight:true},
        {label:'POST → Create', color:'#818cf8'},
        {label:'PUT/PATCH → Update', color:'#818cf8'},
        {label:'DELETE → Delete (idempotent)', color:'#818cf8'},
      ]}},
    { title:'HATEOAS — discoverable APIs', description:'Responses include links to related actions. Clients navigate the API by following links, not hardcoding URLs.',
      code:`// HATEOAS response for order:\n{\n  "orderId": 42,\n  "status": "pending",\n  "_links": {\n    "self":    {"href":"/orders/42"},\n    "cancel":  {"href":"/orders/42/cancel","method":"POST"},\n    "payment": {"href":"/payments/order/42"}\n  }\n}`,
      diagram:{ kind:'boxes', title:'HATEOAS links', items:[
        {label:'self: current resource', color:'#818cf8'},
        {label:'cancel: available action', color:'#10b981', highlight:true},
        {label:'payment: related resource', color:'#818cf8'},
        {label:'client navigates via links, no hardcoded URLs', color:'#10b981'},
      ]}},
    { title:'Error response standards — RFC 7807', description:'Return ProblemDetail (RFC 7807) for all errors: type, title, status, detail, instance.',
      code:`{\n  "type": "https://errors.myapi.com/order-not-found",\n  "title": "Order Not Found",\n  "status": 404,\n  "detail": "Order 42 does not exist or belongs to another user",\n  "instance": "/orders/42"\n}`,
      diagram:{ kind:'boxes', title:'RFC 7807 error', items:[
        {label:'"type": URI identifying error type', color:'#818cf8'},
        {label:'"status": HTTP status code', color:'#818cf8'},
        {label:'"detail": human-readable description', color:'#10b981', highlight:true},
        {label:'"instance": request URI', color:'#818cf8'},
      ]}},
    { title:'Pagination', description:'Use cursor-based pagination for large datasets. Offset pagination (page/size) fails at scale — deep offsets are slow.',
      code:`// Cursor-based (scalable):\nGET /orders?cursor=eyJpZCI6MTAwfQ&limit=20\n// Response:\n{\n  "data": [...20 orders...],\n  "nextCursor": "eyJpZCI6MTIwfQ",  // next page cursor\n  "hasMore": true\n}`,
      diagram:{ kind:'boxes', title:'Pagination', items:[
        {label:'offset: LIMIT 20 OFFSET 10000 → slow', color:'#ef4444'},
        {label:'cursor: WHERE id > lastId LIMIT 20 → O(1)', color:'#10b981', highlight:true},
      ]}},
    { title:'Versioning strategy', description:'URI versioning (/api/v1/) is most visible and cache-friendly. Header versioning (Accept: application/vnd.api.v2+json) is cleaner. Choose one and be consistent.',
      code:`// URI versioning (most common)\nGET /api/v1/orders  // old clients\nGET /api/v2/orders  // new clients with new response format\n\n// Header versioning\nGET /api/orders\nAccept: application/vnd.myapi.v2+json`,
      diagram:{ kind:'boxes', title:'Versioning approaches', items:[
        {label:'URI: /v1/ /v2/ — visible, cache-friendly', color:'#10b981', highlight:true},
        {label:'Header: clean URLs, harder to cache', color:'#818cf8'},
        {label:'default: URI versioning', color:'#10b981'},
      ]}},
  ],
},

'ms-json-dtos': {
  language:'java', fileName:'Dtos.java',
  steps:[
    { title:'DTOs separate API from domain', description:'Never expose domain entities directly via REST. DTOs control what is serialized, add validation, and decouple API shape from DB schema.',
      code:`// Entity (internal, DB-mapped)\n@Entity class Order { Long id; User user; List<Item> items; BigDecimal total; Instant created; }\n// DTO (public API contract)\nrecord OrderResponse(Long id, String customerName, BigDecimal total, String status) {}`,
      diagram:{ kind:'boxes', title:'Entity vs DTO', items:[
        {label:'Entity: DB mapping, internal', color:'#818cf8'},
        {label:'DTO: API contract, public', color:'#10b981', highlight:true},
        {label:'never expose @Entity directly via @RestController', color:'#ef4444'},
      ]}},
    { title:'Jackson — JSON serialization', description:'Jackson converts Java objects to/from JSON. Control field names with @JsonProperty, exclude with @JsonIgnore, handle nulls with @JsonInclude.',
      code:`@JsonInclude(JsonInclude.Include.NON_NULL)  // exclude null fields\npublic record OrderResponse(\n    @JsonProperty("order_id") Long id,        // rename field\n    String status,\n    @JsonIgnore String internalNote           // exclude from JSON\n) {}`,
      highlight:[1,3,5],
      diagram:{ kind:'boxes', title:'Jackson annotations', items:[
        {label:'@JsonProperty: rename', color:'#818cf8'},
        {label:'@JsonIgnore: exclude', color:'#818cf8'},
        {label:'@JsonInclude: skip nulls', color:'#10b981', highlight:true},
        {label:'@JsonSerialize/@JsonDeserialize: custom', color:'#818cf8'},
      ]}},
    { title:'Input DTOs with validation', description:'Request DTOs should have Bean Validation annotations. Fail fast with clear error messages.',
      code:`public record CreateOrderRequest(\n    @NotNull UUID customerId,\n    @NotEmpty @Valid List<OrderItemRequest> items,\n    @NotBlank String shippingAddress,\n    @Future Instant requestedDelivery\n) {}\n// @Valid on @RequestBody → violations → 400 Bad Request`,
      highlight:[2,3,4,5,7],
      diagram:{ kind:'flow', steps:[
        {label:'@Valid triggers Bean Validation', done:true},
        {label:'constraint violation found', done:true},
        {label:'MethodArgumentNotValidException → 400', active:true},
        {label:'ControllerAdvice formats field errors → JSON'},
      ]}},
    { title:'MapStruct — no-boilerplate mapping', description:'MapStruct generates mapping code at compile time between entity and DTO. Zero runtime reflection.',
      code:`@Mapper(componentModel = "spring")\npublic interface OrderMapper {\n    OrderResponse toDto(Order order);\n    Order toEntity(CreateOrderRequest request);\n    // MapStruct generates implementation at compile time\n}`,
      highlight:[1,3,4],
      diagram:{ kind:'boxes', title:'MapStruct', items:[
        {label:'toDto(entity): generated at compile time', color:'#10b981', highlight:true},
        {label:'toEntity(dto): zero runtime reflection', color:'#10b981'},
        {label:'faster than ModelMapper or manual code', color:'#818cf8'},
      ]}},
    { title:'Versioned DTOs', description:'When the API changes, introduce a new DTO version. Keep old DTOs for backward compatibility.',
      code:`// v1 (still supported)\nrecord OrderResponseV1(Long id, String status) {}\n// v2 (new fields)\nrecord OrderResponseV2(Long id, String status, BigDecimal total, List<ItemDto> items) {}\n\n@GetMapping(value="/{id}", produces="application/vnd.api.v2+json")\nOrderResponseV2 getOrderV2(@PathVariable Long id) {}`,
      highlight:[2,4,6,7],
      diagram:{ kind:'boxes', title:'DTO versioning', items:[
        {label:'OrderResponseV1: old clients', color:'#818cf8'},
        {label:'OrderResponseV2: new clients', color:'#10b981', highlight:true},
        {label:'both served, old version deprecated over time', color:'#818cf8'},
      ]}},
  ],
},

'ms-openapi': {
  language:'java', fileName:'OpenApi.java',
  steps:[
    { title:'OpenAPI spec as contract', description:'An OpenAPI 3.x spec defines the API contract. Consumers and producers agree on the spec. Generate server stubs or client SDKs from it.',
      code:`# openapi.yml\nopenapi: "3.0.3"\ninfo:\n  title: Order Service API\n  version: "1.0.0"\npaths:\n  /orders:\n    get:\n      summary: List orders\n      responses:\n        "200":\n          content:\n            application/json:\n              schema:\n                $ref: "#/components/schemas/OrderList"`,
      language:'yaml', highlight:[1,2,6,7,8],
      diagram:{ kind:'boxes', title:'OpenAPI contract', items:[
        {label:'producers: implement the spec', color:'#818cf8'},
        {label:'consumers: generate client from spec', color:'#10b981', highlight:true},
        {label:'spec-first: design before code', color:'#10b981'},
      ]}},
    { title:'Springdoc auto-generates spec', description:'springdoc-openapi reads @RestController annotations and generates OpenAPI JSON at /v3/api-docs. Swagger UI at /swagger-ui.html.',
      code:`<!-- Add to pom.xml -->\n<dependency>\n    <groupId>org.springdoc</groupId>\n    <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>\n    <version>2.5.0</version>\n</dependency>\n// Auto-exposes:\n// /v3/api-docs\n// /swagger-ui.html`,
      diagram:{ kind:'flow', steps:[
        {label:'@RestController methods scanned', done:true},
        {label:'springdoc generates OpenAPI JSON', done:true},
        {label:'/swagger-ui: interactive docs', active:true},
        {label:'/v3/api-docs: machine-readable spec'},
      ]}},
    { title:'@Operation and @Schema annotations', description:'Enrich the generated spec with descriptions, examples, and constraints.',
      code:`@Operation(summary="Create order", description="Creates a new order for the authenticated user")\n@ApiResponse(responseCode="201", description="Order created successfully")\n@ApiResponse(responseCode="400", description="Invalid request body")\n@PostMapping\nResponseEntity<OrderResponse> create(\n    @RequestBody @io.swagger.v3.oas.annotations.parameters.RequestBody(description="Order to create")\n    @Valid CreateOrderRequest req\n) {}`,
      highlight:[1,2,3],
      diagram:{ kind:'boxes', title:'Spec enrichment', items:[
        {label:'@Operation: endpoint summary/description', color:'#818cf8'},
        {label:'@ApiResponse: status code docs', color:'#818cf8'},
        {label:'@Schema on DTO: field descriptions', color:'#10b981', highlight:true},
      ]}},
    { title:'Contract testing with Pact', description:'Consumer-driven contract tests: the consumer defines expected interactions; the provider verifies them. CI fails if the contract breaks.',
      code:`// Consumer side (order-service consuming user-service):\n@PactTestFor(providerName = "user-service")\nvoid testGetUser() {\n    PactDslWithProvider builder = ConsumerPactBuilder.consumer("order-service");\n    builder.uponReceiving("GET user 42")\n           .path("/users/42")\n           .willRespondWith().status(200)\n           .body(new PactDslJsonBody().stringType("name", "Alice"));\n}`,
      highlight:[4,5,6,7,8],
      diagram:{ kind:'sequence', actors:['consumer','Pact Broker','provider CI'], messages:[
        {from:'consumer', to:'Pact Broker', label:'publish contract'},
        {from:'provider CI', to:'Pact Broker', label:'fetch consumer contracts'},
        {from:'provider CI', to:'provider CI', label:'verify all consumer expectations → CI pass/fail'},
      ]}},
    { title:'API gateway exposes aggregated spec', description:'Use an API gateway to serve one unified OpenAPI spec across all microservices.',
      code:`# springdoc gateway aggregation\nspringdoc:\n  swagger-ui:\n    urls:\n      - name: order-service\n        url: /openapi/order-service\n      - name: user-service\n        url: /openapi/user-service`,
      language:'yaml', highlight:[4,5,6,7,8],
      diagram:{ kind:'boxes', title:'Aggregated spec', items:[
        {label:'/openapi/order-service → order spec', color:'#818cf8'},
        {label:'/openapi/user-service → user spec', color:'#818cf8'},
        {label:'Swagger UI shows all services', color:'#10b981', highlight:true},
      ]}},
  ],
},

'ms-versioning': {
  language:'java', fileName:'Versioning.java',
  steps:[
    { title:'Why API versioning?', description:'Once an API is live, consumers depend on it. Breaking changes (removing fields, changing types) must be versioned to avoid breaking existing clients.',
      code:`// Breaking changes: require new version\n// - Remove a field from response\n// - Change field type (String → Integer)\n// - Make an optional field required\n// Non-breaking changes: no version needed\n// - Add new optional field to response\n// - Add new optional query parameter`,
      diagram:{ kind:'boxes', title:'Breaking vs non-breaking', items:[
        {label:'add new field → non-breaking', color:'#10b981', highlight:true},
        {label:'remove field → BREAKING → new version', color:'#ef4444'},
        {label:'change type → BREAKING → new version', color:'#ef4444'},
      ]}},
    { title:'URI versioning — most common', description:'/api/v1/ and /api/v2/ coexist. Old clients use v1, new clients use v2. Most visible, easy to test.',
      code:`@RequestMapping("/api/v1/orders")\n@RestController\nclass OrderControllerV1 {}\n\n@RequestMapping("/api/v2/orders")\n@RestController\nclass OrderControllerV2 {}`,
      highlight:[1,5],
      diagram:{ kind:'boxes', title:'URI versioning', items:[
        {label:'/api/v1/orders → V1 controller', color:'#818cf8'},
        {label:'/api/v2/orders → V2 controller', color:'#10b981', highlight:true},
        {label:'cache-friendly: different URLs', color:'#10b981'},
      ]}},
    { title:'Header versioning', description:'API-Version or Accept header selects the version. Cleaner URLs but harder to cache and test with a browser.',
      code:`@GetMapping("/orders")\npublic ResponseEntity<?> getOrders(\n    @RequestHeader(value="API-Version", defaultValue="1") int version) {\n    return switch (version) {\n        case 1 -> ResponseEntity.ok(ordersV1());\n        case 2 -> ResponseEntity.ok(ordersV2());\n        default -> ResponseEntity.badRequest().build();\n    };\n}`,
      highlight:[3,4,5,6],
      diagram:{ kind:'boxes', title:'Header versioning', items:[
        {label:'API-Version: 1 → v1 response', color:'#818cf8'},
        {label:'API-Version: 2 → v2 response', color:'#10b981', highlight:true},
        {label:'not cache-friendly (different headers, same URL)', color:'#f59e0b'},
      ]}},
    { title:'Deprecation strategy', description:'Announce deprecation, set a sunset date, add Deprecation and Sunset response headers. Monitor client usage before removing.',
      code:`@GetMapping\nResponseEntity<OrderResponseV1> getOrdersV1() {\n    return ResponseEntity.ok(response)\n        .header("Deprecation", "true")\n        .header("Sunset", "2027-01-01T00:00:00Z")\n        .header("Link", "<https://api.example.com/v2/orders>; rel=successor-version")\n        .body(response);\n}`,
      highlight:[4,5,6],
      diagram:{ kind:'flow', steps:[
        {label:'announce deprecation in changelog', done:true},
        {label:'add Deprecation + Sunset headers', done:true},
        {label:'monitor: count v1 calls in metrics', active:true},
        {label:'when calls reach 0: remove v1'},
      ]}},
    { title:'Semantic versioning for services', description:'Tag service releases with semver: MAJOR.MINOR.PATCH. Major = breaking change. Minor = new features, backward compatible. Patch = bug fixes.',
      code:`// version 1.2.3\n// 1 = MAJOR: breaking API change\n// 2 = MINOR: new features, backward compatible\n// 3 = PATCH: bug fixes only\n// Never bump MAJOR without a migration plan`,
      diagram:{ kind:'boxes', title:'Semantic versioning', items:[
        {label:'PATCH: 1.2.3 → 1.2.4 (bug fix)', color:'#10b981'},
        {label:'MINOR: 1.2.4 → 1.3.0 (new feature)', color:'#10b981', highlight:true},
        {label:'MAJOR: 1.3.0 → 2.0.0 (breaking change)', color:'#ef4444'},
      ]}},
  ],
},

'ms-service-mesh': {
  language:'yaml', fileName:'service-mesh.yml',
  steps:[
    { title:'What is a service mesh?', description:'A service mesh adds an infrastructure layer (sidecar proxies) for service-to-service communication: mTLS, retries, circuit breaking, observability.',
      code:`# Istio Envoy sidecar injected alongside every pod:\n# Your app container: plain HTTP\n# Envoy sidecar: handles encryption, retries, tracing\n# Your code: zero changes needed`,
      diagram:{ kind:'boxes', title:'Service mesh components', items:[
        {label:'control plane: policy configuration', color:'#818cf8'},
        {label:'data plane: envoy sidecars per pod', color:'#10b981', highlight:true},
        {label:'app code: unchanged, uses plain HTTP', color:'#10b981'},
      ]}},
    { title:'mTLS — automatic encryption', description:'Envoy proxies negotiate mTLS between services. No code changes. Certificates are rotated automatically by the mesh control plane.',
      code:`# Istio PeerAuthentication: enforce mTLS\napiVersion: security.istio.io/v1beta1\nkind: PeerAuthentication\nmetadata:\n  namespace: default\nspec:\n  mtls:\n    mode: STRICT  # require mTLS for all service-to-service calls`,
      language:'yaml', highlight:[7,8],
      diagram:{ kind:'sequence', actors:['order-service','Envoy A','Envoy B','user-service'], messages:[
        {from:'order-service', to:'Envoy A', label:'plain HTTP'},
        {from:'Envoy A', to:'Envoy B', label:'mTLS (encrypted)'},
        {from:'Envoy B', to:'user-service', label:'plain HTTP'},
      ]}},
    { title:'Traffic management — canary deploy', description:'Route 5% of traffic to v2 while 95% goes to v1. Observe metrics, then gradually shift.',
      code:`apiVersion: networking.istio.io/v1alpha3\nkind: VirtualService\nmetadata:\n  name: order-service\nspec:\n  http:\n  - route:\n    - destination:\n        host: order-service\n        subset: v1\n      weight: 95\n    - destination:\n        host: order-service\n        subset: v2\n      weight: 5`,
      language:'yaml', highlight:[8,10,11,12,13,14,15],
      diagram:{ kind:'boxes', title:'Canary traffic split', items:[
        {label:'95% → order-service v1 (stable)', color:'#818cf8'},
        {label:'5% → order-service v2 (canary)', color:'#10b981', highlight:true},
        {label:'observe v2 metrics → ramp up or rollback', color:'#10b981'},
      ]}},
    { title:'Istio observability', description:'Envoy sidecars emit metrics (Prometheus), traces (Zipkin), and logs automatically. Kiali shows the service topology graph.',
      code:`# Automatically available:\n# - Request rates, error rates, latencies (Grafana)\n# - Distributed traces (Jaeger/Zipkin)\n# - Service dependency graph (Kiali)\n# Zero app-level instrumentation needed`,
      diagram:{ kind:'flow', steps:[
        {label:'request passes through Envoy sidecars', done:true},
        {label:'Envoy emits metrics to Prometheus', done:true},
        {label:'Envoy emits trace spans to Jaeger', active:true},
        {label:'Kiali: live service mesh topology'},
      ]}},
    { title:'Linkerd — lightweight alternative', description:'Linkerd is simpler than Istio: smaller footprint, easier to operate. Uses Rust-based micro-proxies. Good for teams starting with service mesh.',
      code:`# Install Linkerd\nlinkerd install | kubectl apply -f -\n# Inject sidecar into namespace\nkubectl annotate namespace default linkerd.io/inject=enabled\n# All new pods get a Linkerd proxy sidecar`,
      diagram:{ kind:'boxes', title:'Istio vs Linkerd', items:[
        {label:'Istio: full-featured, complex', color:'#818cf8'},
        {label:'Linkerd: simple, lightweight, fast', color:'#10b981', highlight:true},
        {label:'both: mTLS + metrics + tracing', color:'#10b981'},
      ]}},
  ],
},

'ms-soa-vs-ms': {
  language:'java', fileName:'SoaVsMs.java',
  steps:[
    { title:'SOA — Service-Oriented Architecture', description:'SOA (mid-2000s): large services share an Enterprise Service Bus (ESB). Services communicate via standardized SOAP/XML protocols. Heavy governance.',
      code:`// SOA characteristics:\n// - Enterprise Service Bus (ESB) in the middle\n// - SOAP/WSDL protocols\n// - Services can be large (still monolithic-ish)\n// - Centralized governance and registry`,
      diagram:{ kind:'boxes', title:'SOA structure', items:[
        {label:'ESB: central integration bus', color:'#f59e0b'},
        {label:'SOAP/WSDL: heavy protocols', color:'#818cf8'},
        {label:'large coarse-grained services', color:'#818cf8', highlight:true},
      ]}},
    { title:'Microservices — evolution of SOA', description:'Microservices (2010s): fine-grained, independently deployable services. REST/events replace ESB. Decentralized governance.',
      code:`// Microservices vs SOA:\n// - No ESB: services talk directly (REST/Kafka)\n// - REST/gRPC/events: lightweight protocols\n// - Fine-grained: small, focused services\n// - Decentralized: each team owns their service`,
      diagram:{ kind:'boxes', title:'Key differences', items:[
        {label:'SOA: ESB → MS: direct communication', color:'#818cf8'},
        {label:'SOA: SOAP → MS: REST/gRPC', color:'#818cf8'},
        {label:'SOA: coarse → MS: fine-grained', color:'#10b981', highlight:true},
        {label:'SOA: centralized → MS: decentralized', color:'#10b981'},
      ]}},
    { title:'Smart endpoints, dumb pipes', description:'SOA put intelligence in the ESB (routing, transformation, orchestration). Microservices put intelligence in the endpoints; keep the message bus simple.',
      code:`// SOA: smart ESB does routing, transformation\nESB.route(message).transform().deliver();\n// Microservices: smart services, dumb Kafka\nkafkaTemplate.send("order-events", event);  // just publish\nOrderConsumer.onMessage(event);             // consumer handles logic`,
      diagram:{ kind:'boxes', title:'Smart endpoints, dumb pipes', items:[
        {label:'SOA: ESB handles logic (fragile)', color:'#f59e0b'},
        {label:'MS: services handle logic', color:'#10b981', highlight:true},
        {label:'Kafka: just a dumb pipe', color:'#10b981'},
      ]}},
    { title:'SOA still relevant', description:'SOA patterns apply at larger organizational levels (enterprise integration). EDA (Event-Driven Architecture) merges SOA and microservices ideas.',
      code:`// SOA concepts still used today:\n// - Service contracts (OpenAPI = modern WSDL)\n// - Service registry (Eureka, Consul = modern UDDI)\n// - Message transformation (MapStruct = modern XSLT)`,
      diagram:{ kind:'boxes', title:'SOA patterns in MS', items:[
        {label:'service registry: Eureka (SOA: UDDI)', color:'#818cf8'},
        {label:'service contract: OpenAPI (SOA: WSDL)', color:'#818cf8', highlight:true},
        {label:'message bus: Kafka (SOA: ESB, simpler)', color:'#10b981'},
      ]}},
    { title:'Choose based on context', description:'Microservices for independent teams and deployments. SOA for enterprise integration across organizational boundaries.',
      code:`// Use microservices when:\n// - One company, multiple teams, one product\n// Use ESB/SOA when:\n// - Multiple organizations integrating their systems\n// - Legacy system integration required`,
      diagram:{ kind:'boxes', title:'When to use each', items:[
        {label:'microservices: within one org/product', color:'#10b981', highlight:true},
        {label:'SOA/EAI: cross-org integration', color:'#818cf8'},
        {label:'both: clear service contracts essential', color:'#818cf8'},
      ]}},
  ],
},

'ms-conway-law': {
  language:'java', fileName:'ConwayLaw.java',
  steps:[
    { title:'Conway\'s Law', description:'"Any organization that designs a system will produce a design whose structure is a copy of the organization\'s communication structure." — Mel Conway, 1968.',
      code:`// If org has: frontend team + backend team + DB team\n// → System has: frontend layer + backend layer + DB layer (layered arch)\n// If org has: checkout team + catalog team + payment team\n// → System has: checkout-service + catalog-service + payment-service`,
      diagram:{ kind:'boxes', title:'Conway Law examples', items:[
        {label:'2 teams: monolith with 2 layers', color:'#818cf8'},
        {label:'5 teams: 5 microservices', color:'#10b981', highlight:true},
        {label:'org structure mirrors system structure', color:'#818cf8'},
      ]}},
    { title:'Inverse Conway Maneuver', description:'Don\'t let org structure happen accidentally. Deliberately design the org to produce the architecture you want.',
      code:`// Want microservices? → Create small autonomous teams\n// Want loosely coupled services? → Minimize team dependencies\n// Amazon 2-pizza rule: teams small enough\n//   that 2 pizzas can feed everyone`,
      diagram:{ kind:'flow', steps:[
        {label:'choose target architecture first', done:true},
        {label:'design org structure to match', done:true},
        {label:'autonomous teams → autonomous services', active:true},
      ]}},
    { title:'Team Topologies', description:'Stream-aligned teams: each owns a business domain end-to-end. Platform teams: provide infrastructure. Enabling teams: upskill stream teams.',
      code:`// Stream-aligned: checkout team owns checkout-service\n// Platform: k8s team manages shared platform\n// Enabling: security team helps teams adopt security practices`,
      diagram:{ kind:'boxes', title:'Team Topologies', items:[
        {label:'stream-aligned: business domain', color:'#10b981', highlight:true},
        {label:'platform: shared infrastructure', color:'#818cf8'},
        {label:'enabling: expertise sharing', color:'#818cf8'},
        {label:'complicated subsystem: specialized tech', color:'#818cf8'},
      ]}},
    { title:'Communication overhead — Brooks\'s Law', description:'Adding people to a late project makes it later. Communication overhead grows as n(n-1)/2. Microservices reduce coupling between teams.',
      code:`// Team of 10: 10*9/2 = 45 communication channels\n// Two teams of 5: 5*4/2 + 5*4/2 = 20 channels\n// + 1 API contract between them\n// Smaller, more autonomous teams = less coordination overhead`,
      diagram:{ kind:'boxes', title:'Communication channels', items:[
        {label:'1 team of 10: 45 channels', color:'#ef4444'},
        {label:'2 teams of 5: 20 internal + 1 API', color:'#10b981', highlight:true},
        {label:'well-defined API = team boundary', color:'#10b981'},
      ]}},
    { title:'Practical implication', description:'Before designing microservices boundaries, look at your org chart. Align service boundaries with team boundaries. Misalignment causes constant cross-team coordination.',
      code:`// Anti-pattern: microservice boundary crosses team\n// order-service (team A) calls inventory-service (team A)\n// → still in same team → could be a module in a monolith\n// Good: each team owns one service (their domain)`,
      diagram:{ kind:'boxes', title:'Alignment check', items:[
        {label:'same team owns both services → merge?', color:'#f59e0b'},
        {label:'different teams → service boundary correct', color:'#10b981', highlight:true},
        {label:'Conway\'s Law predicts this', color:'#818cf8'},
      ]}},
  ],
},

'ms-centralised-logging': {
  language:'yaml', fileName:'logging.yml',
  steps:[
    { title:'The problem: logs across 20 services', description:'10 microservices × 10 pods = 100 sources. Without centralized logging, debugging a request requires SSHing into 20 machines.',
      code:`# Without centralized logging:\n# kubectl logs pod/order-service-abc123 \n# kubectl logs pod/user-service-xyz789\n# kubectl logs pod/payment-service-def456\n# ... 97 more pods`,
      diagram:{ kind:'flow', steps:[
        {label:'request fails across 5 services', done:true},
        {label:'check logs on each pod manually?', done:true},
        {label:'centralized: query by traceId in one place', active:true},
      ]}},
    { title:'ELK Stack — Elasticsearch, Logstash, Kibana', description:'Filebeat ships logs from each pod to Logstash, which transforms and sends to Elasticsearch. Kibana queries and visualizes.',
      code:`# Filebeat DaemonSet on each k8s node\napiVersion: apps/v1\nkind: DaemonSet\nmetadata:\n  name: filebeat\nspec:\n  template:\n    spec:\n      containers:\n      - name: filebeat\n        image: elastic/filebeat:8.x`,
      diagram:{ kind:'sequence', actors:['pods','Filebeat','Logstash','Elasticsearch','Kibana'], messages:[
        {from:'pods', to:'Filebeat', label:'stdout logs'},
        {from:'Filebeat', to:'Logstash', label:'ship + enrich'},
        {from:'Logstash', to:'Elasticsearch', label:'index'},
        {from:'Kibana', to:'Elasticsearch', label:'query by traceId'},
      ]}},
    { title:'Structured JSON logging', description:'Log as JSON so Elasticsearch can index each field. Include traceId, level, service, message in every log line.',
      code:`# application.yml — Spring Boot 3.4+ structured logging\nlogging:\n  structured:\n    format:\n      console: ecs   # Elastic Common Schema\n# Output:\n# {"@timestamp":"...","traceId":"abc123","level":"INFO",\n#  "service":"order-service","message":"Order 42 placed"}`,
      highlight:[3,4,5,8],
      diagram:{ kind:'boxes', title:'Structured log fields', items:[
        {label:'@timestamp: sort by time', color:'#818cf8'},
        {label:'traceId: find all logs for one request', color:'#10b981', highlight:true},
        {label:'service: which microservice', color:'#818cf8'},
        {label:'level: filter WARN/ERROR', color:'#818cf8'},
      ]}},
    { title:'Kibana queries', description:'Search logs by traceId, service, level, or time range. Create dashboards for error rates.',
      code:`# Kibana KQL examples:\n# Find all logs for a request:\ntraceId:"abc123"\n# Find all errors in order-service last hour:\nservice:"order-service" AND level:"ERROR"\n# Spike: errors > 100 in last 5 min`,
      diagram:{ kind:'flow', steps:[
        {label:'enter: traceId:"abc123"', done:true},
        {label:'Elasticsearch returns all logs for that request', done:true},
        {label:'see full request flow across all services', active:true},
      ]}},
    { title:'Log retention and cost', description:'Logs are expensive to store. Retain ERROR/WARN longer, INFO shorter. Use log sampling for DEBUG.',
      code:`# Hot retention (fast): 7 days\n# Warm retention (slower): 30 days  \n# Cold/archive: 1 year (regulatory)\n# Cost control:\n# - Sample DEBUG to 1%\n# - Drop health check logs\n# - Compress and archive old logs`,
      diagram:{ kind:'boxes', title:'Log retention tiers', items:[
        {label:'ERROR: 90 days (investigations)', color:'#ef4444'},
        {label:'WARN: 30 days', color:'#f59e0b'},
        {label:'INFO: 7 days', color:'#10b981', highlight:true},
        {label:'DEBUG: sampled, 1 day', color:'#818cf8'},
      ]}},
  ],
},

'ms-spring-boot': {
  language:'java', fileName:'MsSpringBoot.java',
  steps:[
    { title:'Spring Boot in microservices', description:'Spring Boot is the de-facto standard for Java microservices: embedded Tomcat, auto-configuration, Actuator, Spring Cloud integrations.',
      code:`@SpringBootApplication\npublic class OrderServiceApp {\n    public static void main(String[] args) {\n        SpringApplication.run(OrderServiceApp.class, args);\n    }\n}\n// Result: standalone fat JAR, embedded Tomcat on :8080\n// java -jar order-service.jar`,
      highlight:[1,8],
      diagram:{ kind:'boxes', title:'Spring Boot microservice', items:[
        {label:'spring-boot-starter-web: REST + Tomcat', color:'#10b981', highlight:true},
        {label:'spring-boot-starter-actuator: health/metrics', color:'#818cf8'},
        {label:'spring-boot-starter-data-jpa: DB access', color:'#818cf8'},
        {label:'spring-cloud-*: service discovery, config', color:'#818cf8'},
      ]}},
    { title:'Spring Actuator for microservice health', description:'Actuator exposes /health, /metrics, /info, /env. K8s uses liveness and readiness probes based on /health.',
      code:`management:\n  endpoint:\n    health:\n      show-details: always\n      group:\n        liveness.include: livenessState\n        readiness.include: readinessState,db\n  endpoints:\n    web.exposure.include: health,metrics,info,prometheus`,
      language:'yaml', highlight:[4,5,6,7,9],
      diagram:{ kind:'boxes', title:'Actuator endpoints', items:[
        {label:'/actuator/health: k8s probe', color:'#10b981', highlight:true},
        {label:'/actuator/prometheus: metrics', color:'#818cf8'},
        {label:'/actuator/info: build info', color:'#818cf8'},
        {label:'/actuator/env: config (secure!)', color:'#f59e0b'},
      ]}},
    { title:'Spring Cloud integrations', description:'Spring Cloud adds: Config Server, Eureka, OpenFeign, Gateway, LoadBalancer, Resilience4j, Sleuth/Micrometer.',
      code:`<!-- Spring Cloud BOM -->\n<dependency>\n    <groupId>org.springframework.cloud</groupId>\n    <artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>\n</dependency>\n<dependency>\n    <groupId>org.springframework.cloud</groupId>\n    <artifactId>spring-cloud-starter-openfeign</artifactId>\n</dependency>`,
      diagram:{ kind:'boxes', title:'Spring Cloud starters', items:[
        {label:'eureka-client: service registry', color:'#818cf8'},
        {label:'openfeign: REST client', color:'#10b981', highlight:true},
        {label:'config: centralized config', color:'#818cf8'},
        {label:'resilience4j: circuit breaker', color:'#818cf8'},
      ]}},
    { title:'Graceful shutdown', description:'Configure graceful shutdown so in-flight requests complete before the pod dies. Critical for zero-downtime deployments.',
      code:`server:\n  shutdown: graceful    # wait for in-flight requests\nspring:\n  lifecycle:\n    timeout-per-shutdown-phase: 30s  # max wait`,
      language:'yaml', highlight:[2,5],
      diagram:{ kind:'flow', steps:[
        {label:'k8s sends SIGTERM to pod', done:true},
        {label:'Spring: stop accepting new requests', done:true},
        {label:'wait for in-flight to complete (max 30s)', active:true},
        {label:'all done → exit 0 (clean shutdown)'},
      ]}},
    { title:'Dockerfile best practices', description:'Multi-stage build: compile in builder, copy only JAR to minimal runtime image. Use layered JARs for faster Docker cache.',
      code:`FROM eclipse-temurin:21-jdk AS builder\nWORKDIR /app\nCOPY . .\nRUN ./mvnw -q package -DskipTests\n\nFROM eclipse-temurin:21-jre\nWORKDIR /app\nCOPY --from=builder /app/target/*.jar app.jar\nEXPOSE 8080\nENTRYPOINT ["java","-jar","app.jar"]`,
      highlight:[1,6,8,10],
      diagram:{ kind:'boxes', title:'Docker best practices', items:[
        {label:'multi-stage: JDK build → JRE runtime', color:'#10b981', highlight:true},
        {label:'JRE image: 3× smaller than JDK', color:'#10b981'},
        {label:'layered JAR: faster Docker cache', color:'#818cf8'},
      ]}},
  ],
},

'ms-when-not': {
  language:'java', fileName:'WhenNotMs.java',
  steps:[
    { title:'Microservices are NOT a silver bullet', description:'The industry over-adopted microservices. Many teams now run "distributed monoliths" — all the complexity of microservices with none of the benefits.',
      code:`// Distributed monolith anti-pattern:\n// - 20 services, but they all call each other synchronously\n// - They share a database\n// - They must be deployed together\n// - Same team owns all 20 services\n// = monolith complexity + distributed system complexity`,
      diagram:{ kind:'boxes', title:'Distributed monolith traps', items:[
        {label:'synchronous call chains → cascading failures', color:'#ef4444'},
        {label:'shared database → coupling defeats the purpose', color:'#ef4444', highlight:true},
        {label:'one team, 20 services → no independence', color:'#ef4444'},
      ]}},
    { title:'When monolith is better', description:'Solo developer, startup MVP, new product with unclear domain boundaries, small team (< 5 engineers). Monolith is simpler and faster to ship.',
      code:`// Use monolith when:\n// - < 5 engineers on the same product\n// - MVP / exploring product-market fit\n// - Bounded contexts not yet clear\n// - No confirmed scaling bottlenecks\n// → Ship fast, split later if needed`,
      diagram:{ kind:'boxes', title:'Monolith is better when', items:[
        {label:'small team: < 5 engineers', color:'#10b981', highlight:true},
        {label:'MVP: domain boundaries unclear', color:'#10b981'},
        {label:'no confirmed scaling need', color:'#10b981'},
      ]}},
    { title:'Modular monolith — best of both', description:'A modular monolith has clear internal modules (boundaries, no cross-module direct DB access) but deploys as one unit. Easy to extract to microservices later.',
      code:`// Modular monolith structure:\n// ├── ordering/   (module)\n// │   ├── OrderingService.java\n// │   └── OrderingRepository.java (private)\n// ├── billing/    (module)\n// │   ├── BillingService.java\n// └── BillingRepository.java (private)\n// Modules communicate via interfaces, not direct calls`,
      diagram:{ kind:'boxes', title:'Modular monolith', items:[
        {label:'ordering module: own package, hidden impl', color:'#10b981'},
        {label:'billing module: clean interface', color:'#10b981', highlight:true},
        {label:'deploy: one JAR (simple)', color:'#818cf8'},
        {label:'extract module to service later when needed', color:'#10b981'},
      ]}},
    { title:'Operational maturity required', description:'Microservices require: CI/CD pipelines, container orchestration (k8s), distributed tracing, centralized logging, service discovery. Small teams often lack this.',
      code:`// Microservices operational checklist:\n// [ ] CI/CD pipeline per service\n// [ ] Container orchestration (k8s)\n// [ ] Distributed tracing\n// [ ] Centralized logging (ELK/Loki)\n// [ ] Service discovery (Eureka/k8s)\n// Missing any? → Microservices are painful`,
      diagram:{ kind:'boxes', title:'Required operational maturity', items:[
        {label:'CI/CD: automated per-service deploy', color:'#818cf8'},
        {label:'k8s: container scheduling', color:'#818cf8'},
        {label:'observability: traces, logs, metrics', color:'#ef4444', highlight:true},
        {label:'without all three: don\'t use microservices', color:'#ef4444'},
      ]}},
    { title:'Extract when pain is confirmed', description:'Start with a monolith. Extract a service only when you have a confirmed bottleneck: team deployment conflict, scaling mismatch, or distinct technology need.',
      code:`// Extract a service when:\n// 1. Two teams constantly conflict deploying the same monolith\n// 2. One component needs 10× more resources than others\n// 3. Component needs a different tech (Python ML model)\n// Proof of problem required before extraction`,
      diagram:{ kind:'flow', steps:[
        {label:'start: modular monolith', done:true},
        {label:'pain confirmed: team/scaling conflict', done:true},
        {label:'extract ONE service, validate', active:true},
        {label:'repeat for next confirmed pain point'},
      ]}},
  ],
},

}}; // end microservices remaining

