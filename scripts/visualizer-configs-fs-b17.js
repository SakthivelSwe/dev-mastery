module.exports = [
  {
    path: "full-stack", slug: "concurrency-basics",
    steps: [
      { title: "Concurrency vs Parallelism", description: "Concurrency: many tasks in progress. Parallelism: many tasks executing at once.", code: "// Concurrency: single core, interleaved\n// Task A: ▓▓░░▓▓░░\n// Task B: ░░▓▓░░▓▓\n// (context switches)\n\n// Parallelism: multiple cores, simultaneous\n// Core 1: ▓▓▓▓▓▓▓▓ (Task A)\n// Core 2: ▓▓▓▓▓▓▓▓ (Task B)\n\n// JS runtime:\n// - Single-threaded event loop (concurrent, not parallel)\n// - Web Workers / Worker Threads = parallel\n\n// Java:\n// - Native threads (parallel on multi-core)\n// - Virtual threads (Project Loom): lightweight", highlight: [1,2,3,4,6,7,8,10,11,12,14,15,16], diagram: {"kind":"boxes","items":[{"label":"Concurrency: interleaved","color":"#1e88e5"},{"label":"Parallelism: simultaneous","color":"#43a047"}]} },
      { title: "Race Conditions", description: "Multiple threads modifying shared state without coordination.", code: "// Race condition example\nlet counter = 0;\n\n// Multiple threads/async tasks:\nasync function increment() {\n  const val = counter;      // read\n  await sleep(0);           // context switch here!\n  counter = val + 1;        // write stale value\n}\n\n// 1000 concurrent increments should = 1000\n// May end up = 800-900 due to races\n\n// Fix in Java:\nsynchronized void increment() {\n  counter++;  // atomic in critical section\n}\n\n// Or AtomicInteger:\nprivate final AtomicInteger counter = new AtomicInteger(0);\ncounter.incrementAndGet();\n\n// JS: usually no shared memory (event loop)\n// Web Workers: use SharedArrayBuffer + Atomics", highlight: [2,5,6,7,8,10,11,14,15,16,17,20,21,22,23], diagram: {"kind":"flow","steps":[{"label":"Two threads read val"},{"label":"Both compute val+1"},{"label":"Both write same"},{"label":"Lost update"},{"label":"Fix with sync/atomic"}]} },
      { title: "Synchronization Primitives", description: "Locks, semaphores, atomic operations, channels.", code: "// Locks (mutex): only one thread at a time\nconst lock = new AsyncLock();\nawait lock.acquire('resource', async () => {\n  // critical section\n});\n\n// Semaphore: N concurrent access\nconst sem = new Semaphore(3);\nasync function limitedOp() {\n  await sem.acquire();\n  try { await work(); }\n  finally { sem.release(); }\n}\n\n// Atomic operations\nlet counter = new SharedArrayBuffer(4);\nconst view = new Int32Array(counter);\nAtomics.add(view, 0, 1);\n\n// Channels (Go-style)\n// Communicate via messages, not shared memory\n// JS: Web Workers postMessage\n// Node: Worker Threads", highlight: [2,3,4,5,8,9,10,11,12,15,16,17,18,21,22,23], diagram: {"kind":"boxes","items":[{"label":"Lock: exclusive","color":"#1e88e5"},{"label":"Semaphore: N","color":"#43a047"},{"label":"Atomic ops","color":"#fb8c00"},{"label":"Channels","color":"#8e24aa"}]} }
    ]
  },
  {
    path: "full-stack", slug: "java-memory-model",
    steps: [
      { title: "JVM Memory Areas", description: "Heap, Stack, Method Area, PC Register — JVM organizes memory for execution.", code: "// JVM memory regions:\n\n// Heap (shared):\n// - All objects allocated here\n// - Garbage collected\n// - Young + Old generations\n\n// Stack (per thread):\n// - Method call frames\n// - Local variables, references\n// - Cleared when method returns\n\n// Method Area (Metaspace):\n// - Class metadata, static fields\n// - JIT-compiled code\n\n// PC Register:\n// - Current instruction per thread\n\n// Native Method Stack:\n// - For JNI (native calls)\n\n// Direct Memory (off-heap):\n// - NIO buffers", highlight: [4,5,6,7,9,10,11,12,14,15,16,18,19,21,22,24,25], diagram: {"kind":"boxes","items":[{"label":"Heap: objects","color":"#1e88e5"},{"label":"Stack: frames","color":"#43a047"},{"label":"Metaspace","color":"#fb8c00"},{"label":"PC Register","color":"#8e24aa"}]} },
      { title: "Happens-Before Relationship", description: "JMM guarantees ordering of memory operations across threads.", code: "// Without happens-before, reads may see stale values\n\n// Rules that establish happens-before:\n// 1. Program order (within a thread)\n// 2. Monitor lock: unlock happens-before subsequent lock\n// 3. volatile write happens-before volatile read\n// 4. Thread.start() happens-before actions in new thread\n// 5. Thread termination happens-before join()\n\n// Example: volatile\npublic class Flag {\n  private volatile boolean ready = false;\n  private int data;\n\n  public void publish(int value) {\n    data = value;\n    ready = true;  // volatile write\n  }\n\n  public int read() {\n    while (!ready) { }  // volatile read\n    return data;  // guaranteed to see published value\n  }\n}", highlight: [4,5,6,7,8,12,13,14,15,16,17,18,20,21,22], diagram: {"kind":"flow","steps":[{"label":"Thread A writes"},{"label":"Volatile flag = true"},{"label":"Thread B reads flag"},{"label":"Sees all A's writes before flag"},{"label":"Happens-before guarantee"}]} },
      { title: "synchronized and volatile", description: "synchronized: mutual exclusion + visibility. volatile: visibility only.", code: "// volatile: visibility (not atomicity)\nprivate volatile boolean running = true;\n\n// Thread 1:\nwhile (running) { doWork(); }\n\n// Thread 2:\nrunning = false; // guaranteed visible to Thread 1\n\n// synchronized: atomicity + visibility\nprivate int counter = 0;\n\npublic synchronized void increment() {\n  counter++; // read-modify-write is atomic\n}\n\n// Or explicit lock\nprivate final ReentrantLock lock = new ReentrantLock();\n\npublic void increment() {\n  lock.lock();\n  try { counter++; }\n  finally { lock.unlock(); }\n}\n\n// AtomicInteger: no lock, still atomic\nprivate final AtomicInteger counter = new AtomicInteger();\ncounter.incrementAndGet();", highlight: [1,2,4,5,7,8,10,11,12,13,14,15,17,18,19,20,21,22,23,26,27,28], diagram: {"kind":"boxes","items":[{"label":"volatile: visibility","color":"#1e88e5"},{"label":"synchronized: both","color":"#43a047"},{"label":"Atomic: lock-free","color":"#fb8c00"}]} }
    ]
  },
  {
    path: "full-stack", slug: "java-class-loading-subsystem",
    steps: [
      { title: "Class Loaders", description: "Bootstrap, Extension/Platform, System — hierarchy of class loaders.", code: "// Class loader hierarchy:\n// Bootstrap ClassLoader (native)\n//   → loads java.* (rt.jar / core modules)\n\n// Platform (Extension) ClassLoader\n//   → loads javax.*, ext libraries\n\n// System (Application) ClassLoader\n//   → loads classpath\n\n// Delegation model:\n// 1. Child asks parent first\n// 2. Only if parent can't find, child loads\n// 3. Prevents duplicates, enforces core class integrity\n\n// Get class loader\nClassLoader cl = String.class.getClassLoader();  // null (bootstrap)\nClassLoader cl2 = MyClass.class.getClassLoader(); // System\n\n// Custom class loader (for hot reload, plugins)\nclass MyClassLoader extends ClassLoader { ... }", highlight: [2,3,5,6,8,9,11,12,13,14,17,18,21], diagram: {"kind":"flow","steps":[{"label":"Bootstrap"},{"label":"Platform"},{"label":"System"},{"label":"Delegation up first"},{"label":"Load if not found"}]} },
      { title: "Class Loading Phases", description: "Loading → Linking (Verify, Prepare, Resolve) → Initialization.", code: "// Phase 1: Loading\n// - Find .class file\n// - Read binary\n// - Create Class object in memory\n\n// Phase 2: Linking\n// 2a. Verification: check bytecode is valid\n// 2b. Preparation: allocate memory for static fields (default values)\n// 2c. Resolution: resolve symbolic references to direct references\n\n// Phase 3: Initialization\n// - Run static initializers\n// - Assign static field values\n\npublic class Example {\n  static int x = 10;  // initialized in phase 3\n  static { System.out.println(\"init!\"); } // in phase 3\n\n  int y = 20;         // set on instance creation\n}\n\n// Class is initialized on first \"active use\":\n// - new instance, static method call, static field access", highlight: [1,2,3,4,6,7,8,9,11,12,13,15,16,17,18,19,20,22,23], diagram: {"kind":"flow","steps":[{"label":"Load .class"},{"label":"Verify bytecode"},{"label":"Prepare defaults"},{"label":"Resolve refs"},{"label":"Initialize"}]} },
      { title: "When Classes are Loaded", description: "Lazy loading — classes loaded only when needed.", code: "// A class is initialized when:\n// 1. new instance created\n new MyClass();\n\n// 2. Static method called\n MyClass.staticMethod();\n\n// 3. Static field accessed (except constants)\n int x = MyClass.STATIC_FIELD;\n\n// 4. Reflection\n Class.forName(\"com.example.MyClass\");\n\n// 5. Subclass loaded (loads parent)\n// 6. JVM starts main class\n\n// NOT loaded by:\n// - null reference declaration\n MyClass x = null;  // MyClass not loaded\n\n// - final constant field access\n int p = MyClass.PI;  // if final static, no init\n\n// Class.forName vs ClassLoader.loadClass:\n// forName: initializes (runs static blocks)\n// loadClass: doesn't initialize", highlight: [2,3,5,6,8,9,11,12,15,16,19,20,22,23,25,26,27], diagram: {"kind":"flow","steps":[{"label":"Reference class"},{"label":"JVM: loaded?"},{"label":"No → load + init"},{"label":"Yes → use"},{"label":"Lazy loading"}]} }
    ]
  },
  {
    path: "full-stack", slug: "collections-overview",
    steps: [
      { title: "Collection Types", description: "List, Set, Map, Queue — different data structures for different needs.", code: "// Java collections hierarchy:\n// Collection\n//   ├── List (ordered, duplicates)\n//   │     ArrayList, LinkedList\n//   ├── Set (no duplicates)\n//   │     HashSet, TreeSet, LinkedHashSet\n//   └── Queue (FIFO or priority)\n//         ArrayDeque, PriorityQueue\n// Map (not Collection)\n//   HashMap, TreeMap, LinkedHashMap\n\n// JavaScript equivalents:\n// Array = List\n// Set = Set\n// Map = Map (or Object for string keys)\n\n// Choose by access pattern:\n// - Random access → ArrayList\n// - Frequent insert/delete → LinkedList\n// - Fast lookup → HashSet, HashMap\n// - Sorted → TreeSet, TreeMap\n// - LRU cache → LinkedHashMap", highlight: [3,4,5,6,7,8,9,10,12,13,14,15,17,18,19,20,21,22], diagram: {"kind":"boxes","items":[{"label":"List: ordered","color":"#1e88e5"},{"label":"Set: unique","color":"#43a047"},{"label":"Map: key-value","color":"#fb8c00"},{"label":"Queue: FIFO","color":"#8e24aa"}]} },
      { title: "Time Complexities", description: "Big-O for common operations — pick the right structure.", code: "// ArrayList:\n// - get(i): O(1)\n// - add(end): O(1) amortized\n// - add(middle): O(n)\n// - contains: O(n)\n\n// LinkedList:\n// - get(i): O(n)\n// - add(end): O(1)\n// - add(middle): O(1) if you have reference\n// - contains: O(n)\n\n// HashMap:\n// - get/put/remove: O(1) average, O(n) worst\n// - containsKey: O(1) average\n\n// TreeMap (sorted):\n// - get/put/remove: O(log n)\n// - firstKey/lastKey: O(log n)\n\n// HashSet:\n// - add/contains/remove: O(1) average\n\n// Rule: usually HashMap/HashSet default\n// TreeMap/TreeSet only if you need sorted order", highlight: [1,2,3,4,5,7,8,9,10,11,13,14,15,17,18,19,21,22,24,25], diagram: {"kind":"flow","steps":[{"label":"ArrayList: O(1) get"},{"label":"HashMap: O(1) lookup"},{"label":"TreeMap: O(log n) sorted"},{"label":"Choose per pattern"},{"label":"Big-O matters"}]} },
      { title: "Concurrent Collections", description: "ConcurrentHashMap, CopyOnWriteArrayList — thread-safe.", code: "// Regular HashMap: not thread-safe!\n// Multiple threads writing → data corruption\n\n// ConcurrentHashMap: safe, high performance\nMap<String, Integer> counts = new ConcurrentHashMap<>();\ncounts.compute(\"key\", (k, v) -> v == null ? 1 : v + 1);\ncounts.putIfAbsent(\"key\", 0);\n\n// Lock-free reads, striped locks for writes\n\n// CopyOnWriteArrayList: safe, immutable snapshots\n// Great for read-heavy, write-rare\nList<Listener> listeners = new CopyOnWriteArrayList<>();\nfor (Listener l : listeners) l.onEvent(e); // safe during iteration\n\n// BlockingQueue: producer/consumer\nBlockingQueue<Task> queue = new LinkedBlockingQueue<>(100);\nqueue.put(task);     // blocks if full\nTask t = queue.take(); // blocks if empty\n\n// Synchronized wrappers (older, slower):\n// Collections.synchronizedMap(new HashMap<>());", highlight: [4,5,6,7,8,10,11,12,13,14,15,17,18,19,20,21], diagram: {"kind":"boxes","items":[{"label":"ConcurrentHashMap","color":"#1e88e5"},{"label":"CopyOnWrite","color":"#43a047"},{"label":"BlockingQueue","color":"#fb8c00"}]} }
    ]
  },
  {
    path: "full-stack", slug: "arraylist-vs-linkedlist",
    steps: [
      { title: "Internal Structure", description: "ArrayList: dynamic array. LinkedList: doubly-linked nodes.", code: "// ArrayList\n// [_][_][_][_][_][_][_][_][_][_]  ← contiguous array\n// Auto-resizes when full (new array, copy)\n// Default: doubles capacity\n\nList<Integer> arr = new ArrayList<>();\narr.add(1); arr.add(2); arr.add(3);\narr.get(0); // O(1) direct index\n\n// LinkedList\n// [1] ⇄ [2] ⇄ [3]\n// Each node: value + prev + next pointers\n\nList<Integer> ll = new LinkedList<>();\nll.add(1); ll.add(2); ll.add(3);\nll.get(0); // O(n) - traverse from head", highlight: [2,3,4,6,7,8,11,12,14,15,16], diagram: {"kind":"boxes","items":[{"label":"ArrayList: array","color":"#1e88e5"},{"label":"LinkedList: nodes","color":"#43a047"}]} },
      { title: "When to Use Which", description: "ArrayList wins 90% of the time.", code: "// Use ArrayList when:\n// - Random access needed (get by index)\n// - Iterating frequently\n// - Adding mostly to the end\n// - Better cache locality (contiguous)\n\n// Use LinkedList when:\n// - Frequent insert/remove in middle (with iterator)\n// - Implementing queue/deque (use ArrayDeque instead!)\n// - You have direct node references\n\n// Reality check:\n// LinkedList is almost always slower in practice\n// - Cache misses on pointer chasing\n// - Memory overhead per node\n// - Even for \"insert in middle\", ArrayList with shift is often faster\n\n// Rule: default to ArrayList\n// Only switch to LinkedList with profiling data\n\n// For queues: ArrayDeque > LinkedList\n// For stacks: ArrayDeque > Stack (which is legacy)", highlight: [1,2,3,4,5,7,8,9,10,12,13,14,15,16,18,19,21,22], diagram: {"kind":"flow","steps":[{"label":"Random access?"},{"label":"ArrayList"},{"label":"Middle insert?"},{"label":"Still ArrayList usually"},{"label":"Queue? ArrayDeque"}]} },
      { title: "Performance Comparison", description: "ArrayList shines for reads; LinkedList only for specific insertion patterns.", code: "// Benchmark 1: get(i) 1M times on 1M elements\n// ArrayList: ~5ms\n// LinkedList: ~30 seconds (traverses each time!)\n\n// Benchmark 2: add(end) 1M elements\n// ArrayList: ~15ms\n// LinkedList: ~40ms (allocation per node)\n\n// Benchmark 3: add(0, x) 100k times (front insert)\n// ArrayList: ~10 seconds (shifts everything)\n// LinkedList: ~5ms\n\n// Benchmark 4: iterate 1M\n// ArrayList: ~5ms (cache friendly)\n// LinkedList: ~20ms (pointer chasing)\n\n// Modern hardware favors ArrayList:\n// - CPU cache lines\n// - Branch prediction\n// - Prefetching\n\n// Conclusion:\n// LinkedList is rarely worth it in modern Java", highlight: [1,2,3,5,6,7,9,10,11,13,14,15,17,18,19,20,22,23], diagram: {"kind":"flow","steps":[{"label":"get: ArrayList 6000x faster"},{"label":"add end: ArrayList wins"},{"label":"add front: LinkedList wins"},{"label":"iterate: ArrayList wins"},{"label":"Default: ArrayList"}]} }
    ]
  }
];
