module.exports = [
  {
    path: "system-design", slug: "sd-database-indexing",
    steps: [
      { title: "Why Indexes?", description: "Full table scan: O(n). Index scan: O(log n). For 1M rows, that's 1M vs 20 operations.", code: "-- Without index: full table scan\nEXPLAIN SELECT * FROM users WHERE email = 'alice@example.com';\n-- Seq Scan on users (cost=0.00..18334.00 rows=1 width=72)\n-- Scans ALL 1M rows\n\n-- With index: B-tree index scan\nCREATE INDEX idx_users_email ON users(email);\nEXPLAIN SELECT * FROM users WHERE email = 'alice@example.com';\n-- Index Scan using idx_users_email (cost=0.43..8.45 rows=1)\n-- Reads 20 nodes in B-tree ← 50,000x faster", highlight: [2,6,7,9], diagram: {"kind":"flow","steps":[{"label":"Query: WHERE email="},{"label":"No index → scan 1M rows"},{"label":"Index → B-tree O(log n)"},{"label":"20 comparisons"},{"label":"Return row"}]} },
      { title: "B-Tree Index", description: "B-tree is the default index — supports equality, range, ORDER BY, and IS NULL.", code: "-- B-tree: balanced tree, all leaves at same depth\n-- Good for: =, <, >, BETWEEN, LIKE 'prefix%', ORDER BY\nCREATE INDEX idx_orders_date ON orders(created_at);\n\n-- Composite index: order matters!\n-- (a, b) index can satisfy: WHERE a=1, WHERE a=1 AND b=2\n-- Cannot satisfy: WHERE b=2 alone\nCREATE INDEX idx_users_name ON users(last_name, first_name);\n\n-- Covering index: all needed columns in index\nCREATE INDEX idx_covering ON orders(user_id, status, total)\n-- Query can be satisfied from index alone (no table fetch)", highlight: [3,8,10,11], diagram: {"kind":"boxes","items":[{"label":"Root Node","color":"#1e88e5"},{"label":"Branch Nodes","color":"#42a5f5"},{"label":"Leaf Nodes (data)","color":"#90caf9"}]} },
      { title: "Hash and GiST Indexes", description: "Hash for equality only; GiST/GIN for full-text, JSON, geometric data.", code: "-- Hash index: O(1) equality only\nCREATE INDEX idx_hash ON sessions USING HASH (token);\n-- Faster than B-tree for = only, no range queries\n\n-- GIN index: full-text search, JSONB, arrays\nCREATE INDEX idx_gin ON products USING GIN (description_tsvector);\nCREATE INDEX idx_jsonb ON events USING GIN (payload);\n-- Query JSONB:\nSELECT * FROM events WHERE payload @> '{\"type\": \"purchase\"}';\n\n-- GiST: geometric, range types\nCREATE INDEX idx_location ON stores USING GIST (coordinates);\nSELECT * FROM stores ORDER BY coordinates <-> point(40.7,-74.0) LIMIT 5;", highlight: [2,5,6,8,11,12], diagram: {"kind":"boxes","items":[{"label":"B-Tree: range/eq","color":"#1e88e5"},{"label":"Hash: eq only","color":"#43a047"},{"label":"GIN: FTS/JSON","color":"#fb8c00"},{"label":"GiST: geo/range","color":"#8e24aa"}]} },
      { title: "Index Selection Strategy", description: "Not all queries need indexes — over-indexing slows down writes.", code: "-- Guidelines:\n-- Index columns in WHERE, JOIN, ORDER BY, GROUP BY\n-- Selectivity: index only if < 10-15% rows returned\n-- Write overhead: each index = extra write on INSERT/UPDATE/DELETE\n\n-- EXPLAIN ANALYZE for real query cost:\nEXPLAIN (ANALYZE, BUFFERS) \nSELECT u.name, COUNT(o.id)\nFROM users u JOIN orders o ON u.id = o.user_id\nWHERE o.status = 'pending'\nGROUP BY u.id;\n-- Look for: Seq Scan (bad), Index Scan (good)\n\n-- pg_stat_user_indexes: find unused indexes\nSELECT indexname, idx_scan FROM pg_stat_user_indexes\nWHERE idx_scan = 0 AND schemaname = 'public';", highlight: [6,7,8,9,10,14,15], diagram: {"kind":"flow","steps":[{"label":"Query written"},{"label":"EXPLAIN ANALYZE"},{"label":"Seq Scan found"},{"label":"Add index"},{"label":"Index Scan"},{"label":"Monitor writes"}]} },
      { title: "Partial and Expression Indexes", description: "Index only a subset of rows or index computed expressions.", code: "-- Partial index: index only matching rows\nCREATE INDEX idx_active ON users(email) WHERE status = 'active';\n-- Only indexes active users (maybe 10% of table)\n-- Smaller index, faster for active user queries\n\n-- Expression index: index computed value\nCREATE INDEX idx_lower_email ON users(LOWER(email));\n-- Now this uses index:\nSELECT * FROM users WHERE LOWER(email) = 'alice@example.com';\n\n-- Functional: index timestamp truncated to day\nCREATE INDEX idx_date ON events(DATE_TRUNC('day', created_at));\nSELECT * FROM events WHERE DATE_TRUNC('day', created_at) = '2024-01-01';", highlight: [2,7,9,12,13], diagram: {"kind":"flow","steps":[{"label":"Partial: WHERE clause"},{"label":"Expression: LOWER()"},{"label":"Only matching rows indexed"},{"label":"Smaller + faster"},{"label":"Used in queries"}]} }
    ]
  },
  {
    path: "system-design", slug: "sd-sql-databases",
    steps: [
      { title: "PostgreSQL for Large Scale", description: "PostgreSQL is the go-to for complex workloads — MVCC, JSONB, full-text search, partitioning.", code: "-- MVCC: Multi-Version Concurrency Control\n-- Readers never block writers!\n-- Each transaction sees a consistent snapshot\nBEGIN;\nSELECT * FROM orders WHERE status = 'pending';\n-- Other transactions can still INSERT/UPDATE!\nCOMMIT;\n\n-- JSONB: relational + document in one\nCREATE TABLE events (\n  id BIGSERIAL PRIMARY KEY,\n  type TEXT NOT NULL,\n  payload JSONB NOT NULL,  -- indexed, queryable!\n  created_at TIMESTAMPTZ DEFAULT NOW()\n);", highlight: [4,5,6,9,12], diagram: {"kind":"boxes","items":[{"label":"MVCC: no read locks","color":"#1e88e5"},{"label":"JSONB: flexible docs","color":"#43a047"},{"label":"Table Partitioning","color":"#fb8c00"}]} },
      { title: "Table Partitioning", description: "Partition large tables to improve query performance and data management.", code: "-- Range partitioning by date\nCREATE TABLE orders (\n  id BIGSERIAL,\n  created_at DATE NOT NULL,\n  user_id BIGINT\n) PARTITION BY RANGE (created_at);\n\n-- Create partitions\nCREATE TABLE orders_2024_q1 PARTITION OF orders\n  FOR VALUES FROM ('2024-01-01') TO ('2024-04-01');\nCREATE TABLE orders_2024_q2 PARTITION OF orders\n  FOR VALUES FROM ('2024-04-01') TO ('2024-07-01');\n\n-- Query still uses 'orders' table\n-- PostgreSQL only scans relevant partitions", highlight: [2,9,10,11,12,15], diagram: {"kind":"flow","steps":[{"label":"Query: WHERE created_at"},{"label":"Partition pruning"},{"label":"Only scan Q1 partition"},{"label":"Skip other partitions"},{"label":"Fast result"}]} },
      { title: "Connection Pooling", description: "PostgreSQL connections are expensive — use PgBouncer to pool connections.", code: "// Problem: each connection = 5-10MB RAM, OS thread\n// 1000 app servers × 10 connections = 10,000 connections → 100GB RAM!\n\n// PgBouncer: pool 1000 app connections → 100 DB connections\n// Transaction mode: most efficient\n// Session mode: needed for prepared statements\n\n// Node.js with pg pool\nconst pool = new Pool({\n  max: 20,           // max connections per server\n  idleTimeoutMillis: 30000,\n  connectionTimeoutMillis: 2000,\n});\n\n// Architecture: App → PgBouncer (6432) → PostgreSQL (5432)", highlight: [9,10,11,12,14], diagram: {"kind":"flow","steps":[{"label":"1000 App connections"},{"label":"PgBouncer pools"},{"label":"100 DB connections"},{"label":"Huge RAM savings"}]} },
      { title: "Window Functions", description: "Window functions compute aggregates without collapsing rows — ranks, running totals, lag.", code: "-- Rank users by purchase amount per region\nSELECT\n  name,\n  region,\n  total_purchases,\n  RANK() OVER (PARTITION BY region ORDER BY total_purchases DESC) AS rank,\n  SUM(total_purchases) OVER (PARTITION BY region) AS region_total,\n  LAG(total_purchases, 1) OVER (ORDER BY created_at) AS prev_month\nFROM user_stats;\n\n-- Running total\nSELECT date, amount,\n  SUM(amount) OVER (ORDER BY date ROWS UNBOUNDED PRECEDING) AS running_total\nFROM daily_revenue;", highlight: [6,7,8,12,13], diagram: {"kind":"flow","steps":[{"label":"PARTITION BY region"},{"label":"RANK() within partition"},{"label":"SUM() running total"},{"label":"LAG() previous row"},{"label":"No GROUP BY collapse"}]} },
      { title: "PostgreSQL vs MySQL", description: "PostgreSQL: more features, better for analytics. MySQL: simpler, wider hosting support.", code: "// PostgreSQL advantages:\n// - MVCC (true multi-version)\n// - JSONB with GIN indexing\n// - Full ANSI SQL compliance\n// - Advanced indexing (GiST, GIN, BRIN)\n// - Table inheritance, window functions\n// - Better for complex analytical queries\n\n// MySQL advantages:\n// - Faster for simple reads (some cases)\n// - Better replication tooling historically\n// - More hosting options (shared hosting)\n// - InnoDB: reliable, widely tested\n// - Default for many PHP/WordPress stacks", highlight: [2,3,4,5,10,11,12], diagram: {"kind":"boxes","items":[{"label":"PostgreSQL: features","color":"#1e88e5"},{"label":"MySQL: simplicity","color":"#fb8c00"}]} }
    ]
  },
  {
    path: "system-design", slug: "sd-nosql-databases",
    steps: [
      { title: "Document Databases (MongoDB)", description: "Store JSON-like documents — no schema, nested data, flexible queries.", code: "// MongoDB document\nconst product = {\n  _id: ObjectId(),\n  name: 'Laptop Pro',\n  category: 'electronics',\n  price: 1299.99,\n  specs: {\n    ram: '16GB',\n    storage: '512GB',\n  },\n  tags: ['sale', 'laptop'],\n  reviews: [{ user: 'alice', rating: 5 }]\n};\n\n// Query nested field\ndb.products.find({ 'specs.ram': '16GB', tags: 'sale' });", highlight: [2,7,8,11,14], diagram: {"kind":"flow","steps":[{"label":"JSON Document"},{"label":"Nested Objects"},{"label":"Arrays"},{"label":"No joins needed"},{"label":"Flexible schema"}]} },
      { title: "Column-Family (Cassandra)", description: "Cassandra: designed for massive write throughput, distributed, no single point of failure.", code: "-- Cassandra: model around queries, not entities\n-- Data: partition key → rows\nCREATE TABLE user_events (\n  user_id   UUID,\n  event_time TIMESTAMP,\n  event_type TEXT,\n  payload   TEXT,\n  PRIMARY KEY (user_id, event_time)\n) WITH CLUSTERING ORDER BY (event_time DESC);\n-- user_id = partition key (which node?)\n-- event_time = clustering key (order within partition)\n\n-- Query (uses partition key = efficient)\nSELECT * FROM user_events WHERE user_id = ?", highlight: [3,8,9,10,14], diagram: {"kind":"flow","steps":[{"label":"Partition Key → Node"},{"label":"Clustering Key → Order"},{"label":"Row stored together"},{"label":"Efficient range scan"}]} },
      { title: "Key-Value (Redis Advanced)", description: "Redis data structures: Strings, Lists, Sets, Sorted Sets, Hashes, Streams.", code: "// String: counter, cache\nawait redis.set('page:views', 0);\nawait redis.incr('page:views'); // atomic increment\n\n// Sorted Set: leaderboard\nawait redis.zadd('leaderboard', 1000, 'alice');\nawait redis.zadd('leaderboard', 850, 'bob');\nawait redis.zrevrange('leaderboard', 0, 9); // top 10\n\n// Stream: event log\nawait redis.xadd('events', '*', 'type', 'purchase', 'userId', '123');\n\n// Pub/Sub: real-time messaging\nawait redis.publish('notifications', JSON.stringify({ userId: '123', msg: 'Order shipped' }));", highlight: [2,5,6,7,10,13], diagram: {"kind":"boxes","items":[{"label":"String/Counter","color":"#1e88e5"},{"label":"Sorted Set (leaderboard)","color":"#43a047"},{"label":"Stream (events)","color":"#fb8c00"},{"label":"Pub/Sub","color":"#8e24aa"}]} },
      { title: "Graph Databases (Neo4j)", description: "Graph DBs excel at relationship traversal — social networks, recommendations, fraud detection.", code: "// Cypher query language (Neo4j)\n// Find friends of friends\nMATCH (user:User {name: 'Alice'})\n  -[:FOLLOWS]->(friend:User)\n  -[:FOLLOWS]->(fof:User)\nWHERE NOT (user)-[:FOLLOWS]->(fof)\nAND user <> fof\nRETURN fof.name, COUNT(friend) AS mutualFriends\nORDER BY mutualFriends DESC LIMIT 10;\n\n// Products bought together\nMATCH (p:Product)<-[:BOUGHT]-(u:User)-[:BOUGHT]->(rec:Product)\nWHERE p.id = $productId\nRETURN rec, COUNT(u) AS frequency ORDER BY frequency DESC;", highlight: [3,4,5,11,12,13], diagram: {"kind":"flow","steps":[{"label":"(Alice) -[FOLLOWS]→ (Bob)"},{"label":"Graph traversal"},{"label":"Find friends of friends"},{"label":"Exclude known friends"},{"label":"Recommendation list"}]} },
      { title: "Search Engines (Elasticsearch)", description: "Elasticsearch provides distributed full-text search with powerful aggregations.", code: "// Index a document\nawait client.index({\n  index: 'products',\n  document: {\n    name: 'Laptop Pro 16',\n    description: 'Fast laptop with M3 chip',\n    price: 1299,\n    category: 'electronics'\n  }\n});\n\n// Full-text search with aggregations\nconst results = await client.search({\n  index: 'products',\n  query: { multi_match: { query: 'fast laptop', fields: ['name^2', 'description'] } },\n  aggs: { price_histogram: { histogram: { field: 'price', interval: 100 } } }\n});", highlight: [2,12,14,15], diagram: {"kind":"flow","steps":[{"label":"Index documents"},{"label":"Inverted index built"},{"label":"Search query"},{"label":"BM25 scoring"},{"label":"Ranked results"}]} }
    ]
  },
  {
    path: "system-design", slug: "sd-search-engines",
    steps: [
      { title: "Inverted Index", description: "Search engines use inverted indexes — map words to documents containing them.", code: "// Documents\nDoc1: \"the quick brown fox\"\nDoc2: \"the lazy dog fox\"\nDoc3: \"quick brown dog\"\n\n// Inverted Index built:\n// \"quick\" → [Doc1, Doc3]\n// \"brown\" → [Doc1, Doc3]\n// \"fox\"   → [Doc1, Doc2]\n// \"dog\"   → [Doc2, Doc3]\n// \"lazy\"  → [Doc2]\n\n// Query: \"quick fox\"\n// Intersect: {Doc1,Doc3} ∩ {Doc1,Doc2} = {Doc1}", highlight: [6,7,8,9,13,14], diagram: {"kind":"flow","steps":[{"label":"Documents"},{"label":"Tokenize + stem"},{"label":"Build inverted index"},{"label":"Query tokens"},{"label":"Set intersection"},{"label":"Ranked results"}]} },
      { title: "TF-IDF Ranking", description: "TF-IDF scores documents: frequent terms in doc, rare terms in corpus = high score.", code: "// TF: Term Frequency in document\n// TF(t, d) = (count of t in d) / (total terms in d)\n\n// IDF: Inverse Document Frequency\n// IDF(t) = log(N / df(t))\n// N = total docs, df = docs containing term\n// Rare term → high IDF\n\n// TF-IDF = TF × IDF\n// High score: term appears often in doc, rare in corpus\n\n// BM25 (Elasticsearch default): improved TF-IDF\n// Saturates TF (diminishing returns for extra occurrences)\n// Accounts for document length", highlight: [2,5,6,9,12,13], diagram: {"kind":"flow","steps":[{"label":"TF: frequent in doc"},{"label":"IDF: rare in corpus"},{"label":"TF-IDF score"},{"label":"BM25 improvement"},{"label":"Ranked results"}]} },
      { title: "Elasticsearch Cluster", description: "Elasticsearch shards data across nodes — primary + replica shards for HA.", code: "// Cluster architecture\n// Node 1: Primary shards 0,1 + Replica of 2\n// Node 2: Primary shards 2,3 + Replica of 0\n// Node 3: Primary shards 4   + Replica of 1,3,4\n\n// Create index with shards\nPUT /products\n{\n  \"settings\": {\n    \"number_of_shards\": 5,      // spread writes\n    \"number_of_replicas\": 1     // HA\n  }\n}\n\n// Shard selection: hash(docId) % numShards\n// Replica: read load balancing", highlight: [2,3,4,10,11,15,16], diagram: {"kind":"boxes","items":[{"label":"Node 1: shards 0,1","color":"#1e88e5"},{"label":"Node 2: shards 2,3","color":"#43a047"},{"label":"Node 3: shard 4","color":"#fb8c00"}]} },
      { title: "Autocomplete with Elasticsearch", description: "Use completion suggester or edge n-grams for real-time autocomplete.", code: "// Edge n-gram analyzer\nPUT /products\n{\n  \"settings\": {\n    \"analysis\": {\n      \"filter\": {\n        \"edge_ngram\": { \"type\": \"edge_ngram\", \"min_gram\": 1, \"max_gram\": 20 }\n      },\n      \"analyzer\": {\n        \"autocomplete\": {\n          \"type\": \"custom\",\n          \"tokenizer\": \"standard\",\n          \"filter\": [\"lowercase\", \"edge_ngram\"]\n        }\n      }\n    }\n  }\n}\n// \"Lapt\" → matches \"Laptop\", \"Laptop Pro\"", highlight: [7,12,13,18], diagram: {"kind":"flow","steps":[{"label":"User types \"Lapt\""},{"label":"Edge n-gram: \"L\",\"La\",\"Lap\",\"Lapt\""},{"label":"Index lookup"},{"label":"Matching docs"},{"label":"Ranked suggestions"}]} },
      { title: "Search Architecture", description: "Sync DB to search index via change events — never query Elasticsearch as primary store.", code: "// Sync pipeline: DB → Elasticsearch\n// Approach 1: CDC (Change Data Capture)\n// Debezium reads PostgreSQL WAL\n// Publishes changes to Kafka\n// Consumer updates Elasticsearch\n\n// Approach 2: Dual write (simpler, risk of inconsistency)\nasync function createProduct(data) {\n  const product = await db.insert('products', data);\n  await elasticsearch.index({ index: 'products', document: product });\n  return product;\n}\n\n// Rule: PostgreSQL is source of truth\n// Elasticsearch is derived, can be rebuilt", highlight: [4,5,9,10,13,14], diagram: {"kind":"flow","steps":[{"label":"DB write"},{"label":"CDC/Kafka"},{"label":"ES consumer"},{"label":"ES indexed"},{"label":"Search available"}]} }
    ]
  }
];
